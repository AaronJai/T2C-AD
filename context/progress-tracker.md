# Progress Tracker

> Single source of truth for build status across sessions. Update the relevant row at the
> **start** of a step (→ `in progress`) and at the **end** (→ `done`, with Outputs +
> Handoff). Do not start a step whose dependencies aren't all `done`.

**Status legend:** `not started` · `in progress` · `done` · `blocked` (note why in the row)

---

## Phase 0 — Contracts & Foundations *(no model, no DB)*

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 0.1 | `0.1-types.md` | — | done | `pipeline/types.py` + `pipeline/__init__.py`. Exports all shared dataclasses (`SchemaElement`, `SchemaRepr`, `SchemaCandidate`, `CandidateMapping`, `EntityCandidate`, `EntityLookupResult`, `AmbiguityResult`, `SchemaMapping`, `ValidationResult`, `ExecutionResult`, `BenchmarkItem`, `EvaluationResult`, `PipelineState`), the 7 type literals, and `_build_cypher_pattern`. Handoff: every later step imports its contracts from `pipeline.types`; no type here is redefined elsewhere. All 4 contract tests pass (`tests/test_types.py`). **Amended 2026-06-10** (spec 0.1 amendment): `PipelineState` gained `evaluation_history` + `first_validation_result` (per-attempt data for 4.2) and `previously_tried_mappings` is never cleared; see decisions-log 2026-06-10. |
| 0.2 | `0.2-llm.md` | — | done | `pipeline/llm/` package: `base.py` (`BaseLLM` ABC, `GenerationConfig`, `Completion`), `huggingface.py` (`HuggingFaceLLM`), `openai.py` (`OpenAILLM`), `anthropic.py` (`AnthropicLLM`), `__init__.py` (re-exports + `build_llm` factory). Handoff: later steps `from pipeline.llm import BaseLLM, GenerationConfig, Completion, build_llm`. SL (3.1)/QG (3.6) hold an injected `BaseLLM`; condition builders (5.3) call `build_llm(spec)` from config. No module hardcodes a model name. Now-set acceptance (clean import, abstract ABC + FakeLLM ranking, factory dispatch/ValueError) pass via `tests/llm/test_base.py` (6 tests). Integration checks (real HF beam scores, OpenAI/Anthropic smoke) deferred — need GPU/API keys. **Amended 2026-06-10**: `transformers>=4.40,<4.46` pinned in `pyproject.toml` (diverse beam search removed from core transformers in 4.56; `evaluation_strategy` renamed in 4.46); see decisions-log 2026-06-10. |
| 0.3 | `0.3-schema.md` | 0.1 | done | `pipeline/schema.py` exposes `build_pole_schema_repr() -> SchemaRepr` (9 node labels, 28 relationship paths, node + temporal-edge property lists; `format="nodes_and_paths"`). Built once from fixed definitions, not read from Neo4j. Handoff: prompt-building stages (3.1, 3.4, 3.6, 5.1, 6.1) import it and serialise via `SchemaRepr` methods from 0.1 — no consumer constructs a `SchemaRepr` by hand. Now-set acceptance (counts; label/rel-type consistency with `SyntheticPoliceKG.cypher`; serialiser substrings + one cypher line per path) passes via `tests/test_schema.py` (3 tests). Integration check (parity with live `db.labels()`/`db.relationshipTypes()`) deferred — needs Neo4j. |
| 0.4 | `0.4-benchmark-loader.md` | 0.1 | done | `pipeline/data/__init__.py` + `pipeline/data/benchmark_loader.py` exposing `load_benchmark(path) -> list[BenchmarkItem]`, `ambiguous_items(items)`, `items_by_type(items)`. No remapping — loads the canonical file and validates defensively (invalid/empty/compound `ambiguity_type`, `is_ambiguous`↔type mismatch, interpretation-presence mismatch all raise `ValueError`). Handoff: 5.4 (eval harness) and 2.2/2.3 (probe) import these to load the identical 125-item set; `ambiguous_items` gives the 50-question subset, `items_by_type` the stratified groups (schema=13, entity=11, intent=11, temporal=15, None=75). All acceptance criteria pass via `tests/data/test_benchmark_loader.py` (6 tests). **Phase 0 complete.** |

## Phase 1 — Training Data Prep

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 1.1 | `1.1-pattern-extraction.md` | 0.1 | done | `pipeline/schema_linker/__init__.py` + `pipeline/schema_linker/pattern_extraction.py` exposing `extract_schema_pattern(cypher) -> Optional[str]` (canonical committed-pattern format) and `has_relationship_type(pattern) -> bool` (the rel-type exclusion predicate). Self-contained depth-aware MATCH-clause parser — CyVer not used (needs live driver; see decisions-log). Handoff: 1.2 builds SL targets and 1.3 builds QG `committed_pattern` by calling `extract_schema_pattern`, then excluding rows where it returns `None`, `has_relationship_type` is False, or the CyVer SyntaxValidator (live driver) fails. Output format is the contract `_build_cypher_pattern` (0.1) and 3.5 must match. All acceptance criteria pass via `tests/schema_linker/test_pattern_extraction.py` (14 tests); CyVer SyntaxValidator filtering deferred to 1.2/1.3 (needs Neo4j). |
| 1.2 | `1.2-sl-training-data.md` | 1.1, 0.3 | done | `pipeline/schema_linker/prompts.py` (`SL_SYSTEM`, `build_sl_prompt(question, schema_block)` — the shared train/inference prompt contract) + `pipeline/schema_linker/training_data.py` (`load_ozsoy_split`, `build_sl_training_instance`, `build_sl_dataset` → writes `data/ozsoy_schema_linker_train.jsonl` + `_eval.jsonl`, returns kept/excluded-by-reason report). Per-row filter = parse-fail (`extract_schema_pattern` None) / node-only (`has_relationship_type` False) / CyVer `SyntaxValidator`. CyVer pass is conditional on a live Neo4j driver (env-configured) and deferred otherwise — see decisions-log. Handoff: 2.1 trains on the two jsonl files (`{"prompt","completion"}` lines, completion-only loss); 3.1 imports `build_sl_prompt` verbatim. Now-set acceptance (criteria 1–3) passes via `tests/schema_linker/test_training_data.py` (6 tests); full-run yield (~85–90%) + CyVer syntax filtering deferred (needs dataset + Neo4j). |
| 1.3 | `1.3-qg-training-data.md` | 1.1 | done | `pipeline/query_generator/__init__.py` + `pipeline/query_generator/prompts.py` (`SYSTEM_PROMPT_WITH_PATTERN`, `SYSTEM_PROMPT_ZERO_SHOT`, `build_qg_prompt(question, schema_block, committed_pattern=None)` — shared train/inference/C1 prompt; with-pattern → C2&C3, without → C1 zero-shot) + `pipeline/query_generator/training_data.py` (`build_qg_training_instance`, `build_qg_dataset` → writes `data/ozsoy_qg_train.jsonl` + `_eval.jsonl`, returns counts report). QG `completion` = full gold Cypher verbatim; `committed_pattern` = `extract_schema_pattern(gold)`. Per-row filter = parse-fail ONLY (node-only rows are KEPT — valid QG targets; no rel-type/CyVer filter, unlike 1.2; see decisions-log 2026-06-07). Same `seed=13` as 1.2 → consistent SL/QG partition. Handoff: 2.4 trains on the two jsonl files (`{"prompt","completion"}` lines, completion-only loss); 3.6 (inference) + 5.3 (C1 zero-shot) import `build_qg_prompt` verbatim. Now-set acceptance (criteria 1–3, incl. the train↔inference format-consistency guard) passes via `tests/query_generator/test_training_data.py` (9 tests); full-run yield (~90% of 35.9k) deferred (needs dataset). **Phase 1 complete.** **Amended 2026-06-10**: `build_qg_prompt` gained inference-only `error_feedback=None` (retry-feedback block; byte-identical output when None - training data/in-flight fine-tune unaffected); see decisions-log 2026-06-10. |

## Phase 2 — Training & Probe Checkpoints *(GPU + Ozsoy data)*

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 2.1 | `2.1-sl-training.md` | 1.2, 0.2 | done | `pipeline/sft.py` (`build_model_inputs` — completion-only loss masking; `run_sft(config) -> adapter_path` — LoRA/QLoRA harness reused unchanged by 2.4; `measure_token_lengths` — `max_seq_length` sub-task helper) + `config/models.yaml` (base registry: `mistral7b`, `llama31-8b`; per-model `base`/`target_modules`/`dtype`/`load_in_4bit`) + `config/schema_linker_train.yaml` (shared hyperparameters only; `max_seq_length: 1024`, not 512) + `experiments/train_schema_linker.py` (`--model_key`; `build_run_config` merges registry+hp → namespaced `checkpoints/{model_key}/sl_adapter`; unknown key exits with known list). Handoff: 2.4 reuses `pipeline/sft.py` + `config/models.yaml` verbatim (QG = own YAML + `qg_adapter`); 3.1 loads `checkpoints/{model_key}/sl_adapter` via `HuggingFaceLLM(base, peft_adapter_path=...)`; 2.2/2.3 probe that adapter; 5.4 resolves base+adapter from the registry. Now-set acceptance (criteria 1–3) passes via `tests/test_sft.py` (7 tests; full suite 55). Deferred (GPU): 3-epoch train + eval-loss + adapter weights, sibling-dir namespacing, and the token-length measurement — now run on Kaya (job 490628): SL p99=3473, `max_seq_length` raised 1024 → 3584 in `config/schema_linker_train.yaml`; see decisions-log 2026-06-08. First full-run attempt (job 490755) OOM'd at step 0 (`max_seq_length: 3584` × `per_device_train_batch_size: 4` exceeds 16 GB V100); `config/schema_linker_train.yaml` adjusted to `per_device_train_batch_size: 2` / `gradient_accumulation_steps: 16` (effective batch unchanged); see decisions-log 2026-06-08. Smoke test (`kaya/05_smoke_sl.slurm`, job 490807) confirmed headroom: stable 14400 MiB/16384 MiB, no OOM, ~126s/optimizer step — full SL run ≈50h, fits the `gpu` partition's 72h cap (`10_train_sl.slurm` `--time` raised `24:00:00 → 3-00:00:00`); `run_sft` gained resume-from-checkpoint + idempotent skip-if-saved so a chained job can pick up mid-run if needed; see decisions-log 2026-06-08 ("Kaya ops" row). Queued for the full run. |
| 2.2 | `2.2-diversity-penalty-sweep.md` | 2.1, 1.2, 0.2, 0.3, 0.4 | done | `experiments/probe_utils.py` (`generate_beams`, `normalised_entropy`, `roc_auc`, `covered`, `hallucinated`, `gold_patterns`) + `experiments/diversity_penalty_sweep.py` (`score_penalty`, `select_penalty`, `write_results`, `write_inference_config`, `main`). Sweeps `diversity_penalty ∈ {0.2,0.5,1.0}` with k=5 diverse beams; picks max entropy AUC under the Cov@5 ≥ 0.85 guardrail (hallucination as tiebreak), falling back + flagging if no penalty qualifies. Loads the adapter from the namespaced `checkpoints/{model_key}/sl_adapter` (deviation from spec's bare path — see decisions-log). Handoff: 2.3 reuses `probe_utils`; once the GPU sweep runs, `config/schema_linker_inference.yaml` carries the locked `diversity_penalty` that 3.1's `GenerationConfig` reads, and the recorded entropy AUC at the chosen penalty is the figure 2.3 must beat (vs ≈0.62 baseline). Now-set acceptance (criteria 1–2) passes via `tests/test_probe_utils.py` (20 tests; full suite 75). **GPU sweep complete** (`kaya/30_sweep.slurm`, job 511000): wrote `results/diversity_penalty_sweep.json` + `config/schema_linker_inference.yaml` (`diversity_penalty: 0.2`, chosen via the documented fallback — Cov@5=0.000 for all three penalties tested; see decisions-log 2026-06-15). Required an 0.2 bugfix to `pipeline/llm/huggingface.py._decode` (group-beam-search `beam_indices`/`outputs.scores` off-by-one in `transformers==4.45.2`; see decisions-log 2026-06-15). |
| 2.3 | `2.3-probe-rerun.md` | 2.1, 2.2 | done | `experiments/entropy_probe.py`: three pure scorers (`h_norm_candidates` [headline, normalised candidates], `h_norm_beams` [raw], `top_beam_dominance` [`1−max_prob`]) reusing 2.2 `probe_utils` (`generate_beams`/`normalised_entropy`/`roc_auc`/`_normalise_pattern`); `compute_auc_table`, `build_finding`, `write_results`, `read_diversity_penalty` (fails clearly pointing to 2.2 if `config/schema_linker_inference.yaml`/key absent), `generate_all_beams`+`cache_beams`/`load_cached_beams` (caches `results/beams_pole.jsonl` so ablations rescore GPU-free), `main`. No code contract exported — artefact is evidence. Now-set acceptance (criterion 1: three scorers compute from hand-built beams + feed `roc_auc`) passes via `tests/test_entropy_probe.py` (15 tests; full suite 90). Two interpretive decisions logged (signal-mapping; namespaced adapter path). **GPU probe run complete** (`kaya/40_probe.slurm`, job 511001): wrote `results/entropy_probe_pole.json` + cached `results/beams_pole.jsonl` (125 items, `diversity_penalty=0.2`). All three signals land ~0.587–0.594, flat vs the ~0.62 general-model baseline and flat across ablations (spread 0.007 ≤ 0.01) — per spec this is acceptable (entropy is a secondary signal; 3.3 may use the simplest scorer, `h_norm_candidates`/normalised entropy, without loss). **Phase 2 fully complete** (training + both probes) for `mistral7b`. |
| 2.4 | `2.4-qg-training.md` | 1.3, 0.2, 2.1 *(sft.py only)* | done | `config/query_generator_train.yaml` (shared QG hyperparameters only; identical in shape to the SL YAML, differing in `data` paths → `data/ozsoy_qg_{train,eval}.jsonl`; `max_seq_length: 1024`) + `experiments/train_query_generator.py` (`--model_key`; `build_run_config` merges `config/models.yaml` registry + this YAML → namespaced `checkpoints/{model_key}/qg_adapter`; reuses `pipeline.sft.run_sft`/`build_model_inputs` from 2.1 verbatim — no QG-specific harness or masking; unknown key exits with known list). Handoff: 3.6 loads `checkpoints/{model_key}/qg_adapter` via `HuggingFaceLLM(base, peft_adapter_path=...)` paired with the matching base for greedy Cypher generation. Now-set acceptance (criteria 1–2) passes via `tests/test_train_query_generator.py` (4 tests; full suite 94). Deferred (GPU): 3-epoch train + eval-loss + adapter weights; QG token-length sub-task — now run on Kaya (job 490628): QG p99=3486, `max_seq_length` raised 1024 → 3584 in `config/query_generator_train.yaml` (shared cap with SL; see decisions-log 2026-06-08). First full-run attempt (job 490756) OOM'd at step 0 (same `max_seq_length`/batch-size cause as 2.1); `config/query_generator_train.yaml` adjusted to `per_device_train_batch_size: 2` / `gradient_accumulation_steps: 16` (effective batch unchanged); see decisions-log 2026-06-08. Re-run pending — and at the ~126s/optimizer-step rate the SL smoke test measured (job 490807), QG's 3030 steps need ≈106h, more than the `gpu` partition's 72h max, so a single job cannot finish it. `pipeline.sft.run_sft` gained resume-from-checkpoint + idempotent skip-if-saved (it now picks up the last `save_strategy: epoch` checkpoint and is a no-op once `adapter_model.safetensors` exists); `20_train_qg.slurm` `--time` raised `24:00:00 → 3-00:00:00` and its header documents chaining two such jobs via `sbatch --dependency=afterany`; see decisions-log 2026-06-08 ("Kaya ops" row). **Phase 2 complete (per trained model_key)** once both adapters finish training. |

## Phase 3 — Inference Components *(runtime I/O order)*

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 3.1 | `3.1-schema-linker.md` | 0.1, 0.2, 0.3, 2.1, 2.2 | done | `pipeline/schema_linker/inference.py` (`sl_generation_config`, `extract_pattern_from_completion`), `postprocessing.py` (`ParsedPattern`, `parse_schema_pattern`, `align_beam_candidates`, `normalise_candidate_scores`, `build_candidate_mapping`), `linker.py` (`SchemaLinker`, `SchemaLinkerError`). Handoff: 3.2 reads `CandidateMapping.mentions` to scope entity labels; 3.3 reads relationship-type slot scores (incl. `rel_n_temporal`) for schema entropy; 3.4/3.5 get the full distribution; orchestrator calls `top1_mapping()` on the unambiguous bypass. `SchemaLinker.link(question, schema) -> CandidateMapping`; inject `HuggingFaceLLM(base, peft_adapter_path=checkpoints/{model_key}/sl_adapter)` + read `diversity_penalty` from `config/schema_linker_inference.yaml` (5.3). Now-set acceptance (criteria 1–5) passes via `tests/schema_linker/test_postprocessing.py` (11 tests; full suite 108). **Two deviations** (decisions-log 2026-06-15): `build_candidate_mapping` gained keyword-only `min_valid_beams` (wires the linker's unused param); temporal slot encodes the active filter on `SchemaElement.parent`. **Criterion 6 measured GPU-free** from the deterministic cached `results/beams_pole.jsonl` (identical prompt/config/adapter to `link`, so a GPU re-run is byte-identical): the parser **resolves the `--`-suffix artefact**, lifting **Cov@5 0.000 → 0.208** (EM@1 0.144). **⚠ Cov@5 0.208 ≪ ≥85% target (success criterion #5)** — an intrinsic `mistral7b/sl_adapter` quality gap (schema-plausible but structurally mismatched beams; not single-hop truncation, not node-set strictness), **flagged for 2.1/2.2**, not a 3.1 defect; 3.1's `CandidateMapping` contract is satisfied and unblocks 3.2. |
| 3.2 | `3.2-entity-lookup.md` | 0.1, 3.1 *(live Neo4j)* | done | `pipeline/entity_lookup/`: `registry.py` (`ENTITY_REGISTRY` — Person/Organisation/Case/Location are entity-searchable; Vehicle/Phone/Incident excluded), `cache.py` (`CachedNode`, `EntityCache` + `EntityCache.load(driver, database_name=None)` — one Cypher query per label, skips name-less nodes, immutable), `span_extractor.py` (`extract_entity_spans` — regex Operation/multi-word-proper/single-cap-excl-first-word/quoted + `_KNOWN_SUBURBS`, longest-first deduped with subspan removal), `matcher.py` (`fuzzy_match_score` = rapidfuzz `token_set_ratio`/100; `compute_posterior_scores` — uniform-prior Bayesian posterior over ≥threshold matches, sums to ~1), `lookup.py` (`entity_lookup(question, candidate_mapping, cache, fuzzy_threshold=0.75, max_candidates_per_mention=10) -> EntityLookupResult`; `_candidate_labels` always includes Person+Organisation). `__init__.py` re-exports all. Added `rapidfuzz` to `pyproject.toml`. Handoff: 3.3 reads `EntityLookupResult.entity_mentions[span]` posteriors for entity entropy (single match → posterior 1.0 → entropy 0); 3.4/3.5 inject matched `EntityCandidate`s as prompt context; 5.1 calls `EntityCache.load(driver)` once and passes the cache + a `CandidateMapping` (from 3.1) into `entity_lookup`. Acceptance criteria 1–4 (no-DB) pass via `tests/entity_lookup/test_entity_lookup.py` (11 tests). **Criterion 5 ran for real on Kaya** (job 511153, node k005, `bolt://localhost:7687` via the `55_neo4j.slurm` Apptainer pattern): `EntityCache.load` returned **exactly** Person 35 / Organisation 5 / Case 4 / Location 18 (total 62 name-searchable), CachedNodes correctly shaped (e.g. Location name_values=[suburb, address], display_name=suburb; Case display_name="Operation Ironside — …"). One interpretation logged (decisions-log 2026-06-15): >max-candidates truncation renormalises the retained posteriors to keep `EntityCandidate.posterior_score` a valid distribution for 3.3. |
| 3.3 | `3.3-bayesian-scorer.md` | 0.1, 3.1, 3.2 | done | `pipeline/ambiguity/__init__.py` (re-exports `compute_ambiguity_scores`) + `pipeline/ambiguity/bayesian_scorer.py` exposing `_shannon_entropy_normalised(scores) -> float` and `compute_ambiguity_scores(candidate_mapping, entity_lookup) -> (schema_entropy, entity_entropy)`, both ∈ [0,1]. Pure arithmetic, no GPU/DB. schema_entropy = max H_norm across relationship_type slots (incl. temporal `active` variants); entity_entropy = max H_norm across entity mentions with ≥2 candidates; 0.0 when no qualifying slot. Implemented verbatim from spec — no deviations. Handoff: 3.4 imports `compute_ambiguity_scores` and injects both scores into the AD prompt as secondary evidence (entropy informs, LLM decides — never a hard gate). All 4 acceptance criteria + worked examples pass via `tests/ambiguity/test_bayesian_scorer.py` (12 tests; full suite 131 under the `t2c` conda env). **Phase 3 core (3.1–3.3) complete.** |
| 3.4 | `3.4-ambiguity-detector.md` | 0.1, 0.2, 0.3, 3.1, 3.2, 3.3 | done | `pipeline/ambiguity/prompts.py` (`AD_SYSTEM_PROMPT` — taxonomy + curated POLE schema + 4 authored few-shot examples; `_format_candidate_block`, `_format_entity_block` — shared serialisers; `build_ad_user_turn`) + `pipeline/ambiguity/detector.py` (`ambiguity_detector(question, candidate_mapping, entity_lookup, ad_llm, threshold_schema=0.6, threshold_entity=0.8) -> AmbiguityResult`; `_parse_ad_output` — JSON extraction + validation with deterministic entropy-threshold fallback; never raises). LLM primary, entropy secondary (informs prompt, gates only the fallback). Handoff: 3.5 imports `_format_candidate_block`/`_format_entity_block` from `pipeline.ambiguity.prompts` and reads `AmbiguityResult.detected_types`; the orchestrator gates on `is_ambiguous` (False → `candidate_mapping.top1_mapping()`; True → Disambiguator). `pipeline/ambiguity/__init__.py` left re-exporting only `compute_ambiguity_scores` (keeps the package torch-free; import the detector from its module). Offline acceptance criteria 1–4 pass via `tests/ambiguity/test_detector.py` (17 tests; full suite 148 under the `t2c` env). Two deviations logged (decisions-log 2026-06-15): authored few-shot text ("Pass 4 §5.4" source absent from repo); temporal slot renders the `{active:true}` filter. **Criterion 5 ran for real on Kaya** (`kaya/45_ad_probe.slurm` + `kaya/ad_probe.py`, job 511162, V100/k035, ~5.7 min — combined GPU+Neo4j: real SchemaLinker `CandidateMapping` + live Entity Lookup + base `Mistral-7B-v0.3` as the AD LLM, 9-question stratified sample): the stage runs end-to-end, never raised, and returns well-formed `AmbiguityResult`s with coherent rationales (criterion met mechanically). **Honest finding:** all 9 classified `is_ambiguous=False` because `schema_entropy=entity_entropy=0.00` on every item — the SL collapses to single-candidate mappings (the known `mistral7b/sl_adapter` Cov@5=0.208 / 2.2 Cov@5=0.000 diversity gap), so the AD's secondary signal is flat and base 7B defers to it. Attributed upstream (3.1/2.1/2.2), not a 3.4 defect; a stronger/instruct backend (5.3 API condition) is the documented path and full Detection F1 lands in 4.2. See decisions-log 2026-06-15.|
| 3.5 | `3.5-disambiguator.md` | 0.1, 0.2, 3.1, 3.2, 3.4 | done | `pipeline/disambiguator/prompts.py` (`DIS_SYSTEM_PROMPT` — system prompt + 3 authored few-shot examples; `_format_tried_block`; `build_disambiguator_prompt`, reusing 3.4's `_format_candidate_block`/`_format_entity_block`) + `pipeline/disambiguator/disambiguator.py` (`disambiguator(question, ambiguity_result, candidate_mapping, entity_lookup, dis_llm, previously_tried, mode="automated") -> Optional[SchemaMapping]`; `_parse_disambiguator_output` — JSON extract + null→None + repeat/malformed→best-untried fallback; `_pattern_to_committed` — best-effort committed dict via candidate match + `parse_schema_pattern`; `_best_untried_candidate`/`_ranked_committed` — deterministic score-ranked fallback). `__init__.py` re-exports `disambiguator`, `DIS_SYSTEM_PROMPT`, `build_disambiguator_prompt`. Handoff: orchestrator (5.2) calls `disambiguator(...)`; on a `SchemaMapping` appends it to `previously_tried` and sends `cypher_syntax` to the QG (3.6); on `None` ends the disambiguation loop. `mode="interactive"` raises `NotImplementedError` (6.2 builds the clarification path). Offline acceptance criteria 1–5 pass via `tests/disambiguator/test_disambiguator.py` (12 tests; full suite 160 under the `t2c` env). Two deviations logged (decisions-log 2026-06-16): authored few-shot text ("Pass 4 §6.3" source absent); criterion-6 run findings. **Criterion 6 ran for real on Kaya** (`kaya/47_dis_probe.slurm` + `kaya/dis_probe.py`, job 512038, V100/k027, ~5.6 min — real SchemaLinker + live Entity Lookup + base `Mistral-7B-v0.3` as the dis LLM, 7-question ambiguous sample, 2 attempts each): committed a `SchemaMapping` for **7/7**; retry path honoured `previously_tried` (repeat→fallback fired) and returned `None` when exhausted. Honest note: base 7B sometimes emits out-of-schema patterns (committed verbatim by design — cypher_syntax authoritative, CyVer 3.7 catches them); the known `mistral7b/sl_adapter` diversity gap means some mappings are single-/empty-candidate. **Phase 3 detection/disambiguation (3.4–3.5) complete.** |
| 3.6 | `3.6-query-generator.md` | 0.1, 0.2, 1.3, 2.4 | done | `pipeline/query_generator/generator.py`: `QueryGenerator(llm)` with `generate(question, schema_mapping, schema, error_feedback=None) -> str` (greedy, `max_new_tokens=256`; reuses `build_qg_prompt` from 1.3 so inference == training format) + `_clean_cypher(text) -> str` (fence strip + trailing-prose trim + literal-`\n`/`\t`/`\r` unescape). Branch on `SchemaMapping.cypher_syntax`: `!= ""` → committed-pattern prompt (C2/C3); `== ""` → zero-shot (C1). Handoff: orchestrator (5.2) calls `QueryGenerator(HuggingFaceLLM(base, peft_adapter_path=checkpoints/{model_key}/qg_adapter)).generate(...)` and passes the returned **executable** Cypher to CyVer (3.7); on a syntax failure 5.2 re-calls with composed `error_feedback`. Offline criteria 1–3 pass via `tests/query_generator/test_generator.py` (7 tests; full suite 167 under `t2c`). **Criterion 4 ran for real on Kaya** (`kaya/49_qg_probe.slurm` + `kaya/qg_probe.py`, jobs 512508→512707, V100/k036, ~3 min, GPU only — no Neo4j): trained `mistral7b/qg_adapter`, 8-question stratified sample, both modes — emitted clean `MATCH … RETURN` for **8/8 in both committed-pattern and zero-shot (16/16)**. **One deviation** (decisions-log 2026-06-16, user-approved): `_clean_cypher` unescapes literal `\n` (Ozsoy gold stores clause breaks as the escape `\n`; 1.3 keeps it verbatim, the 2.4 adapter reproduces it) — normalised at the QG cleanup point so output is valid Cypher for 3.7/3.8; upstream Ozsoy-`\n` property flagged for the record, not re-trained. |
| 3.7 | `3.7-cyver-validator.md` | 0.1 *(live Neo4j)* | done | `pipeline/validation/__init__.py` (re-exports `cyver_validator`) + `pipeline/validation/cyver_validator.py` exposing `cyver_validator(cypher, components) -> ValidationResult`. Runs Syntax → Schema → Properties against the live graph via the pre-built validators duck-typed on `components` (`syntax_validator`/`schema_validator`/`properties_validator`/`database_name`); short-circuits and returns typed routing on first failure: syntax invalid → `("syntax","query_generator")`; schema `<1.0` → `("schema","schema_linker")`; properties `not None and <1.0` → `("properties","schema_linker")`; `properties_score is None` (no props) or all-pass → `(None,"accept")`. CyVer's diagnostic dicts retained in `metadata`. No `SchemaRepr` passed; CyVer import lives in 5.1, not here (module is import-light — only `pipeline.types`). **Implemented verbatim from spec — no deviations, no decisions-log entry.** Handoff: orchestrator (5.2) reads `route_to` — `accept` → DB executor (3.8); `query_generator` → regenerate (inner loop ≤2); `schema_linker` → re-link (outer loop ≤3). 5.1 constructs the three real `CyVer` validators once and injects them via `PipelineComponents`. Offline acceptance criteria 1–5 pass via `tests/validation/test_cyver_validator.py` (6 tests; full suite 173 under the `t2c` env). **Criterion 6 ran for real on Kaya** (`kaya/57_cyver_probe.slurm` + `kaya/cyver_probe.py`, job 513073, k011, ~22 s, Neo4j only — no GPU): the three real validators built against the live SyntheticPoliceKG graph routed all 5 crafted queries correctly — valid(props)→accept (schema=1.0,props=1.0), valid(no-props)→accept (props=None), bad-syntax→query_generator (error_type=syntax), unknown-label `Persn`→schema_linker (schema=0.5), unknown-property `naem`→schema_linker (props=0.0). Confirms the `props=None→accept` edge case and CyVer's partial-score behaviour against POLE. |
| 3.8 | `3.8-db-executor.md` | 0.1 *(live Neo4j)* | done | `pipeline/execution/__init__.py` (re-exports `db_executor`) + `pipeline/execution/db_executor.py` exposing `db_executor(cypher, driver, database="neo4j", timeout_s=10.0) -> ExecutionResult`. Runs the query in a managed read transaction (`session.execute_read`) whose server-side timeout is armed via `@neo4j.unit_of_work(timeout=timeout_s)`; records = `[record.data() ...]`; success path → `success=True, result_set, error=None, execution_time_ms`; any `Neo4jError`/`DriverError` (syntax/constraint/transient or server-side timeout) → `success=False, result_set=[], error=str(e)`, time still set. Empty result set is a legitimate success. Module is import-light (only `pipeline.types` + `neo4j`). **One deviation** (decisions-log 2026-06-16): timeout set via `unit_of_work` (driver 6.2 `execute_read` has no per-call `timeout=`) and catch broadened to `(Neo4jError, DriverError)` per the spec's "broadest relevant base" note. Handoff: 4.1 (Semantic Evaluator) executes benchmark ground-truth Cypher with the **same** executor + driver and frozenset-compares `ExecutionResult.result_set`; 5.1 holds the driver on `PipelineComponents`; 5.2 calls `db_executor` on CyVer `accept`. Offline acceptance criteria 1–3 pass via `tests/execution/test_db_executor.py` (4 tests; full suite 177 under the `t2c` env). **Criterion 4 ran for real on Kaya** (`kaya/59_db_executor_probe.slurm` + `kaya/db_executor_probe.py`, job 513261, k005, ~25 s, Neo4j only — no GPU): against the live SyntheticPoliceKG, real POLE queries returned rows (5 Person), empty result → `success=True, result_set=[]`, a relationship query returned the `Incident` node as a plain property dict (neo4j temporal `Date`/`Time` preserved), and a heavy `[*1..30]` traversal hit the timeout → `success=False, execution_time_ms≈2499`, `error=Neo.ClientError.Transaction.TransactionTimedOutClientConfiguration` (confirms `unit_of_work` armed the server-side timeout). **Phase 3 complete — all eight inference components (3.1–3.8) exist; `question → … → ExecutionResult` is implementable. Phase 4 (4.1 Semantic Evaluator, 4.2 metrics) is next.** |

## Phase 4 — Evaluation *(live Neo4j)*

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 4.1 | `4.1-semantic-evaluator.md` | 0.1 *(live Neo4j)* | done | `pipeline/evaluation/__init__.py` (re-exports `semantic_evaluator`) + `pipeline/evaluation/semantic_evaluator.py` exposing `semantic_evaluator(execution_result, benchmark_item, generated_cypher, cyver_result, condition, retry_count, driver, database_name, embedding_model=None, is_first_attempt=False) -> EvaluationResult`, plus helpers `_result_sets_equal`/`_is_id_field`/`_normalise_value`/`_run_query`/`_execute_ground_truth`. Scores EX (vs `cypher_default`) and relaxed AREA (vs any interpretation), executing ground truth **live** with the same driver/`.data()` extraction as 3.8. failure_mode: `invalid_query` (exec failed or CyVer≠accept; never executes GT) / `None` (correct) / `valid_non_default` (matches a non-default interp, ambiguous items only) / `wrong_result`. Comparison is frozenset-equality (order/dup-insensitive, float-normalised); optional `embedding_model` adds a row-aligned cosine fallback for free-text fields only (IDs via `_is_id_field` + numerics always exact; no model → exact only). **One deviation** (decisions-log 2026-06-24): `_normalise_value` now recurses into unhashable container values (`dict`/`list` from a generated `RETURN p`/`collect()`) so the fast-path frozenset is total — the first full 125×3 run (job 683734) crashed on C1 with `TypeError: unhashable type: 'dict'`. **Second deviation** (decisions-log 2026-06-27, user-approved): `_result_sets_equal` is now **column-name-insensitive** — records compared by ordered value tuple (RETURN order), not `record.data()` column names, so a correct answer under a different variable/alias (`x.name` vs gold `p.name`, `RETURN x` vs `RETURN v`) counts as correct; both fast path and cosine fallback fixed. A job-914770 spot-check showed name-keying made EX a strict lower bound (QG uses Ozsoy naming, C1 zero-shot — neither matches the gold's `p`/`i`/`loc_i`/`v`). Suite now 22 evaluator tests. Handoff: orchestrator (5.2) records the `EvaluationResult` on `PipelineState` and, in Condition 3, retries the disambiguator when `failure_mode='wrong_result'` with retry budget left; 4.2 aggregates per-question `EvaluationResult`s into the metric bundle. Offline acceptance criteria 1–5 pass via `tests/evaluation/test_semantic_evaluator.py` (15 tests; full suite **192** under the `t2c` env). **Criterion 6 ran for real on Kaya** (`kaya/61_semantic_evaluator_probe.slurm` + `kaya/semantic_evaluator_probe.py`, job 639269, k010, ~23 s, Neo4j only — no GPU): all four branches confirmed against the live SyntheticPoliceKG — default==default→correct (Q-001), a non-default interpretation with a differing live result set→`valid_non_default` (Q-016), garbage rows→`wrong_result`, exec-failed→`invalid_query`. **New dep:** `sentence-transformers` added to `pyproject.toml` + installed into `t2c` (5.6.0; verified the `transformers<4.46` diverse-beam pin is untouched — the module imports only `torch`, the caller builds the SentenceTransformer). |
| 4.2 | `4.2-metrics.md` | 0.1, 4.1 | done | `pipeline/evaluation/metrics.py` exposing `QuestionRecord` (terminal aggregate — `final`/`history`/`first_validation`, with `first_evaluated()`), `MetricBundle` (Table 1–3 fields), `compute_metrics(records, ad_predictions=None) -> MetricBundle` (every denominator over questions, all n present; `ad_predictions=None` → DSR/Detection NaN), and `format_metric_tables(bundles: dict[Condition, MetricBundle]) -> str` (three markdown tables, C1→C2→C3 column order over present conditions; NaN cells render `—`; Table 3 is C2/C3 only). `compute_metrics`/`MetricBundle`/`QuestionRecord` implemented verbatim from spec — no deviations. `metrics.py` not re-exported from `pipeline/evaluation/__init__.py` (left as 4.1's `semantic_evaluator`-only export — 5.4 imports from `pipeline.evaluation.metrics`). Note: per the given `compute_metrics`, `semantic_repair_pct` is 0.0 (not NaN) for C2, so Table 3's C2 cell renders `0.0`, not `—` — both the metric code and the "render `—` when NaN" rule are followed literally; no special-case. Handoff: 5.4 assembles one `QuestionRecord` per question per condition from each `PipelineState` (`evaluation_result`→`final`, `evaluation_history`→`history`, `first_validation_result`→`first_validation`; C3's `ad_predictions` separately), calls `compute_metrics` per condition, and prints `format_metric_tables`. All 5 acceptance criteria (incl. the amended per-attempt semantics 4a–d) pass via `tests/evaluation/test_metrics.py` (10 tests; full suite **202** under the `t2c` env). **Phase 4 complete.** |

## Phase 5 — Integration

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 5.1 | `5.1-components.md` | 0.1, 0.2, 3.1, 3.2, 3.6 *(live Neo4j)* | done | `pipeline/components.py` exposing `PipelineComponents` — a `@dataclass(kw_only=True)` container holding the constructed stage objects (`query_generator: QueryGenerator`, `schema_linker: Optional[SchemaLinker]`, `ad_llm`/`dis_llm: Optional[BaseLLM]`) plus shared graph resources (`neo4j_driver`, `database_name`, the three pre-built CyVer validators `syntax_validator`/`schema_validator`/`properties_validator`, `entity_cache`, `schema`, `embedding_model`, `use_prefilter`). `PipelineComponents.build(*, ...)` opens the neo4j driver, builds the three CyVer validators once with it, and loads `EntityCache` (`load_entity_cache=True` default; `False` → `entity_cache=None`); context-manager (`__enter__`/`__exit__`/`close`) closes the driver. CyVer import lives here (per 3.7 handoff); `SentenceTransformer` is `TYPE_CHECKING`-only to keep import light. **Implemented verbatim from spec — no deviations.** Handoff: 5.3 constructs one `PipelineComponents` per condition (C2 `beam_k=1`, C3 `beam_k=5`); 5.2 reads `schema_linker.link`, `query_generator.generate`, `ad_llm`, `dis_llm`, `entity_cache`, the three validators (via `cyver_validator`), `neo4j_driver`, `database_name`, `schema`, `embedding_model`, `use_prefilter`; 3.7 reads the validators + `database_name` off it. Now-set acceptance criteria 1–3 pass via `tests/test_components.py` (6 tests; full suite **208** under the `t2c` env). Deferred (live Neo4j): real `EntityCache.load` counts + CyVer construction against the POLE graph. |
| 5.2 | `5.2-orchestrator.md` | 3.1–3.8, 4.1, 5.1 | done | `pipeline/orchestrator.py` exposing `Orchestrator` (`run(question, benchmark_item, condition, components) -> PipelineState`) + module-level `_compose_error_feedback(cypher, vr)`. Drives one question through the three feedback loops — OUTER schema retry (≤3, `retry_count++`, `previously_tried` PERSISTS, CyVer diagnostics carried as `error_feedback`), DISAMBIGUATION semantic retry (≤1, C3 only, no budget cost), INNER QG+CyVer syntax retry (≤2, no budget cost, re-prompts QG with `error_feedback`) — and condition routing (baseline→empty mapping; schema_grounded→top1; disambiguation_enhanced→SL+EntityLookup+AD+Disambiguator). Uses the actual Phase 3 APIs (`schema_linker.link`, `entity_lookup`, `ambiguity_detector`, `disambiguator`, `query_generator.generate`, `cyver_validator`, `db_executor`, `semantic_evaluator`); `cosine_prefilter` (6.1) imported lazily only when `use_prefilter`. Records per-attempt data for 4.2 (`first_validation_result` set once; `evaluation_history` appended each eval, `evaluation_result`==last) + C3 `ad_predictions` once per outer iter. **One deviation** (decisions-log 2026-06-24): the SL `link` call is wrapped in `try/except SchemaLinkerError` → on a question the SL parses to 0 in-schema beams (3.1's raise) it sets `is_failed`/`failure_reason="schema_linker_no_valid_beams"` and breaks, so the full 125×3 run (job 710534) records that question as `invalid_query` instead of aborting on it (suite now 10 orchestrator tests). Handoff: 5.4 calls `Orchestrator().run(...)` for every question × condition, builds a `QuestionRecord` from `evaluation_result`/`evaluation_history`/`first_validation_result` and (C3) reads `ad_predictions`; 5.3 builds the per-condition `PipelineComponents` it consumes. Acceptance criteria 1–8 (no GPU/DB; stub stages) pass via `tests/test_orchestrator.py` (8 tests; full suite **216** under the `t2c` env). End-to-end over the benchmark deferred to 5.4 (live system).|
| 5.3 | `5.3-condition-builders.md` | 0.2, 3.1, 3.6, 5.1 | done | `experiments/build_condition1.py` (`build_condition1` — zero-shot baseline; single `build_llm(base_model_spec)`, `schema_linker/ad_llm/dis_llm=None`, `load_entity_cache=False`), `experiments/build_condition2.py` (`build_condition2` — `SchemaLinker(sl_llm, beam_k=1)` + SFT QG, two HF adapter backends, no AD/Dis, no cache), `experiments/build_condition3.py` (`build_condition3` — `SchemaLinker(sl_llm, beam_k=5, diversity_penalty=...)` + SFT QG + few-shot AD/Dis with swappable `ad_spec`/`dis_spec`, `dis_llm is ad_llm` when both default, `load_entity_cache=True`). **Implemented verbatim from spec — no signature/code deviation.** SL/QG backends are hardcoded HuggingFace (no swap point); AD/Dis are the config swap points (default base model, overridable to OpenAI/Anthropic via a `build_llm` spec). Handoff: 5.4 calls each builder with config values (base+adapters from `config/models.yaml`/`checkpoints/{model_key}`, `diversity_penalty` from `config/schema_linker_inference.yaml`, AD/Dis specs from `config/pipeline.yaml`), runs the orchestrator over the benchmark inside the `with` block, and closes it; swapping the base model or AD/Dis backend is a config edit, no code change. Offline acceptance criteria 1–4 pass via `tests/test_condition_builders.py` (6 tests; full suite **222** under the `t2c` env). **Criterion 5 ran for real on Kaya** (`kaya/63_build_condition_probe.slurm` + `kaya/build_condition_probe.py`, job 670833, 2×V100/k042, Neo4j in-job): **C1 PASS** (single 4-bit model loaded, working `PipelineComponents`, valid zero-shot Cypher); **C2/C3 OOM** loading the 2nd model — the fixed-signature C2/C3 builders load separate full-precision 7B models with `device_map="auto"`, which exceeds Kaya's 2×16 GB V100 ceiling (~29 GB C2 / ~43 GB C3). Not a builder defect: every constituent load is individually Kaya-proven (3.1/3.6/3.4/3.5/3.2) and the constrained-memory path is the spec's **5.4** concern ("4-bit / one base + adapter-switching"). **Flagged for 5.4:** to run C3 on V100s the harness must load one 4-bit base and share/adapter-switch for SL/QG/AD. See decisions-log 2026-06-23. **RESOLVED 2026-06-23 (Tier-1):** `build_condition2`/`build_condition3` gained keyword-only `load_in_4bit`/`torch_dtype` (defaults `False`/`bfloat16` preserve prior behaviour) threaded into the internally-built HF specs only (caller `ad_spec`/`dis_spec` passed through verbatim); `run_evaluation` reads them from the active registry entry. 4-bit brings C2≈9 GB / C3≈14 GB — fits a single 16 GB V100, no multi-node needed. Full suite **232** (+3 builder tests). See decisions-log 2026-06-23 (5.3/5.4 row). **C3 OOM fix 2026-06-26 (decisions-log):** the full run (job 770784) cleared C1+C2 then OOM'd in C3's SL `beam_k=5` generate — three independent `device_map="auto"` loads piled lopsidedly onto GPU 1 while GPU 0 was underused. `build_condition3` now pins SL→cuda:0 (own card), QG+AD→cuda:1 when ≥2 GPUs (else unchanged 'auto'); slurm sets `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`. Full suite **238** (+2 builder tests). |
| 5.4 | `5.4-run-evaluation.md` | 0.3, 0.4, 4.2, 5.2, 5.3 | done | `config/pipeline.yaml` (neo4j + `active_model` + benchmark/SLI paths + `ad`/`dis` swap points + `results_dir`) + `experiments/run_evaluation.py` exposing `run_condition(benchmark, components, condition) -> (records, ad_predictions)` (one `QuestionRecord` per item; never-evaluated state → synthesized `invalid_query` final with `is_first_attempt=False`, `history==[final]`, `first_validation` carried; `ad_predictions` concatenated across states) and `main(config_path="config/pipeline.yaml")` (resolves base+`checkpoints/{active_model}/{sl,qg}_adapter` from `config/models.yaml` via `active_model`; reads locked `diversity_penalty` from `config/schema_linker_inference.yaml`; runs C1/C2/C3 sequentially in `with` blocks; `compute_metrics(..., None)` for C1/C2, real `ad_preds` for C3; prints `format_metric_tables`; `_persist` writes `results/tables_{key}.md` + `results/metrics_{key}.json`). Fail-loud: unknown `active_model`→`SystemExit`, unset `NEO4J_PASSWORD`→`KeyError`, missing `diversity_penalty`→`KeyError`. **One config-content deviation** (decisions-log 2026-06-23): committed `ad: null`/`dis: null` (default to the active base, the spec's own documented fallback) instead of the spec example's literal `Mistral-7B-v0.1` `ad` block — avoids loading a 2nd inconsistent base (registry base is v0.3) and the 5.3 multi-model OOM. Now-set acceptance criteria 1–3 pass via `tests/test_run_evaluation.py` (7 tests; full suite **229** under the `t2c` env). Deferred (live Neo4j + GPU): the full 125×3 run producing populated Tables 1–3. **OOM resolved 2026-06-23 (Tier-1):** `run_evaluation` now threads the registry's `load_in_4bit`/`dtype` into all three builders (C1 spec, C2/C3 SL/QG + C3 default AD), so the run loads 4-bit/fp16 like training did and C2/C3 fit a single 16 GB V100 — see decisions-log 2026-06-23 (5.3/5.4 row). Full suite **232** under `t2c`. **✅ FIRST FULL END-TO-END RUN COMPLETE — 2026-06-27** (`kaya/65_run_evaluation.slurm`, job 914770, 2×V100/k029, ~4h50m, Neo4j in-job): all 125×3 conditions ran with no OOM/crash (the 4.1 dict fix + 5.2 SchemaLinkerError guard + 5.3 C3 device-pinning all held); wrote `results/tables_mistral7b.md` + `results/metrics_mistral7b.json`. This clears 5.4's last deferred acceptance criterion. **Headline (mistral7b, EX/EA as fractions):** C1 0/125, C2 1/125 (0.008), C3 1/125 (0.008); KG-valid 22.4/10.4/12.8%; syntax 92.8/48.8/66.4%; C3 Detection F1 0.233 (prec 0.50, rec 0.15), DSR 0. **Two honest reads:** (1) near-floor accuracy and C3≈C2 — the documented upstream `mistral7b` adapter quality gap (SL Cov@5 0.208/EM@1 0.144 per 3.1; base-Mistral AD under-detection per 3.4, rec 0.15 confirms), NOT a pipeline defect; the `ad`/`dis` API swap points are the documented path to real numbers. (2) **⚠ EX/EA are a LOWER BOUND — evaluator column-name strictness (spot-check 2026-06-27):** `_result_sets_equal`'s `norm` keys each row on the `record.data()` column name (`p.name`, `v`, …), so a substantively-correct answer is scored wrong when the generated query uses a different variable letter/alias than the benchmark gold (verified: `{'x.name':v}`≠`{'p.name':v}`). The QG (Ozsoy-trained naming) and C1 (arbitrary naming) rarely match the gold's `p`/`i`/`loc_i`/`v`, so true EX > reported. **EVALUATOR FIXED 2026-06-27** (decisions-log 4.1, user-approved): `_result_sets_equal` is now column-name-insensitive (value-tuple comparison), so EX/EA no longer punish variable/alias mismatch. ⚠ **The job-914770 results in `results/` predate this fix and are stale — re-run `sbatch kaya/65_run_evaluation.slurm` to regenerate Tables 1–3 before any thesis use.** Full suite **243**. **Phase 5 complete.** |

## Phase 6 — Optional / Extension

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 6.1 | `6.1-prefilter.md` | 0.1 | not started | |
| 6.2 | `6.2-gradio-demo.md` | 0.3, 3.4, 3.5, 5.1, 5.2, 5.3 | not started | |

---

## Target module layout (build map)

```
pipeline/
├── types.py                         [0.1]  all shared dataclasses incl. SchemaRepr/SchemaElement
├── llm/
│   ├── base.py                      [0.2]  BaseLLM, GenerationConfig, Completion
│   ├── huggingface.py               [0.2]
│   ├── openai.py                    [0.2]
│   └── anthropic.py                 [0.2]
├── schema.py                        [0.3]  build_pole_schema_repr() + POLE constants
├── data/
│   └── benchmark_loader.py          [0.4]
├── schema_linker/
│   ├── pattern_extraction.py        [1.1]
│   ├── training_data.py             [1.2]
│   ├── prompts.py                   [1.2]  build_sl_prompt (shared train/inference)
│   ├── inference.py                 [3.1]
│   ├── postprocessing.py            [3.1]
│   └── linker.py                    [3.1]
├── query_generator/
│   ├── training_data.py             [1.3]
│   ├── prompts.py                   [1.3]  build_qg_prompt (shared train/inference/C1)
│   └── generator.py                 [3.6]
├── entity_lookup/
│   ├── registry.py                  [3.2]
│   ├── cache.py                     [3.2]
│   ├── span_extractor.py            [3.2]
│   ├── matcher.py                   [3.2]
│   └── lookup.py                    [3.2]
├── ambiguity/
│   ├── bayesian_scorer.py           [3.3]
│   ├── detector.py                  [3.4]
│   └── prompts.py                   [3.4]
├── disambiguator/
│   ├── disambiguator.py             [3.5]
│   └── prompts.py                   [3.5]
├── validation/
│   └── cyver_validator.py           [3.7]
├── execution/
│   └── db_executor.py               [3.8]
├── evaluation/
│   ├── semantic_evaluator.py        [4.1]
│   └── metrics.py                   [4.2]
├── components.py                    [5.1]
├── orchestrator.py                  [5.2]
├── sft.py                           [2.1]  build_model_inputs, run_sft (shared SL+QG harness)
└── prefilter.py                     [6.1]

experiments/
├── train_schema_linker.py          [2.1]
├── train_query_generator.py        [2.4]
├── probe_utils.py                  [2.2]  beam + entropy + AUC helpers
├── diversity_penalty_sweep.py      [2.2]
├── entropy_probe.py                [2.3]
├── build_condition1.py             [5.3]
├── build_condition2.py             [5.3]
├── build_condition3.py             [5.3]
└── run_evaluation.py               [5.4]

app/
└── gradio_demo.py                  [6.2]

config/
├── models.yaml                     [2.1]  base-model registry (key → base, target_modules, dtype, 4bit)
├── schema_linker_train.yaml        [2.1]  shared SL hyperparameters (no per-model fields)
├── schema_linker_inference.yaml    [2.2]  locked diversity_penalty for 3.1
├── query_generator_train.yaml      [2.4]  shared QG hyperparameters (no per-model fields)
└── pipeline.yaml                   [5.4]  neo4j + active_model + AD/Dis backend config

data/
└── benchmark-updated.json          [canonical, provided]

checkpoints/                        # namespaced by model_key (supports N bases)
└── {model_key}/                    # e.g. mistral7b/, llama31-8b/
    ├── sl_adapter/                 [2.1 output]
    └── qg_adapter/                 [2.4 output]
```
