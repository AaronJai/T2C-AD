# Decisions Log

A lightweight record of choices made **during implementation that deviate from the
spec** — a library behaving differently than described, an interface needing an extra
parameter, a contract that had to change. Not for pre-planned design decisions (those
live in `project-overview.md` §10) and not for routine status (that's `progress-tracker.md`).

When you pick up this project weeks later and ask "why is this the way it is?", the answer
should be here.

## How to add an entry

One row per deviation. Keep the *why* concrete.

| Date | Step | Spec said | What we did | Why |
|------|------|-----------|-------------|-----|
| 2026-06-07 | 1.1 | Prefer CyVer's internal parser for isolating the MATCH clause | Wrote a self-contained depth-aware MATCH-clause parser; did not use CyVer | CyVer's `SyntaxValidator` requires a *live* `neo4j.Driver` (it runs EXPLAIN against the DB) and exposes no offline AST/parser. Neo4j isn't available until phase 3.2, and 1.1's acceptance criteria must run now with no DB. The spec explicitly allows a targeted MATCH-clause parser. The final CyVer SyntaxValidator filtering pass still happens later in 1.2/1.3 where a driver exists. |
| 2026-06-07 | 1.1 | Implementation block declares only `extract_schema_pattern` | Also exposed `has_relationship_type(pattern) -> bool` from the same module | Acceptance criterion #2 requires testing the relationship-type *filter predicate* now. Defining it alongside extraction keeps one source of truth for "pattern has a rel type"; 1.2/1.3 import it as one clause of their exclusion filter rather than re-deriving it. |
| 2026-06-07 | 1.2 | `_syntax_ok(pattern)` runs CyVer's `SyntaxValidator` on the pattern | Made the CyVer pass *conditional*: a lazily-built validator (Neo4j driver from `NEO4J_URI`/`NEO4J_USERNAME`/`NEO4J_PASSWORD`) runs when configured; otherwise `_syntax_ok` returns True (filtering deferred) | CyVer's `SyntaxValidator` issues `EXPLAIN` against a *live* `neo4j.Driver`; Neo4j isn't available until phase 3.2, and the spec's public signatures (`build_sl_training_instance`, `build_sl_dataset`) take no driver, so there is no injection path. The now-set acceptance (criteria 1–3) exercises only node-only/parse-fail exclusions, not syntax. The full-run CyVer pass activates automatically once a DB is configured. Consistent with the 1.1 deferral above. |
| 2026-06-07 | 1.3 | The 1.1 tracker handoff predicted 1.3 would exclude rows where `has_relationship_type` is False **or** the CyVer `SyntaxValidator` fails (the same filter as 1.2) | QG excludes a row on **parse failure only** — node-only rows are kept and no rel-type or CyVer filter is applied | The 1.3 *spec* is explicit and overrides the 1.1 forward-looking note: a node-only question is still a valid QG target (the QG must learn to generate single-node Cypher), so unlike the SL adapter, QG must not drop those rows. `build_qg_training_instance` therefore returns None iff `extract_schema_pattern` is None. Not a deviation from the 1.3 spec — recorded here to reconcile it with the 1.1 handoff so a future session doesn't "fix" QG to match SL. |
| 2026-06-07 | 2.1 | "As the first sub-task… compute the token-length distribution over the 1.2 train split and set `max_seq_length` to ~the 99th percentile. Record the measured percentile and the chosen value." | Kept the spec default **`max_seq_length: 1024`** (explicitly **not** 512) and shipped `pipeline.sft.measure_token_lengths(jsonl, tokenizer)` to run the measurement; the actual percentile run is **deferred** | The 1.2 jsonl (`data/ozsoy_schema_linker_*.jsonl`) is not materialised yet — 1.2's full dataset run was itself deferred (needs the ~36k-row Ozsoy download), so there is no train split to measure against now. The measurement is offline (tokenizer only, no GPU); when the jsonl exists, run `measure_token_lengths` and, per the spec, raise toward 1536/1600 if p99 > 1024 (or lower if comfortably under) and update this row + the YAML. |
| 2026-06-07 | 2.1 | `pipeline/sft.py` declares exactly `build_model_inputs` + `run_sft` | Also exposed `measure_token_lengths(jsonl, tokenizer, percentiles)` (and a private `_percentile`) from the same module; `experiments/train_schema_linker.py` factors the spec's inline `main()` assembly into a testable `build_run_config(model_key, registry, hp)` | `measure_token_lengths` operationalises the required `max_seq_length` sub-task above (one source of truth for "how rows are tokenised", reusing `build_model_inputs`' tokenisation convention). `build_run_config` makes acceptance criterion 3 (config assembly + unknown-key exit) unit-testable without invoking `run_sft` (which would load a 7B base); `main()` calls it and is otherwise verbatim to the spec. No declared signature changed. |
| 2026-06-07 | 2.1 | (deps unspecified) | Added `pyyaml`, `accelerate`, `bitsandbytes` to `pyproject.toml` | `pyyaml` reads the two new config files (needed at import for the entry point + tests); `accelerate` backs `transformers.Trainer` and `bitsandbytes` provides the 4-bit QLoRA path — both lazily imported inside `run_sft`, so the offline test suite still runs without them. |
| 2026-06-07 | 2.2 | Load the SL adapter from `checkpoints/sl_adapter` (`HuggingFaceLLM(base, peft_adapter_path="checkpoints/sl_adapter")`) | Load from the **namespaced** `checkpoints/{model_key}/sl_adapter` (default `model_key=mistral7b`), resolving `base`/`dtype`/`load_in_4bit` from `config/models.yaml` | 2.1 actually writes adapters to `checkpoints/{model_key}/sl_adapter` (multi-base namespacing — see 2.1 handoff); the bare `checkpoints/sl_adapter` in the 2.2 spec predates that. Reading from the namespaced path is the only way to find the adapter 2.1 produced, and keeps the base/quantisation choice in one registry rather than hardcoding a base in the sweep. |
| 2026-06-07 | 2.2 | `probe_utils.py` declares exactly `generate_beams`, `normalised_entropy`, `roc_auc`, `covered`, `hallucinated` | Also exposed `gold_patterns(item) -> list[str]` (+ private `_normalise_pattern`, `_referenced`) from the same module | The spec describes the `gold_patterns` derivation in prose immediately under the probe block and feeds it into `covered`; defining it as one helper keeps a single source of truth for "gold patterns of an item" that the sweep and 2.3 share, rather than re-deriving inline. No declared signature changed. |
| 2026-06-07 | 2.3 | Three signals: H_norm (candidates) = "entropy over distinct pattern strings (`normalised_entropy`)"; H_norm (beams) = "entropy over raw beam slots (no dedup)"; Top-beam dominance = `1 − max_pattern_probability` | **candidates** = `normalised_entropy` over beams with variable names stripped (`_normalise_pattern`); **beams** = `normalised_entropy` over the raw beam strings; **dominance** = `1 − max_count/k` over the same normalised candidate distribution | Taken literally, "raw beam slots (no dedup)" is degenerate (k distinct slots → H always 1). The only reading that makes all three non-trivial AND matches the spec's own ablation axes ("entropy vs dominance vs **normalisation**") is that *normalisation* is the candidates-vs-beams axis: candidates canonicalise variable-renamed beams into the same schema pattern, raw beams do not. `normalised_entropy` is the shared entropy engine applied to two different input lists. Reuses `_normalise_pattern` (2.2) as the single definition of pattern identity — same notion `covered` uses. If the SL emits consistent variable names this normalisation is a near-no-op, consistent with the expected flat ablation. |
| 2026-06-07 | 2.3 | Load the adapter via `HuggingFaceLLM(base, peft_adapter_path="checkpoints/sl_adapter")` | Load from the **namespaced** `checkpoints/{model_key}/sl_adapter`, resolving base/dtype/4bit from `config/models.yaml` (default `model_key=mistral7b`) | Same reason as the 2.2 row above — 2.1 writes adapters under `checkpoints/{model_key}/sl_adapter`; the bare path predates the multi-base namespacing. Mirrors 2.2's loader verbatim for consistency. |
| 2026-06-07 | 2.4 | "Re-run the 2.1 token-length sub-task on the 1.3 jsonl specifically; the QG `max_seq_length` may need to be larger than the SL one. Record the measured value." | Kept the spec default **`max_seq_length: 1024`** and deferred the actual percentile run | The 1.3 jsonl (`data/ozsoy_qg_*.jsonl`) is not materialised yet — 1.3's full dataset run was itself deferred (needs the ~36k-row Ozsoy download), so there is no QG split to measure against now. The measurement is offline (`pipeline.sft.measure_token_lengths`, tokenizer only, no GPU); when the jsonl exists, run it and raise `max_seq_length` toward 1536/1600 if p99 > 1024 (QG prompts add the `Committed pattern:` block + a full Cypher completion, so they run longer than SL — expect a higher p99) and update this row + the YAML. Mirrors the 2.1 deferral above. |
| 2026-06-07 | 2.4 | `experiments/train_query_generator.py` is "identical to `train_schema_linker.py` except" the train-YAML default and `qg_adapter` output dir | Did exactly that, and (as in 2.1) factored the inline `main()` assembly into a testable `build_run_config(model_key, registry, hp)` | Keeps acceptance criterion 1 (config assembly + unknown-key exit) unit-testable without invoking `run_sft` (which would load a 7B base). Verbatim mirror of the 2.1 entry point — same structure already logged for 2.1. No declared signature changed. |
| 2026-06-08 | 2.1/2.4 (Kaya branch) | `config/models.yaml` `mistral7b` `dtype: bfloat16`; SL+QG train YAMLs `bf16: true, fp16: false` | On the `Kaya` branch set `mistral7b` `dtype: float16` and both train YAMLs to `bf16: false, fp16: true` | The deferred Phase-2 GPU runs target Kaya's NVIDIA **V100** (Volta), which has no bf16 support — bf16 training/`bnb_4bit_compute_dtype` would error or silently degrade. fp16 is the correct precision for Volta and `bnb_4bit` NF4 QLoRA is supported there. Scoped to the `Kaya` branch so other compute targets (e.g. RTX 50-series / Ampere+) keep bf16. `llama31-8b` left at bfloat16 (not trained on Kaya per the single-base decision). |
| 2026-06-08 | 2.1/2.4 | The deferred 2.1/2.4 rows above: measure token lengths on the materialised 1.2/1.3 jsonl and set `max_seq_length` to ~the 99th percentile, "raise toward 1536/1600 if p99 > 1024" | Materialised the jsonl on Kaya (`kaya/build_datasets.py`, job 490628) and ran `measure_token_lengths`: **SL** p50/p90/p95/p99/max = 749/3152/3460/3473/3523 (n=15262 train rows); **QG** = 633/2309/3439/3486/3646 (n=32339). Set `max_seq_length: 3584` in *both* `config/schema_linker_train.yaml` and `config/query_generator_train.yaml` (28×128, a tensor-core-friendly multiple; covers both measured p99s with headroom) | The measured p99 (~3480) is more than double the 1536/1600 ceiling the deferred rows anticipated — the 2025v1 `schema` field renders **bimodally** across `data_source`s (compact ~700-token schemas vs. verbose ~3000+ token ones), so roughly 40% of rows exceed 1024. At the 1024 default, `build_model_inputs` (`pipeline/sft.py`) either truncates the completion (the actual training target) or, when the *prompt alone* exceeds the cap, masks every label to `-100` — a zero-loss row that wastes a training slot. Chose literal "~p99" coverage over the fallback ceiling because Kaya provides multiple V100s to absorb the ~3.5x memory/step-time cost over 1024 (vs. the RTX 5080 this default was chosen for). One shared value across both adapters (rather than two close-but-different caps) keeps the harness simple; QG's slightly higher p99 is still within the 3584 cap. **Risk noted:** at `per_device_train_batch_size: 4` this raises real OOM exposure on a 16 GB V100 — if the smoke test OOMs, halve the batch size and double `gradient_accumulation_steps` to preserve the effective batch of 32 (see `kaya/README.md`). |
| 2026-06-08 | 2.1/2.4 | The risk noted in the row above ("if the smoke test OOMs, halve the batch size and double `gradient_accumulation_steps`") | The predicted OOM happened for real — `10_train_sl.slurm` (job 490755) and `20_train_qg.slurm` (job 490756) both died at step 0 with `torch.OutOfMemoryError` (15.63/15.77 GiB in use, failed to allocate 784 MiB) before the smoke test (`05_smoke_sl.slurm`) was run. Applied the predicted fix verbatim in both `config/schema_linker_train.yaml` and `config/query_generator_train.yaml`: `per_device_train_batch_size: 4 → 2`, `gradient_accumulation_steps: 8 → 16` (effective batch unchanged at 32) | `_JsonlSFTDataset` pre-pads every row to `max_seq_length` (`pipeline/sft.py`), so the 3.5x jump in `max_seq_length` (1024 → 3584, logged just above) inflates per-step activation memory by roughly the same factor — 4 sequences × 3584 tokens of fp16 activations no longer fits a 16 GB V100 alongside the 7B 4-bit base + LoRA. Halving the micro-batch and doubling accumulation keeps the optimisation trajectory (effective batch, LR schedule) identical while roughly halving peak activation memory. Re-run `kaya/05_smoke_sl.slurm` to confirm headroom before resubmitting the full 24h jobs. |
| 2026-06-08 | 2.1/2.4 (Kaya ops) | `run_sft`'s declared 5 steps end at "train; save the adapter to output_dir; return that path" — no resume/chaining behaviour, since wall-clock cluster limits are outside the pipeline contract | Added two small additions to `run_sft` in `pipeline/sft.py`: (a) it now passes `resume_from_checkpoint=get_last_checkpoint(output_dir)` to `trainer.train()`, picking up the last `save_strategy: epoch` checkpoint if one exists; (b) it returns `output_dir` immediately, *before* loading the 7B base, if `output_dir/adapter_model.safetensors` already exists (a prior run already finished). Bumped `10_train_sl.slurm`/`20_train_qg.slurm` `--time` from `24:00:00` to the `gpu` partition's max `3-00:00:00` (confirmed via `sinfo`), and documented a 2-job `sbatch --dependency=afterany` chain in `20_train_qg.slurm`'s header | The `05_smoke_sl.slurm` run (job 490807) measured ~126s/optimizer step on Kaya's V100 at `max_seq_length: 3584`. SL's 1428 steps (~50h) fit the 72h cap with ~22h margin — a `--time` bump alone suffices. QG's 3030 steps (~106h) exceed even the partition's 3-day max, so a single job structurally cannot finish it; it must span ≥2 allocations. `save_strategy: "epoch"` already checkpoints ~every 35h (1 of 3 epochs), landing well inside a 72h window, so resuming from the last epoch checkpoint across two chained 72h jobs comfortably covers the full run. The idempotent early-return makes the chain self-terminating (a second job that finds the adapter already saved is a fast no-op) without needing signal-trap-based self-resubmission. Neither change touches the documented `run_sft(config: dict) -> str` contract, the config-key contract, or the 5-step flow — purely additive for cluster wall-clock reasons. |
| 2026-06-09 | 2.1/2.4 (Kaya ops) | `run_sft` did not set `per_device_eval_batch_size`; HuggingFace `Trainer` defaults it to 8 | Set `per_device_eval_batch_size: 1` in both `config/schema_linker_train.yaml` and `config/query_generator_train.yaml`; added `per_device_eval_batch_size=train_cfg.get("per_device_eval_batch_size", 1)` to `TrainingArguments` in `pipeline/sft.py` (default 1 if absent) | SL job 490875 ran all of epoch 1 (~16.5h) successfully, then OOM'd at step 476 during the per-epoch evaluation pass: `torch.OutOfMemoryError: CUDA out of memory. Tried to allocate 1.53 GiB … GPU 0 has 1.24 GiB free`. The eval forward pass at the default batch=8 × seq=3584 tokens requires ~4× the activation memory of a training step and has no gradient checkpointing, pushing a 16 GB V100 over its limit. No checkpoint was saved (crash before `save_pretrained`). The smoke test never caught this because it was killed by its 30-min walltime well before the epoch 1 boundary. Batch=1 keeps the same loss curve (CE loss is batch-size-invariant when averaged over the eval set) at the cost of a slower eval pass; eval metrics are diagnostic only and gate nothing downstream. |
| 2026-06-10 | 0.1 (spec amendment) | `PipelineState.previously_tried_mappings` "CLEARED on a Schema Linker outer-loop reset"; no per-attempt evaluation fields | Spec 0.1 amended + `pipeline/types.py` retrofitted: `previously_tried_mappings` is now NEVER cleared (persists across outer schema retries too), and two fields added — `evaluation_history: list[EvaluationResult]` (every evaluated attempt, in order) and `first_validation_result: Optional[ValidationResult]` (set once) | Pre-implementation feasibility review found two design bugs. (1) Every stage decodes with `do_sample=False`, so an outer retry re-runs the deterministic SL on identical input → identical candidate set; clearing the tried list would let the Disambiguator recommit the exact mapping that just failed CyVer, making `MAX_SCHEMA_RETRIES` a no-op. (2) 4.2's metrics filtered final-only results by `is_first_attempt`, which made Pass@1 biased (retried questions vanished from the denominator) and forced `structural_repair_pct`/`semantic_repair_pct` to 0 structurally; the new fields give 4.2 real per-attempt data (consumed via `QuestionRecord` in the amended 4.2/5.4 specs). Both fields are defaulted → purely additive; no Phase 0–2 consumer reads `PipelineState`, so nothing built so far changes. Amended specs synced into `context/feature-specs/` (0.1, 0.2, 1.3, 3.5, 3.6, 4.2, 5.2, 5.4) + `project-overview.md` §10. |
| 2026-06-10 | 1.3 (spec amendment) | `build_qg_prompt(question, schema_block, committed_pattern=None)` — no retry path | Spec 1.3 amended + `pipeline/query_generator/prompts.py` retrofitted: added `error_feedback: Optional[str] = None`; when set, a `Previous attempt (rejected by the validator …)` block is inserted before `Question:`. With `error_feedback=None` the output is **byte-identical** to the training format (regression-tested in `tests/query_generator/test_training_data.py`) | Same deterministic-retry finding as the 0.1 row: without changed input, the QG regenerates the identical invalid query on every inner syntax retry, so the 5.2 retry loops (and Table 3's repair metrics) could never repair anything. The block is INFERENCE-ONLY — training rows never set it, so the in-flight Kaya fine-tunes (jobs 492005/492006) are unaffected; no jsonl regeneration or retraining needed. Accepted trade-off: the feedback block is unseen at training time (deliberate train/inference skew); documented fallback is temperature sampling on retries if feedback-prompted repair underperforms. The orchestrator-side composition (`_compose_error_feedback`) lands with 5.2. |
| 2026-06-10 | 0.2 (spec amendment) | `pyproject.toml` left `transformers` unpinned; `_gen_kwargs` passes `num_beam_groups`/`diversity_penalty` straight to `model.generate` | Pinned `transformers>=4.40,<4.46` in `pyproject.toml` (matching `kaya/setup_env.sh`); spec 0.2 gained a "Version pin" section + a `num_beam_groups=2` smoke-test acceptance criterion | Group (diverse) beam search was **removed from core transformers in v4.56.0** (now Hub-only via `custom_generate="transformers-community/group-beam-search"`). Training never touches diverse beam search, so an unpinned env would train fine and only fail at the 2.2 sweep / 3.1 inference — the next steps after training finishes. The `<4.46` bound (rather than `<4.56`) matches the existing Kaya constraint (`run_sft` uses `TrainingArguments(evaluation_strategy=...)`, renamed in 4.46) so one pin satisfies both. Any future inference env must satisfy this pin or switch `_gen_kwargs` to the Hub `custom_generate` path and re-verify `compute_transition_scores` alignment. |
| 2026-06-11 | (Kaya ops, pre-3.2) | n/a — no spec yet covers running Neo4j on Kaya | Added `kaya/50_neo4j_pull.sh` (one-time login-node `apptainer pull docker://neo4j:5`) + `kaya/55_neo4j.slurm` (per-session job running SyntheticPoliceKG via Apptainer, bolt on `localhost:7687`); documented in `kaya/README.md`. Verified end-to-end: `data/SyntheticPoliceKG.cypher` loads (118 nodes total, matching the 9 POLE labels) and persists in `$MYGROUP/neo4j_data/` | Two Apptainer-on-Kaya quirks, found by trial: (1) Kaya's site-wide `apptainer.conf` hard-binds the **host's** `/opt` over every container's `/opt`, shadowing this image's JDK at `/opt/java/openjdk` (`neo4j`/`cypher-shell` fail with "JAVA_HOME is not defined correctly") — fixed with `--no-mount /opt`. (2) the official `neo4j:5` image's translated **startscript is empty** (its OCI ENTRYPOINT/CMD live in the runscript, which `apptainer instance start` does not run), so an instance comes up with no neo4j process; the script instead runs `neo4j console` directly via `apptainer exec --writable-tmpfs` (needed because `/var/lib/neo4j/run/neo4j.pid` is on the read-only image root). First-run also does `ALTER CURRENT USER SET PASSWORD` via `cypher-shell` since `NEO4J_AUTH=...` is normally applied by the (bypassed) docker-entrypoint. Pre-emptive: this is what 3.2 (Entity Lookup, needs live Neo4j) and 5.x integration will connect to; `bolt://localhost:7687` is reachable only from the job's node — use `srun --jobid=<id> --overlap` (not `ssh`) to reach it. |
| 2026-06-15 | 2.2 | `select_penalty` picks the `diversity_penalty` with max entropy AUC under the Cov@5 ≥ 0.85 guardrail (hallucination as tiebreak), "falling back + flagging if no penalty qualifies" | Ran the sweep (`kaya/30_sweep.slurm`, job 511000) over all 125 benchmark items × `diversity_penalty ∈ {0.2,0.5,1.0}`. **None reached Cov@5 ≥ 0.85 — measured Cov@5 = 0.000 for all three penalties.** `select_penalty` took its documented fallback: chosen `diversity_penalty = 0.2` (best AUC=0.587 among the tied Cov@5=0 candidates). Written to `config/schema_linker_inference.yaml` + `results/diversity_penalty_sweep.json`. The script printed `"RECORD THIS IN decisions-log.md"` per its own fallback-flagging design — this is that record | Diagnostic spot-check of `results/beams_pole.jsonl` against `gold_patterns()`: for several items (Q-001 SUSPECTED_OF→Incident, Q-002 INVESTIGATES→Incident, Q-003 OWNS→Vehicle, Q-005 VICTIM_OF→Incident) the SL gets the **first relationship hop right** but every beam ends in a bare, unterminated `--` — the canonical committed-pattern format's filter-suffix marker (cf. Ozsoy training completions like `'...--, --(...)--...'`) with nothing following it, so `covered()`'s exact normalised-string match never matches a gold pattern that has no such suffix (e.g. Q-004 gold=`(:Incident)-[:OCCURRED_AT]->(:Location)` vs beam=`(:Incident)-[:OCCURRED_AT]->(:Location)--` — schema-correct but fails the exact match by the trailing `--` alone). Other items (Q-006: predicted Incident-OCCURRED_AT-Location vs gold Case-CONTAINS-Incident) are genuinely schema-wrong, so this is not purely a formatting artefact. **Not fixed here** — `covered()`/pattern post-processing is 3.1's concern (`pipeline/schema_linker/postprocessing.py` per the build map); investigate with `results/beams_pole.jsonl` + `results/diversity_penalty_sweep.json` as the starting evidence when 3.1 is implemented. The 2.3 probe's separate entropy-AUC finding (0.592 vs ~0.62 baseline, flat ablations) was already deemed acceptable by spec design (entropy is a secondary signal) and needs no action. |
| 2026-06-15 | 0.2 | `HuggingFaceLLM._decode` calls `self.model.compute_transition_scores(outputs.sequences, outputs.scores, beam_indices, normalize_logits=True)` per the documented API, with `beam_indices = getattr(outputs, "beam_indices", None)` passed through unmodified | Trim `beam_indices` to `beam_indices[:, :len(outputs.scores)]` before the call, when not `None` | First real GPU run of the 2.2 sweep (`kaya/30_sweep.slurm`, job 510742) crashed in `_decode` on every item: `RuntimeError: Size does not match at dimension 1 expected index [5, 22] to be no larger than self [163840, 21]`. Root cause is in `transformers==4.45.2` itself (pinned `<4.46`, see decisions-log 2026-06-10): for `num_beam_groups>1` (diverse beam search, used by `generate_beams` with `k=5`), `model.generate()`'s returned `outputs.beam_indices` has 22 valid (non `-1`) columns but `outputs.scores` has only 21 step-tensors, so `compute_transition_scores`'s internal `scores.gather(0, indices)` sees `indices.shape[1]=22 > scores.shape[1]=21` and raises. `compute_transition_scores` was written assuming these always match. Trimming `beam_indices` to `len(outputs.scores)` columns drops only the (already-degenerate) extra trailing beam-index entry, leaving the per-token transition scores for all actually-scored steps intact — the ranking `_decode` produces from `scores.sum(dim=1)` is unaffected beyond a negligible one-token truncation on each beam. Affects `_decode` for ANY `num_beams>1` (not just group beam search) if the same off-by-one recurs, but only group beam search was exercised so far; greedy (`num_beams=1`, `beam_indices=None`) is unaffected (`getattr` short-circuits). 0.2's deferred "real HF beam scores" acceptance check (`progress-tracker.md`) is this code path — now exercised for the first time by 2.2. |
| 2026-06-12 | 2.1/2.4 (Kaya ops) | `run_sft` resumes via `trainer.train(resume_from_checkpoint=last_checkpoint)`, relying on `transformers`' own checkpoint loading | QG job 492006 hit its 72h `TIMEOUT` at step 2021/3030 (67%, `checkpoint-2021` saved by `save_strategy: "epoch"` — 3030/3 = 1010 steps/epoch, so this is the end-of-epoch-2 checkpoint). The chained job 492007 started correctly via `--dependency=afterany` but crashed in 2.5 minutes: `trainer.train(resume_from_checkpoint=...)` → `_load_rng_state` → `torch.load(rng_file)` raised `_pickle.UnpicklingError` (`Weights only load failed ... Unsupported global: GLOBAL numpy._core.multiarray._reconstruct`). Fixed by wrapping the `trainer.train(resume_from_checkpoint=...)` call in `run_sft` (`pipeline/sft.py`) with a scoped monkeypatch: `torch.load = functools.partial(torch.load, weights_only=False)`, restored in a `finally` block immediately after | `torch==2.6.0` changed `torch.load`'s default to `weights_only=True`; `transformers==4.45.2`'s `_load_rng_state` (pinned <4.46 per the 2026-06-10 row above) calls `torch.load(rng_file)` with no kwargs and was written before this default flip, so it can't unpickle the numpy `RandomState` arrays in its own `rng_state.pth`. Verified standalone that `torch.load(rng_state.pth, weights_only=False)` loads cleanly (`dict_keys(['python','numpy','cpu','cuda'])`) — the file is ours, just-written by our own training run minutes earlier, so relaxing `weights_only` for this one call is safe. Scoped to only the resume call (not a global `torch.load` override) and restored immediately after, so it can't mask an unrelated unsafe load elsewhere. `checkpoint-2021` is intact; re-running the chain now resumes from there instead of restarting QG from step 0. |
| 2026-06-15 | 3.1 | `build_candidate_mapping(question, completions, schema)` (3-arg); "If < min_valid remain, the caller (linker) decides (raises)"; `SchemaLinkerError` lives in `linker.py` | Added a keyword-only `min_valid_beams: int = 1` to `build_candidate_mapping`; it raises `SchemaLinkerError` itself when fewer than that many beams parse to in-schema patterns (imported lazily inside the function to avoid the linker↔postprocessing import cycle). `SchemaLinker.link` passes `min_valid_beams=self.min_valid_beams` | The spec's `linker.link` sketch stores `min_valid_beams` but never reads it, and the fixed 3-arg signature gives the linker no way to enforce it without re-parsing the beams (parsing happens inside `build_candidate_mapping`). The keyword default keeps the documented 3-arg call working unchanged while actually wiring the documented "fewer than min_valid → raise" behaviour. Raising lives where the parse result is known; the lazy import is the standard cycle-break since `linker` imports `build_candidate_mapping` at module load. |
| 2026-06-15 | 3.1 | Temporal variant rule: "same rel type, differing property filter ... a SEPARATE slot keyed `rel_{n}_temporal`" — but `SchemaElement` has no field for a property filter | Encoded the active-filter distinction on the relationship_type `SchemaElement.parent`: `parent="active"` for the `{active:true}` branch, `parent=None` for the unfiltered branch. Both branches keep the same `name`/`source_label`/`target_label` | `SchemaElement` (0.1, not to be redefined) carries no props field, yet the temporal slot needs two *distinct* candidates that are the same rel type. `parent` (doc: "property's owning node/rel label") is the only free field; reusing it as the temporal marker keeps both candidates as `relationship_type` elements (so 3.3, which reads relationship-type slot scores, sees them uniformly) while making them distinct. Spec acceptance #5 only asserts the `rel_1_temporal` slot key exists, which this satisfies. |
| 2026-06-15 | 3.1 | Acceptance #6: `SchemaLinker.link` over the 125-item POLE benchmark reports Cov@5/EM@1; the **known `--`-suffix issue** (2.2 measured Cov@5=0.000) is "exactly postprocessing.py's job" — the parser should ignore the dangling suffix | Computed criterion 6 GPU-free from the cached `results/beams_pole.jsonl` (deterministic: the cache was generated with the *identical* prompt `to_prompt_string(include_properties=True)`, config `k=5, diversity_penalty=0.2`, and namespaced `mistral7b/sl_adapter` that `SchemaLinker.link` uses, with `do_sample=False`, so a GPU re-run is byte-identical and was not separately submitted). With `parse_schema_pattern` parsing beams to `SchemaElement`s and comparing structure (suffix ignored): **Cov@5 = 0.208, EM@1 = 0.144** (vs the 2.2 exact-string Cov@5 = 0.000). The `--` artefact is resolved (e.g. Q-004 `(:Incident)-[:OCCURRED_AT]->(:Location)--` now matches its gold). | The postprocessing parser does its job — removing the trailing-`--` artefact lifts Cov@5 from 0.000 to 0.208. **But 0.208 ≪ the ≥85% target (success criterion #5), and this residual is NOT a 3.1 defect:** it is the intrinsic schema-linking accuracy of the `mistral7b/sl_adapter` on POLE. Not single-hop truncation (beam0 hop-counts span 1–7; only 26 of 102 multi-hop golds have a single-hop top beam) — the beams are schema-plausible but structurally mismatch the gold (wrong rel choice / path structure). Holds under a looser relationship-set-only match (also 0.208), so it is not a node-set strictness artefact. **Flagged for 2.1/2.2** (training quality / beam diversity), per workflow rule 2 (report an upstream gap, don't fix it inside 3.1). 3.1's contract — a `CandidateMapping` distribution — is satisfied regardless and unblocks 3.2–3.5. |
| 2026-06-15 | 3.2 | `entity_lookup` `> max_candidates_per_mention` → "keep top-N by posterior" (renormalisation not stated) | After truncating to the top-N posteriors, **renormalise** the retained posteriors to sum to ~1.0 | `EntityCandidate.posterior_score` (0.1) is documented as a "normalised Bayesian posterior", and 3.3 computes entity *entropy* over these scores, which is only meaningful for a distribution summing to 1. Truncating without renormalising would leave a sub-unit distribution. The >max case is a flagged over-generic-span warning path, so the exact values are not load-bearing; renormalising is the smallest change that keeps the field contract valid for 3.3. |
| 2026-06-15 | 3.4 | The few-shot examples appended to `AD_SYSTEM_PROMPT` are described by content (question + type + scores per type) with the note "Exact text in Pass 4 §5.4" | Authored the four few-shot examples (schema/entity/intent/temporal) in `pipeline/ambiguity/prompts.py` faithfully to the spec's described content, rendered in the exact layout my `build_ad_user_turn` + `_format_candidate_block`/`_format_entity_block` serialisers produce, each ending in the JSON response with a 1–2 sentence rationale | "Pass 4 §5.4" is an external design document not present in the repo, so the literal source text is unavailable. The spec fully fixes each example's semantic content (the four questions — "Who is connected to the Riverside robbery?", "Show incidents linked to James.", "What is the relationship between Alice and the Sesame Street incident?", "Who owns the vehicle used in the robbery?" — their gold types, and the salient scores). The few-shot text is internal teaching content: nothing downstream imports it (3.5 imports only the two serialisers), so authoring it is not a contract change. Matching the serialiser layout keeps the in-prompt examples byte-consistent with what the model actually sees at inference. |
| 2026-06-15 | 3.4 | `_format_candidate_block` renders relationship candidates as `(Src)-[:TYPE]->(Tgt)` | For the temporal active-variant slot (`rel_n_temporal`, where 3.1 encodes the `{active:true}` filter on `SchemaElement.parent == "active"`), append ` {active:true}` inside the relationship bracket: `(Person)-[:OWNS {active:true}]->(Vehicle)` | The temporal slot carries two candidates that are the *same* rel type/source/target, differing only by the active filter. Rendering both as the bare `(Person)-[:OWNS]->(Vehicle)` would make them indistinguishable in the prompt — defeating the point of surfacing temporal ambiguity to the AD LLM. Showing the filter is consistent with the system prompt's temporal definition and the few-shot temporal example. No signature change; non-relationship elements still render as their bare name per spec. |
| 2026-06-15 | 3.4 | Acceptance criterion 5: `ambiguity_detector` "runs end-to-end on a sample of benchmark questions (with a real `BaseLLM` backend) and returns sensible classifications" | Ran it for real on Kaya (`kaya/45_ad_probe.slurm`, job 511162, V100/k035, ~5.7 min): real SchemaLinker `CandidateMapping` (`mistral7b/sl_adapter`, dp=0.2) + live Neo4j Entity Lookup + base `Mistral-7B-v0.3` as the AD LLM, over a 9-question stratified sample (2 schema, 2 entity, 2 temporal, 1 intent, 2 null). The stage ran end-to-end without raising, produced well-formed JSON `AmbiguityResult`s, and parsed cleanly every time. **But all 9 were classified `is_ambiguous=False` — including the 7 gold-ambiguous ones — because `schema_entropy=entity_entropy=0.00` on every item.** | The detector behaves correctly given its inputs; the miss is upstream signal, not a 3.4 defect. The SchemaLinker collapses to a *single* candidate per slot for almost every question (e.g. Q-016 produced 0 valid beams → empty mapping; Q-001/Q-002 all 5 beams identical → single-candidate, score 1.00), so 3.3 entropy is 0 and the AD prompt's candidate block shows "single candidate with score 1.00". Entity Lookup likewise returned a single match per mention (even Q-020 "between Whitfield and Petrova" matched one PER node each). The base (non-instruct) 7B then leans on that flat evidence and the rationales literally cite it ("single candidate with a score of 1.00 → unambiguous"). This is the SAME intrinsic `mistral7b/sl_adapter` quality gap already flagged for 2.1/2.2 (Cov@5 0.000 → 0.208) — the linker's beams lack diversity, so the secondary entropy signal the AD is fed is empty. The LLM-primary design *could* still flag ambiguity from the question text alone (the question IS in the prompt), but base Mistral-7B-v0.3 does not; a stronger/instruct backend (the 5.3 API condition) is the documented path to better detection, and full Detection F1 is computed in 4.2. Criterion 5 is met in the contract/mechanical sense (real backend, end-to-end, sensible well-formed output that correctly reflects the evidence); the under-detection is recorded honestly and attributed upstream. No detector code changed. |
| 2026-06-16 | 3.5 | The few-shot examples appended to `DIS_SYSTEM_PROMPT` are described by content (3 examples) with the note "include verbatim from Pass 4 §6.3" | Authored the three few-shot examples (schema first-attempt → generic `(p:Person)-[r]->(i:Incident)`; schema retry honouring the tried block → highest specific `(p:Person)-[:SUSPECTED_OF]->(i:Incident)`; temporal present-tense → `(p:Person)-[:OWNS {active:true}]->(v:Vehicle)`) in `pipeline/disambiguator/prompts.py`, faithful to the spec's described content and rendered in the exact layout my `build_disambiguator_prompt` + the shared 3.4 serialisers produce, each ending in the `{"committed_pattern", "rationale"}` JSON | Same situation as the 3.4 row: "Pass 4 §6.3" is an external design document not in the repo, so the literal source text is unavailable. The spec fully fixes each example's semantic content (which pattern each commits, and that example 2 demonstrates honouring "previously tried"). The few-shot text is internal teaching content — nothing downstream imports it — so authoring it is not a contract change. Matching the serialiser layout keeps the in-prompt examples byte-consistent with what the model sees at inference (example 2 includes the `_format_tried_block` output; 1 and 3 omit it). |
| 2026-06-16 | 3.6 | `_clean_cypher` is scoped (spec + docstring) to "strip markdown code fences and any trailing prose"; nothing about escape sequences | `_clean_cypher` additionally **unescapes literal `\n` / `\t` / `\r` to real whitespace** as its first step (`text.replace("\\n","\n")…`) before fence extraction and the keyword/blank-line trim | The criterion-4 Kaya run (job 512508) revealed the `mistral7b/qg_adapter` emits clause breaks as the **two-character escape `\n`** (backslash+n), e.g. `MATCH (p:Person)\nWHERE …\nRETURN p`, never a real newline. **Root cause is upstream**: the Ozsoy text2cypher gold completions store newlines as the literal escape `\n` (32324/32339 rows of `data/ozsoy_qg_train.jsonl`), 1.3 keeps the completion verbatim, and the 2.4 adapter faithfully reproduces it. A literal backslash is not valid Cypher whitespace, so every generated query would be rejected by CyVer (3.7) and the executor (3.8); the benchmark's own `cypher_default` is single-line real whitespace, confirming the executable form. **User-approved (2026-06-16)** to normalise at the QG cleanup point rather than re-run 2.4 training (~100+ GPU-h) or defer to 3.7: `_clean_cypher` is the designated output-normaliser and this keeps its downstream contract ("returns the bare executable Cypher statement") true with a local, retraining-free change. Upstream Ozsoy-`\n` property flagged here for the record (not fixed in 1.3/2.4). Covered by `tests/query_generator/test_generator.py::test_clean_unescapes_literal_newlines`. |
| 2026-06-16 | 3.6 | Acceptance criterion 4: `generate` "runs over benchmark questions and emits clean `MATCH … RETURN …` Cypher" | Ran it for real on Kaya (`kaya/49_qg_probe.slurm` + `kaya/qg_probe.py`, job 512508, V100/k036, ~3 min): trained `mistral7b/qg_adapter` (greedy), 8-question stratified sample, both committed-pattern (C2/C3, committed via a real SchemaLinker `top1_mapping()`) and zero-shot (C1) modes. The stage ran end-to-end without raising and emitted a well-formed `MATCH … RETURN` statement for **all 8 questions in both modes** (16/16). | Criterion met. The probe's first run printed a "0/8 clean" tally — a **harness regex artefact, not a QG defect**: the check used `\bRETURN\b`, and the model's literal `\n` glued an `n` directly before `RETURN` (`…}\nRETURN`), so the `\b` word boundary never matched. Two fixes applied post-run: `_clean_cypher` now unescapes `\n` (row above), and the probe regex dropped the `\b` anchors. A clean re-run with both fixes (`logs/t2c_qg_probe_512707.out`, job 512707, V100/k036) reports **8/8 clean in both modes (16/16)** and the outputs now carry real newlines (e.g. `MATCH (p:Person)-[:SUSPECTED_OF]->(i:Incident {name:'armed robbery on William Street'}) \nRETURN p`), confirming the unescape works end-to-end on live model output. Intrinsic QG quality on the text2cypher split stays an optional informal spot-check per 2.1 — the QG's contribution is demonstrated by C2-over-C1 on POLE in Phase 5. No generator contract changed. |
| 2026-06-16 | 3.5 | Acceptance criterion 6: end-to-end selection "runs on a sample of ambiguous benchmark questions (with a real `BaseLLM` backend) and returns committed `SchemaMapping`s" | Ran it for real on Kaya (`kaya/47_dis_probe.slurm` + `kaya/dis_probe.py`, job 512038, V100/k027, ~5.6 min): real SchemaLinker `CandidateMapping` (`mistral7b/sl_adapter`, dp=0.2) + live Neo4j Entity Lookup + base `Mistral-7B-v0.3` as the dis LLM, over a 7-question ambiguous stratified sample (2 schema, 2 entity, 2 temporal, 1 intent), each driven for two attempts (2nd with the 1st commitment in `previously_tried`). **Committed a `SchemaMapping` for 7/7 questions; the retry path worked — attempt 2 either committed a different pattern or returned `None` when exhausted, and the repeat→fallback warning fired correctly.** | The stage works mechanically and satisfies the contract. Two honest observations, both attributable upstream / to base-model quality, not a 3.5 defect: (1) base (non-instruct) Mistral-7B-v0.3 sometimes emits out-of-schema patterns (e.g. `RELATES_TO`, a non-existent rel type) — the disambiguator commits `committed_pattern` **verbatim** by design (cypher_syntax is authoritative for the QG; CyVer 3.7 catches invalid queries and routes back), so this is expected and the documented base-model limitation flagged in 3.1/3.4. (2) Q-016's SchemaLinker produced 0 valid beams → empty `CandidateMapping`; the disambiguator still committed the LLM's pattern on attempt 1, and attempt 2 correctly returned `None` (no untried candidate to fall back to). A stronger/instruct backend (5.3 API condition) is the documented path to higher-quality committed patterns; DSR is computed in 4.2. No disambiguator code changed. |
| 2026-06-16 | 3.8 | Code sketch sets the per-query server-side timeout via a comment on `session.execute_read` ("set a tx timeout per driver version"); catches `Neo4jError` only | Decorated the read transaction function with `@neo4j.unit_of_work(timeout=timeout_s)` so `session.execute_read` applies a server-side BEGIN timeout; broadened the `except` to `(Neo4jError, DriverError)` | In the installed driver (`neo4j==6.2.0`) `Session.execute_read(fn, *args, **kwargs)` passes `*args/**kwargs` to `fn`, not to the transaction — there is no per-call `timeout=` on managed transactions. The driver's `_run_transaction` instead reads `getattr(transaction_function, "timeout", None)` and applies it at BEGIN; `neo4j.unit_of_work(timeout=...)` is the public API that sets that attribute. This keeps the spec's `execute_read` design (so acceptance #2/#3 mock `execute_read`) while actually arming the server-side timeout the spec requires. The broadened catch follows the spec note ("catch the broadest relevant neo4j exception base so timeouts and transient errors are captured as success=False"): the server-side `TransactionTimedOut` is a `Neo4jError`, but client-side/transient driver faults are `DriverError` (a sibling base, not a `Neo4jError` subclass in 6.x — both share only the internal `GqlError`), so catching both is what makes a timeout/transient return `success=False` rather than raise. |
| 2026-06-23 | 5.3 | Acceptance criterion 5: "Each builder loads the real models and returns a working PipelineComponents (C3 also loads the live EntityCache)" — run for real on Kaya | Ran `kaya/63_build_condition_probe.slurm` + `kaya/build_condition_probe.py` (job 670833, 2×V100-16GB/k042, Neo4j in-job). **C1 PASS**: `build_condition1` loaded a single 4-bit `Mistral-7B-v0.3` (its full `base_model_spec` lets the probe pass `load_in_4bit`/`float16`), returned a working `PipelineComponents` (live driver + 3 CyVer validators, `schema_linker/ad_llm/dis_llm=None`, `entity_cache=None`), and generated valid zero-shot Cypher. **C2 and C3 OOM** loading the *second* model (`torch.OutOfMemoryError`, GPU1 full). Builders implemented verbatim — **no code/signature change**; criterion 5 recorded as met for C1, hardware-blocked for C2/C3. | The spec fixes the builder signatures (5.4 consumes them; the edge-note says "none change these signatures") and only `build_condition1` takes a full `build_llm` spec. `build_condition2/3` take a bare `base_model: str` and hardcode `{"backend":"huggingface","model_name_or_path":...}` with **no `load_in_4bit`/`torch_dtype`/`device_map` hook**, so each `HuggingFaceLLM` loads full-precision with `device_map="auto"`. On 2×16 GB V100s `auto` spreads the *first* 7B (~14.5 GB) across **both** cards, leaving insufficient contiguous room for the second — so C2 (SL+QG, ~29 GB) and C3 (SL+QG+AD, ~43 GB) cannot coexist within the 32 GB ceiling (multi-node needs a Kaya support ticket). This is the documented GPU-constrained case the spec routes to **5.4** ("4-bit load via the spec's `load_in_4bit`, or one base model + adapter-switching"), **not** to these builders. Every constituent real load is already Kaya-proven individually — SL `sl_adapter` (3.1), QG `qg_adapter` (3.6/job 512707), base as AD/Dis LLM (3.4 job 511162 / 3.5 job 512038), `EntityCache.load` (3.2 job 511153) — and `PipelineComponents.build`'s driver+validators+cache path is offline-verified (5.1) and exercised here through C1. So C2/C3 wiring is verified by composition; the multi-full-model load is a hardware ceiling, not a 5.3 defect. **Flagged for 5.4:** to run C3 on V100s the harness must load one 4-bit base and share/adapter-switch for SL/QG/AD rather than instantiate three full models — the builders' fixed signatures (esp. C2/C3's bare `base_model` with no 4-bit hook) constrain how 5.4 can supply this. |
| 2026-06-23 | 5.4 | `config/pipeline.yaml` example sets `ad: {backend: "huggingface", model_name_or_path: "mistralai/Mistral-7B-v0.1"}` (and `dis: null`), with the note "swappable; defaults to active base" and "If `ad` is omitted, it defaults to the active model's base." | Committed the config with **`ad: null` and `dis: null`** instead of the literal v0.1 `ad` block | Two reasons. (1) The spec's example `ad` string is `Mistral-7B-v0.1`, but the active `mistral7b` registry base is `Mistral-7B-v0.3` — committing v0.1 would load a *second, different* base purely as the AD backend, which is both internally inconsistent with the registry and contributes directly to the C2/C3 multi-full-model OOM flagged in the 2026-06-23 5.3 row. (2) The harness `main()` already implements the documented fallback (`ad_spec = cfg.get("ad") or {"backend": "huggingface", "model_name_or_path": base}`), and the spec explicitly documents "`ad` omitted → defaults to the active base", so `ad: null` is the spec's own sanctioned path to "share one base for SL/QG/AD" rather than a new behaviour. Pure config-value choice (no code/signature change); the researcher can still drop in an API spec (e.g. Anthropic) for a stronger AD/Dis backend per the 3.4/3.5 under-detection findings. The committed `config/pipeline.yaml` documents this in-line. |
| 2026-06-26 | 5.3/5.4 | `build_condition3` builds its three 7B loads (SL, QG, default base AD) each via `build_llm` with no `device_map` → each `HuggingFaceLLM` uses its `device_map="auto"` default *independently*. The 2026-06-23 Tier-1 row assumed C3 ≈ 14 GB "fits a single 16 GB V100", with `gpu:2` as mere headroom. | Added `_device_maps()` to `build_condition3`: when `torch.cuda.device_count() >= 2`, pin **SL → `{"": 0}` (cuda:0 alone), QG → `{"": 1}`, AD → `{"": 1}`** in the internally-built HF specs (caller-supplied `ad_spec`/`dis_spec` still pass through verbatim, no device injection); with <2 GPUs inject nothing so the `"auto"` default applies (single-card path unchanged). Also set `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` in `kaya/65_run_evaluation.slurm`. | The first full run past the 5.2 SchemaLinker fix (job 770784, k041, 2×V100-16GB) completed **all of C1 (125) and C2 (125)** then **OOM'd ~2h40m in during C3** — in the SL `beam_k=5` `_group_beam_search` generate: `torch.OutOfMemoryError … GPU 1 … 15.76 GiB in use`. Both GPUs were present (`nvidia-smi` in `.out` shows two empty 16 GB V100s), so this was **placement, not capacity**: three *independent* `device_map="auto"` loads don't coordinate, so accelerate piled them lopsidedly onto GPU 1 (≈full) while GPU 0 sat underused, and the SL's beam_k=5 KV-cache spike on GPU 1 tipped it over. C1/C2 survived because they load only 1–2 models; C3's third is what unbalances it. Pinning gives the beam_k=5 SL its own 16 GB card (~4 GB weights + ~2–3 GB KV) and lets QG (beam_k=1) + AD (single forward) share the other (~8 GB weights) — using the full 32 GB the node always had. Kept the builder's fixed 5.3 signature (the device logic is internal, gated on GPU count so 1-GPU/CPU callers and the offline suite are unaffected). The 2026-06-23 "fits one card" estimate stands as a *fallback* (the <2-GPU branch), but the real run needs both cards used deliberately. Covered by `tests/test_condition_builders.py::{test_condition3_pins_devices_when_two_gpus,test_condition3_no_pin_on_single_gpu}`; full suite 238. Tier-2 (one shared 4-bit base + PEFT adapter-switching) still available if C3 ever pinches again. |
| 2026-06-24 | 5.2 | The orchestrator calls `components.schema_linker.link(question, working_schema)` directly (spec 5.2 code block, C2 & C3) with no error handling — the spec never anticipated `link` raising. | Wrapped the single SL call (now shared by the C2/C3 branch) in `try/except SchemaLinkerError`: on raise, set `state.is_failed=True`, `state.failure_reason="schema_linker_no_valid_beams"`, and `break` the outer loop (no retry — the SL is deterministic, so a retry reproduces the empty beam set). Also bound `schema_fail=False` before the outer loop so the early break doesn't hit the post-loop `if schema_fail …` with an unbound name. | The first full 125×3 run past the 4.1 dict fix (job 710534) reached **C2 (schema_grounded)** and died with `SchemaLinkerError: Only 0 parseable in-schema beam(s) among 1 completions; need >= 1.` propagating uncaught out of `orch.run`→`run_condition`→`main`, aborting all 125 questions on one un-linkable question. This is the gap between **3.1** (which *added* the `min_valid_beams` raise — decisions-log 2026-06-15) and **5.2** (which doesn't catch it); C2 runs the SL at `beam_k=1`, so a single greedy completion that doesn't parse in-schema raises, and the SL's known-low POLE coverage (Cov@5 0.208, EM@1 0.144) makes this common. A question the pipeline cannot link to any in-schema beam is a genuine per-question failure → `invalid_query`, exactly the state `run_evaluation.run_condition` already synthesizes when `evaluation_result is None` (so all 125 still appear in the metrics, the un-linkable ones as failures — the honest EX/Pass denominator). The orchestrator is the right home (it already owns `is_failed`/`failure_reason`); catching in `run_condition` instead would be coarser and lose the per-question state. C3 note: a SL failure short-circuits before `ambiguity_detector`, so that question contributes no `ad_prediction` (it genuinely has none) — acceptable for Detection-F1 (4.2). Covered by `tests/test_orchestrator.py::test_schema_linker_no_valid_beams_is_recorded_failure` (parametrized C2/C3); full suite 236. |
| 2026-06-27 | 4.1 | `_result_sets_equal` normalises each record to a `frozenset((key, _normalise_value(v)) …)` — i.e. comparison is keyed on the `record.data()` **column name** (the spec's "frozenset of (key, value) tuples"); the edge-case note says "benchmark queries return scalar properties, so this is exact". | Made the comparison **column-name-insensitive**: each record is reduced to the ordered tuple of its `_normalise_value`'d *values* (RETURN-clause order), names dropped. Fixed both the fast path and the cosine fallback (which previously did `if g.keys() != t.keys(): return False` and indexed `g[k]`/`t[k]` — also name-keyed); the fallback now aligns rows by a value-based sort key and compares position-by-position. Column count and per-position values must still match (not a value bag). User-approved 2026-06-27. | Post-hoc spot-check of the first full run (job 914770: EX C1 0/125, C2/C3 1/125) found EX/EA were a strict **lower bound**, not just low model quality. The benchmark gold returns variable-prefixed columns (`RETURN p.name` → `.data()` key `"p.name"`, `RETURN v` → `"v"`, `RETURN l.address, l.suburb`); 123/125 gold queries use no `AS`. The QG was fine-tuned on **Ozsoy** gold (its own variable conventions) and C1 is zero-shot, so neither reproduces the gold's `p`/`i`/`loc_i`/`v` — a substantively-correct answer keyed `{"x.name": v}` never matched gold `{"p.name": v}` (verified live before the fix). Name-keying conflates "wrong answer" with "right answer, different alias", which both deflates the headline EX and distorts the C1/C2/C3 comparison. EX in the text2cypher literature compares result-set *values*, not the model's column aliases, so value-tuple comparison is the correct EX semantics; keeping RETURN-clause order (rather than a per-row value bag) avoids conflating distinct columns and matches the gold's intended output order. Existing 4.1 tests use identical column names on both sides so they're unaffected; 5 new tests cover diff-variable / alias / whole-node / multi-column / wrong-value-still-False (suite 243). **Re-run required** — this changes every EX/EA/Pass@1 number; the job-914770 results in `results/` predate the fix and should be regenerated before any thesis use. The [[4.1 dict-normalisation fix]] (2026-06-24) is unaffected (`_normalise_value` still recurses into containers; node dicts now compare by value under any variable). |
| 2026-06-24 | 4.1 | `_normalise_value(v)` returns `round(v,6) if isinstance(v,float) else v` — scalar-only; the edge-case note assumes "benchmark queries return scalar properties, so this is exact". | Made `_normalise_value` recurse into unhashable containers: `dict` → `frozenset` of normalised items (order-insensitive), `list`/`tuple` → tuple of normalised items (order-preserving). `norm`'s fast-path `frozenset` is now total over any record value. | The first full 125×3 run (`kaya/65_run_evaluation.slurm`, job 683734) crashed on **C1 (baseline)** at `semantic_evaluator.py:36` with `TypeError: unhashable type: 'dict'`. The spec's scalar assumption only binds the *ground-truth* side; the *generated* side — esp. the unconstrained zero-shot C1 base model — can RETURN a whole node (`RETURN p` → `record.data()` = `{"p": {…props…}}`, a dict value) or a list (`collect()`), which is unhashable, so `norm` aborted the entire evaluation on one malformed-but-valid generation. Recursing keeps the comparison total and semantically correct (a node-dict vs a scalar gold compares unequal → `wrong_result`, the right verdict, instead of a crash; two identical node dicts compare equal). The fallback path already tolerated dicts (`str(v)` sort key, `gv != tv` compare), so only the fast path needed it; no signature/contract change. Covered by `tests/evaluation/test_semantic_evaluator.py::{test_node_dict_value_does_not_crash,test_list_value_does_not_crash}` (suite 17/17). |
| 2026-06-23 | 5.3/5.4 | The 5.3 builder signatures (`build_condition2`/`build_condition3`) take a bare `base_model: str` and hardcode `{"backend":"huggingface","model_name_or_path":...}` HF specs with **no quantisation hook**, so each `HuggingFaceLLM` loads full-precision (`load_in_4bit=False`, `torch_dtype="bfloat16"`) with `device_map="auto"` — the cause of the C2/C3 OOM flagged in the 2026-06-23 5.3 row. | **Tier-1 fix for the deferred full 125×3 run.** Added keyword-only `load_in_4bit: bool = False` + `torch_dtype: str = "bfloat16"` params to `build_condition2`/`build_condition3` and thread them into the *internally-built* HF specs only (SL, QG, and C3's default base AD/Dis); a caller-supplied `ad_spec`/`dis_spec` is passed through verbatim (no quant injection). `run_evaluation.main` now reads `load_in_4bit`/`dtype` from the active registry entry (`config/models.yaml`) and passes them to all three builders, and folds them into C1's `base_model_spec` and C3's default `ad_spec`. | The adapters were trained as **QLoRA 4-bit + fp16 on V100** (`config/models.yaml` `mistral7b`: `load_in_4bit: true`, `dtype: float16`), so 4-bit is the *matched* inference path, not a degradation — the builders were simply ignoring the registry's own load flags. A 4-bit 7B is ~4–5 GB, so C2 (SL+QG) ≈ 9 GB and C3 (SL+QG+AD) ≈ 14 GB now fit a single 16 GB V100, well inside the 32 GB (2×16 GB) node — **no multi-node / Kaya support ticket needed**, the OOM was precision, not capacity. Builder signatures gained only keyword-only params with defaults that preserve prior behaviour for any caller that omits them (HuggingFaceLLM's own defaults); the 5.3 fixed-signature contract is otherwise intact. Defaults left at `False`/`bfloat16` (not the V100 values) so non-V100 callers/tests are unaffected; the registry is the single source of the V100 4-bit/fp16 choice. Full suite 232 passed (+3 new builder tests: quant threads into SL/QG + default AD, caller AD spec stays clean). Tier-2 (one shared 4-bit base + PEFT adapter-switching) and Tier-3 (API AD/Dis via the existing `ad`/`dis` swap points) remain available if C3's beam_k=5 activations still pinch, but Tier-1 alone clears the documented ceiling. |
| 2026-06-30 | 5.3/5.4 + 2.2/2.3/3.1 (API end-to-end) | The original specs scoped API backends to the **Ambiguity Detector / Disambiguator only** (the `ad`/`dis` swap points): 5.3's `build_condition2`/`build_condition3` hardcode `{"backend":"huggingface","peft_adapter_path":...}` for SL/QG, the SL gets its candidate distribution exclusively from **diverse beam search** (`sl_generation_config`), and 2.2/2.3 + `schema_linker_inference.yaml` are local-only (sweep `diversity_penalty`, typed to `HuggingFaceLLM`). | Made the WHOLE pipeline run on one config-selected model (local **or** API). (a) **SL decoding is backend-aware**: added `sl_sampling_config` + a keyword-only `decoding` arg on `SchemaLinker` — beam search for local HF (default, byte-identical), temperature sampling of `beam_k` completions for API. (b) **`AnthropicLLM` supports multi-sample**: `num_return_sequences>1` issues that many calls, uniform `-1.0` scores → frequency weighting in `build_candidate_mapping`. (c) **Condition builders are spec-driven**: `build_condition2/3` take `sl_spec`/`qg_spec`/`base_spec` + `sl_decoding`; HF-only 4-bit/device-pinning guarded by `backend=="huggingface"`. (d) **`run_evaluation` branches on registry `kind`** (`local`/`api`); `config/models.yaml` gained `claude-sonnet`/`gpt-4o` entries. (e) **Sweep/probe generalized**: `generate_beams` takes a `BaseLLM` + decoding spec; the sweep sweeps `temperature` for API; `schema_linker_inference.yaml` carries a per-model `decoding` block (legacy flat `diversity_penalty` still honoured). | Diverse beam search (beam groups + per-beam logprobs) is a HuggingFace-only capability — Anthropic has no logprobs and OpenAI no beam groups — but the architectural claim is "distribution, not argmax," not "beam search specifically," so temperature sampling is a faithful substitute (OpenAI logprob-weighted, Anthropic frequency-weighted). User-confirmed 2026-06-30. **C2/C3 reframed as stage-presence, not SFT**, for API models (no fine-tuning/adapters): C1 zero-shot QG; C2 + schema-linking + committed pattern; C3 + disambiguation — the gradient isolates the schema-grounding/disambiguation STAGE. All back-compat preserved: local-Mistral path unchanged (decoding defaults to beam; `select_penalty`/`read_diversity_penalty`/`write_results` keep their tested signatures), full suite **258** passed. See feature-specs/phase5/5.5. |
| 2026-06-30 | 2.3 (probe cache) | The 2.3 probe caches beams at the fixed path `results/beams_pole.jsonl` and reads a flat `diversity_penalty`. | `entropy_probe` now reads the per-model `decoding` block (`read_sl_decoding`, with the flat-`diversity_penalty` fallback kept for local back-compat) and defaults its cache/result paths to `results/beams_{model_key}.jsonl` / `results/entropy_probe_{model_key}.json`. | A single fixed cache path would let an API (sampling) run silently rescore a local (beam) model's cached beams — wrong evidence. Model-keying the cache makes caches non-colliding automatically. The existing `results/beams_pole.jsonl` (mistral evidence) is left in place; a mistral re-run regenerates to `results/beams_mistral7b.jsonl`. |
| 2026-07-06 | Phase 7 created (governance amendment) | `ai-workflow-rules.md` §6: "`benchmark-updated.json` is already canonical"; `project-overview.md` §5/§6: the KG is 9 labels / 28 rel types and the benchmark is 125 questions uniformly stratified over 1–5 hops; no dataset-rebuild step existed anywhere in the plan. | Created **Phase 7 — Dataset v3** (specs `feature-specs/phase7/7.1`–`7.5`, tracker rows added): a concise 6-label / 11-rel-type schema (`SyntheticPoliceKG-v3.cypher`), a rebuilt 120-question benchmark (`benchmark-v3.json`, 80 ambiguous at 20/type, gold hops ≤3 with `num_hops` as metadata only, shipped with an executable validation harness incl. a pairwise-distinct-interpretations gate), in-domain POLE SL/QG SFT data + continue-SFT adapters (`{sl,qg}_adapter_v3`), and a gated revalidation runbook (beams→sweep→probe→AD→E2E). Amended `ai-workflow-rules.md` §6 (v2 frozen as canonical baseline; v3 canonical once 7.2 passes) and `project-overview.md` §5/§6/§8/§9/§10. v2 artefacts and the job-957276 results are frozen, never edited; v3 is selected by config (`dataset_version`/`results_tag`) so both coexist. | The first valid full E2E run (job 957276, 2026-06-29) put all three conditions at the floor (EX 0.008, C3≈C2≈C1) with the root cause localised to SL intrinsic coverage: Cov@5 0.208 vs the ≥0.85 success criterion #5 (decisions-log 2026-06-15/3.1; `docs/results-walkthrough.md`). Diagnosis: (a) the SL is *zero-shot* on POLE — trained only on Ozsoy generic schemas (1.2), never on a POLE example — and (b) v2's 28 rel types contain accidental near-synonyms (`RELATES_TO`/`RELATED_TO`/`LINKED_TO`, seven `*_AT` edges) that deflate coverage as lexical noise, distinct from the *designed* ambiguity the thesis studies; (c) v2's 4–5-hop strata (40% of items) test long-path compositionality, orthogonal to disambiguation, and its per-type ambiguous cells (n=11–15) are statistically thin; (d) nothing ever verified v2 interpretations return distinct non-empty results, which DSR/EA silently require. A concise schema alone can't reach 0.85 zero-shot — hence the paired in-domain SFT step (7.4) and the cheap-first gates (7.5) so a still-broken substrate is caught in minutes, not after a 5h run. |
| 2026-07-06 | 7.2 | Gate 4 "schema closure": every label/relationship type/property referenced by every Cypher exists in the v3 schema (7.1 inventory) | Implemented gate 4 via the real CyVer `SchemaValidator` + `PropertiesValidator` (reusing 3.7's `cyver_validator` routing) against the **loaded v3 graph**, not a hand-rolled static inventory parser | The loaded `SyntheticPoliceKG-v3.cypher` graph *is* the 7.1 inventory (7.1 acceptance verifies `db.labels()`==6 / `db.relationshipTypes()`==11, which the harness's fresh load reproduces), so CyVer closure against the live graph checks exactly "referenced element exists in the v3 schema" — and reuses a battle-tested component instead of a brittle Cypher-parsing property extractor. CyVer's triple-level check (label→rel→label connectivity) is strictly stronger than mere existence; gate 5 (non-empty execution) independently guards any schema-valid-but-empty edge. `cyver_validator` short-circuits, so a syntax failure is reported under gate 3 only (schema not reached) — the intended split. |
| 2026-07-06 | 7.2 | Author gold Cypher to the 7.1 skeleton; "iterate `SyntheticPoliceKG-v3.cypher` seeds and gold Cypher together until green — seed adjustments are normal authoring, not spec deviations" | Added two active `OWNS` edges to the v3 seed file (PER-V3-013 Santos→VEH-V3-007, PER-V3-018 Orlov→VEH-V3-008), converting Q-V3-103/116 from "only-historical ownership" into standard current-vs-all temporal items | The skeleton seeded Santos/Orlov with *only* a historical `OWNS` edge, so their "current only" interpretation returns empty — which violates gate 5 (every interpretation Cypher must be non-empty, the DSR/EA measurability precondition). An interpretation that is correctly-empty is indistinguishable from a failed query, so this benchmark cannot contain one. Chosen vehicles (VEH-007/008) are referenced by no other question, and Q-V3-017 ("Santos previously owned") still resolves to the untouched historical edge. Recorded per the spec's own note that this is authoring, not a deviation — logged for traceability since it edits a 7.1-owned artefact. |
| 2026-07-06 | 7.3 | Touch points 3–4: v3 AD/Dis prompts reach the stages "via the existing injection path"; 5.1/5.2 (`PipelineComponents`, orchestrator) marked out of scope + "no shared type or public signature changes" | **No injection path existed** — `ambiguity_detector`/`disambiguator` hardcoded the module constants `AD_SYSTEM_PROMPT`/`DIS_SYSTEM_PROMPT`, and the orchestrator invokes them with only `components.ad_llm`/`dis_llm`, so a condition builder could not select a prompt without touching one of the two out-of-scope modules. Scope relaxed (user-approved) to a **minimal additive change**: (a) keyword-only `system_prompt: str = AD_SYSTEM_PROMPT` / `= DIS_SYSTEM_PROMPT` on the two stage fns (used instead of the constant in the messages list); (b) `ad_system_prompt`/`dis_system_prompt: str` fields + `build()` kwargs on `PipelineComponents`, defaulting to the same v2 constants; (c) one-line-per-call-site read in the orchestrator (`system_prompt=components.ad_system_prompt`/`dis_system_prompt`). All defaults are the frozen v2 constants, so every existing caller/test renders byte-identical prompts (criterion 1 regression guard). Selection happens in the condition builders from `dataset_version` (via `run_evaluation.resolve_dataset` → `build_condition3`), as the spec intends. | Presented to the user as options (additive-param+plumbing vs. an in-scope LLM wrapper vs. defer); they chose the additive path. It is the smallest change that satisfies "selection in the condition builders from `dataset_version`" AND "v2 byte-identical", given the container already carries per-run version state (`schema: SchemaRepr`) — `ad_system_prompt` is the same kind of state. The alternative (wrapping the `ad_llm`/`dis_llm` to rewrite the system turn) is more fragile and hides the prompt from the stage's own contract. The entity registry uses the identical additive pattern (`EntityCache.load(..., *, registry=None)` + `PipelineComponents.build(..., entity_registry=None)`), both defaulting to the frozen v2 `ENTITY_REGISTRY`. |
| 2026-07-07 | 7.1/7.2/7.3 (id format, user-directed) | The 7.1 spec's example ids carry a version infix (`PER-V3-001`, `INC-V3-006`, `Q-V3-001`, …), and 7.1/7.2 authored all v3 artefacts to that format | Stripped the `-V3-` infix from **every instance id** in the v3 artefacts (`PER-001`, `INC-006`, `CASE-001`, `LOC-001`, `VEH-001`, `DEV-001`, `Q-001`…`Q-120`) via the exact rewrite `(PER|INC|CASE|LOC|VEH|DEV|Q)-V3- → \1-` across `data/SyntheticPoliceKG-v3.cypher`, `data/benchmark-v3.json`, `data/benchmark-v3-skeleton.json`, the v3 AD few-shot prompt text + comments (`pipeline/ambiguity/prompts.py`), comments in `pipeline/disambiguator/prompts.py`, the `tests/test_v3_integration.py` few-shot assertion, and `scratchpad/gen_benchmark_v3.py`. Verified: 0 residual `-V3-`, 120 unique `Q-\d{3}` ids, every id referenced by a benchmark Cypher exists in the KG file, 7.2 offline gates (distribution/hops/coverage) green, DB-free tests pass | The file names (`*-v3.*`) and config (`dataset_version`/`results_tag`) already version the artefacts; an id-level infix is redundant noise in every gold query and every prompt/entity render, and inflates tokens the QG must emit exactly. User-directed 2026-07-07 ("caught after 7.1 — my bad"). **Note:** v3 question ids now numerically overlap v2's (`Q-001`…) — never mix per-question records across versions without carrying `results_tag`. Live-DB consequences: the Kaya v3 graph must be reloaded (`kaya/70_neo4j_v3.slurm`, default wipe+reload) and the 7.2 harness re-run to regenerate `results/benchmark_v3_validation.json` (the committed job-1010692 report predates the rename); 7.3's deferred live criterion 4 is unaffected in substance. |
| 2026-07-07 | 7.1/7.3 (Case name split, user-directed) | Case `case_name` stored the composite `'Operation Ironside — Northbridge Series'` (7.1 seed); v3 Case schema = `case_id, case_name, status, opened_date`; `ENTITY_REGISTRY_V3` Case `name_props=['case_name']` | Split each Case name into `case_name` (bare operation, e.g. `'Operation Ironside'`) + a new `descriptor` property (`'Northbridge Series'`, `'Drug Network'`, `'Financial Crimes'`, `'Midland Taskforce'`) in `data/SyntheticPoliceKG-v3.cypher`; added `descriptor` to Case props in `build_pole_v3_schema_repr()` (`pipeline/schema.py`) and to Case `name_props` in `ENTITY_REGISTRY_V3` (`pipeline/entity_lookup/registry.py`) so the descriptor-named questions (Q-022 "Midland Taskforce case", Q-059 "Financial Crimes case") still fuzzy-resolve in Entity Lookup. v2 registry/schema untouched (frozen). No gold query filters on the composite string (only `RETURN c.case_name` in Q-005/031/090, self-consistent under live execution), so no benchmark Cypher edited. | Removes a confound in the C3-vs-C2 comparison: with the composite name, the naive exact-match commit `{case_name:'Operation Ironside'}` returned **empty** on 7 case-name items (Q-004/021/022/023/036/059/096), scoring `wrong_result` for C1/C2 while C3's Entity Lookup resolved the composite by fuzzy match — inflating the disambiguation gap with an entity-resolution effect on items (some unambiguous) that don't test disambiguation. Short case_name makes the direct commit succeed for all conditions; `descriptor` preserves the human tag as a second resolvable handle. User-directed 2026-07-07. **Live-DB consequence:** included in the same required Kaya reload + 7.2 harness re-run as the id de-versioning (decisions-log 2026-07-07 id row); 3.2 Case `display_name` now renders `'Operation Ironside'` (from the first name_prop) rather than the composite — no test asserted the old value. |
| 2026-07-07 | 7.2 (benchmark gold corrections, user-directed "Group A") | Five items had a `cypher_default` that contradicted the question's plain reading or an incomplete interpretation set (audit 2026-07-07) | Corrected the defaults/interps of 5 v3 benchmark items (count-neutral; ambiguity_type + num_hops unchanged, offline gates 2/7/8 re-verified green): **Q-091** "How many…" default now returns `count(i)` (was the list; list demoted to interp); **Q-092** "recent incidents" (plural) default now all-newest-first (was `LIMIT 1`); **Q-111** "Where **did** Kim live?" default now the previous address (`active:false`, was current); **Q-088** "did **contact**" default now outgoing `-[:CALLED]->` (was undirected, i.e. duplicated the "both" interp); **Q-067** "Where does James live?" default broadened to `… OR p.alias='James'` + added a 4th interpretation for Michael Nguyen (PER-015, alias 'James') to mirror Q-069's alias handling. Only these items' `cypher_default`/`default_interp`/`interpretations` changed; no KG edit. | The defaults are the EX ground truth; a default that contradicts the question penalises a correct model (Q-091/092/111/088) or, for Q-067, silently drops the seeded alias-collision interpretation from the ambiguous set. Group B (weak/contradictory intent *labels*, blocked by the exact-20-per-type gate) and Group C (Q-055/060 role-union) deliberately deferred — user took "just A for now" 2026-07-07. **Live-DB consequence:** rides the same Kaya reload + 7.2 harness re-run as the id/case-name changes; gates 5 (nonempty) and 6 (pairwise-distinct) must re-confirm the 5 new defaults/interps live — Q-067's 4 addresses (LOC-016/003/013/006) and Q-088/091/092/111's readings are distinct/non-empty by construction but unverified until the harness runs. |
| 2026-07-07 | 7.4 | `enumerate_v3_patterns()` should "walk `build_pole_v3_schema_repr()` and emit every valid 1–3-hop schema pattern"; "with 11 relationship types this is a few hundred patterns" | Implemented as a deterministic depth-≤3 graph walk from every node label, both traversal directions per directed edge (one undirected entry for `ASSOCIATED_WITH`), disallowing a relationship type from being reused within one walk (prevents trivial there-and-back loops), plus one extra `{active:true}` variant per temporal hop present. This enumerates **840** patterns (all 11 rel types covered; every one validates against `parse_schema_pattern`) — more than the spec's "few hundred" estimate because `Person` has 8 outgoing/incoming edge choices and 3-hop walks through it combinatorially branch (e.g. bouncing between two Person→Incident role edges is itself a *designed* schema-ambiguity axis of this thesis, so it isn't pruned) | The spec states a goal ("exhaustive", "deterministic") and a rough magnitude, not an exact count or algorithm — genuinely under-specified, so implemented the literal reading (exhaustive walk) rather than guessing at an arbitrary pruning rule with no stated criterion. 840 is still "hundreds", not thousands, and every pattern is schema-valid by construction (unit-tested). Row-volume control (the spec's actual hard target, "~1,500–2,500 kept rows") is handled separately by the new `max_rows` config knob below, decoupling pattern-space size from output-row count. |
| 2026-07-07 | 7.4 | "Question synthesis — for each pattern, generate NL questions two ways: Templates … slot-filled … LLM paraphrase" (no algorithm specified for composing a multi-hop pattern's question) | For 1-hop patterns each of the 11 relationship types gets ~2 hand-written forward/backward phrasings (`_REL_CLAUSE`) that read as a direct question. For 2–3-hop patterns, reused the *same* per-relationship-type phrasing table as a recursive noun-phrase wrap (leaf entity → nested referring expression → terminal-attribute question), rather than authoring a separate full-question template per multi-hop relationship-type combination (combinatorially infeasible and not what "hand-written per relationship type" asks for literally). Added a `max_rows` config knob (deterministic shuffle+slice after the disjointness gate, default 2000 in `config/pole_sft_data.yaml`) since the exhaustive pattern list × templates × paraphrases overshoots the spec's ~1,500–2,500 target by a wide margin (verified: even with 0 paraphrases, template generation alone yields ~3,000 kept rows) | Multi-hop NL composition has no single obviously-correct algorithm and the spec explicitly designs the LLM-paraphrase stage to smooth over stiff/mechanical template phrasing, so a recursive compositional scheme (verified to read as grammatical, if occasionally literal, English — e.g. "What make is the vehicle currently owned by the person who lives at Northbridge?") is a defensible reading that reuses one hand-authored table instead of an unbounded one. `max_rows` is additive (defaults to no cap when absent) and doesn't change the two-key jsonl contract; per-relationship-type coverage stays far above the ≥30-row acceptance floor after trimming (verified: minimum observed coverage ≈155–226 rows even at max_rows=2000, vs. every type contributing ≥7% of the pre-trim pool). |
| 2026-07-10 | 7.4/2.2/2.3 (Kaya ops, measurement bug) | `experiments/probe_utils.py::covered()` — the Cov@5 guardrail every gate (2.2 sweep, 2.3 probe, 7.4's `sl_v3_cov_check.py`, 7.5's future G1) relies on — compared beams to gold via `_normalise_pattern` (whitespace/var-name strip only) | Added `_canonical_pattern()` + rewrote `covered()` to compare **structurally**: reuses `pipeline/schema_linker/postprocessing.py`'s `parse_schema_pattern`/`_rel_element`/`_has_active` (the same parser the live SL postprocessing uses) to build a direction-canonicalised, dangling-`--`-connector-invariant signature per pattern, instead of raw normalised text. `_normalise_pattern` itself (used separately by `entropy_probe.py` for beam-diversity entropy, a different purpose) is untouched. 3 new regression tests (`tests/test_probe_utils.py`); full suite 289 | First `sl_v3_cov_check.py` run on the refreshed `sl_adapter_v3` (job 1026051) measured sample Cov@5=0.240 (n=25) — well under 7.5's G1≥0.60 bar. Raw-beam inspection (job 1026080, after adding per-item beam/gold printing to the harness) showed most "misses" were beams that got the schema link exactly right but anchored from the opposite node (e.g. gold `(p:Person)-[:SUSPECTED_OF]->(:Incident)` vs beam `(:Incident)<-[:SUSPECTED_OF]-(p:Person)`) or carried the already-known dangling `--` suffix (decisions-log 2026-06-15) — both of which the live SL postprocessing already treats as equivalent/ignorable, so `covered()` was measuring a stricter standard than the deployed pipeline actually applies. Re-run (job 1026181) with the fix: **sample Cov@5 = 0.640** — clears G1. Remaining misses are genuine: `schema`-type items still 0/5 (the adapter chains multiple relationship types into one beam instead of proposing them as separate candidates, or picks a wrong relationship outright), and a few `entity` items miss a `{active:true}` temporal qualifier the gold requires — the fix does not paper over either. **Same bug affected the frozen v2 2.2 sweep's Cov@5=0.000** (job 511000): offline re-scoring the cached `results/beams_pole.jsonl` with the fixed `covered()` gives 0.152, not 0.000 — confirmed, not applied (v2 artefacts frozen per `ai-workflow-rules.md`§6, never edited). The separate frozen **3.1 criterion-6 figure (Cov@5=0.208, EM@1=0.144, job-less/one-off 2026-06-15 script, `docs/results-walkthrough.md`)** already used schema-aware structural comparison via `parse_schema_pattern`, so it is NOT expected to share this bug; a best-effort reconstruction using the same cached beams gave 0.152 (not 0.208) but the original script was never committed/preserved, so the gap can't be reconciled and is left unresolved — **not treated as evidence the 0.208 figure is wrong**, just noted as irreconcilable. User-directed 2026-07-10: leave all v2/frozen numbers exactly as recorded; only `covered()` (and hence future/v3 measurements) changes. |
| 2026-07-11 | 7.4 (schema-type SFT gap) | Question synthesis §2: "Templates: hand-written per relationship type (~3–5 surface forms each) … so questions look like benchmark questions." All `_REL_CLAUSE` forms are relation-*specific* ("the witness of X"). | Added a dedicated **single-hop schema-ambiguity register** to `pipeline/pole_sft_data.py` (`_SCHEMA_CONNECTOR_FORMS` + `_schema_ambiguous_pairs`/`_schema_connector_np`/`_schema_ambiguous_rows`, `schema_connector_samples_per_form` config knob, default 30). For every node pair carrying >1 relationship type (data-driven — in v3 exactly Person→Incident: {SUSPECTED_OF, WITNESSED, VICTIM_OF, INVESTIGATES}) it emits vague-connector questions ("people connected to {incident}", "who is involved in {incident}", … incl. "people associated with {incident}") as **single-hop** rows, generated *outside* the recursive `_chain_np` walk, with the role relation cycled round-robin so the register is paired with all four role types across the corpus. 240 raw rows → 117 kept in the final 2000-row set, balanced ~26–33/role. Adapters retrained (jobs 1028720 SL / 1028721 QG, continue-SFT from v2, template-only data matching the 0.640 baseline's mode). 3 new tests (`tests/test_pole_sft_data.py`, suite 9→12). | 7.4 criterion-4 intrinsic check found `schema`-type items 0/5 (sample Cov@5=0.640 overall): the adapter never saw the deliberately-vague benchmark register, so at inference it chains all four Person→Incident role types into one compound multi-hop beam or reuses a wrong relation (the Q-045 "associated with" collision), never emitting the clean single-hop `(Person)-[:role]->(Incident)` beam the schema gold set requires (`covered()` is an OR — one such beam suffices). Teaching the register at single-hop with round-robin role assignment (a) makes single-hop role edges the natural beams and (b) spreads the SL relation-slot mass so diverse beam search surfaces multiple role candidates — what 7.5's AD/disambiguator also need. Generated outside `_chain_np` on purpose: an **earlier** fix that added a generic form to `_REL_CLAUSE` leaked into the recursive walk (527 "connected with" rows, 451 of them 3-hop), *reinforcing* the chaining failure and dropping Cov@5 0.640→0.520 (that regressed adapter — formerly `checkpoints/mistral7b/{sl,qg}_adapter_v3_regressed_connector_fix_20260710` — was **deleted 2026-07-12** as a known-worse dead end; its number and root cause are recorded here, so the weights held nothing to re-derive). The 0.640-baseline v3 adapters are preserved at `…_v3_baseline_cov0640`. Not a spec contract change — an enhancement realising §2's "look like benchmark questions" intent; recorded here because it materially changes the generated dataset. **Result (job 1028730, 2026-07-12): worked — sample Cov@5 0.640→0.800, `schema` 0/5→5/5**, and the beams now propose the four Person→Incident role types as *separate* single-hop candidates (the interpretation diversity 7.5 needs), with Q-045's "associated with" collision resolving to INVESTIGATES/VICTIM_OF instead of ASSOCIATED_WITH. One intent item regressed (Q-082 over-chains an extra SUSPECTED_OF hop, plausibly from the denser role-edge training) — single sample item, deferred to 7.5's full G1 to confirm vs noise. |
| 2026-07-12 | 7.5 / 5.4 / 2.2 / 2.3 (adapter versioning) | 7.5 says its gates are "all now version-aware via 7.3" and G4 just runs `run_evaluation` with `dataset_version: v3`. | 7.3 only versioned the schema / entity registry / AD-Dis prompts — **not the adapter path**, which stayed hardcoded to the frozen v2 `checkpoints/{key}/{sl,qg}_adapter` in `run_evaluation.py::_resolve_model_specs`, `diversity_penalty_sweep.py::_build_llm`, and `entropy_probe.py`. So G2/G4 would have silently run v3 substrate against the pre-schema-fix v2 adapters (no error). Added `pipeline/schema.py::adapter_suffix_for_version(version)` (`'' `for v2, `'_v3'` for v3 — single source of truth) and threaded it: `resolve_dataset` returns `adapter_suffix`, `_resolve_model_specs` takes it and builds `{sl,qg}_adapter{suffix}`; the two probe scripts derive it from their existing `--dataset_version` flag. `kind: api` entries are unaffected (no adapters). 4 tests added (`tests/test_v3_integration.py`). | Isolating the fix here (not editing 7.5's spec) because it's a real code gap the spec's "version-aware via 7.3" claim glossed over. Deriving the suffix from `dataset_version` keeps v2 byte-identical (empty suffix) while a v3 run auto-selects the substrate-matched adapters — no new flag to remember, matching 7.3's config-driven pattern. Verified: `config/pipeline.yaml` (`dataset_version: v3`) now resolves SL/QG to `checkpoints/mistral7b/{sl,qg}_adapter_v3`. **7.5 note:** the G2 Kaya runners must still pass `--dataset_version v3` (+ `--results_tag v3`) so the sweep/probe pick up `_v3`; the code now honours it, but the flag isn't defaulted to v3. |
| 2026-07-12 | 7.5 G2 (v3 sweep + probe findings) | The 2.2 sweep picks the `diversity_penalty` with best entropy AUC under the Cov@5 ≥ 0.85 guardrail, "falling back + flagging if no penalty qualifies"; the 2.3 probe's prior (v2) finding was that the three scorers are FLAT (spread ≤ 0.01), so 3.3 may use the simplest without loss. | Ran G2 on the v3 substrate with `sl_adapter_v3`. **Sweep (job 1032339):** dp ∈ {0.2,0.5,1.0} → Cov@5 {0.683, 0.692, **0.725**}, AUC {0.649, 0.649, **0.682**}. **No penalty cleared Cov@5 ≥ 0.85**, so `select_penalty` took its documented fallback and flagged it (this record) — but unlike v2 (all three at Cov@5 0.000, fallback picked least-bad AUC), here **dp=1.0 wins on BOTH Cov@5 and AUC**, a clean choice. Locked `diversity_penalty: 1.0` into `config/schema_linker_inference.yaml` (was v2's 0.2 — the config is keyed by model_key, not version, so this overwrites the mistral7b entry; v2 results are frozen artefacts, not re-derived, so no v2 impact). **Probe (job 1032344, at dp=1.0):** headline h_norm_candidates AUC = **0.689 > 0.62** (criterion #6 met; v2 was 0.592), and **ablations are NOT flat (spread 0.028 > 0.01)** — the probe's `build_finding` explicitly emits "record this in decisions-log.md" for the non-flat case. | Two reportable v3 findings, both the *opposite* of v2 and both dissertation-relevant. (1) The fallback firing is expected (0.85 is a hard bar the substrate doesn't yet meet at 0.725), but the selection is unambiguous, not a least-bad compromise. (2) The non-flat ablation is the direct confirmation of the v2 post-mortem's correlation hypothesis: v2's flatness was *because* the beams had no diversity (Cov@5 0.000), so no scoring function could separate ambiguous from unambiguous; v3's beams genuinely spread across schema candidates, so the scoring function now discriminates and the headline signal clears the baseline. h_norm_candidates remains the best scorer, so 3.3's choice of normalised entropy is still correct — the finding strengthens, not changes, the 3.3 design. |
| 2026-07-12 | 7.5 G4 (found: 4.2 hardcoded table labels) | `format_metric_tables` (4.2) renders Table 1 header "Overall (125)", Table 2 "Ambiguity-specific (50)", and per-type rows "(n=13/11/11/15)". | Left the code as-is (workflow rule 2 — report an earlier step's gap, don't silently fix it mid-step) but recorded it: those parenthetical counts are **hardcoded v2 constants**. The v3 run is 120 items / 80 ambiguous / **20 per type**, so the labels in `results/tables_mistral7b_v3.md` are wrong. The metric *values* are correct (computed over the real v3 denominators — e.g. ambiguous EA 0.275 = 22/80 and schema EA 0.30 = 6/20 are integer only under the v3 counts, not the v2 ones). | Purely cosmetic (display strings, not computation), but it would mislead a reader of the v3 tables, so it is called out prominently in `docs/results-walkthrough-v3.md` (which states the true denominators) and flagged for 4.2. Not fixed under 7.5 because 4.2 is a `done` step outside this step's module scope; the correct fix is to thread the benchmark's actual `n_total`/`n_ambiguous`/per-type counts into the header strings, a 4.2 edit. |
| 2026-07-13 | 4.2 (dynamic table denominators — the fix for the row above) | `format_metric_tables` hardcoded "(125)"/"(50)"/"(n=13/11/11/15)" in the Table 1/2 headers and per-type row labels; `MetricBundle` stored only ratios, discarding every denominator `compute_metrics` had computed. | Made the labels data-driven. Added three fields to `MetricBundle` — `n_total`, `n_ambiguous`, `per_type_n: dict` — populated from values `compute_metrics` **already** had in hand (`n`, `len(ambiguous)`, and a new `per_type_n[t]=len(sub)` captured in the existing per-type loop; no extra iteration, no caller plumbing). `format_metric_tables` reads them off a reference bundle (`bundles[present[0]]` — denominators are identical across conditions, same question set) and renders `f"Table 1 — Overall ({ref.n_total})"`, `f"… Ambiguity-specific ({ref.n_ambiguous})"`, and per-type labels `f"EA — {t} (n={ref.per_type_n[t]})"` via a comprehension (with `t=t` default-arg binding so each lambda captures its own type). Signature of `format_metric_tables(bundles)` unchanged. Also refreshed the stale prose counts in the `MetricBundle`/`compute_metrics` docstrings ("all 125"→"all n_total", "50 ambiguous"→"n_ambiguous"). `tests/evaluation/test_metrics.py::test_format_metric_tables_three_tables_with_em_dash` updated to assert the derived counts ("(2)"/"(1)"/"n=1"/"n=0") instead of the old constants — it now guards the dynamic behaviour. Full metrics suite (10) green. | Single source of truth: the count in each label is literally the denominator the ratio was divided by, so they can never drift again (the exact v2→v3 breakage). Reused values already local to `compute_metrics` rather than re-deriving in the formatter or threading benchmark metadata through `run_evaluation`, so the change is minimal and additive. Done as its own commit **after** the user commits 7.5 (user-directed 2026-07-13); `results/tables_mistral7b_v3.md` was already hand-corrected to 120/80/20 when the label bug was found — regenerating it from this fixed code would reproduce the same corrected numbers. |
| 2026-07-14 | 8.1 (SL API prompt variant — the documented exception to 1.2's "do not fork it") | 1.2/`ai-workflow-rules.md`: the shared train/inference completion prompt (`SL_SYSTEM`/`build_sl_prompt`) is **not** forked. | Added a **separate, additive** instruct-model prompt in `pipeline/schema_linker/prompts.py` — `SL_SYSTEM_API` (states the output contract explicitly: one bare pattern line; labels carry a leading colon `(:Person)`; bracketed rel types; the only permitted property filter is `{active: true}` on a temporal edge; no entity values / WHERE / RETURN / prose / fences / extra hops) + `build_sl_prompt_api(question, schema_block)` (same schema-block+question layout as `build_sl_prompt`, plus 3 authored v3-grounded few-shots: single-hop role edge, temporal `{active:true}` variant, 2-hop chain). `SL_SYSTEM`/`build_sl_prompt` are left **byte-identical** (asserted in a test) — the fine-tuned Mistral's training-data contract is untouched. Selection is keyword-only `prompt_style` on `SchemaLinker` and `probe_utils.generate_beams` (default `"completion"`; `"instruct"` → the API variant). | This is 8.1's locked decision #3 (user-confirmed 2026-07-13) and the 5.5 "may need a few-shot variant" risk realised. The reverted Claude attempts showed a capable instruct model answers SL correctly but in its own house style (`(Person)` w/o colon, extra `{crime_type: …}`, markdown wrapping), which the Mistral-tuned parser scored ~all wrong (Cov@5 0.067). Telling the model the contract is one half of the fix (the tolerant parser is the other); forking would corrupt the training-data prompt, so the variant is strictly additive and the shared prompt stays frozen. Wiring `prompt_style` from the registry `kind: api` lands in 8.2 — 8.1 only exposes the parameter. |
| 2026-07-14 | 8.1 (canonical-signature entropy changes the frozen-cache probe AUC 0.689→0.667 — locked decision #2 vs acceptance criterion #2 conflict) | Locked decision #2: entropy is computed over canonical `_canonical_pattern` structural signatures (not raw strings) **everywhere** `probe_utils.normalised_entropy` is used. Acceptance criterion #2: that exact change, rescored offline on the frozen `results/beams_mistral7b_v3.jsonl`, must leave the 7.5 G2b probe **AUC unchanged at 0.689** (with Cov@5 0.725 / EM@1 0.667). | Implemented decision #2 faithfully: `normalised_entropy` now counts distinct `_entropy_signature(b)` — `_canonical_pattern(b)` (the same direction-/dangling-`--`-invariant signature `covered()` uses), with a junk guard falling back to the raw string when the signature is empty (unparseable garbage stays distinct). Ran the offline rescore guard from the frozen dp=1.0 cache. **Cov@5 (0.725), EM@1 (0.667) and every per-type cell are byte-for-byte unchanged** (the parsing half of the guard passes exactly). **The headline probe AUC moved 0.689 → 0.667** and `h_norm_beams` 0.682 → 0.667 (now equal to `h_norm_candidates`, since both canonicalise). User-directed 2026-07-14: **accept 0.667 and proceed** (chosen over an API-only fork, which decision #2 forbids). | Criterion #2's "0.689 unchanged" was an incorrect prior: 51/120 items in the cache carry **superficial** variation the old raw-string entropy counted as distinct outcomes — the dangling `--` suffix (decisions-log 2026-06-15) on some-but-not-all beams, direction flips (`(:A)<-[:R]-(:B)` vs `(:B)-[:R]->(:A)`), garbage tails. Collapsing exactly those is the *entire purpose* of decision #2 (so 8.3's temperature-sampled completions aren't entropy-inflated). So **0.689 was inflated by the `--` formatting artefact; 0.667 is the true structural-entropy AUC** — and it still clears the 0.62 baseline, so 7.5 criterion #6 ("post-SFT AUC > 0.62") still holds. Two consequences recorded: (a) `h_norm_candidates` and `h_norm_beams` now compute identically, degenerating 2.3's three-signal ablation to two values and superseding 7.5 G2b's recorded "spread 0.028" — the 2.3 test premise `test_h_norm_beams_treats_raw_surface_strings` was updated to the new canonical behaviour; (b) the 7.5 G2b headline AUC under the 8.1 entropy basis is 0.667, not 0.689 (the `results/entropy_probe_mistral7b_v3.json` local artefact is gitignored, not committed). Not a defect — the change does exactly what it was locked to do; the frozen v2 numbers are untouched (v2 uses the pre-8.1 code path only via its own frozen artefacts). |
| 2026-07-14 | 8.2 | Modules list names `experiments/run_evaluation.py` (prompt_style wiring only) but not the condition builders | Also added a keyword-only `prompt_style: str = "completion"` to `experiments/build_condition2.py` and `build_condition3.py`, threaded into their `SchemaLinker(...)` construction | Contract #3 requires run_evaluation to pass `prompt_style="instruct"`/`"completion"` (by registry `kind`) *to the SchemaLinker*, but run_evaluation never constructs the SchemaLinker — the condition builders do. Threading the derived value through the two SL-bearing builders is the only way to honour the contract; it is necessary plumbing, not a behaviour change. The `"completion"` default keeps the local fine-tuned path byte-identical (acceptance #3), and `build_condition1` (no SchemaLinker) is untouched. |
| 2026-07-14 | 8.3 (G2a temperature-sweep fallback for `claude-sonnet`) | G2: sweep T∈{0.3,0.7,1.0}, lock `decoding[claude-sonnet]` under a Cov@5 ≥ 0.85 guardrail, "documented fallback if none qualifies — as 7.5 G2a". | No temperature cleared the 0.85 guardrail (all three sat at Cov@5 ≈ 0.66), so selection took the documented fallback = best Cov@5/AUC trade-off → **`temperature: 0.3, k: 5`** (Cov@5 0.667, the highest; entropy AUC 0.525) locked in `config/schema_linker_inference.yaml::decoding[claude-sonnet]`. Sweep artefact `results/diversity_penalty_sweep_claude-sonnet_v3.json` (job 1055791). | Mirrors 7.5 G2a's fallback path exactly (there no dp cleared 0.85 either). Cov@5 is the guardrail axis, so the fallback maximises it; 0.3 also has the lowest hallucination (0.000, tied) and the flattest-but-honest entropy. The three temperatures are within noise on every axis (Cov@5 spread 0.008, AUC spread 0.012), so the choice is not load-bearing — the SL decodes near-deterministically regardless (see the criterion-#6 row below). |
| 2026-07-14 | 8.3 (criterion #6 NOT met for `claude-sonnet`: entropy AUC 0.525 < 0.62) | G2 criterion #6 bar: post-SFT/canonical entropy AUC > 0.62 (v3-mistral 0.689). | Recorded the honest miss: at the locked T=0.3 the 2.3 entropy probe (rescored CPU-only from G1's cache `results/beams_claude-sonnet_v3.jsonl`, job 1055791) gives headline AUC **0.525**, flat across all three signals (spread 0.000). Did NOT stop — G2 is a sweep+score step (only G1's 0.60 is a hard go/no-go); noted for the scorecard and proceeded to G3/G4. | An instruct model at low temperature samples near-deterministically: the G0.5 smoke showed all 5 SL samples byte-identical on every question, so the candidate distribution collapses to one canonical pattern and the *secondary* entropy signal has almost nothing to discriminate. This is exactly the locked design's premise — **LLM self-assessment is PRIMARY, entropy SECONDARY** (project-overview §7) — and the flip side of the strong direct AD reasoning the smoke showed (Q-041 correctly `is_ambiguous:true, schema` with a schema-ambiguity rationale). The canonical-pattern entropy (8.1) is what makes 0.525 *honest* rather than inflated for a chatty model; unlike v3-mistral (whose diverse-beam SL genuinely spreads → AUC 0.689), Claude puts its interpretation diversity into a single multi-edge completion, not across samples. Not a defect; the dissertation reports #6 met by v3-mistral, not by the API model, and reads the API model's contribution through detection (G3) + end-to-end (G4) instead. |
| 2026-07-14 | 8.3 (G0.5 caught an 8.2 backend bug — Anthropic `system=null` on user-only prompts) | 8.3 is evidence-only; all code exists after 8.1/8.2. But G0.5's raw-output smoke exists precisely to "catch a format/environment problem for cents" before the multi-hour chain. | Fixed a one-line live-API bug in `pipeline/llm/anthropic.py::generate_chat` (0.2 module, decoding corrected by 8.2): it sent `system=system or None`, i.e. `system=None` whenever the prompt had no system turn (the SL sampling path and the zero-shot QG path both send a user-only prompt). The live `claude-sonnet-4-6` API rejects `"system": null` with `400 invalid_request_error: "system: Input should be a valid array"`, so **the very first SL call of G0.5 (job 1055747) crashed**. Now `system` is built into a kwargs dict and included **only when non-empty** (omitted, never null). Added regression test `tests/llm/test_anthropic.py::test_user_only_prompt_omits_system_not_null`; backend suite 8/8, full offline suite re-green. Verified against the live API directly: `system=None`→400, omitted→200 (both a bare string and a content-block array also 200 — the bug is null specifically). | Confirmed with a ~$0.001 live probe before touching code: `system=None` is the only failing form (string and array both work), so the minimal correct fix is to omit the parameter, not to switch to content-block arrays. This is squarely within G0.5's stated purpose (catch format/env problems cheaply). Not silently absorbed — recorded here + in the 8.3 tracker row + reported to the user; the local fine-tuned path is unaffected (HF backend untouched, and Anthropic with a system prompt was already correct). Re-ran G0.5 after the fix. |
| 2026-07-16 | 8.3 follow-up (QG output-contract fix, no new phase spec — scoped as a decisions-log-only follow-on, user-directed) | Post-mortem on why `claude-sonnet` EX stayed at the floor despite high validity (Table 1: syntax 97–100%, schema 100%, KG-valid 63–83%). Grepping the G4 job log (`logs/t2c_run_eval_api_v3_1055879.err`) for Neo4j "property does not exist" notifications found the QG hallucinating generic property names (`type` 486×, `description` 338×, `id` 221×, ... — see `results/property_hallucination_claude-sonnet_v3.txt`) despite the real names (`crime_type`, `incident_id`, ...) never actually being shown to it: `pipeline/query_generator/generator.py` called `schema.to_prompt_string(include_properties=False)` unconditionally, on every backend, since step 3.6's original design — the local fine-tuned QG only knows the real names because Phase 7.4's POLE-specific SFT baked them into its weights. Separately, the QG prompt (`build_qg_prompt`) has zero few-shot examples on any backend, unlike SL/AD/Dis, which all carry authored examples for the API path — so the API QG had no worked example of the gold's tight `RETURN p.name`-style projection, producing `RETURN p, r, i` whole-node answers instead. | Added a backend-conditional instruct prompt, mirroring the 8.1 SL pattern exactly: `pipeline/query_generator/prompts.py` gained `QG_SYSTEM_API` (explicit output contract: never return whole nodes/relationships, use `RETURN DISTINCT` for duplicate-prone questions, never invent a property name outside the schema's Properties list) + `_QG_FEW_SHOT_API` (four examples reused **verbatim** from `data/pole_qg_train.jsonl` rows 44/51/31/46 — not hand-authored, so the API model learns from the same curriculum the fine-tuned model trained on) + `build_qg_prompt_api(question, schema_block, committed_pattern, error_feedback)`. `QueryGenerator.__init__` gained a keyword-only `prompt_style: str = "completion"` (mirrors `SchemaLinker`): `"completion"` (default) is byte-identical to pre-fix — `include_properties=False`, `build_qg_prompt`, protects the local fine-tuned model's train/inference prompt match; `"instruct"` gets `include_properties=True` (the properties block the SL already receives) + `build_qg_prompt_api`. Threaded `prompt_style` into `QueryGenerator(...)` in `build_condition2.py`/`build_condition3.py`, alongside the existing `SchemaLinker(..., prompt_style=prompt_style)` call — **`build_condition1.py` deliberately left untouched**, since `build_qg_prompt`'s own docstring states the zero-shot format is kept comparable to the published Ozsoy/text2cypher checkpoint results, an invariant this fix must not break. `experiments/run_evaluation.py` needs no change — it already derives `prompt_style` from registry `kind` and passes it to both builders. Added `tests/query_generator/test_prompts_api.py` (mirrors the SL one: byte-identical-shared-prompt guard, output-contract assertions, few-shot content, error-feedback carry) + 2 new cases in `tests/query_generator/test_generator.py` (default vs instruct `prompt_style` behaviour) + fixed `tests/test_condition_builders.py::_FakeQueryGenerator`, which didn't yet accept the new kwarg. **Full offline suite: 332 passed.** | This is a *fix*, not yet a *measurement* — no live API run has been made against the new prompt. The natural next step is a fresh G0.5 (~$0.10, eyeball the new QG output on a sample) then a full G4 re-run (~$30–60) on `claude-sonnet`, using a **new `results_tag`** (e.g. `v3-qgfix`, set in `config/pipeline.yaml`) so the recorded `results/tables_claude-sonnet_v3.md`/`metrics_claude-sonnet_v3.json` (job 1055879, the frozen Phase 8 scorecard artefacts) are never overwritten — explicit user requirement. Scoped as a decisions-log entry only (not a new numbered phase spec), user-directed 2026-07-16: this is a bounded, additive prompt/config change to an existing component (mirrors the precedent of the 8.3 `anthropic.py` G0.5 fix), not new architecture. |
| 2026-07-16 | 8.3 follow-up (v2→v3 Cov@5 attribution ablation: schema redesign vs. Phase 7.4 in-domain SFT) | User raised a methodological concern: v2→v3 is documented in `docs/results-walkthrough-v3.md`/`CURRICULUM.md` as a "one-substrate-change design" that "isolates the substrate" — but 7.4 also introduced a brand-new in-domain-SFT training phase that never existed for v2, so the recorded Cov@5 jump (0.208→0.725) could be mostly/entirely a training-data effect, not a substrate effect, making the "isolates the substrate" framing an overclaim. | Ran the missing ablation using the EXISTING pre-7.4 checkpoints (`checkpoints/mistral7b/sl_adapter`, generic-Ozsoy-only, untouched per 7.4's own "v2 adapters untouched" note) against the v3 schema/benchmark — no new training needed. Extended `kaya/g1_sl_coverage_v3.py` with `--adapter_suffix`/(sentinel `""`) and a fresh `--no_adapter` flag; ran three points: **no adapter at all (job 1061290): Cov@5 = 0.000** (raw base model never learned this prompt's completion format); **generic-Ozsoy-only, v3 schema (job 1061277): Cov@5 = 0.483**; **+ 7.4 in-domain SFT (recorded): Cov@5 = 0.725**. Full table: v2/generic-only=0.208, v3/none=0.000, v3/generic-only=**0.483**, v3/+7.4=0.725. Separately, extended `kaya/ad_probe_v3.py` with the same override + a new `90_ad_probe_v3_ablation.slurm` (isolated Neo4j data dir per run, `$GROUP/neo4j_data_v3_ablation{suffix}`, to avoid colliding with a concurrently-running v3 Neo4j job — first attempt (jobs 1061502/1061503) failed exactly this way, "Neo4j web server failed to start", fixed by giving each ablation job its own data directory) — ran the same 10-question stratified AD probe (7.5 G3's own methodology) with the SAME base (non-instruct) AD, varying only the SL adapter: **no adapter (job 1061546): 3/10, TP=1 FP=0 FN=7 TN=2**; **generic-only (job 1061545): 4/10, TP=2 FP=0 FN=6 TN=2** — **identical to the recorded full-SFT v3-mistral G3 probe** (job 1032391, also TP=2 FP=0 FN=6 TN=2). | **Both halves resolve cleanly, in opposite directions.** (1) Cov@5 attribution: the schema redesign alone contributes real, substantial value (0.208→0.483 on generic-only training, more than doubling coverage purely from removing v2's lexical-noise relationship types), and 7.4's in-domain SFT contributes a further, separate increment on top (0.483→0.725) — so it is genuinely a two-factor result, not one factor masquerading as the other; the "one-substrate-change design" framing is corrected to name both factors rather than implying training was held constant. (2) Detection: recall is **flat** across Cov@5 0.483 and 0.725 with the same base AD (identical TP/FP/FN/TN), and only modestly worse at Cov@5=0.000 — i.e. **detection recall is gated by which model reasons over the evidence (base 7B vs. instruct/API), not by schema-linker coverage.** This directly corroborates, with SL quality now the controlled variable, what the claude-sonnet vs. v3-mistral AD-backend swap already suggested from the other direction (F1 30.6→73.9 from an AD swap alone, at comparable Cov@5). **Caveat on statistical power:** both AD-probe numbers are n=10 stratified-sample reads (the same methodology as the existing G3 gate checks, never the source of the recorded headline Detection F1/recall, which comes from the full 120-item G4 run) — treat as directional/qualitative confirmation, not a headline percentage. Docs updated: `docs/results-walkthrough-v3.md`, `docs/results-walkthrough-v3-claude-sonnet.md`, `docs/CURRICULUM.md` corrected to state both attribution factors and cite the flat-detection-recall finding. |
| 2026-07-16 | 8.3 follow-up (QG fix verified live — supersedes the `_v3` claude-sonnet Phase 8 scorecard) | The QG output-contract fix above was implemented but not yet measured against the live API (previous entry). Needed a G0.5 smoke + G4 re-run under a non-colliding `results_tag` before deciding whether to adopt it as canonical. | **G0.5 (job 1061322 → `results/api_smoke_claude-sonnet_v3-qgfix.txt`):** direct before/after on the same Q-041 question. Before: `MATCH (p:Person)-[r]->(i:Incident {name: 'Northbridge robbery'}) RETURN p, r, i` (invented `i.name`, whole-node return). After: `MATCH (p:Person)-[r]->(i:Incident)-[:OCCURRED_AT]->(l:Location {suburb:'Northbridge'}) RETURN DISTINCT p.name` (correctly reasons "Northbridge" is the incident's *location*, filters on a real property, returns the gold's exact tight projection). Clean, proceeded to G4. **G4 (job 1061343, `results_tag: v3-qgfix`, ~1h vs. the original's 2h50m):** C1 barely moved (EX 5.8→7.5, EA 5.8→7.5 — expected, `build_condition1` is untouched by this fix) but **C2/C3 transformed**: EX 5.0→**37.5** (C2), 4.2→**52.5** (C3); EA 5.0→**57.5** (C2), 5.0→**62.5** (C3); ambiguous EA 25.0→**50.0** (C2), 27.5→**57.5** (C3); DSR 0.0→**49.2**. Detection F1/prec/rec **exactly unchanged** (73.9/83.6/66.2 — correct, since AD/Dis/SL are untouched by a QG-only fix; good sanity check). **Success criteria #1 (C3 > C2 on ambiguous AREA) and #2 (monotonic C1 ≤ C2 ≤ C3 on EX) now both MET for claude-sonnet** — previously #1 was floor-noise and #2 failed (non-monotonic decline). **Adopted as canonical, user-directed 2026-07-16:** `results/tables_claude-sonnet_v3.md`/`metrics_claude-sonnet_v3.json` (the pre-fix numbers) copied to `..._v3_pre-qgfix.{md,json}` for the record, then the `_v3-qgfix` files copied over the canonical `_v3` names; `config/pipeline.yaml::results_tag` reverted from the temporary `v3-qgfix` back to `v3`. `docs/results-walkthrough-v3-claude-sonnet.md` and `CURRICULUM.md` Module 8 rewritten to the new numbers. | Mirrors this project's own precedent for a correctness fix (the 2026-06-27 evaluator column-name fix: "results predate this fix and are stale, must be regenerated before thesis use") — this is a real bug fix (the API QG was never shown its own schema's property names, on any backend, since step 3.6's original design), not a hyperparameter tune, so the corrected numbers are the ones that belong in the dissertation. Old numbers preserved on disk (`_pre-qgfix` suffix, mirroring the `_v3_baseline_cov0640` checkpoint-preservation convention from 7.4) rather than deleted, so the property-hallucination narrative and its evidence remain fully reconstructable. No further API spend needed beyond the G0.5+G4 already run (~$30-60, comparable to the original G4). |
| 2026-07-16 | 8.3 follow-up (full C3-only SL+QG matched-tier ablation — "detected AND resolved", refines the AD-probe ablation above) | The AD-probe ablation (previous entry) tested only binary detection (n=10), varying SL while holding QG fixed at the 7.4 adapter. User pushback, verbatim: "are the ablations for ad probe on the 3 levels of local model really enough and not an e2e" and "because its not just whether ambiguity is detected, its if its detected and resolved well hence the final result no?" — requiring the actual C3 condition (Disambiguator+QG+CyVer+execution+evaluation) at full 120-item scale. Further pushback ("why are we only swapping the SL adapter and not the entire thing? is that not what the original one did?") required swapping SL **and** QG together to the same training tier, matching the claude-sonnet run's one-model-everywhere design (confirmed with the user: AD/Dis were never fine-tuned at any tier in this project, so swapping SL+QG together already is the complete analogue of "match the whole training tier," not a partial one). | Built `kaya/c3_only_ablation_v3.py` (+ `kaya/91_c3_only_ablation_v3.slurm`) — a Kaya harness restricted to `disambiguation_enhanced` only (mirrors `experiments/run_evaluation.py`'s v3 path), with SL and QG's `peft_adapter_path` both driven off one `--adapter_suffix`/`--no_adapters` pair. Ran three tiers on the full 120-item v3 benchmark: **full 7.4 SFT** (job 1061589 — EX 9.2, EA 23.3, ambiguous_EA 27.5, **DSR 5.0**, F1 30.6/Prec 75.0/Rec 19.2, bit-for-bit identical to `metrics_mistral7b_v3.json`'s `disambiguation_enhanced` block — the harness reproducing the recorded run exactly is itself a validity check); **generic-Ozsoy-only** (job 1062145 — EX 1.7, EA 2.5, ambiguous_EA 1.3, **DSR 0.0**, F1 29.6/Prec 70.6/Rec 18.8); **no adapter at all** (job 1061590 — EX 3.3, EA 4.2, ambiguous_EA 2.5, **DSR 0.0**, F1 32.0/Prec 80.0/Rec 20.0). Two bugs found and fixed en route: (1) an incomplete rename left `args.no_sl_adapter` referenced in the script body while argparse only declared `--no_adapters`, crashing jobs 1061578/1061579 with `AttributeError: 'Namespace' object has no attribute 'no_sl_adapter'` — fixed by aligning both references to `args.no_adapters`; (2) the slurm wrapper's `SL_ARGS="--adapter_suffix ${SL_ADAPTER_SUFFIX:-_v3}"` silently fell back to `_v3` even when the caller passed `SL_ADAPTER_SUFFIX=""`, because bash's `${VAR:-default}` treats an empty string the same as unset — job 1061589 (intended as the generic-only run) actually re-ran the full-SFT tier as a result, which is exactly why its numbers matched the recorded run byte-for-byte on first inspection. Fixed by switching to `${VAR-default}` (no colon, only falls back on true-unset) inside a bash array (`SL_ARGS=(--adapter_suffix "${SL_ADAPTER_SUFFIX-_v3}")` — a plain string would still lose an empty value to word-splitting), verified against both the empty-string and unset cases before resubmitting as job 1062145. | **This refines, and partially corrects, the AD-probe ablation's framing (previous entry).** Detection (F1/Prec/Rec) is flat across all three tiers — now confirmed at full 120-item statistical power, not just n=10 — so the probe's finding holds up: detection recall really doesn't track SL/QG quality. But **DSR collapses to 0.0 at both weaker tiers vs. 5.0 at full SFT**: once an item is correctly flagged ambiguous, the Disambiguator→QG→CyVer→execution chain still needs a correct schema-linked pattern to actually resolve it, and without the 7.4 in-domain SFT it essentially never does, even though the AD keeps flagging roughly the same fraction of items as ambiguous either way. So "SL/QG quality doesn't matter, only the AD model does" — the natural over-reading of the AD-probe-only result — is wrong: SL/QG quality is not a spent lever, it specifically gates **resolution**, not **detection**, and this is exactly the distinction the user's pushback was aiming at. Secondary, smaller-N observation: the schema-redesign-alone tier (Cov@5=0.483, generic-only) is actually the *worst* of the three on EX/EA/ambiguous_EA here, marginally below no-adapter (Cov@5=0.000) — but the gap is 1-2 items either way, so the ordering between the two weak tiers shouldn't be read as meaningful; the clean, load-bearing contrast is 7.4-SFT vs. either weaker tier, not weak-tier vs. weak-tier. Docs updated: `docs/results-walkthrough-v3.md` (new full-C3-ablation subsection, softened AD-probe-only language, corrected "what this means" point 2), `docs/CURRICULUM.md` Module 8 (8.3 point 2, 8.4, 8.5, closing seminar sentence). |
| 2026-07-16 | 8.3 follow-up (C2-only matched-tier ablation — does the C3-over-C2 gap itself survive weaker SL/QG tiers?) | The C3-only ablation above only ran Condition 3. User question: since C3 was already tested across three tiers, is skipping the full C1-C3 sweep for the ablation justifiable as "diminishing returns," or does something get missed? On inspection: `build_condition1.py` builds `QueryGenerator(base)` with no adapter and no Schema Linker at all — C1 is **provably invariant** to this ablation (not a judgment call), so it was rightly skipped. `build_condition2.py`, however, **does** build `SchemaLinker(sl_llm, ...)`/`QueryGenerator(qg_llm, ...)` from the same `sl_spec`/`qg_spec` carrying the adapter path that C3 varies — so C2 was a real gap, not a proven no-op: without it, "disambiguation adds value over schema-grounding" (success criterion #1, C3 > C2) had only been confirmed at the full-SFT tier, and it was an open question whether that gap survives, widens, or vanishes at weaker SL/QG tiers. | Built `kaya/c2_only_ablation_v3.py` (+ `kaya/92_c2_only_ablation_v3.slurm`), mirroring `c3_only_ablation_v3.py`/`91_c3_only_ablation_v3.slurm` exactly (same `--adapter_suffix`/`--no_adapters` pair, swapping SL+QG together) but calling `build_condition2`/`run_condition(..., "schema_grounded")` with `ad_predictions=None` (C2 has no AD/Dis, so no detection metrics). The bash empty-string fix (`${VAR-default}` in an array, not `${VAR:-default}` in a plain string) was written in from the start this time, verified before submitting. Ran two tiers on the full 120-item benchmark (the full-7.4-SFT tier needs no new run — it's already the recorded `schema_grounded` block in `metrics_mistral7b_v3.json`, same reasoning as skipping C1): **generic-Ozsoy-only** (job 1062660 — EX 1.7, EA 1.7, ambiguous_EA 0.0); **no adapter at all** (job 1062661 — EX 3.3, EA 4.2, ambiguous_EA 2.5). | **The C3-over-C2 gap on ambiguous EA shrinks monotonically as the tier weakens, and only fully closes at the floor**: +2.5pp at full 7.4 SFT (recorded, 27.5 vs 25.0), +1.3pp at generic-only (1.3 vs 0.0), +0.0pp at no-adapter (2.5 vs 2.5 — C2 and C3 identical on every metric at this tier). This is not a contradiction of the DSR finding (previous entry) — DSR is already 0.0 at both weak tiers, so nothing gets *fully* resolved at either — but a finer-grained view underneath it: disambiguation is still contributing *something* (partial credit via relaxed AREA) at generic-only even though it never closes out to a correct execution, and only becomes completely inert once SL+QG have nothing usable to hand it at all (no-adapter). **Caveat: all of these are small absolute counts** (n=80 ambiguous; 1.3pp = 1 item), so the monotonic shape (gap present → smaller → zero) should be read as directionally consistent with "the architecture's value-add needs some minimum grounding to have anything to disambiguate over," not asserted as a statistically robust trend on its own. Docs updated: `docs/results-walkthrough-v3.md` (new C2-vs-C3 subsection following the full-C3-ablation one, run provenance). |
| 2026-07-17 | 8.3 follow-up (QG properties ablation — is the local EA/EX floor a harness artifact?) | `QueryGenerator` derives the QG prompt's properties block solely from `prompt_style` (`generator.py`: `include_properties = self.prompt_style == "instruct"`), so the local "completion" path can never be shown the schema's property list. The standing rationale (`prompts.py`, `generator.py` docstrings) is that the local fine-tuned QG doesn't need it because Phase 7.4's POLE SFT "baked the real names into its weights". | Added an additive, default-preserving `include_properties: Optional[bool] = None` override to `QueryGenerator.__init__` (None → the `prompt_style`-derived value, so every existing call site is byte-identical), threaded as `qg_include_properties` through `build_condition2`/`build_condition3`, and exposed as `--qg_include_properties` on both `kaya/c2_only_ablation_v3.py` and `kaya/c3_only_ablation_v3.py` (+ `QG_INCLUDE_PROPERTIES=1` on `kaya/91`/`kaya/92`; results tag gains `_qgprops` so nothing collides with the recorded properties-off cells). Ran the full 6-cell grid: C2 and C3 × {full 7.4 SFT, generic-only, no-adapter}. **Only properties are flipped — deliberately NOT the API fix's few-shots** — so the factor is attributable. Test double `_FakeQueryGenerator` updated for the new kwarg (suite back to 332 passed). | **The standing rationale is only half-true, and the measured consequence is large.** Counting real v3 property names across all 1800 POLE QG training completions (`data/pole_qg_train.jsonl`): **14 of the 24 schema properties appear ZERO times** — `incident_id`, `vehicle_id`, `case_id`, `person_id`, `location_id`, `device_id`, `date`, `date_of_birth`, `opened_date`, `descriptor`, `alias`, `gender`, `model`, `postcode`. Only 10 were ever trained (`name` 1086, `crime_type` 726, `suburb` 434, `phone_number` 383, `status` 293, `make` 266, `case_name` 173, `address` 142, `plate` 97, `colour` 177). So for those 14 the QG has **no source at all**: not its prompt (`include_properties=False`), not its weights (never trained), and not the SL's committed pattern (bare topology — only 1 of 120 rows in `results/beams_mistral7b_v3.jsonl` carries any property literal). The asymmetry is baked into data generation too: `pipeline/pole_sft_data.py:569` builds SL rows with `schema_block_props` but `:571` builds QG rows with `schema_block_noprops`, while `schema_linker/linker.py:53` passes `include_properties=True` unconditionally on every backend — **the QG is the only stage never given the property list**. Measured cost — **⚠ THIS PREDICTION WAS FALSIFIED BY THE RUN ITSELF; see the results entry below, and do not cite it**: "21 of 120 benchmark items (17.5%) are unwinnable by construction — every acceptable gold (default *and* all interpretations) RETURNs a property the QG cannot name — capping local EA at 82.5%, concentrated in `entity` (9/20 dead) vs `temporal` (2/20 dead)". With properties ON those 21 items ARE nameable, so the ceiling should have risen to ~100%; measured EA rose only 23.3→28.3. Property naming was therefore never the binding constraint, and `entity` did not move at all (20.0 in all four C2/C3 × on/off configurations). `incident_id` alone accounts for 14 of the 21. (Gold *filtering* on an opaque ID is NOT unwinnable — results are compared, not query text — so the honest figure is 21, not the 53 items that merely touch an untrained property.) Corroborating: 651 property-does-not-exist notifications in the recorded local G4 log (job 1032398), every one a near-miss of a real name (`vehicle_make`→`make`, `plate_number`→`plate`, `description`→`descriptor`, `city`/`street`→`suburb`/`address`, `type`→`crime_type`). Motivating comparison: local SL EM@1 **0.667** ≈ claude-sonnet **0.650** (near-parity), yet C2 EA 23.3 vs 57.5 — same schema-linking quality in, 34pp apart out, so the gap is downstream of the SL. The API path received exactly this fix (`include_properties=True` + few-shots) and moved EA 5.0→57.5 at unchanged SL quality; the local path never did. **Interpretation caveat recorded up front:** the v3 QG adapter's training mixture (`data/pole_qg_train_mixed.jsonl`) is 37% property-rich Ozsoy replay rows and 31% property-free POLE rows, so the property-rich *format* is in-distribution but POLE-content-with-properties is a combination it never saw — a null result would therefore NOT falsify the diagnosis (the definitive fix is `pole_sft_data.py:571` → `schema_block_props` + a QG retrain, deliberately out of scope per user decision 2026-07-17). Evaluator ruled out as the cause first: `_result_sets_equal` is order-, duplicate- and column-name-insensitive, `use_embedding_model: true` was set, and benchmark gate 5 confirms every gold returns non-empty. |
| 2026-07-17 | 8.3 follow-up (Neo4j port collision — `kaya/91`, `kaya/92`) | The per-job Neo4j isolation added on 2026-07-16 gave each concurrent ablation job its own data directory via `NEO4J_DATA_SUFFIX`, on the assumption that this was sufficient to let jobs run concurrently. Both runners still hardcoded `bolt://localhost:7687` (and the container's HTTP `:7474`). | Added a per-job Bolt port, `BOLT_PORT=$((20000 + SLURM_JOBID % 20000))`, plumbed to `cypher-shell -a "$NEO4J_URI"` and to the harness via `--neo4j_uri "$NEO4J_URI"`. Implemented by copying the image's whole conf dir out (`apptainer exec … tar -cf - -C /var/lib/neo4j/conf .`) into `$DATA/conf`, binding it back as `/conf` with `--env NEO4J_CONF=/conf`, rewriting `server.http.enabled=true`→`false` **in place with sed** and appending only `server.bolt.listen_address`. Also made the readiness loop `exit 1` instead of falling through silently. | **The data dir was never the whole story: the PORT was shared per node.** Slurm co-located jobs 1063900+1063901 on k029 and 1063903+1063904 on k032; the loser's server failed to bind (`Neo4j web server failed to start`), after which its `cypher-shell` readiness probe **succeeded against the WINNER's database** and it ran `--file $CYPHER` into a live run — visible as Forseti lock contention in the loser's log. This is a silent-corruption hazard, not just a crash: the surviving run 1063900 was cleared only by counting the live graph after the fact (nodes 4/12/16/40/14/10 and 195 relationships, exactly matching `data/SyntheticPoliceKG-v3.cypher` — the duplicate load had died on the lock without committing). Two implementation traps found while fixing: (1) Neo4j 5 hard-errors `server.https.enabled declared multiple times` if a key already ACTIVE in the shipped conf is appended — only `server.bolt.listen_address` ships commented out, so `http.enabled` must be sed-rewritten and `https.enabled` left alone (already false); (2) `neo4j console` is invoked directly via `apptainer exec`, bypassing the Docker image's entrypoint, so the `NEO4J_<setting>` env-var convention does NOT apply — config must go through a real conf file + `NEO4J_CONF`. Verified under the exact failure condition: resubmitted jobs 1063942+1063943 both landed on k032 and came up cleanly on ports 23942/23943. The old hardcoded-7687 runners (`kaya/55`, `kaya/70`, `kaya/81`, `kaya/65`) are untouched and remain single-job-per-node only. |
| 2026-07-17 | 8.3 follow-up (QG properties — RESULTS of the 6-cell ablation + the new canonical local column; supersedes the prediction in the entry above) | The entry above predicted that the QG's missing Properties list capped local EA at 82.5% via 21 structurally-unwinnable items, and framed `include_properties=False` as an experimental condition to be tested. | Ran the full 6-cell C2/C3 × {full 7.4 SFT, generic-only, no-adapter} grid with `--qg_include_properties` (jobs 1063899/1063900/1063941/1063902/1063942/1063943), then — on the user's design argument that a stage given "the schema" cannot be given 90% of it — **reframed properties-off as a DEFECT rather than a condition** and made properties=True the canonical local configuration: new `config/pipeline_v3_qgprops.yaml` + `kaya/93_run_evaluation_v3_qgprops.slurm` produce the full C1+C2+C3 column at `results/{tables,metrics}_mistral7b_v3_qgprops.{md,json}` (job 1065123, 2:53:47). The recorded properties-off `_v3` artefacts are untouched, so the pair is a clean single-factor comparison. C1 included: it is structurally invariant to the *adapter* ablation (loads none) but NOT to this one — it still renders a schema block, and the Ozsoy corpus its zero-shot format claims comparability with is itself property-rich (90.3% of `data/ozsoy_qg_train.jsonl` rows), so properties=True is what actually honours C1's stated rationale. | **Canonical column, properties OFF→ON** (both via run_evaluation): C1 EX 3.3→5.0, EA 4.2→**10.8**, KG-Valid 42.5→**62.5**; C2 EX 10.8→**15.8**, EA 23.3→**28.3**, KG-Valid 65.8→**74.2**; C3 EX 9.2→**12.5**, EA 23.3→**27.5**, KG-Valid 65.0→**71.7**. Improves on every metric in every condition. Ambiguous EA is now cleanly monotonic **C1<C2<C3: 10.0→27.5→31.2**. **Detection identical across all 7 runs** (F1 30.6/Prec 75.0/Rec 19.2 at full SFT; 29.6 and 32.0 at the weak tiers) — a QG-only change cannot move the AD, and never did: the single-factor manipulation is clean. Few-shots were deliberately NOT added (unlike the API fix, which changed both at once), so the factor is attributable to properties alone. **Three corrections to the entry above, recorded so a future session does not re-derive them:** (1) the 82.5% ceiling was falsified — property naming was not the binding constraint; (2) `entity` EA is immovable at 20.0 across C2/C3 × properties on/off — C3 *has* Entity Lookup, so it is gated by neither property naming nor entity resolution, and the cause remains unknown; (3) the no-adapter tier did NOT stay pinned at the floor despite SL Cov@5=0.0 (C2 EA 4.2→10.0). **A flagged confound turned out to be empirically null:** the `c2_only`/`c3_only` harnesses never set `embedding_model` (→ semantic-evaluator fallback disabled) whereas run_evaluation does, but the canonical run reproduced the ablation cells EXACTLY (C2 EX 15.8/EA 28.3; C3 EX 12.5/EA 27.5), so the fallback never rescued an item (consistent with semantic_repair_pct=0.0 throughout) and the 6-cell grid stands as reported. The same null applies to the earlier adapter ablations compared against `metrics_mistral7b_v3.json`. **The fix does NOT rescue the local column** — local C2 EA 28.3 vs claude-sonnet 57.5 at near-identical SL quality (EM@1 0.667 vs 0.650): properties closed ~5 of a ~34pp gap (~15%). It removes a real fairness confound (Claude's QG always saw the full schema; mistral's never did) and leaves the residual gap as a capability finding rather than a measurement artifact. **Root cause NOT fixed and deliberately out of scope** (user decision 2026-07-17): `pipeline/pole_sft_data.py:571` still builds QG training rows with `schema_block_noprops` (vs `:569` SL rows with `schema_block_props`), so 14 of 24 v3 properties still appear zero times in the QG's SFT completions and properties=True remains an inference-time partial correction with train/inference mismatched — empirically net-positive, but the complete fix is a QG retrain. |
| 2026-07-17 | docs (results walkthroughs — presentation convention) | No rule governs how the walkthroughs present superseded results; both v3 docs had accreted a "stated claim → later correction note" structure (a top-of-file supersession notice, a "What changed and why" section narrating the QG output-contract bug with before→after tables, and ablation tables carrying properties-off numbers annotated by a properties-on correction sentence). | Normalised both v3 walkthroughs to **present only canonical numbers in present tense**. `results-walkthrough-v3.md`: G4 runbook row now cites job 1065123 (was 1032398, the superseded properties-off run, while the tables below it were already 1065123); the Stage-2 C3 matched-tier and C2/C3 gap tables were regenerated from the properties-ON cells (`results/c[23]_only_mistral7b_v3_*_qgprops.json`, jobs 1063899/1063900/1063941/1063902/1063942/1063943) replacing the properties-off numbers + their correction notes. `results-walkthrough-v3-claude-sonnet.md`: dropped the supersession notice and the "What changed and why" section, folding the API QG prompt into the Method-caveat section as a **design fact** (output contract + `include_properties=True` + four `pole_qg_train.jsonl` few-shots); G4 row now cites 1061343; removed "previously failed"/"unchanged from the original run"/"was a fixable bug" framing; refreshed the stale v3-mistral scorecard column (crit #1 27.5>25.0 → **31.2>27.5**; #4 schema +10 → **+15pt, entity immovable at 20.0**; #7 42–66% → **62–74% valid**) which had been quoting the properties-off run. | User direction 2026-07-17: a results walkthrough should read as the current state of the evidence; **"if we need to trace specific changes / why something was done we would refer to the decisions log"** — so run history belongs here, not in the walkthroughs. No numbers were changed except to their canonical values: every table was re-derived from the source JSON, and the properties-off arms remain on disk and are still cited in each doc's Run-provenance table (as the properties ablation's OFF cells / `*_pre-qgfix.*`), so nothing is lost — only relocated. The genuine two-arm **properties ablation** in v3.md is retained as-is: an ablation arm is a result, not a stale claim. |
| 2026-07-27 | 5.4 follow-up (per-question qualitative artefact — new capability, not a spec deviation) | `run_evaluation.py` (5.4) computes a full per-question `QuestionRecord` (gold interpretations, generated Cypher, failure_mode, and — for C3 — the Ambiguity Detector's call and the Disambiguator's committed mapping) for every item, but `_persist` only ever wrote the aggregated `MetricBundle`; the per-question detail was discarded. There is no spec file for this (no Phase 9 exists) — user asked directly for a qualitative C2-vs-C3 pass (which items flipped, and whether the flips look like real disambiguation vs retry-budget luck) now that the aggregate numbers show a lift. | Added `run_condition`'s third return value `details: list[dict]` (question/gold/generated-Cypher/AD-and-Dis fields, additive — `records`/`ad_predictions` unchanged) and a new `_persist_details` writing `results/qual_records_{model_key}{suffix}.json`. Added `experiments/analyze_qualitative.py`, a standalone script (no pipeline imports, so it runs offline) that joins C2/C3 by `question_id`, buckets into both_correct/both_wrong/fixed/regressed, stratifies by `ambiguity_type`, and emits a markdown report with full case studies for `fixed`/`regressed`. Also switched `config/pipeline.yaml`'s `neo4j.uri`/`database` from `bolt://localhost:7687`/`neo4j` to `neo4j://127.0.0.1:7687`/`policev3` — Kaya is in maintenance (2026-07-27), so the v3 run needed to point at the user's local Neo4j Desktop instance instead. | Not implementing this meant the 0.375→0.525 EX lift (Claude Sonnet, v3) had no example-level evidence behind it — indistinguishable from a lucky retry-budget effect (C3 gets more regeneration attempts than C2) without seeing which specific items flipped and whether the AD/Disambiguator's output on those items was coherent. Kept additive/offline: existing `records`/`bundles`/`metrics_*.json` outputs are untouched (verified by inspection, not re-run — no live Neo4j/API access in this environment), and the new script was validated against a hand-built synthetic `qual_records` fixture (3 items exercising both_correct/fixed/regressed) before being pointed at real output. The actual v3 rerun against `policev3` and the resulting report are the user's to produce and review next; not run from here. |
| 2026-07-27 | 5.4 follow-up #2 (qualitative doc + both_wrong sampling) | The user ran `run_evaluation.py` against `policev3` (results: EX 39.2→54.2, tracking the canonical 37.5→52.5) and asked for the fixed/regressed case studies to be turned into a dissertation-ready write-up, plus wanted the "where it still struggles" side backed by real examples, not just the fixed/regressed evidence already gathered. | Extended `experiments/analyze_qualitative.py`: `sample_by_type()` draws a deterministic (sorted-`question_id`, not random) sample of up to N `both_wrong` items per `ambiguity_type`; the stratification table now reports all four buckets (not just fixed/regressed) per type so "handled well" vs "still struggling" is readable directly from one table; the report gains a `both_wrong sample` section. Wrote `docs/qualitative-analysis-v3-claude-sonnet.md`, matching the existing `docs/results-walkthrough-*.md` convention (byline, "narrative in one line", tables, then evidence) rather than inventing a new format — user confirmed markdown-in-docs/ over a standalone .docx. | The stratification table surfaced two clean, opposite findings: temporal (65% fixed) and schema (20% fixed) are where the module works, both resolvable as a schema-element/property-filter choice — what a `SchemaMapping` can express. Intent (0% fixed, despite correct AD detection on inspected examples) and entity (5% fixed, despite correct AD detection on inspected examples) are where it doesn't — and reading the `both_wrong` samples showed *why* in each case: intent ambiguity needs a RETURN-clause-level decision a `SchemaMapping` has no field for; entity ambiguity is detected but the fix never reaches the QG's WHERE-clause name-matching pattern (C2 and C3 emit the identical broken `{name:'Chen'}`-style filter). Both are architectural-ceiling findings, not tuning gaps, and are exactly the "continue analysing from here" threads the user asked for. Unambiguous items (n=40) showed 0 fixed/0 regressed — the module never destabilizes questions that didn't need it, a precision point distinct from the recall story above. |
| 2026-07-27 | 5.4 follow-up #3 (artefact index + two step-through diagrams) | Entries above name `experiments/analyze_qualitative.py` and the *pattern* `results/qual_records_{model_key}{suffix}.json`, written before the user's actual run resolved `{suffix}`, so neither gives a concrete path; the user separately asked for two illustrative step-through diagrams (Q-105 temporal, Q-041 schema) for supervisors/dissertation, which were built and saved but never logged anywhere. Gap noticed when the user asked whether this artefact trail was actually complete — it wasn't. | No code changes. Recording, once, where every artefact from this 2026-07-27 thread actually lives, resolved: **run_evaluation.py output** — `results/tables_claude-sonnet_v3_localdb.md`, `results/metrics_claude-sonnet_v3_localdb.json` (aggregated), `results/qual_records_claude-sonnet_v3_localdb.json` (per-question detail, the join key for everything below). **analyze_qualitative.py output** — `results/qualitative_analysis_claude-sonnet_v3_localdb.md` (bucket counts, full stratification table, all 18 fixed case studies, the `both_wrong` sample). **Write-up** — `docs/qualitative-analysis-v3-claude-sonnet.md` (dissertation-ready narrative built from the above). **Diagrams** — `docs/diagram-q105-temporal-stepthrough.svg` and `docs/diagram-q041-schema-stepthrough.svg` (pipeline step-through for one temporal and one schema fixed-case, built from the real `qual_records` fields for those two question_ids, including the discovery — not yet folded into the write-up prose — that the Schema Linker runs independently per condition, and on Q-041 both C2's and C3's own SL calls landed on the identical biased `ASSOCIATED_WITH`-chain reading; only C3's downstream AD/Disambiguator catches it). | This project's own convention is "if we need to trace why/where, refer to the decisions log" (2026-07-17 docs entry) — a log entry that names a script but not the file it actually produced doesn't satisfy that, especially once a config value (`results_tag`) resolves the filename after the entry was written. Rule going forward for this thread: when a run resolves a templated path, or a follow-up artefact (diagram, doc) is created outside `run_evaluation.py`/`analyze_qualitative.py`, add or amend a decisions-log line with the concrete path — don't rely on the pattern alone. |
| 2026-07-27 | 5.4 follow-up #4 (color-coded per-question comparison doc) | User asked for one more artefact: a .docx with all 80 ambiguous questions grouped into 4 tables (one per ambiguity_type), columns Question / C2 output / C3 output, cell background colored green/red instead of a separate correct-column. | Wrote `docs/build_qual_comparison_docx.js` (docx-js; reads a small per-type JSON extracted from `qual_records_claude-sonnet_v3_localdb.json`) and ran it to produce `docs/qual-comparison-tables-v3-claude-sonnet.docx` (landscape, 9 pages — all 20 rows per type, ShadingType.CLEAR green/red per cell, a small italic failure_mode/✓ note under each query). Verified by converting to PDF and reading the rendered pages before finalizing. **Operational note, not a design decision:** `npm install docx` was first run directly inside `docs/`, leaving `node_modules`/`package.json` in the tracked repo; `rm -rf` on it failed with `EPERM` (connected-folder delete protection), resolved via `allow_cowork_file_delete`, then the build was redone from a scratch directory outside the repo and only the final `.docx` + the source `.js` generator were copied in. | Cell shading (not a 4th text column) was the user's own simplification — implemented as `ShadingType.CLEAR` per the skill's own gotcha list (`SOLID` renders black). All 80 ambiguous items are shown, not a curated subset, since the point of "4 tables, one per type" is the full at-a-glance pattern (e.g. the temporal table's near-solid red→green wall from Q-105 onward), which a sample would understate. Recording the `npm install` mistake and its fix so a future session doesn't repeat it inside a tracked folder: build scratch Node/npm work in the outputs scratchpad, not inside a connected repo folder, even for a "one-off" script. |
| 2026-07-28 | 5.4 follow-up #5 (EA has a regression EX doesn't — new artefact + finding) | User asked whether the "0 regressions" finding holds for EA (`matches_any_interpretation`) the same as EX (`is_correct`), since both had only been reported for EX. Checked by re-bucketing all 80 ambiguous items on `matches_any_interpretation` instead of `is_correct`: it does **not** hold — EA has exactly 1 regression (EX still has 0). | Parameterized `docs/build_qual_comparison_docx.js` with a `METRIC=ex\|ea` env var (was hardcoded to EX); per-type summary lines are now computed from the data instead of hand-typed, so they can't drift from a re-run. Extended the per-type extraction JSON with both `c2/c3_correct_ex` and `c2/c3_correct_ea` fields (previously only EX). Ran both: `docs/qual-comparison-tables-v3-claude-sonnet.docx` (EX, regenerated, unchanged content) and `docs/qual-comparison-tables-v3-claude-sonnet-ea.docx` (new). The EA doc's legend carries an extra note explaining the one regression inline. Verified both by PDF render before finalizing (confirmed Q-104 renders green C2 / red C3 under EA, matching the script's printed bucket counts). Linked the EA doc from `docs/qualitative-analysis-v3-claude-sonnet.md`'s byline. | **The regression, Q-104 "Who owns the Holden Commodore?" (temporal):** under EX both C2 and C3 are wrong (`both_wrong` — C2 is `valid_non_default`, C3 is `wrong_result`), so it never appeared in the EX comparison at all. Under EA, C2's unfiltered query happens to satisfy the benchmark's non-default "including previous owner" reading (EA=True), while C3 — despite its AD correctly flagging temporal ambiguity (`ad_llm_rationale` names the OWNS active/from_date/to_date properties explicitly) and retrying (`n_attempts=2`, `n_mappings_tried=2`) — ends up with a malformed property filter (`{model:'Holden Commodore'}` instead of `{make:'Holden', model:'Commodore'}`), matching no interpretation (EA=False). This is a QG/mapping execution defect introduced during the extra retry, not a disambiguation-reasoning failure — the ambiguity was correctly identified; the fix attempt broke on an unrelated bug. Full stratified EA counts (fixed/regressed/both_correct/both_wrong): schema 1/0/12/7, entity 1/0/9/10, intent 1/0/12/7, temporal 2/1/15/2 — EA is a much more lenient metric than EX (both_correct jumps 47→70 overall), so most of the EX "fixed" drama (18 items) collapses under EA (5 items) because C2 already satisfies *some* interpretation on most of them; this doesn't contradict the EX-based "not random" argument, it's a different, coarser question. |
| 2026-08-02 | Phase 9 (new, no spec yet) — external POLE benchmark: plan + current status | Supervisor feedback on the qualitative-analysis walkthrough/diagram: the exact-match improvement "resonates with author intent" since the student authored both `benchmark-updated.json`/`benchmark-v3.json` and their ground truth — a validity concern, since the same person designing the benchmark and the architecture being tested against it makes the result harder to read as generalisation. Suggested next step: find/use an existing benchmark or graph schema found online (CySpider named as one example, or another POLE-schema dataset) rather than authoring another benchmark from scratch, and craft/reword questions against it. | Chose `neo4j-graph-examples/pole` (public Neo4j example dataset — real Greater Manchester, UK street-crime data from data.gov.uk, POLE schema, ~61.5k nodes/105.8k relationships), downloaded and loaded into local Neo4j Desktop. Audited the schema and data interactively (11 labels, 17 relationship types, per-label counts/samples, relationship property inventory, duplicate-name clusters, KNOWS-family overlap, PARTY_TO/INVOLVED_IN endpoint types, officer/area superlative ties, crime-type-vs-violent-crime ranking divergence, per-day crime-date distributions for one area and citywide, busiest-phone call timeline) before writing any benchmark items — every claim below traces to a real query result, not an assumption. Drafted a 100-item skeleton (20 entity / 20 schema / 20 intent / 20 temporal / 20 unambiguous control), same shape as 7.1's `benchmark-v3-skeleton.json` (question/ambiguity_type/default_interp/planned_interpretations/data_support, no Cypher yet). Currently held outside the repo as a working draft, not yet copied into `data/` — see the new Phase 9 section in `progress-tracker.md` for the file name and full status. | **Two structural findings materially shaped the plan and are worth a future session knowing without re-deriving them.** (1) This graph has no role-split Person→Crime edges and, more importantly, **no dated/state-change relationships anywhere** — every relationship's property list is empty except `FAMILY_REL.rel_type`, so v2/v3's temporal mechanism (`active`/`from_date`/`to_date` on edges) has no analogue here. Temporal ambiguity for this external set was **redefined** (user-decided) to same-day-vs-trailing-week date/time-window ambiguity over `Crime.date`/`PhoneCall.call_date`+`call_time` — a different mechanism under the same category label, which must be stated explicitly wherever v2/v3 and this set are compared, not glossed over. (2) Schema ambiguity was reframed away from a role-edge axis (there isn't one) onto two axes this graph actually supports: the Person-Person KNOWS-family (confirmed default-vs-narrow via a `specific_without_generic=0` check — every specific relationship type has a parallel generic `KNOWS` edge, so "any connection" is a genuine superset, not a redundant 5th reading) and Crime-participant scope (`PARTY_TO` person vs `INVOLVED_IN` vehicle/object, confirmed non-empty on both sides for a real subset of crimes). **Current status, honestly:** 4 of the 100 skeleton items are flagged rather than asserted — 3 superlative/definitional comparisons (Chief-Inspector tie, BL1-vs-others area comparison, BL2-vs-M6 "dangerous" comparison) that looked promising but turned out, on the actual returned data, not to hold as genuine ambiguity, plus one same-template placeholder (a second area's per-day breakdown) never pulled. These are left in the draft marked `NEEDS VERIFICATION`/`PARTIAL` with the reason, rather than silently dropped or asserted — next session's job is to resolve or replace them, then move to authoring `cypher_default`+interpretation Cypher for all 100 and running `experiments/validate_benchmark.py` (unmodified — it was built version-agnostic in 7.2 specifically so a future benchmark like this one wouldn't need a new harness) before anything here is canonical. No spec file exists yet for this phase; write one (closer to 7.1's design-step shape than 7.2's authoring-step shape) before starting the Cypher-authoring pass. |
| 2026-08-02 | Phase 9 follow-up — the 4 flagged items resolved, skeleton now fully `CONFIRMED` | The 4 items above (I10, I11, I15, T20) were flagged `PARTIAL`/`NEEDS VERIFICATION`, not asserted. User ran one targeted verification query per item against the live local `pole` Neo4j instance (this environment has no network path to it) and pasted the raw output back for interpretation. | **I10** (Chief Inspector, "aside from Monelli Kort"): full tie-grouped distribution showed ranks 1-3 (40/39/38, Monelli Kort/Stave Urban/Febry Roberto) are each unique — the original single-exclusion phrasing had no tie, confirming the original flag. Rank 4 (n=37) is a genuine 4-way tie (Jeandot Whitney/Gorrie Esma/Rahlof Evey/Syddie Dottie). Reworded to exclude the top three by name so the remainder lands on the real tie; kept the arbitrary-tiebreak-vs-return-all `planned_interpretations` shape used by I01/I02. **I15** (area "more dangerous", BL2 vs M6): confirmed not divergent (BL2 leads both total and violent), as flagged. An auto pairwise-divergence query over the full area total/violent table found many genuine total-vs-violent divergent pairs; picked M14-vs-M6 (M14 leads total 562>540, M6 leads violent 176>144) as a pair not already used by I04 (BL3/M40) or I14 (M9/BL9). **T20** (BL1 per-day, "same-day vs trailing-week"): pulled full BL1 per-day August counts (same query shape as M1's, correctly sorted by numeric day via `toInteger(split(date,'/')[0])` rather than string date). Picked 15 August as the reference date: same-day = 26, trailing 7-day window (09/08-15/08) = 182 — both non-empty, genuinely different magnitudes. **I11** (area, "most crime aside from M1") turned out to have no independent fix: the full area distribution (excluding M1) is unique all the way down to BL9 (623, rank 5); the *only* real area tie in the whole dataset is the 562 3-way tie (BL2/BL4/M14) at rank 6-8 — already claimed by I03 ("aside from BL9"). Every way of reaching a different area tie either reused I03's exact tie group under a narrower scope, or required an unnatural rank/threshold cutoff ("aside from the top 13 areas") to phrase as a question. Rather than silently force one, per this project's own "genuinely ambiguous → stop and ask" rule, presented the user 3 options (swap axis to Sergeant rank; reuse the 562 tie BL-scoped, accepting partial overlap with I03; drop and leave at 99 items) — user chose **swap axis to Sergeant rank**. Ran the same tie-search query scoped to `rank:'Sergeant'`: ranks 1-3 (50/46/44, DeSousa Madelon/Notti Kania/Klehyn Olenolin) unique, rank 4 (n=42) a genuine 2-way tie (Macias Hernando/Shurmore Rea). I11 rewritten to this axis (mirrors I01/I02/I10's officer-rank "aside from top N" mechanism — Inspector/Police Constable/Chief Inspector/Sergeant each now have one), `num_hops` flipped 2→1 to match. | All 4 edits applied directly to `scratchpad/benchmark-pole-external-skeleton-draft.json` (still outside `data/`, per the existing convention — not yet canonical). Verified post-edit: file still parses as valid JSON, still exactly 100 items, and all four `question_id`s now carry `data_support: "CONFIRMED — ..."` traceable to a real query result, none invented. **All 100 items in the skeleton are now `CONFIRMED`** — the "resolve the 4 flagged items" step from the 2026-08-02 Phase 9 entry above is done. Next per that entry's own next-steps: write the formal Phase 9 spec (still doesn't exist), then author `cypher_default` + per-interpretation Cypher for all 100, then run `experiments/validate_benchmark.py` before this file is canonical. |
| 2026-08-02 | Phase 9 spec files written (9.1/9.2/9.3, no code yet) | User asked for the Phase 9 spec files, specifying: one dedicated to creating the benchmark (Cypher authoring + validation, testable against a live Neo4j at `bolt://localhost:7687`/db `neo4j`/user `neo4j`/password `changeme`, plus "the ability to run other queries for information"); the eventual full C1-C3 run to happen **locally, API-backed, not Kaya** (Kaya left optional/open for later); and the full run to also produce the qualitative-analysis outputs the 2026-07-27 thread built for v3, not just tables/metrics. | Split into **three** spec files rather than the one explicitly asked for, following this project's own 7.1/7.2/7.3 granularity (design / authoring+validation / pipeline-integration+run are different depends-on chains and shouldn't be one file): `9.1-pole-external-design.md` (retroactive — records the already-complete 9.1 design/skeleton work from the two rows above, since every other step in this project has a spec and 9.2 needs one to depend on formally); `9.2-pole-external-benchmark.md` (the step the user explicitly asked for — Cypher authoring rules, a new `experiments/pole_query.py` ad hoc query CLI answering "ability to run other queries", and a `BenchmarkProfile` generalisation of `validate_benchmark.py` so the existing v3 harness gains a `pole_external` profile instead of a forked copy); `9.3-pole-external-local-run.md` (config-only pipeline integration mirroring 7.3, a new standalone `config/pipeline_pole_external.yaml` instead of mutating the shared `config/pipeline.yaml` which is mid-flight on v3 work, and an explicit acceptance criterion requiring `analyze_qualitative.py` to be run — clarifying that `run_evaluation.py` already writes `qual_records_*.json` unconditionally since 2026-07-27, so the qualitative *data* needs no new code, only the existing downstream report script needs to actually be run against the new artefact). Progress-tracker's existing placeholder 9.1 row (previously spec-less, "in progress") updated to point at the new file and marked `done`; two new rows added for 9.2/9.3, both `not started`. | **Design choices made while drafting, worth a future session knowing without re-deriving:** (1) `validate_benchmark.py`'s gates 3-6 (syntax/schema-closure/non-empty/divergence) are *already* graph-agnostic — they route through CyVer's live-introspecting validators and `db_executor` against whatever the connection points at — so only gates 2 (distribution) and 8 (coverage), which read hardcoded module-level constants today, actually need the new `BenchmarkProfile` parameter; the docstring's claim of full version-agnosticism was aspirational, not yet true, until 9.2 does this. (2) `POLE_EXTERNAL_PROFILE.min_coverage` is set to **1**, not v3's 3 — real relationship types here are structurally rare (`PARTY_TO` 55 edges total, `FAMILY_REL` 155) and forcing 3 items per type would mean contriving repetitive questions against the coverage gate rather than against genuine ambiguity, which would work against the whole point of this external-validity check (real data, not authored-to-fit). (3) Flagged one genuine open question for whoever implements 9.3 rather than guessing: this schema's `Location` has no single identifier property (unlike v2/v3's `location_id`), so the entity registry's `id_prop: None` may or may not be compatible with however 3.2's `EntityCache`/`EntityLookupResult` actually handle a missing id — spec says check at implementation time, don't assume. (4) The connection contract needed **zero new code** — `validate_benchmark.py`'s existing `NEO4J_URI`/`NEO4J_USERNAME`/`NEO4J_DATABASE` env-var defaults already exactly match the instance the user described; only `NEO4J_PASSWORD=changeme` needs exporting, never committed. |
| 2026-08-02 | 9.2 | Ambiguous items: 2-4 interpretations; author cypher_default + per-interpretation Cypher for all 100 skeleton items against the real POLE-external schema; BenchmarkProfile min_coverage=1 with any 0-coverage relationship type recorded, not silently accepted | (1) Four entity items (E04 Fuller, E05 Murray, E06 Nguyen, E07 Austin) have a real 5-way surname tie in the live graph; capped interpretations at 4 (dropped the alphabetically-last first name in each: Rachel Fuller, William Murray, William Nguyen, Scott Austin) rather than exceed the 2-4 cap. cypher_default still returns the full unfiltered surname set (all 5 rows), so the ambiguity itself is not understated — only the enumerated `interpretations` list is capped. (2) Live verification (via the new `pole_query.py`) found the skeleton's S06/S07 default_interp text undercounted Mildred Kelly's KNOWS-family connections: stated 2 people (Stephen Perez, Jeffrey Campbell) but the live any-type undirected query returns 3 (adds Bruce Baker via KNOWS_SN, not previously queried by the 9.1 session). Corrected `default_interp` to '3 people' in the final file; the narrow KNOWS_LW/KNOWS_PHONE interpretation Cyphers were already correct and unaffected. (3) Three of the 20 unambiguous control items (U06-U10 vehicle-reg lookups, U11-U15 postcode lookups) were repurposed rather than kept as originally skeletoned: two vehicle-reg items became `HAS_PHONE`/`HAS_EMAIL` lookups (Benjamin Hamilton's phone, Rachel Turner's email) and one postcode item became a `POSTCODE_IN_AREA` lookup (WN3 4NL -> area WN3) — these three relationship types had zero coverage under every other item's design and would have failed gate 8 (even at the relaxed min_coverage=1) without a dedicated item; all three replacement facts were verified live and are non-empty. Final coverage: all 17 pole_external relationship types clear min_coverage=1 (min observed count 1, for HAS_PHONE/HAS_EMAIL/POSTCODE_IN_AREA/CALLER/CALLED — no 0-coverage type to record). |
| 2026-08-03 | 9.2 amendment — user due-diligence pass on `data/benchmark-pole-external.json` found 7 schema items and 19 of 20 temporal items were structurally weaker than intended; all fixed and re-validated live before 9.3 starts | Schema: S02/S04/S05/S06/S09/S10 paired a padded "any KNOWS-family type" default against a single narrower interpretation (S02: family-only; S04/S06/S10: living-with-only; S05: phone-only; S09: duplicated S08's Nancy-Campbell digital-contact axis) — none is genuinely ambiguous, since the default and the interpretation always compute the same result set. Temporal: 15 of 20 items were "area M1 around \<date\>" with only 1 item (T20) on the phone-call axis, a heavy skew away from the schema's second real temporal-bearing entity (`PhoneCall.call_date`/`call_time`). | **Root cause, confirmed live (not assumed):** every specific KNOWS-family edge (`KNOWS_SN`/`KNOWS_PHONE`/`KNOWS_LW`/`FAMILY_REL`) has a parallel generic `:KNOWS` edge in this graph (re-confirmed `specific_without_generic=0`, same fact 9.1 found) — so *any* union of KNOWS-family types that includes the generic edge always returns the same person set as the full union. This makes "broad vs narrow over KNOWS-family types" structurally incapable of producing two different result sets in this graph; the *only* two mechanisms that do produce genuinely divergent readings are (a) the digital-comm channel split `KNOWS_SN` vs `KNOWS_PHONE` (disjoint, non-overlapping — already used correctly in S03/S07/S08) and (b) the crime-participant scope split `PARTY_TO` vs `INVOLVED_IN` (already used in S11-S20). Tested and rejected one candidate fix before adopting this conclusion: pairing "any type" against "excluding FAMILY_REL" (on two fresh subjects, Amanda Alexander and Arthur Willis) produced byte-identical result sets in both cases, confirming the same structural cause rather than a per-item authoring slip. **Fix applied:** S01 retyped to the explicit union (now single-interpretation, consistent with the same structural finding — user-directed, accepted rather than replaced). S02/S04/S05/S06/S09/S10 fully replaced with 6 new items on the two working axes — 3 fresh 1-hop digital-comm subjects (Ann Fox, Bonnie Gilbert, Brenda Edwards) and 3 new 2-hop items (a fixed `KNOWS_LW` housemate hop followed by an ambiguous digital-comm hop, via Theresa Powell/Amanda Alexander, Kathy Wheeler/Alan Ward, Eugene Ferguson/Harry Garrett) — the schema block's first multi-hop items. Temporal T05/T06/T09-T15 (9 of the 15 "M1 around \<date\>" items) replaced with 6 new phone-pair "last contact" items (T20's mechanism, 6 fresh CALLER/CALLED pairs) + 3 new "calls around \<date\>" items (same-day-vs-trailing-week window applied to a phone's own call history rather than a crime's) — giving a 10 crime-date / 10 phone-call split. The 6 M1 dates kept (T01/T02/T03/T04/T07/T08 → days 11/26/4/9/20/31) were chosen for spread across the month and strong same-day-vs-trailing-week divergence (~3-8x); T16 (BL1)/T17-T19 (citywide)/T20 unchanged. All new items' Cypher was executed live against `bolt://localhost:7687` before being written (no invented numbers). **Re-validation:** all 8 gates green on the live graph (`results/benchmark_pole_external_validation.json`, overwritten — supersedes the original 9.2 report per the user's explicit direction that amendment runs replace the original rather than accumulate); distribution still 100/80/{20,20,20,20}; coverage min 1 across all 17 relationship types (`FAMILY_REL`→1, `KNOWS`→1, both now solely from S01's retyped union — expected, not a gap); offline suite `tests/test_validate_benchmark.py` + `tests/test_pole_query.py` (23 tests) still green. `data/benchmark-pole-external.json` remains canonical under this amended content; 9.3 can proceed. |
| 2026-08-03 | 9.2 amendment follow-up — S01 fix reverted, replaced with a genuine item; two unambiguous-control items repurposed to fix the resulting coverage gap | User pushed back on the S01 fix above: the `[r]` wildcard and the explicit KNOWS-family union compute the *identical* result set (the same structural fact the amendment itself found), so retyping it doesn't make the question ambiguous — it's naturally how someone would write "who is X connected to" without enumerating the type inventory, and the question itself is unambiguous either way. User manually reverted S01's Cypher back to the pre-amendment `[r]` form and asked for a real replacement. | Replaced S01 with a 4th fresh 1-hop digital-comm item (Alan Hicks — union 8 people, KNOWS_PHONE 1/Cynthia Knight, KNOWS_SN 7), same axis and rigor as the 6 replacements above, live-verified before writing. This dropped `KNOWS` and `FAMILY_REL` to 0 coverage — S01's reverted-then-removed union clause had been the *only* item in the whole 100 exercising either literal type, an easy trap to fall into when a single item is silently your sole source of coverage for two rel types (a good example of why gate 8 exists rather than eyeballing it — this was live-caught by `gate_coverage`, not spotted by inspection). **Fixed the same way 9.2 originally closed its 3 zero-coverage gaps (decisions-log 2026-08-02):** repurposed 2 of the 5 near-identical `Officer badge → rank` unambiguous-control items (U01–U05 were otherwise repetitive — 5 instances of the same 0-hop property-lookup mechanism) into single-relationship-type lookups that are unambiguous by construction: U04 → "Who is Betty Kennedy related to by family?" (`FAMILY_REL`, 2 people: Donna Frazier, Anne Clark) and U05 → "Who does Denise Rodriguez know?" (generic `KNOWS`, 1 person: Alice Mcdonald) — both fresh subjects, live-verified, non-empty. Also improves control-set diversity (was 5x the same mechanism, now 3x) as a side effect, not just a coverage patch. **Re-validated live, all 8 gates green again**, report re-overwritten. Offline suite (23 tests) re-confirmed green. |
| 2026-08-05 | 9.3 | "Location has no single identifier property in this schema — either relax `EntityCache`'s non-null `id_prop` assumption or key Location lookups by `address` instead; resolve whichever is less invasive once 3.2's actual code is re-read at implementation time — this is exactly the kind of 'spec leaves something genuinely ambiguous' case `ai-workflow-rules.md` §3 says to flag rather than guess silently, so implementation should stop and check before picking." | Stopped and asked per the spec's own instruction (AskUserQuestion). Presented both options with their code implications; user chose keying `Location` by `address` (`ENTITY_REGISTRY_POLE_EXTERNAL`'s Location entry: `"id_prop": "address"`, not `None`). | Re-reading `pipeline/entity_lookup/cache.py::EntityCache.load` confirmed the Cypher builder does `f"n.\`{id_prop}\` AS node_id"` unconditionally — a `None` id_prop would either crash on `None` interpolation or (if guarded) require touching shared 3.2 code also used by the v2/v3 paths, with a byte-identity burden to prove those stay unaffected. Keying by `address` needed **zero** changes to `cache.py`/`EntityCandidate` — `address` is already `Location`'s first `name_prop` (so `node_id` and `display_name` are the same string, functionally fine, cosmetically redundant) — matching the "less invasive" framing the spec itself suggested as the likely answer. No code outside `pipeline/entity_lookup/registry.py` changed for this. |
| 2026-08-05 | 9.3 | Full live acceptance criteria 4-6 (`run_evaluation` against `config/pipeline_pole_external.yaml`, `analyze_qualitative.py` on the resulting `qual_records_*.json`) require a live external-POLE Neo4j at `bolt://localhost:7687` + `ANTHROPIC_API_KEY`, run **locally** (explicitly not this Kaya-oriented session/environment) | Implemented and offline-tested everything (schema/registry/prompts/`resolve_dataset`/config); did not attempt the live run — this session has neither `ANTHROPIC_API_KEY` nor a reachable `bolt://localhost:7687` (confirmed: `nc -z localhost 7687` fails). Criteria 1-3 verified; 4-6 deferred to the user's own machine, per the spec's own framing ("Running locally, not on Kaya") and this project's standing convention (rule 6/7) of implementing against the contract and marking live acceptance deferred when the required live resource isn't available in the implementing session. | No code consequence — this is an environment/availability note, not a design deviation. Recorded so a future session doesn't assume the live run already happened. To complete 4-6: `export NEO4J_PASSWORD=...` + `export ANTHROPIC_API_KEY=...`, confirm the external POLE graph is loaded and reachable at `bolt://localhost:7687`/db `neo4j`, then `python -m experiments.run_evaluation config/pipeline_pole_external.yaml` followed by `python -m experiments.analyze_qualitative results/qual_records_claude-sonnet_pole_external_local.json`. |
| 2026-08-05 | 9.3 | Live acceptance criteria 4-6 deferred (row above) pending local Neo4j + API key | User supplied both (Neo4j confirmed running at `bolt://localhost:7687`, 61,521 nodes, password `changeme`; a live `ANTHROPIC_API_KEY`) in the same session. Fixed one environment blocker found on the first attempt (`sentence_transformers` import crashed transitively via `transformers.integrations` pulling in a TensorFlow/Keras-3 codepath incompatible with the installed `transformers` version — `pip install tf-keras` resolved it; unrelated to any 9.3 code). Ran `python -m experiments.run_evaluation config/pipeline_pole_external.yaml` (~3h15m wall clock, 100×3 conditions, 0 tracebacks) then `python -m experiments.analyze_qualitative results/qual_records_claude-sonnet_pole_external_local.json`. All three run artefacts + the qualitative report now exist in `results/`. | **Headline (claude-sonnet, %):** EX C1 20.0 / C2 44.0 / C3 47.0; EA 20.0/48.0/53.0; **ambiguous EA 6.2/37.5/42.5 → C3 > C2 (success criterion #1 MET)**; monotonic C1≤C2≤C3 on both EX and EA (criterion #2 MET); Detection F1 77.6 (prec 96.3, rec 65.0) — comparable to Phase 8's v3 claude-sonnet run (73.9); DSR 38.9; KG-valid 83.0/100/100; structural repair 100%/100%, semantic repair 0.0/11.3 (the disambiguation retry loop measurably fixes some wrong_result cases here, unlike v3's 0.0/0.0). **Per-type EA — genuine architectural gains where the mechanism is representable as a schema-element choice:** entity 0→85→95 (huge C3 gain — the dominant win, matches this schema's real duplicate-name density), schema 0→20→30 (C3 gain), intent 25→45→45 (flat C2→C3, consistent with intent being the hardest type per project-overview §9 criterion #4). **Temporal EA stuck at 0/0/0 across all three conditions — investigated via the qualitative report, root cause is NOT a disambiguation-architecture failure:** the AD correctly detects temporal ambiguity and the Disambiguator correctly commits the right schema path (OCCURRED_AT→LOCATION_IN_AREA), but `SchemaMapping`/`committed_pattern` (0.1's contract) only carries schema-element choices (relationship types, node labels) — it has no slot for a *literal value* like a date-window boundary. This external graph's temporal ambiguity mechanism (9.1 finding: same-day-vs-trailing-week on `Crime.date`, a raw string in `DD/MM/YYYY` format) is a WHERE-clause literal-value choice, not a schema-pattern choice — structurally outside what the Disambiguator's output contract can express, unlike v2/v3's edge-state (`active:true`) mechanism which IS a schema-element choice (a relationship-property filter) and which the same architecture handles fine. Compounding it: the un-fine-tuned QG independently emits `c.date >= '2017-08-09'` (ISO format) against data stored as `'09/08/2017'` (DD/MM/YYYY) — a format mismatch that alone would zero every temporal item regardless of disambiguation. This is a genuine, informative external-validity finding (the architecture's schema-level commitment mechanism doesn't cover value-level/WHERE-clause ambiguity) rather than a defect to fix inside 9.3's scope — recorded per `ai-workflow-rules.md` §2 ("if you find an earlier step is wrong or incomplete, stop and report it — do not silently fix it mid-step"). Not fixed here. |

| 2026-08-06 | 9.3 follow-up / 9.4 spec | User asked to investigate the 9.3 temporal 0/0/0 finding further and plan a fix, rather than leave it as an out-of-scope architectural finding. | Re-examined the 9.3 finding directly against `results/qual_records_claude-sonnet_pole_external_local.json` and `data/benchmark-pole-external.json` rather than taking the 2026-08-05 summary at face value, and found the picture more specific than "one architectural gap": (1) the 20 temporal items are actually **three** structurally different mechanisms, not one — Crime.date day-window (10), PhoneCall.call_date day-window (3), and most-recent-contact-vs-full-history (7, a query-shape/LIMIT choice, not a date-window choice at all); (2) the AD has **0% recall** on the third mechanism (`ad_predicted_ambiguous: False` on all 7 in the live run) because its TEMPORAL few-shot only ever taught the day-window case — this is upstream of and independent from the disambiguation-contract finding; (3) re-checked the "contract can't carry a literal value" claim from 2026-08-05 against v2/v3's own Dis few-shot and found it's **not actually a contract limitation** — `_DIS_FEW_SHOT_V3` Example 3 already embeds `{active:true}` directly in `committed_pattern` for v2/v3's temporal mechanism, proving `cypher_syntax` (an unconstrained string) already carries resolved literals elsewhere in this codebase; the POLE-external Dis few-shot authored in 9.3 just never did the equivalent for the date case; (4) confirmed via direct Cypher inspection that the QG's API-model prompt (`pipeline/query_generator/prompts.py`) has never had a `pole_external` branch at all — unlike every other stage, `_QG_FEW_SHOT_API` is one block reused verbatim from the v2/v3 dataset, with zero date-format or property-name grounding for this schema; this alone explains why even the C1 zero-shot baseline is 0/20 (10/20 items get ISO-format dates against `DD/MM/YYYY` data in every condition; the other 10/20 get hallucinated `PhoneCall` property names like `callDate`/`datetime`/`start` despite the real names being listed in the prompt). | Wrote `context/feature-specs/phase9/9.4-pole-external-temporal-fix.md` scoping the fix as three additive, contract-preserving changes (AD gets a 5th few-shot + widened TEMPORAL definition; Dis's temporal few-shots rewritten to embed the resolved decision — day-of-month extraction idiom for the window mechanism, matching the benchmark's own gold Cypher logic, chosen over full calendar-date arithmetic per the user's direction that it should generalize beyond a same-day/trailing-week binary; ORDER BY/LIMIT embedding for the scope mechanism; QG gets its own real-schema-grounded few-shot branch via a new `qg_few_shot` param threaded through `resolve_dataset`/`build_condition*`/`QueryGenerator`, v2/v3 staying `None`/byte-identical). No `pipeline/types.py` (0.1) contract change needed — `cypher_syntax` already supported this. Progress-tracker gained a `9.4` row (`not started`, depends on 9.3 done). Not implemented this session — spec only, per `ai-workflow-rules.md` (don't start coding before the spec is read/agreed); next session implements against this spec. |
| 2026-08-06 | 9.4 | The spec's own "check at implementation time" flag (§2, per `ai-workflow-rules.md` §3): "confirm `parse_schema_pattern` doesn't raise/crash on the extended patterns (as opposed to silently truncating) before relying on this" — `_pattern_to_committed`/`parse_schema_pattern` were called out as needing a "defensive check" for `committed_pattern` strings that now carry a trailing WHERE/ORDER BY/LIMIT clause (the Dis fix, below). | Verified directly (not just traced) with the three actual new Dis few-shot patterns run through the real `parse_schema_pattern`/`_parse_disambiguator_output` — none raised, and `SchemaMapping.cypher_syntax` came back byte-identical to the input string in every case (see `tests/test_pole_external_integration.py::test_dis_temporal_committed_pattern_parses_and_survives_verbatim`, parametrized over all three). **No code change made to `disambiguator.py`.** | The WHERE/ORDER BY clause is comma-free (only the `LIMIT 1` example's `ORDER BY x DESC, y DESC` has a top-level comma, which silently drops the second sort key from the *secondary* `committed` dict only — never raises) and the WHERE clause's nested `split(...)` call is bracket-balanced, so `parse_schema_pattern`'s existing depth-aware scanner treats the whole trailing clause as one dangling pseudo-relationship with `rtype=None`, which `_pattern_to_committed`'s existing `if rtype is None: continue` guard already skips. `cypher_syntax` is set directly from the LLM's raw string in `_parse_disambiguator_output`, never touched by `_pattern_to_committed`, so it was always going to survive verbatim regardless. This satisfies acceptance criterion 3 with zero changes to `disambiguator.py`; the spec's module list flagged it as a touch point defensively, not as a guaranteed edit. |
| 2026-08-06 | 9.4 | Spec §3: "Add one instruction line to the system prompt: when `committed_pattern` already contains a WHERE/ORDER BY/LIMIT clause, copy it verbatim..." — ambiguous whether this means the shared `QG_SYSTEM_API` (used by every dataset version) or a pole_external-only addition. | Made the instruction line conditional on the caller supplying a `few_shot` override to `build_qg_prompt_api` (i.e. it is appended to `QG_SYSTEM_API` only when `few_shot is not None` — in practice only the pole_external branch ever passes one). `None` (v2/v3) renders the exact pre-9.4 system prompt text. | Acceptance criterion 1 requires v2/v3's rendered QG prompt (system text included) to stay byte-identical to pre-9.4 output. Editing the shared `QG_SYSTEM_API` constant directly would have changed v2/v3's prompt too, since `build_qg_prompt_api` is the one function all three dataset versions call through `QueryGenerator(prompt_style="instruct")`. Tying the instruction to the few-shot override keeps the two changes (new examples + new instruction) coupled to the same trigger, which also matches the instruction's own content — it only makes sense once the Dis few-shot teaches embedded WHERE/ORDER BY/LIMIT clauses, which only the pole_external Dis prompt does. |
| 2026-08-06 | 9.4 | Spec §3 QG few-shot bullets list exactly two worked examples (Crime.date day-window; a PhoneCall query). | `_QG_FEW_SHOT_API_POLE_EXTERNAL` has **three**: the day-window Crime example, a PhoneCall day-window example using the `CALLER\|CALLED` bidirectional alternation (mechanism 2's specific defect — "only matches CALLER, never CALLED"), and a third most-recent-contact example demonstrating the `ORDER BY ... LIMIT 1` convention the Dis fix (above) now embeds for the scope mechanism. | The spec's second bullet ("a PhoneCall most-recent-contact query using ... the bidirectional `<-[:CALLER\|CALLED]-` pattern") conflates two distinct real mechanisms: the alternation pattern is only correct for a single phone's own call history matched in either direction (mechanism 2, T13-15 — confirmed the specific documented defect), not the two-distinct-phones most-recent-contact query (mechanism 3, T05 — gold Cypher uses two directed legs, `<-[:CALLER]-...-[:CALLED]->`, never the alternation). Rather than pick one reading and under-serve the other, both are grounded as separate examples (each traceable to real benchmark items/gold Cypher), plus the ORDER BY/LIMIT example reinforces the new "copy verbatim" system instruction with a worked case. Additive only — does not contradict either literal bullet. |
| 2026-08-06 | 9.4 (ops) | Criterion 5's live rerun (`python -m experiments.run_evaluation config/pipeline_pole_external.yaml`) launched via the Bash tool's own `run_in_background: true` tracking. | That attempt was killed by the harness itself (`status: killed`, no traceback in the log) after ~38 min, partway through Condition 3 — well short of the ~3h15m the 9.3 precedent took. No results files were written (confirmed by timestamp — still the 9.3-dated files). Relaunched fully detached from harness tracking: `nohup /usr/local/bin/python3 -m experiments.run_evaluation ... > /tmp/run_9.4_pole_external.log 2>&1 < /dev/null &` (no `setsid` — not available on macOS), confirmed the process reparented to PPID 1 (fully orphaned, survives the tool call's own process tree). | The harness's background-task tracking appears to have a wall-clock limit well under 3h15m; not documented elsewhere in this repo's ops notes, so recording it here for any future long (>1h) local live run — use a detached `nohup`, poll the PID/log manually (e.g. via `ScheduleWakeup`), and don't rely on the harness's own completion notification for anything longer than roughly half an hour. |
| 2026-08-07 | Phase 9 follow-up (qualitative doc, mirrors the v3 artefact set) | User asked for a qualitative C2-vs-C3 comparison doc for the external POLE run "like we did for our synthetic one," plus how many questions flipped (fixed/regressed) under EX vs EA specifically. | Extracted the 4 ambiguous-type tables from `results/qual_records_claude-sonnet_pole_external_local.json` (the canonical post-9.4 run) into a per-type JSON and adapted `docs/build_qual_comparison_docx.js` (byte-for-structure, metadata/notes updated) to render `docs/qual-comparison-tables-pole-external-claude-sonnet{,-ea}.docx` — green/red-shaded per-question Cypher tables, one per ambiguity_type, `METRIC=ex\|ea` env var as before. Wrote `docs/qualitative-analysis-pole-external-claude-sonnet.md`, the narrative companion (mirrors `docs/qualitative-analysis-v3-claude-sonnet.md`'s structure: headline bucket table, per-type stratification, EX-vs-EA section, what's-working/where-it-struggles, takeaways). Computed EX vs EA bucketing directly (both over the full 100-item join): **EX — fixed 7, regressed 2** (`Q-POLE-E11/E13/E16/S05/T13/T14/U04` fixed; `Q-POLE-E02`, `Q-POLE-I05` regressed); **EA — fixed 7 (identical set), regressed 1** (only `Q-POLE-I05`; `Q-POLE-E02` drops out — C3's specific pick still matches a non-default gold interpretation under the lenient metric). Verified both docx by PDF render before finalizing. | Unlike the v3 substrate — where EA collapsed 18 EX-fixed items down to 5 (most "fixes" only cleared the lenient bar) — here the fixed set is byte-identical between EX and EA; the only metric-dependent item is the regression `Q-POLE-E02`, worth flagging as a genuinely different case (C3 correctly detects the Anne-Rice entity duplicate but commits to one specific person rather than the benchmark's "list both" default — a real regression under EX, a valid non-default resolution under EA). Also recorded, since it fell out of the same analysis: schema ambiguity inverts between graphs — v3's second-best fixed category (4/20), this graph's worst (1/20 fixed, 19/20 both_wrong under EX) — attributed to the digital-contact-channel axis and compound multi-hop crime-connection queries sitting closer to the QG's capability ceiling here than v3's role-edge axis did; intent ambiguity replicates the v3 finding exactly (0/20 fixed on both benchmarks). Files built in the outputs scratchpad (not `docs/` directly) per the 2026-07-27 operational note, then copied in once verified — no stray `node_modules`/`package.json` left in the tracked repo. |
| 2026-08-06 | 9.4 | **CRITERION 5 RAN FOR REAL AND CLEARED THE SUCCESS BAR.** Detached run completed ~13:00 AWST (~1h wall clock — notably faster than 9.3's 3h15m, likely run-to-run API latency variance, not a code effect), wrote `results/tables_claude-sonnet_pole_external_local.md` + `metrics_...json` + `qual_records_...json`, then `python -m experiments.analyze_qualitative results/qual_records_claude-sonnet_pole_external_local.json` regenerated `qualitative_analysis_claude-sonnet_pole_external_local.md`. | **Temporal EA (the headline number): 9.3's 0.0/0.0/0.0 → 9.4's 0.0/85.0/95.0 (C1/C2/C3).** Success bar met decisively — nonzero in C2 **and** C3, and C3 (95.0) > C2 (85.0), i.e. disambiguation demonstrably adds value beyond schema-grounding alone, not just "format-fixed-but-flat" (the spec's own stated risk). Mechanism-level breakdown confirms all three defects closed, not just the aggregate: **mechanism 1** (Crime.date day-window, Defect C/QG fix) 0/10→9/10→9/10; **mechanism 2** (PhoneCall day-window + `CALLER\|CALLED` bidirectional, Defect B+C fix) 0/3→**1/3→3/3** — the one place C3 clearly beats C2, directly showing the Disambiguator's embedded-literal fix earning its keep at the item level; **mechanism 3** (most-recent-vs-full-history, Defect A+B fix) 0/7→7/7→7/7 — AD's 5th few-shot fixed the 0%-recall detection gap and the ORDER BY/LIMIT convention (both the Dis embedding and the QG few-shot teach it) resolved every item. **Side effects on other metrics (positive, not part of the fix's direct scope):** overall EX 20.0/44.0/47.0→20.0/**59.0/64.0**; overall EA 20.0/48.0/53.0→21.0/**64.0/70.0**; Detection F1 77.6→**82.0** (recall 65.0→**71.2**, consistent with the AD fix improving recall on the mechanism-3 items specifically); DSR 38.9→**64.4**. Schema EA ticked up slightly (0/20/30→0/25/30, C2 +5pp) — plausibly incidental (the QG few-shot's Crime.date example may generalize slightly beyond temporal items) rather than a targeted effect. **Intent EA moved within noise** (25/45/45→30/40/35, i.e. C3 −10pp) — intent was untouched by any 9.4 change; attributed to the API's stochastic sampling (temperature 0.3 SL decoding + non-deterministic LLM calls) across two separate live runs on different days, not a regression, consistent with intent being flagged project-wide as the hardest/noisiest type (project-overview §9 criterion 4). Entity EA also improved slightly (0/85/95→0/80/95, C2 −5pp/C3 flat) — same noise attribution. **Phase 9's temporal finding is now "was three specific, fixed defects," not "architecturally unaddressable, out of scope"** — the stronger dissertation result the spec's Handoff section anticipated. v2/v3 artefacts in `results/` untouched; the `pole_external_local`-tagged files were overwritten in place (same tag, per spec — a fix to the same benchmark run, not a new variant). **Step 9.4 done.** |
| 2026-08-21 | 10.1 | §"Getting the data onto Kaya" offers two provenance paths (upstream release dump vs local export) and flags Neo4j version-compat as "a real risk to check, not assume" — a pre-5.26 dump may need `--from-path` + a first-start store upgrade or may not load. Record whatever happens. | Chose **path 1 (upstream release dump)** — the Kaya login node has internet, and the Phase-9 local instance is off-cluster (not reachable to export from). Downloaded `neo4j-graph-examples/pole` `data/pole-50.dump` (the Neo4j-5.x variant; header `DZV1` + zstd magic `28 b5 2f fd`, 4,603,972 bytes, sha256 `f370895b…92aa`), staged as `$MYGROUP/dumps/pole-external/neo4j.dump` (name required by `neo4j-admin database load neo4j`). Loaded via `neo4j-admin database load neo4j --from-path=/dumps --overwrite-destination` on a fresh volume **before** `neo4j console`. **Version-compat outcome: loaded cleanly, no upgrade handshake needed** — the neo4j:5 image is **Neo4j 5.26.27**; the only diagnostic was a non-fatal `WARNING: … deprecated store format: standard` (the dump uses the legacy `standard`/block-less store, still fully supported in 5.26), the server started and served bolt normally, no `dbms.allow_upgrade` flag required. | The upstream path is the natural Kaya path (login-node internet; no off-cluster export dependency). `pole-50` is the 5.x-format dump, matching the image, so the documented pre-5.26 migration risk did not materialise. Spot-check on the loaded graph: **61,521 nodes / 105,840 relationships** — exactly 9.3's live node count and in line with 9.1's ~105.8k, proving the same real graph (not a partial load). All 8 gates green via `validate_benchmark.py --profile pole_external` (job 1100495, `results/benchmark_pole_external_validation.json` `passed:true`), matching 9.2's local result. |
| 2026-08-21 | 10.2 (external SL/QG SFT target derivation + temporal `CALLER\|CALLED` SL target) | Spec: "the two-key contract holds; `extract_schema_pattern(qg) == sl` for every generated row (the 1.3/7.4 train-format guard)"; and for temporal QG completions "reuse the exact patterns 9.4 baked into `_QG_FEW_SHOT_API_POLE_EXTERNAL` (`toInteger(split(...))` day-of-month window incl. `CALLER\|CALLED` bidirectional alternation; `ORDER BY ... LIMIT 1` for scope)". The spec did not prescribe HOW the SL target is produced, only that the guard holds and that no `{active:true}` variant is present. | Two interpretive choices in `build_pole_external_sft_dataset`: **(1)** every external row derives its SL completion as `extract_schema_pattern(cypher)` of its own QG Cypher (dropping the row if that yields no typed pattern), rather than rendering the SL pattern independently as the v3 path does — so the guard holds by construction and the temporal register can freely append a WHERE / ORDER BY / LIMIT suffix to the QG without a second-source pattern drifting. **(2)** the PhoneCall day-window temporal register emits the bidirectional `<-[:CALLER\|CALLED]-` alternation the spec explicitly asks for; consequently its SL target is `(ph:Phone)<-[:CALLER\|CALLED]-(pc:PhoneCall)`, which `parse_schema_pattern` treats as a single non-schema rel type (`CALLER\|CALLED`) and therefore reports `is_valid=False`. This is acceptable: the parse-validity acceptance gate (criterion 1) applies only to `enumerate_pole_external_patterns()` output (verified all 581 valid, no disjunctions, no `{active:true}`), NOT to the temporal-register training rows, and the union IS the benchmark's own default for phone-call temporals. Also **(3)** Email/PostCode-secondary leaf seed values are plausible placeholders (real `Email.email_address` values were not harvestable offline); every other pool (names, areas, phones, regs, crime types, badges, day-windows) is real, sampled from `data/benchmark-pole-external.json` gold Cypher. Emails only ground a referring phrase and gate no acceptance criterion. | Deriving SL from the QG makes the 1.3/7.4 train-format guard a structural invariant instead of a coincidence of two independent renderers, which matters here because external temporal rows carry non-pattern trailing clauses. Emitting `CALLER\|CALLED` follows the spec's explicit instruction and matches the graph's real default (every phone-call temporal item in the benchmark unions the two directions). Placeholder emails avoid fabricating "real-looking" identifiers while keeping the offline generation self-contained (no live Neo4j needed for 10.2, per the spec's "No GPU" / offline framing). |
| 2026-08-22 | 10.5 | The registry entry carries two explicitly open choices the spec asks to record: `base: "Qwen/Qwen2.5-32B"` BASE "(If a strong C1 is wanted, -Instruct is the alternative — record the choice)" and `target_modules: ["q_proj","v_proj"]` ("fuller `["q_proj","k_proj","v_proj","o_proj"]` is the alternative"). The fit-test spec also lists exactly five things to "measure and print, at minimum", one of which is "One QLoRA train step … `per_device_train_batch_size: 2` + gradient checkpointing". | Took the spec's primary option on both: **BASE, not -Instruct** (`Qwen/Qwen2.5-32B`), and **`["q_proj","v_proj"]`**. Added two things beyond the "at minimum" list. (1) `kaya/qwen_fit_test.py --phase train` runs **two legs off one model load** — gradient checkpointing ON (what 10.5 asks for) and gradient checkpointing OFF, the latter caught for `torch.cuda.OutOfMemoryError` and recorded rather than raised. (2) The runner pins `--gres=gpu:v100-32gb:2` rather than the spec's plain `--gres=gpu:2`. | **BASE** keeps the model-axis comparison honest: mistral7b is a v0.3 *base*, so C1 stays a zero-shot base baseline and C2/C3 isolate the SFT effect — an -Instruct Qwen would confound "32B vs 7B" with "instruct-tuned vs not" in the same column. **`q_proj`/`v_proj`** mirrors mistral7b for the same reason (same LoRA surface on both model columns), and is the cheaper surface for a 32B QLoRA run; the fuller set stays available as a config-only change if 10.6's eval loss disappoints. **The second train leg exists because `run_sft` (2.1) never sets `gradient_checkpointing` in its `TrainingArguments`** — so the spec's checkpointed measurement, on its own, would NOT tell 10.6 whether the training path it will actually execute fits. If the plain leg OOMs, that is 10.6's signal that `run_sft` needs the flag (a 2.1 change) or a smaller batch; measuring it costs one extra minute on an already-loaded model. **The gres pin** enforces the spec's own hard requirement ("must run on a v100-32gb node (k017–020), not a 16GB V100") — plain `gpu:2` is schedulable onto the 16GB k028-k044 nodes, where a 32B base cannot load at all. |
| 2026-08-22 | 10.5 (fit-test result) | Criterion 3: "One QLoRA step + one diverse-beam k=5 generate both succeed at the locked precision with no OOM." The registry entry ships `per_device_train_batch_size` implicitly from the 10.3 YAML (2). | **First run (job 1100717, k020, 2× v100-32gb-PCIE) OOM'd the train leg at batch 2** and I re-ran the train leg alone at `BATCH=1` (job 1101258). Measured: **load 4-bit 20.2 GiB total** (device_map="auto" sharded 8.5+12.2 GiB across the two visible cards, but the footprint fits one 32 GB card — for INFERENCE 1 GPU suffices); **load 8-bit 32.9 GiB total** (14.5+19.2 — genuinely spans 2 cards; the ~32 GB the spec predicted); **generate k=5 diverse-beam 40.0 s**, 5 distinct beams, correct top beam `(Person {surname:"George",name:"Andrea"})-[:CURRENT_ADDRESS]->(Location)`. **Train batch 2 OOM** = the fp32 logits upcast for the loss: Qwen's 152,064-vocab × seq 3584 × batch 2 × 4 B = **4.06 GiB exactly** (matches the OOM's "Tried to allocate 4.06 GiB"), landing on the lm_head's card (GPU 1, ~14.4 GiB of sharded weights) on TOP of the base — over 32 GB even with gradient checkpointing ON and sharded across 2 cards. Locked precision stays **4-bit/fp16**; the actionable output for 10.6 is **`per_device_train_batch_size: 1`** (halves the logits tensor), keeping the effective batch via `gradient_accumulation_steps` (2→32 vs the 10.3 YAML's 16). | The fit-test did exactly what the spec built it for — a cheap GO/NO-GO that caught the batch-2 OOM before 10.6 spent days of GPU time discovering it mid-run. The OOM is a large-vocab-loss artefact (152k vocab is ~4.9× Mistral's 32k, so the same seq×batch that trained Mistral-7B fine at 3584×2 does not fit Qwen-32B), NOT a "Qwen won't run" verdict — load and generate both succeed. Batch 1 is the standard fix and is config-only in 10.6's YAMLs (no `run_sft`/pipeline change). 8-bit is ruled out for single-card inference (32.9 GiB > 32 GiB) with no accuracy upside worth two cards, confirming 4-bit as the lock. |
| 2026-08-22 | 10.5 (batch-1 re-run confirmed + a second, 10.6-blocking finding) | Follow-up to the row above: does `per_device_train_batch_size: 1` actually fit, and does `run_sft` (2.1) train Qwen unchanged? | **Re-run job 1101258 (k020, batch 1): the QLoRA step SUCCEEDS** — 3 steps, loss **0.699→0.656→0.601** (real learning), steady **14.90 s/step**, peak single-GPU **25.3 GiB** (comfortable on a 32 GB card), sharded across 2 cards by device_map="auto". Criterion 3 MET (step + generate both succeed, no OOM, latencies recorded). **BUT the harness's second leg (gradient checkpointing OFF = what `run_sft`'s `TrainingArguments` actually does today) OOMs even at batch 1** (378 MiB short, GPU 0 at 31.38/31.73 GiB). So **10.6 CANNOT point `run_sft` at the Qwen YAMLs unchanged** — `run_sft` never sets `gradient_checkpointing`, and Qwen-32B needs it. **Budget:** batch 1 × 3 epochs ≈ **72 h/adapter**, ~**293 h** for all four sl/qg×v3/external adapters — exceeds the 72 h job cap per adapter, so 10.6's planned `--dependency=afterany` chaining is mandatory, not optional. | Confirms the lock (4-bit/fp16, batch 1) with real numbers and hands 10.6 two hard requirements its spec did not yet encode: (1) **enable gradient checkpointing** — a genuine `run_sft`/`TrainingArguments` change (2.1 owns that signature; the change is `gradient_checkpointing=True` + `gradient_checkpointing_kwargs={"use_reentrant": False}`, threaded from a train-config key so v2/v3 Mistral runs stay byte-identical), without which every Qwen run dies at step 0; (2) **budget ~12 days of GPU wall-clock** and chain the jobs. Both are cheaper to know now than to discover 3 min into a 72 h job. The measured 14.9 s/step is the number 10.7's wall-clock budget extrapolates from. |
| 2026-08-22 | 10.4 (runner numbering + G1/G3 gate siblings) | Spec §Modules names the G4 runner `kaya/96_run_evaluation_pole_external_mistral.slurm` and says the step "Reuses ... the `kaya/g1_*`/sweep/probe gate runners from 7.5." | Renumbered the G4 runner to **`kaya/100_run_evaluation_pole_external_mistral.slurm`** (`96_`/`97_` were claimed by 10.3's `96_sl_pole_external_cov_check.slurm` + `97_qwen_fit_test.slurm` after this spec was written), and authored **thin external siblings** for the two gate runners that are version-hardcoded rather than flag-driven: `kaya/g1_sl_coverage_pole_external.py` + `kaya/98_g1_coverage_pole_external.slurm` (G1) and `kaya/ad_probe_pole_external.py` + `kaya/99_ad_probe_pole_external.slurm` (G3). Each is a byte-for-structure copy of its `_v3` sibling differing only in substrate (`build_pole_external_schema_repr`, `data/benchmark-pole-external.json`, `_pole_external` adapter suffix, `registry_for_version("pole_external")`, `AD_SYSTEM_PROMPT_POLE_EXTERNAL`). No pipeline/CLI contract change. | The numbering is cosmetic; keeping the run-runner's descriptive name recognizable matters more than the literal digits, and the collision was created by a later step. `g1_sl_coverage_v3.py` hard-codes `build_pole_v3_schema_repr()`/`_v3` defaults and `ad_probe_v3.py` hard-codes the v3 schema/registry/prompt, so "reuse" is impossible without either editing those 7.5-owned files (rule 2 forbids improving files outside this step) or authoring siblings — 10.3 set the exact precedent (`sl_pole_external_cov_check.py`, "no contract change"). |
| 2026-08-22 | 10.4 (G2 sweep reuse + entropy-probe deferral) | Spec §"SL decoding": run a `78_sweep_v3`-style sweep with `--dataset_version pole_external --results_tag pole_external` to lock `decoding[mistral7b]`, OR "if the sweep is skipped for budget, **reuse the v3-locked `diversity_penalty: 1.0` and say so explicitly**". §Gates G2 also lists an entropy probe `--dataset_version pole_external`. | Took the spec's documented escape: **reuse the v3-locked `diversity_penalty: 1.0`** (config/schema_linker_inference.yaml `decoding[mistral7b]`, unchanged) rather than re-running the sweep — recorded in `config/pipeline_pole_external_mistral.yaml`'s comment. The G2 **entropy probe is deferred**: `experiments/diversity_penalty_sweep.py` and `experiments/entropy_probe.py` both restrict `--dataset_version` to `choices=["v2","v3"]`, so neither accepts `pole_external` without a code change to those 7.5/5.5-owned CLIs (out of 10.4 scope, rule 2). | Directly analogous to 9.3 reusing the Phase-8 claude temperature on this same schema, and justified by evidence: 10.3's 25-item sample Cov@5 = **0.840** at dp=1.0 on this exact adapter/schema shows SL is not decoding-limited here, and G1 re-measures Cov@5 over all 100 items as the real proceed-gate. The entropy probe is criterion-#6 (secondary-signal) evidence, not a proceed-gate; its beam-diversity confirmation is already carried by G1's per-type coverage. A dedicated re-sweep/probe (widen the two CLIs' `choices`) is optional future work, not a blocker for the fine-tuned-local column this step fills. |
| 2026-08-22 | 10.6 (run_sft gradient-checkpointing flag) | §"Required `run_sft` change": thread a `training.gradient_checkpointing` flag (default **False**) from the train config into `run_sft`'s `TrainingArguments`; when true also pass `gradient_checkpointing_kwargs={"use_reentrant": False}`; leave it absent/false in every Mistral YAML so those runs stay byte-identical. Spec says this is a real `pipeline/sft.py` contract touch → record it here. | Did exactly that, and factored the `TrainingArguments(...)` call out into a private `pipeline.sft._training_arguments_kwargs(train_cfg, has_eval) -> dict` that `run_sft` splats. The optional key is documented in `run_sft`'s config-key contract docstring. No public signature changed; `run_sft(config)` is unchanged. | The flag itself is mandated by the 10.5 fit-test (Qwen2.5-32B OOMs at batch 1 without checkpointing; `use_reentrant=False` is the PEFT/4-bit-compatible implementation). Extracting the kwargs dict is what makes acceptance criterion 1 (**"Mistral SL/QG `TrainingArguments` are byte-identical when the flag is absent/false"**) an actual assertion instead of a `git stash` A/B a future session has to redo: `tests/test_sft.py::test_mistral_training_arguments_unchanged_by_the_new_flag` parametrises all six Mistral train YAMLs and compares the full kwargs dict against the frozen pre-10.6 literal. Same precedent as 2.1's `build_run_config` extraction (logged 2026-06-07) — make the contract testable without loading a 7B/32B base. |
| 2026-08-22 | 10.6 (4k-Ozsoy sample step) | §"Stage 1": a 4,000-row seed=13 sample of the full Ozsoy training files — "produce the sample deterministically (a small prep step or `shuf --random-source` with a fixed seed; **record how**)". | New CLI `experiments/sample_ozsoy_subset.py` (`sample_jsonl(src, dest, size, seed=13)`): `random.Random(13).sample(rows, 4000)` over the jsonl **lines in file order**, rows copied verbatim. Ran it → `data/ozsoy_sl_train_4k.jsonl` (from `ozsoy_schema_linker_train.jsonl`, 15,262 rows) and `data/ozsoy_qg_train_4k.jsonl` (from `ozsoy_qg_train.jsonl`, 32,339 rows). Regenerate with `python -m experiments.sample_ozsoy_subset`. Tests: `tests/test_sample_ozsoy_subset.py` (5). | Chosen over `shuf --random-source` because it is the *same* idiom `pipeline.pole_sft_data.build_mixed_training_file` already uses for its seed=13 Ozsoy replay slice (one notion of "a seeded Ozsoy sample" in the project), it is reproducible on any machine from the committed source splits (`shuf --random-source` output depends on the coreutils version), and copying lines verbatim preserves the two-key `{prompt,completion}` train-format contract by construction. The `.jsonl` outputs are gitignored like every other generated split. |
| 2026-08-22 | 10.6 (Kaya runner numbering + one-file DAG) | §Modules: "Kaya runners `kaya/98_train_qwen_{sl,qg}.slurm` (generic + its two continues, dependency-gated)"; §Compute: "`kaya/98_train_qwen_sl.slurm` submits the SL generic and its v3/external continues with `--dependency=afterok:<generic>`; `99` does the same for QG". | Renumbered to **`kaya/101_train_qwen_sl.slurm`** + **`kaya/102_train_qwen_qg.slurm`** (98/99/100 were claimed by 10.4 after this spec was written). Each is ONE file holding both the submitter and the job: run as `bash kaya/101_train_qwen_sl.slurm` (no `SLURM_JOB_ID` in env) it submits the three-job DAG — generic, then v3 and pole_external each `--dependency=afterok:<generic>` with `--time=1-00:00:00` — and exits; run under Slurm it trains the single stage named by `$STAGE` (`generic`\|`v3`\|`pole_external`, default `generic`), selecting the matching train YAML. | Same cosmetic-collision reasoning as the 10.4 renumber row above. The one-file form is because a `--dependency=afterok:<id>` needs the generic job's id at *submit* time, so the DAG cannot be expressed from inside the running job; the alternative (a separate `submit_*.sh` wrapper plus three near-identical `.slurm` files) duplicates the module/env/`nvidia-smi` preamble four times for no gain. Re-running one stage stays a one-liner: `sbatch --time=1-00:00:00 --export=ALL,STAGE=v3 kaya/101_train_qwen_sl.slurm`. |
| 2026-08-22 | 10.6 (generic-stage wall-clock budget) | §Compute: generic ≈**50 h** each ("≈4000 × 3 ÷ batch 1 = 12,000 micro-steps × 14.9 s"), "no single job exceeds the 72 h cap". | Requested the full **`--time=3-00:00:00` (72 h)** for the two generic jobs (the continues keep `--time=1-00:00:00`), and documented in both runner headers that a cap-hit is recoverable by re-submitting the same stage. | The 49.7 h figure counts optimiser steps only; each generic run also does **three full Ozsoy eval passes** (`evaluation_strategy: epoch` at `per_device_eval_batch_size: 1` over 1,695 SL / 3,593 QG eval rows) — a few extra hours at 32B/4-bit that the estimate omits. Asking for the cap costs nothing but queue priority and removes a silent-timeout failure mode. If a job does hit 72 h, `save_strategy: epoch` + `run_sft`'s resume-from-checkpoint (2.1) mean a re-submit continues from the last completed epoch, and the `afterok`-gated continues simply stay queued until it succeeds. |
| 2026-08-22 | 10.4 (QG properties defect found post-G4; run superseded) | 10.4's config was authored as a standalone copy of 9.3's `config/pipeline_pole_external.yaml` (the spec's own instruction: "Standalone copy ... mirrors 9.3's config but local model"), which does not set `qg_include_properties`. | **The first G4 run (job 1101275) is superseded.** `QueryGenerator` derives `include_properties` from `prompt_style` (`generator.py`: None → `prompt_style == "instruct"`), so 9.3's API model got the properties block by default while 10.4's LOCAL mistral7b (`prompt_style="completion"`) did **not** — the QG was never shown the schema's property names. Added `qg_include_properties: true` to `config/pipeline_pole_external_mistral.yaml` and re-ran G4 to the SAME `pole_external_mistral` tag (job 1101814, k035, started 2026-08-22 21:51), overwriting in place per user instruction (one result set only); the properties-off metrics are kept at `results/_superseded/metrics_mistral7b_pole_external_mistral_PROPSOFF_job1101275.json` as a provenance record, not a published result set. | decisions-log 2026-07-17 already reframed properties-off as a **DEFECT rather than an experimental condition** (on the user's design argument that a stage given "the schema" cannot be given 90% of it) and made properties=True the canonical local configuration (`config/pipeline_v3_qgprops.yaml`); 10.4 inherited the defect by copying an API-model config. Evidence it bites here specifically: **4 of the external schema's 27 properties never appear in `data/pole_ext_qg_train_mixed.jsonl`** — incl. `nhs_no`, which the properties-off run mangled as `nhsNo` in Q-POLE-U19/U20 — so for those names the prompt is the QG's ONLY possible source. v3's single-factor ablation measured the cost at C2 EX 10.8→15.8, C3 EX 9.2→12.5, C2 KG-valid 65.8→74.2. **Also confounds the recorded Claude comparison:** Claude ran the instruct path (properties ON), the superseded Mistral run ran completion (properties OFF), so that EX 64-vs-16 gap was NOT single-factor; the re-run makes both sides properties-on. **Audit for other defects came back clean** — `qg_include_properties` was the ONLY key missing vs the canonical local config; 9.4's external QG few-shot is instruct-only BY DESIGN (the local path must preserve 3.6's train/inference match); the 10.2 SFT data is malformation-free (0/5980 rows), so the `(n:)` empty-label and variable-rebinding failures (~21 of 50 C3 invalids) are a genuine 7B generation limit that this fix does NOT address. |
| 2026-08-23 | 10.4 (properties-ON re-run — RESULTS; supersedes job 1101275) | Prediction in the row above: the missing Properties block explains the property-level errors (`nhsNo` for `nhs_no`, `registration` for `reg`), while the `(n:)` empty-label and variable-rebinding failures are a genuine 7B generation limit the fix would NOT address. | **Job 1101814 (k035, 2h55m13s, COMPLETED 0:0, 0 tracebacks, 100×3 verified) — both halves of the prediction confirmed.** Property errors closed: `nhsNo` occurrences **0** (was the mangled form in Q-POLE-U19/U20), `nhs_no` now 63; hallucinated-property invalids 5→4. Syntax malformation **byte-identical**: empty label `(n:)` **9→9**, variable rebinding **12→12** — unchanged, proving these are decoding-capacity, not prompt-content. Aggregate (off→on): EX C1 2→**14** / C2 13→**21** / C3 16→**24**; EA 2→14 / 13→21 / 19→**26**; KG-valid 26→46 / 45→58 / 50→62; invalid_query 50→**38**; correct 16→**24**; DSR 15.2→**30.4**; entity EA 25→**50**. Detection F1 **unchanged at 61.2** — the expected sanity result (the AD is upstream of the QG prompt, so a QG-only change must not move it). Architectural claims survive the correction: ambiguous EA C2 15.0 < C3 23.8 (criterion #1) and monotonic C1<C2<C3 on EX and EA (criterion #2). Per-type moved both ways on n=20 cells (entity 25→50, intent flat 25, schema C3 15→10, temporal C3 20→10) — ±2 questions, at/below this cell size's noise floor; not read as a real regression. | Confirms the properties block was a genuine harness defect worth the re-run, and simultaneously **localises the residual floor to Cypher well-formedness rather than schema understanding**: 0 out-of-schema labels/rel-types, Schema% 90.4, but Syntax% only 73 — the model knows the external schema and cannot reliably emit valid Cypher over it. That is the honest 7B-vs-instruct finding for the dissertation (overview §8: modest absolutes reported honestly), and it is now a **near single-factor** comparison against 9.3/9.4's claude column (EX 20/59/64) since both sides render properties. The unchanged malformation counts are the cleanest available evidence that the remaining gap is model capability, not configuration — a stronger claim than the properties-off run could support. |
| 2026-08-23 | 10.4 (properties-fix re-run RESULTS — supersedes job 1101275) | The entry above applied `qg_include_properties: true` and predicted a partial improvement, noting v3's single-factor ablation (C2 EX 10.8→15.8) and warning the `(n:)`/variable-rebinding syntax failures would be untouched. | **Re-run job 1101814 (k035, 2h55m, 0 tracebacks, 100×3 verified) is now the canonical Mistral× external column.** Fix confirmed at the mechanism level: `nhsNo` hallucinations **2→0**, `nhs_no` now used throughout. Single-factor deltas (off→on): **EX C1 2→14, C2 13→21, C3 16→24**; EA C1 2→14, C2 13→21, C3 19→26; KG-valid 26→46 / 45→58 / 50→62; ambiguous EA C3 21.2→23.8; **DSR 15.2→30.4**; Detection F1 unchanged at 61.2. Both success criteria still hold and are now on trustworthy numbers: monotonic C1<C2<C3 on EX and EA (criterion #2), ambiguous EA C3 23.8 > C2 15.0 (criterion #1). | The improvement is **larger than v3's** (+8pp C2 EX here vs +5pp there) and by far largest on **C1 (+12pp)** — expected, since C1 is zero-shot with no SFT to fall back on, so the prompt is its only schema source; this also retroactively validates the 2026-07-17 decision to include C1 in the properties-on column. Detection F1 holding exactly constant is a correctness check, not a null result: the AD runs upstream of the QG prompt and must be invariant to this flag. **The gap to Claude narrows but does not close** (Mistral 14/21/24 vs Claude 20/59/64 EX), and the residual is now cleanly attributable: with 0 out-of-schema labels/rel-types and clean SFT data, what remains is malformed Cypher generation (empty labels, variable rebinding) — a 7B capacity limit no config reaches, which is the honest architectural-vs-scale story the dissertation wants. Schema (C3 15→10) and temporal (C3 20→10) per-type dips are 1–2 questions on n=20 cells and are noise, not regressions. The Claude comparison is now near single-factor (both sides properties-on).|
| 2026-08-28 | 10.6 (H100 `rrifcs` partition for the last two continues) | §Compute: "All jobs `--gres=gpu:v100-32gb:2` (train peaks 25.3 GiB single-GPU / 43.5 GiB total; `device_map=\"auto\"` shards)". | The last two Stage-2 runs — `qg_adapter_v3` (job **1143214**) and `qg_adapter_pole_external` (job **1143215**) — were submitted to Kaya's new **`rrifcs` partition** (node k172, 4x **H100 NVL 93.6 GiB**, sm90) with **`--gres=gpu:h100:1`** (one GPU each, both concurrent on the node) instead of 2x v100-32gb. **No YAML, config or code change**: same `kaya/102_train_qwen_qg.slurm` with the partition/gres/time overridden on the `sbatch` command line, same `STAGE=` mechanism, same fp16 (NOT switched to bf16 despite sm90 supporting it — the four V100-trained adapters are fp16 and the recipe must stay identical across the six). The v100 jobs 1101280/1101281 were **held, not cancelled**, until the H100 runs succeed — two jobs writing one `output_dir` would corrupt each other's checkpoint state. | Access appeared 2026-08-28 (supervisor/HPC team added the user to `rrifcs-users`). Two independent wins: (a) the 32 GB nodes were **fully fragmented by single-GPU jobs** — all 8 GPUs on k017-k020 held by 2-3-day 1-GPU jobs from other users, so a 2-GPU request needed a whole node to clear by coincidence and Slurm's projected start had slipped to **Aug 30 22:30**; k172 was idle and both jobs started in **3 seconds**. (b) Measured **3.03 s/micro-step vs the V100's 13.98 s (4.6x)**, so ~14.5 h/~15.6 h becomes ~3.2 h/~3.5 h. One H100 NVL holds the whole 43.5 GiB training footprint, so no sharding is needed at all. Smoke-tested on k172 before committing: `/group` mounts, the conda env, the cached `Qwen/Qwen2.5-32B` snapshot, torch 2.6.0+cu124 on sm90 and a bitsandbytes 0.49.2 4-bit matmul all work unmodified. **Consequence for the writeup:** four of the six Qwen adapters were trained on 2x V100-32GB and two on 1x H100-NVL — identical code, config, precision and data, so the difference is hardware nondeterminism only, but it is recorded here for reproducibility. |
| 2026-08-28 | 10.7 (runner numbering) | §Modules names the two G4 runners `kaya/100_run_evaluation_v3_qwen.slurm` and `kaya/101_run_evaluation_pole_external_qwen.slurm`. | Renumbered the whole 10.7 runner set to **104–110**: `104_g1_coverage_qwen.slurm` (G1 + the attribution tiers), `105_sweep_v3_qwen.slurm` (G2a), `106_probe_v3_qwen.slurm` (G2b), `107_ad_probe_v3_qwen.slurm` / `108_ad_probe_pole_external_qwen.slurm` (G3), `109_run_evaluation_v3_qwen.slurm` / `110_run_evaluation_pole_external_qwen.slurm` (G4). Contents are exactly what the spec describes. | `100` was taken by 10.4's Mistral external G4 and `101`–`103` by 10.6's Qwen training DAG and cov-check, all before 10.7 started. Same collision 10.4 hit when the spec asked for `96` (decisions-log 2026-08-22). Numbers are ordering hints in this repo, not contracts. |
| 2026-08-28 | 10.7 (G2a sweep must not mutate the shared SL-inference config as a side effect) | §SL decoding: "Lock it via the G2a sweep (per substrate) in the gate chain" — i.e. run `experiments/diversity_penalty_sweep.py`, which calls `write_inference_config()` on `config/schema_linker_inference.yaml`. | `kaya/105_sweep_v3_qwen.slurm` points the sweep's `--inference_config` at a **scratch copy** and then merges back **only** the chosen `decoding[qwen2.5-32b]` block into the real file with a targeted regex, asserting afterwards that every other `decoding[*]` entry and the flat `diversity_penalty` mirror are unchanged. The lock is still the sweep's choice — only the write path is contained. Verified offline against a simulated dp=0.5 sweep: exactly one line changed. | `write_inference_config()` rewrites the whole YAML. It preserves other models' `decoding` blocks, but (a) it discards every hand-written comment in the file — including the provenance notes on the mistral7b/claude-sonnet locks — and (b) for a local beam sweep it refreshes the legacy flat `diversity_penalty` to whatever *this* model locked, silently changing the fallback any model_key without its own block would read. Neither is a change 10.7 is entitled to make to a config three other recorded runs depend on. |
| 2026-08-28 | 10.7 (external-POLE substrate: G2 sweep + entropy probe deferred, dp reused) | §Gates: "For **each** of v3 and external POLE, run G0→G4 (… G2 sweep+probe …)", with the spec's own fallback: "or reuse mistral's `diversity_penalty: 1.0` as a documented pragmatic default if the sweep is skipped for budget (say which, per 9.3 precedent)". | The v3 substrate gets the real G2 (sweep `kaya/105` + entropy probe `kaya/106`). The external substrate **reuses whatever v3's sweep locks**, recorded as a pragmatic default; its entropy probe is deferred. `config/pipeline_pole_external_qwen.yaml` says so in-line. | `--dataset_version` on both `experiments/diversity_penalty_sweep.py` and `experiments/entropy_probe.py` is `choices=["v2", "v3"]`; there is no `pole_external` branch, and adding one means editing Phase-2 experiment modules that are outside 10.7's scope. This is exactly the deferral 10.4 recorded for Mistral on this substrate (decisions-log 2026-08-22). Evidence the substrate is not decoding-limited at dp=1.0: 10.6's 25-item external sample Cov@5 = 0.760, and 10.4's full-100 Mistral G1 = 0.810 at the same penalty. |
| 2026-08-28 | 10.7 (`--adapter_suffix` / `--no_adapter` added to the external G1 harness) | §Adapter-attribution ablation, cheap tier: "temporarily set the SL `peft_adapter_path` to the no-suffix `sl_adapter`, **or add a `--adapter_suffix ""` override to the G1 runner**". | Took the second option: `kaya/g1_sl_coverage_pole_external.py` gains `--adapter_suffix` (default `_pole_external`) and `--no_adapter`, mirroring the flags `kaya/g1_sl_coverage_v3.py` has carried since the 2026-07 v3 ablation. Both are additive and default-preserving, so 10.4's recorded invocation is byte-identical; the payload now records the adapter actually loaded rather than a hardcoded string. | The spec authorises the edit explicitly, and the alternative — hand-editing a path before each of the three tiers — is precisely the kind of untracked mutation that produced 10.4's superseded run. With the flag, all three attribution tiers are one `TIER=` variable on `kaya/104`, and the tier that was run is recorded inside each result JSON. |
| 2026-08-28 | 10.7 (Neo4j-in-job isolation for the Qwen gates; AD probes pinned to :7687) | The 7.5/10.4 G3 and G4 runners this step mirrors (`kaya/80`, `kaya/99`, `kaya/100`) start Neo4j on the shared per-substrate volume at the fixed port `bolt://localhost:7687`. | Every 10.7 runner gets its **own** data dir (`neo4j_data_{v3,pole_external}_qwen_{ad,g4}`). The two G4 runners additionally get their **own per-job Bolt port** (kaya/93's pattern), injected into a runtime *copy* of the pipeline config so the committed YAML is never mutated. The two G3 AD probes stay on `:7687` because `kaya/ad_probe_{v3,pole_external}.py` hardcode that URI; their headers say not to co-schedule them, and a `NEO4J_READY` guard now aborts the job loudly after 180 s instead of proceeding. | k172 has **4** H100s, so for the first time in this project two GPU jobs are expected to share one node — the exact condition behind kaya/91's 2026-07-17 finding (two co-located jobs both bound `:7687`; the loser silently wrote into the winner's live database). Separate volumes make cross-contamination impossible; separate ports let the two 24-hour G4 runs actually run side by side. Giving the AD probes a port too would mean adding a `--neo4j_uri` flag to two 7.5/10.4 harnesses — outside this step's scope for a 5-minute job that can simply be run in sequence. |
| 2026-08-29 | 10.7 (G2a sweep: no penalty cleared the Cov@5 guardrail — selection rule fell through, as the sweep itself demands be recorded) | `experiments/diversity_penalty_sweep.py`'s `_select_row` prefers the highest entropy-AUC penalty **among those with Cov@5 ≥ 0.85** (`COV_GUARDRAIL`); its fallback branch prints "NO penalty cleared Cov@5 ≥ 0.85; picked best Cov@5/AUC trade-off … **RECORD THIS IN decisions-log.md**". | Recorded here. Job **1144445** (k172 H100, 19m57s, COMPLETED 0:0) swept dp ∈ {0.2, 0.5, 1.0} over all 120 v3 questions with `sl_adapter_v3`: **0.2 → AUC 0.605 / Cov@5 0.725**, **0.5 → 0.605 / 0.717**, **1.0 → 0.570 / 0.750**; hallucination rate 0.000 at every penalty. None reached 0.85, so the fallback picked **dp = 1.0** on best Cov@5. `decoding[qwen2.5-32b]` was therefore re-locked to the value it was seeded at, and the merge-back left the file byte-identical (verified by diff against a pre-sweep copy). | Three consequences worth carrying forward. (1) **No re-run needed:** G1 and all four attribution tiers were already generated at dp=1.0, so `results/beams_qwen2.5-32b_v3.jsonl` is valid for the G2b entropy probe with no `REGEN`, and the three-way attribution stays single-factor on the adapter. (2) **Criterion #6 will not be met at the locked penalty:** entropy AUC 0.570 < the 0.62 baseline, and the sweep's own trade-off is visible — the two penalties with the better AUC (0.605, still < 0.62) cost 2.5–3.3 pp of Cov@5. Since project-overview §7 makes LLM self-assessment PRIMARY and entropy SECONDARY, optimising coverage over a sub-baseline AUC is the right call, but it means Qwen lands nearer claude-sonnet (0.525, flat) than mistral-v3 (0.689) on this criterion — reported honestly, not tuned around. (3) **0.000 hallucination at every penalty** on a 32B in-domain adapter — the out-of-schema label/rel-type failure mode that dogged the 7B is simply absent here. |
| 2026-08-29 | 10.7 (G2b entropy probe: ablations are NOT flat for Qwen — the probe's own "record this" branch) | project-overview §7 records the established finding at 7B scale: entropy AUC ≈ 0.62 for a fine-tuned text2cypher model, with "ablations across entropy/dominance/normalisation … flat, so the scoring function is not the bottleneck — beam diversity at generation time is". `experiments/entropy_probe.py`'s `build_finding` prints a **record-this** message whenever the ablation spread exceeds 0.01. | Recorded. Job **1144484** (35 s — pure GPU-free rescore of the G1 beam cache, valid because the sweep re-locked the same dp=1.0 the cache was generated at). Qwen2.5-32B on v3: `h_norm_beams` **0.570**, `h_norm_candidates` **0.569**, `top_beam_dominance` **0.558** — **spread 0.012 > 0.01**, so unlike every prior probe the scoring function *does* matter here, though only marginally and in the same rank order. Artefact: `results/entropy_probe_qwen2.5-32b_v3.json`. | Two honest reads. (1) **Criterion #6 is missed at 32B scale (0.570 < 0.62)** — scaling the base model 4.6x did NOT rescue the secondary Bayesian signal. That is a *negative result worth reporting*: it strengthens, rather than weakens, §7's locked design implication that LLM self-assessment is primary and entropy secondary — and G3 supplies the positive half of the same story (base Qwen emits parseable AD JSON where base Mistral never did, 8/10 and 9/10 binary detection). (2) The non-flat ablation is a **0.012 spread on n=120**, i.e. ~1.5 questions' worth of ranking — it clears the harness's threshold but should be reported as "marginally non-flat", not as a reversal of the 7B finding. No architecture change: §7 says note it, don't act on it. |
| 2026-08-29 | 10.7 (G3: base Qwen-32B actually PARSES the AD contract — the 7B under-detection finding does not carry over) | §Gates: G3 AD probe, run "per substrate". 7.5/10.4's recorded expectation for a local base model, inherited from 3.4: base 7B emits unparseable AD JSON, so detections come from the `entity_entropy=1.00` fallback and schema/intent/temporal are missed — "API-AD is the documented higher-recall path". | Recorded as a **counter-example at 32B**. Jobs **1144485** (v3, 5m05s) and **1144486** (external, 5m23s), both COMPLETED 0:0 with base Qwen as the AD LLM and its own isolated Neo4j: **v3 8/10 (TP=6 FP=0 FN=2 TN=2)**, **external 9/10 (TP=7 FP=0 FN=1 TN=2)** — against 10.4's base-Mistral 6/10 on the same external probe. Crucially the rationales are substantive and well-formed JSON, not fallbacks: the external run detected an `intent` item on reasoning about a genuine tie ('aside from X' leaving Skynner and Miskimmon both at n=44), a type base Mistral never returned a parseable answer for. | This is the model-capability axis showing up *inside* a stage rather than only in end-to-end accuracy, and it is the mechanism to cite for any C3 gain the G4 runs show: the disambiguation loop can only fire on items the detector flags, and for the first time on a LOCAL model the detector is flagging them on its own reasoning rather than on entity-entropy. Caveats kept explicit: n=10 stratified per substrate (counts recorded, NOT a numeric gate, per 7.5/10.4); FP=0 on both means precision is untested at this n; and two v3 misses are *type* errors on correctly-detected items (Q-101 temporal and Q-082 intent both fired as `entity`), so binary detection overstates per-type competence. The full Detection F1 / per-type picture comes from G4, not from this probe. |
| 2026-08-29 | 10.7 (attribution comparison: the Mistral v3 ladder is NOT all at one penalty — use `_locked`, not the plain `_v3` file) | §Reporting: "Report the **three-way adapter attribution** (no adapter / +generic Ozsoy / +generic+POLE) for Qwen alongside Mistral's equivalent (2026-07-16)" — implying the recorded Mistral cells are directly comparable. | Compared Qwen's ladder against Mistral's **at a matched dp=1.0**, taking Mistral's in-domain cell from `results/g1_sl_coverage_mistral7b_v3_locked.json` (**Cov@5 0.725**, dp=1.0) rather than the file the name suggests, `results/g1_sl_coverage_mistral7b_v3.json` (**0.683, dp=0.2**). Verified by reading the `decoding` block out of each of the five files. Qwen's four 10.7 tiers were all deliberately run BEFORE the G2a sweep so they share the headline G1's dp=1.0. | The two Mistral ablation tiers (`_noadapter` 0.000, `_genericonly` 0.483) were run at dp=1.0, but the plain `_v3` artefact predates the re-lock and sits at dp=0.2 — so the obvious three-file ladder silently mixes two decodings, understating the in-domain increment by 4.2 pp (0.683 vs 0.725). Recorded so a future session writing the dissertation table does not reach for the same-named file. **The corrected like-for-like comparison is the interesting one:** mistral-7B **0.000 -> 0.483 -> 0.725** vs qwen-32B **0.033 -> 0.533 -> 0.750**, and on the `schema` type specifically (the four role-split Person->Incident edges that ARE the designed schema ambiguity) both models go **0.00 -> 0.15 / 0.00 -> 0.95**. Two claims follow, replicated across a 7B and a 32B: (a) the generic Ozsoy adapter contributes **essentially nothing** on the role-split edges — it is in-domain SFT that supplies them entirely; (b) **4.6x more base model buys ~2.5 pp of overall Cov@5 while the in-domain adapter buys ~22-24 pp on top of the generic** — for schema linking the adapter axis dominates the model-scale axis, which is the substrate-vs-architecture-vs-model argument landing on the architecture side. |
| 2026-08-29 | 10.7 (G4 RESULTS both substrates — and the one non-monotonicity, diagnosed not glossed) | §Gates: "v3's 120x3 and external's 100x3 are the two G4 runs"; acceptance 2 requires 0 tracebacks and the eight tagged artefacts. project-overview §9 criterion #2 wants monotonic C3 >= C2 >= C1 on overall EX. | Both runs clean: **1144487** (v3, 120x3, **1h21m57s**) and **1144488** (external, 100x3, **1h28m08s**), 2x H100 each on `rrifcs`, COMPLETED 0:0, **0 tracebacks**, all eight artefacts written under the `v3_qwen` / `pole_external_qwen` tags. **v3 (%):** EX 5.8/20.0/25.0, EA 7.5/35.8/43.3, ambiguous EA 5.0/36.2/42.5, Det F1 60.5 (P 69.6 R 53.4), DSR 19.6. **external (%):** EX 40.0/**37.0**/53.0, EA 41.0/45.0/64.0, ambiguous EA 28.7/38.8/60.0, Det F1 82.2 (P 90.9 R 75.0), DSR 56.1. The **external C2 EX (37.0) sits BELOW C1 (40.0)** — recorded as a miss on criterion #2 for that cell rather than smoothed over. | **The dip is churn, not a systematic regression, and the diagnosis is in the qual records:** C1->C2 loses 12 items and gains 9 (net -3 on n=100), spread evenly across entity/temporal/null, split 6 `wrong_result` / 6 `invalid_query`, and C3 recovers 5 of the 12. Mechanism visible at Q-POLE-E12 ("What address is on file for Hughes?"): zero-shot C1 filters on `surname:"Hughes"`, the fine-tuned QG substitutes `name:'Hughes'`. Base Qwen-32B is simply strong enough zero-shot on this graph (**C1 EX 40.0** vs claude 20.0 and mistral 14.0) that a beam_k=1 schema-grounded commit occasionally costs more than it adds — a *ceiling* effect on C1, not a C2 defect. EA, ambiguous EA and every C3 comparison stay monotonic. **The headline positive is the external `temporal` cell: C3 fixes 13 of 20 and regresses 0 (EX 7/20 -> 20/20)** — the cleanest demonstration of the architectural claim the project has produced, since the questions and the schema linker are unchanged and only detect-then-commit is added. Also notable: qwen **matches the API model on detection** (F1 82.2 vs claude 82.0, with better recall 75.0 vs 71.2) — the first evidence here that the architecture does not need a frontier API model. Qualitative buckets non-empty on both (v3 fixed 8 / regressed 2; external fixed 16 / **regressed 0**). |
| 2026-08-29 | 10.7 (acceptance #5 read as a NEW doc, not an edit to the existing walkthroughs) | §Reporting: "extend the `docs/results-walkthrough*` scorecards to a full matrix". Acceptance criterion 5, same step: "Scorecard(s) updated to the three-model matrix; **all prior artefacts untouched** — mistral v2/v3/external and claude v3/external results in `results/` and `docs/` are byte-unchanged." | Wrote a NEW page, `docs/results-walkthrough-qwen-32b.md`, carrying the full three-model x two-substrate matrix and the re-scored 7-criteria scorecard, and left `docs/results-walkthrough{,-v3,-v3-claude-sonnet}.md` byte-unchanged (verified by checksum alongside the 53 `results/` files; `git status` shows no tracked file modified under `results/` or `docs/`). | The two sentences pull in opposite directions — "extend the existing scorecards" cannot be done to files that must stay byte-unchanged. Resolved in favour of the **acceptance criterion**, which is the testable one, and which exists to protect the recorded provenance of five earlier runs. A new page satisfies the intent of both: the matrix exists in `docs/`, cross-links to all three companions, and nothing recorded is rewritten. If a future session wants one merged scorecard page, the safe move is a fourth summary doc, not an edit to the historical ones. |
| 2026-08-29 | 11.1 (`rrifcs` is now the documented default GPU target; **fp16 retained on sm90**) | §"`kaya/README.md` — new section": record the partition facts, the submit idiom, the measured throughput, and a **precision rule** — "sm90 has native bf16, but **keep `dtype: float16`** as `config/models.yaml` sets it … A switch is a deviation → decisions-log." | Added `kaya/README.md` § "H100 on `rrifcs` (Phase 11+)" and a pointer at the top of the file redirecting any Qwen-sized (32B/72B) job away from the V100 instructions above it. Partition facts re-verified today by `scontrol` (one node `k172`; `gpu:h100:4` H100 **NVL** 95,830 MiB = **93.6 GiB** each, sm90, driver 575.57.08; 96 CPUs; 1,547,000 MB RAM; `MaxTime=3-00:00:00` with `DefaultTime=NONE`; `ExclusiveUser=NO`/`OverSubscribe=NO`) plus a **1-GPU smoke, job 1144854**: `/group` mounts, the `t2c` env activates, Slurm isolates the requested card (`device_count()==1`), torch 2.6.0+cu124 sees `H100 NVL sm90 93.1 GiB`. **fp16 is explicitly RETAINED** — `torch.cuda.is_bf16_supported()` is `True` on this card and the README now says in so many words not to act on it. Two spec facts were made more precise rather than copied: `AllowGroups` is `rrifcs-users,**kaya-admins**` (not `rrifcs-users` alone), and the 4.9× figure is recorded against the 10.5 fit-test's 14.9 s/step with the 10.6 in-flight V100 rate (13.98 s → 4.6×) noted beside it, since the decisions-log entry of 2026-08-28 cites the latter and a reader meeting both numbers should not have to guess which is wrong. Neither changes any decision. | The failure mode the spec names is real and one-directional: `kaya/README.md` documented only the V100 `gpu` partition, so a future session following it submits a 72B QLoRA job to a 32 GB card and gets a step-0 OOM. Documenting the partition is cheap; re-deriving it after a wasted queue day is not. The precision rule is load-bearing for the thesis, not for performance: Phase 10/11 is a model-scale comparison (7B → 32B → 72B) at one fixed recipe, and all six Qwen-32B adapters (four V100, two H100) are comparable *only* because precision was held at fp16 across the hardware change. Switching to bf16 on the 72B branch alone would confound the axis under test with a numerics change, which is why the README states it as a prohibition with a deviation path rather than a preference. |
| 2026-08-29 | 11.1 (`kaya/101`/`102` train-YAML selection: **`MODEL_KEY`-keyed case arm + `TRAIN_PREFIX` override**) | §"`kaya/101`/`102` — `MODEL_KEY` env": "The train-YAML selection stays `STAGE`-driven, so 11.3 supplies its own YAML paths via a second env knob (`TRAIN_PREFIX`, or a `MODEL_KEY`-keyed case arm — **implementer's choice, record it**)." | Implemented **both**, with the case arm as the default and `TRAIN_PREFIX` as the escape hatch: `STAGE` is validated against `generic\|v3\|pole_external`, a `case "$MODEL_KEY"` maps `qwen2.5-32b → {schema_linker,query_generator}_train_qwen` (10.6) and `qwen2.5-72b → …_train_qwen72` (11.3), `TRAIN_PREFIX="${TRAIN_PREFIX:-$DEFAULT_PREFIX}"`, and the path is composed as `config/${TRAIN_PREFIX}_${STAGE}.yaml`. An unknown `MODEL_KEY` with no `TRAIN_PREFIX`, and a resolved-but-missing YAML, both fail fast with the fix in the message instead of dying inside `run_sft`. `MODEL_KEY` (and `TRAIN_PREFIX`, when set) are threaded through the submit path's `--export` so the whole `afterok` DAG inherits them. | The case arm is what makes 11.3's own stated invocation — `MODEL_KEY=qwen2.5-72b bash kaya/101_train_qwen_sl.slurm`, one env var, no new slurm files — work as written; requiring `TRAIN_PREFIX` as well would contradict 11.3's §Modules. Keeping `TRAIN_PREFIX` as an override means a future model key needs no edit to these files at all, which is the workflow-§2 problem the step exists to solve. Fail-fast on a missing YAML matters because the failure would otherwise surface hours into a queued 23 h job. Verified by dry-run (stubbed `sbatch`/`python`): with no env set, all six stage×runner combinations emit exactly today's command line, `python -m experiments.train_{schema_linker,query_generator} --model_key qwen2.5-32b --train config/{…}_train_qwen_{generic,v3,pole_external}.yaml`, and the submit path is unchanged but for the additive `,MODEL_KEY=qwen2.5-32b` in `--export`. |
| 2026-08-29 | 11.1 (acceptance 2 partially deferred: `predownload.py --model_key qwen2.5-72b` needs 11.2's registry entry) | Acceptance 2: "`python kaya/predownload.py` (no args) is behaviourally unchanged; `--model_key qwen2.5-72b` re-verifies the existing cache without re-downloading (37/37 shards present)." | The flag is implemented and proven (`--model_key qwen2.5-32b` re-verified 27/27 files, no download; bare invocation unchanged, `model_key=mistral7b`), but **`qwen2.5-72b` is not yet a `config/models.yaml` key** — 11.2 §Modules owns adding it, and workflow §2 forbids 11.1 editing a file another step owns. `predownload.py` therefore exits with `--model_key 'qwen2.5-72b' is not in config/models.yaml. Local (downloadable) keys: llama31-8b, mistral7b, qwen2.5-32b`. The *substance* of the criterion was verified instead by making the identical `snapshot_download(repo_id="Qwen/Qwen2.5-72B")` call under `HF_HUB_OFFLINE=1`: **37/37 shards, none missing, 135.4 GiB on disk == the index's declared `total_size`**, no download. 11.2 should re-run the flag form as a one-line confirmation once the registry entry lands. | This is a spec-ordering artefact, not a defect: acceptance 2 was written assuming the registry entry, but the step that adds it (11.2) `Depends on` this one. Editing `config/models.yaml` here to make the criterion pass literally would take a file out of 11.2's hands and pre-empt the arch/precision fields 11.2 is meant to author. Failing loudly with the list of valid keys is better than a `KeyError` traceback, and the offline `snapshot_download` check proves exactly what the criterion is for — that the 136 GiB cache is complete and the compute nodes (which are offline) will find it. |
| 2026-08-29 | 11.2 (leg 5 **refutes** the spec's own prediction: C3 fits on **two** H100s, not three) | §Fit-test leg 5: "print `3 × measured_load_GiB` beside the 93.6 GiB card size and state explicitly whether C3's SL + QG + AD fit on two cards … At the predicted ~41 GiB each this is **~82 GiB of weights on card 1 before any KV cache**, i.e. it does **not** fit and 11.4 needs a third GPU. Confirm or refute with the real number." | **Refuted.** Measured load is **42.5 GiB**, so card 1 (QG + AD, per `_device_maps()`) carries **85.0 GiB** of weights on a **93.1 GiB** card. Charging *each* co-located model the full **measured** k=5 beam-decode overhead (2.68 GiB = the generate leg's 45.20 GiB peak minus the 42.52 GiB load) gives a worst case of **90.4 GiB — FITS, margin +2.7 GiB**. Recorded as **2 × H100 for the C3 e2e**, with two caveats stated in the summary output and here: (a) the +2.7 GiB is a deliberately conservative bound — QG and AD decode with **k=1**, not k=5, so the real margin is larger — but it is still thin enough that 11.4 should treat a 3-card request as the safe option if the node is quiet; (b) **a third card does not relieve card 1 by itself.** `build_condition3._device_maps()` returns `({"":0},{"":1},{"":1})` for *any* `device_count >= 2`, so with `gpu:h100:3` `cuda:2` sits idle and card 1 still holds two 72B copies. Spreading AD to `cuda:2` is a `_device_maps()` change and is 11.4's call, not this step's. | The spec asked for a confirm-or-refute against a real number and this is the refutation, so recording it as such is the whole point of the leg. The prediction was off for a mundane reason worth writing down: 0.5 B/param understates a 4-bit load, because the **untied** `embed_tokens` and `lm_head` (1.25 B params each at 152,064 × 8,192) stay in fp16 and `bitsandbytes` leaves `lm_head` unquantised — the arithmetic lands at ~41 GiB and the card sees 42.5. The `_device_maps()` caveat is the more expensive half of this entry: without it a future session reads "needs a third GPU", requests `gpu:h100:3`, and OOMs anyway on card 1 with a whole idle card beside it. |
| 2026-08-29 | 11.2 (harness reuse: five **additive** flags on `kaya/qwen_fit_test.py`, no fork) | §Fit-test: "Reuse `kaya/qwen_fit_test.py` with an additive `--model_key` (default `qwen2.5-32b`, so the 10.5 invocation is byte-identical)." | `--model_key` already existed from 10.5, so four *further* additive flags were needed to run a second model without disturbing the first: **`--results_dir`** (10.5's fragments live in `results/qwen_fit_test/` and a 72B run would have overwritten them — the 72B writes `results/qwen72b_fit_test/`), **`--tag`** (the batch-2 headroom leg is a second `train_4bit` fragment and must not clobber the batch-1 one; it lands as `train_4bit_batch2`), **`--budget_profile`** (the summary's training extrapolation was hardcoded to 10.5's `_mixed`-file, 3-epoch assumption; `phase11` extrapolates over 11.3's real recipe — Stage-1 generic on the 4k Ozsoy sample at 3 epochs plus four POLE-only continues at 2), and a parameterised summary header/precision loop (it hardcoded "10.5 FIT-TEST SUMMARY — Qwen2.5-32B on v100-32gb" and always printed an 8-bit row, which 11.2 drops). Every default is 10.5's; verified by parsing 10.5's exact argv through the new parser and by **replaying the four 10.5 fragments through the new summary**, which reproduces its numbers unchanged (20.2 / 32.9 GiB, 14.90 s/step, 40.0 s generate, 72.0 / 74.3 h). 10.5's recorded artefacts under `results/qwen_fit_test/` were not written to (mtimes still 2026-08-22). | A fork of the harness would have been the smaller diff today and the larger cost later: the 7B/32B/72B columns are only comparable because they came out of one measurement path, and two copies drift the moment one is fixed. Keeping 10.5's defaults exact is what lets the replay stand as a regression test — if a later edit changes what the 32B run would have reported, the replay says so. The separate results directory is the load-bearing one: `results/qwen_fit_test/` is 10.5's recorded evidence and a shared default would have silently destroyed it on the first 72B submission. |
| 2026-08-29 | 11.2 (the fit-test's train leg reads the **Qwen** train YAML, and the no-GC leg is skipped) | §Inherited, NOT re-decided: `per_device_train_batch_size: 1` … `gradient_checkpointing: true` · `max_seq_length: 3584`. 10.5's harness defaults to `--train_config config/schema_linker_train_pole_external.yaml` (the **Mistral** YAML, `per_device_train_batch_size: 2`) and always runs a second no-gradient-checkpointing leg. | `kaya/111_qwen72b_fit_test.slurm` passes `--train_config config/schema_linker_train_qwen_pole_external.yaml` (batch 1, grad-accum 32, GC on, seq 3584, fp16 — the 10.6 recipe 11.3 will copy) instead of overriding the Mistral YAML's batch 2 with a `--batch_size 1` flag, and passes `--no_plain_leg` on both train legs. | Two small things, same reason: the parity clause fixes the recipe, so the fit-test should **read** it from the file that states it rather than reconstruct it from CLI flags — a flag can be dropped on a re-run, a YAML path cannot. 10.5 had to override because no Qwen YAML existed yet; six do now. The no-GC leg answered a 10.5 question that is closed: 10.6 added the `gradient_checkpointing` flag to `run_sft` and every Phase-11 YAML sets it `true`, so re-measuring "what if it were off" would burn ~4 minutes of reload per leg to answer nothing. Confirmed by the run: `train_config` is recorded in the fragment as the Qwen YAML with `per_device_train_batch_size: 1`. |
| 2026-08-29 | 11.3 (submission form: per-stage `sbatch` with explicit `--partition/--gres`, not the bare `bash kaya/101` submit path) | §Compute + scheduling gives the invocation as `MODEL_KEY=qwen2.5-72b bash kaya/101_train_qwen_sl.slurm   # and 102 for the QG`, while the same section mandates **`--partition=rrifcs --gres=gpu:h100:1` per job**. | Submitted the same six jobs from the same two files, but as six explicit `sbatch --partition=rrifcs --gres=gpu:h100:1 --mem=200G --cpus-per-task=8 --time=<2-00:00:00 generic \| 1-00:00:00 continue> --export=ALL,STAGE=<stage>,MODEL_KEY=qwen2.5-72b kaya/10{1,2}_train_qwen_{sl,qg}.slurm` calls, wiring the four continues with `--dependency=afterok:<generic>` exactly as the scripts' own submit path does. No slurm file was created or edited. | The two spec lines are in conflict as literally written: `kaya/101`/`102` still carry 10.6's `#SBATCH --partition=gpu --gres=gpu:v100-32gb:2` header, and the submit path passes only `--job-name`/`--time`/`--dependency`/`--export` to the child `sbatch` — so the bare `bash kaya/101 …` form would have queued all six jobs onto the **V100** partition, where a 4-bit 72B (42.5 GiB load, 66.2 GiB peak — 11.2) cannot load at all on a 32 GiB card, and `device_map="auto"` would have silently sharded it across two of them. The explicit per-stage form is the one documented in both files' own "H100 OPTION" header block and in `kaya/README.md` § "Submit idiom" ("Any `gpu`-partition script can be redirected with the same two flags on the `sbatch` command line — no edit to the file"), and it is what actually satisfies the spec's `--partition=rrifcs --gres=gpu:h100:1` requirement. `--mem=200G` matches 11.2's proven job-1144935 setting (the files' 96G header default was sized for a 32B). The alternative — `SBATCH_PARTITION`/`SBATCH_GRES` env vars, which do override `#SBATCH` directives — was rejected as less auditable: nothing in the recorded command line would show which partition the run landed on. Verified post-submit with `scontrol show job 1144949`: `Partition=rrifcs`, `AllocTRES=cpu=8,mem=200G,gres/gpu=1`. A future session re-running 11.3 must use the explicit form (or first give `kaya/101`/`102` an `rrifcs` default, which is 11.x's call, not this step's). |
| 2026-08-30 | 11.4 (device planning — the step's "one real code question") | Extend `_device_maps()` with an `AD -> cuda:2` arm for `device_count() >= 3` and request **`--gres=gpu:h100:3`** for both G4 runners. The spec's arithmetic: at the 72B's ~41 GiB per 4-bit load, `_device_maps()`'s 2-card pinning would put QG **and** AD on card 1 = ~82 GiB before any KV cache, and C3 runs `beam_k=5` group beams, so "this will OOM or thrash". | **Added the `>=3` arm exactly as specified** (`{"":0}, {"":1}, {"":2}`, with the 2-GPU `({"":0},{"":1},{"":1})` and 1-GPU `(None,None,None)` returns frozen byte-identical and guarded by four new tests in `tests/test_condition_builders.py`) — **but requested `gpu:h100:4`, not 3**, on a measurement. A scratchpad-only probe (`/group/pmc084/atan/scratch_11p4/g4_memory_probe.py`, no repo artefact) replayed `run_evaluation.main()`'s exact load sequence at the 72B: **3 cards -> OOM** (job 1148895: after 5 loads the cards hold 76.2 / 78.9 / 52.6 GiB and C3's AD, pinned to `cuda:2`, dies with 428 MiB free); **4 cards -> FITS** (job 1148924: peak **66.5 / 69.2 / 69.1** GiB on the pinned cards and 44.3 GiB on card 3, all of 93.09 — "ALL SIX LOADS RESIDENT — no OOM"). `_device_maps()` itself needed no further change: at `device_count() == 4` it still returns (0, 1, 2), and card 3 is exactly the spill space `device_map="auto"` uses. | **The spec sized the job from C3's three loads; the real peak is six.** `run_evaluation.main()` (5.4) binds `c1`, `c2` and `c3` in one function scope and `HuggingFaceLLM` has no `__del__`/`close()`/`empty_cache()`, so C1's QG and C2's SL+QG are **still resident** when `build_condition3` loads its three: 6 x 41.5 = **249 GiB**, not 124. This was harmless at every earlier scale (10.7 ran 6 x 20.2 = 121 GiB on two cards) and only binds at 72B. The probe also refuted the assumption underneath the spec's arithmetic: `device_map="auto"` **balances** a load across every visible card rather than filling card 0, so C1/C2 spread over all of them and it is their residency — not C3's pinning — that exhausts the budget; adding a card helps precisely because it dilutes that spread. **The residency itself is a pre-existing wart in 5.4, NOT fixed here** (workflow rule 2: don't silently repair an earlier step mid-step) — `del c1, c2` + `torch.cuda.empty_cache()` between conditions would cut the peak to 3 loads, but `run_evaluation.py` underpins every recorded column and changing it is its own decision. Recorded for a future step. Consequence for scheduling is unchanged from the spec: two 4-GPU jobs exceed k172's four cards, so the two G4 runs still go sequentially. |
| 2026-08-30 | 11.4 (G2a sweep — SL decoding lock) | Reference values and precedent both assumed the penalty would stay at the seeded `dp=1.0`: 10.3/10.4 reused it for Mistral and 10.7's 32B sweep re-locked it unchanged, so the spec's G2b/attribution framing ("all at the locked `diversity_penalty`") anticipated no re-measurement. | The sweep (job **1148406**, 35m39s, 3 penalties x 120 v3 items) **re-locked `dp=0.5`** — the first model in the project whose sweep moved off 1.0. Rows: dp 0.2/0.5/1.0 -> entropy AUC **0.630 / 0.643 / 0.556**, Cov@5 0.725 / 0.733 / 0.733, hallucination **0.000 at every penalty**. No penalty cleared the 0.85 Cov guardrail, so the fallback rule picked best (Cov@5, AUC) — a Cov tie at 0.733 broken by AUC. Consequences, all taken: (a) **G1 and all four attribution tiers were re-run at dp=0.5** (jobs 1148537-1148542) so the three-way comparison stays single-factor on the adapter; (b) 11.3's dp=1.0 G1 artefacts were **preserved** under `results/_superseded/*_DP1.0_11p3_job1148276-7.*` rather than silently overwritten; (c) G2b needed **no** `REGEN=1`, because job 1148537 had already rewritten `results/beams_qwen2.5-72b_v3.jsonl` at the locked penalty. The scratch-config discipline held: `git diff` on `config/schema_linker_inference.yaml` is **12 insertions, 0 deletions**, so the mistral7b / qwen2.5-32b / claude-sonnet locks and the flat mirror are provably untouched. | The re-lock is the step's most consequential single number, because **it, not scale, is what meets success criterion #6**. At dp=1.0 the 72B's entropy AUC is 0.556 — *worse* than 10.7's 32B 0.570 — and at dp=0.5 it is **0.643**, the first column in the project to clear the 0.62 bar (G2b job 1148896 confirms 0.643 on the full probe, ablations flat at spread 0.005). Reported that way round deliberately: 10.7 recorded that scaling 4.6x did not rescue the secondary signal and 11.3 found the scale axis flat, and this does not overturn either — it is direct evidence for project-overview 7's actual claim, that **beam diversity at generation time is the bottleneck**, since re-tuning the diversity knob moved the signal past a bar that two rounds of parameter scaling could not. G1 at the re-locked penalty also came out at or above the dp=1.0 measurement (v3 Cov@5 0.733 unchanged; external **0.770** vs 0.750), so nothing was traded for it. |
| 2026-08-30 | 11.4 (the QG-properties invariant — layer 3's shape) | "a permanent, parametrised regression test (`tests/test_pipeline_configs.py`): *for every* `config/pipeline*.yaml` whose `active_model` resolves to a `kind: local` registry entry, assert `qg_include_properties is True`. API-model configs ... must be asserted exempt, not skipped silently", plus a demonstration that it *fails* on a config with the key removed. | Built as specified, with two additions and one placement choice. (1) The API exemption is asserted **mechanically**, not narratively: the test imports the real `_resolve_prompt_style` from `run_evaluation`, asserts it maps `api -> instruct`, and asserts the key is *absent* (an api config that sets it explicitly is not the exempt case and fails). (2) A **third** parametrisation runs over *every* pipeline config, local and API, builds the real `QueryGenerator` the way `run_evaluation` would and asserts the **rendered prompt** contains `Properties:` and the schema's property names — so the invariant is checked at the outcome both routes exist for, not just at the YAML key. (3) The `_device_maps()` guard tests went into the **existing** `tests/test_condition_builders.py` rather than a new file, since that is the module's own test file. **Guard demonstrated to fail** (evidence kept at `scratchpad/guard_demo_failure.txt`): a temporary `config/pipeline_guarddemo_v3_qwen72.yaml` with the single line removed turned 17 passed into **2 failed / 17 passed**, tripping both the YAML assert and the rendered-prompt assert, the latter printing the real QG prompt with no Properties block. Demo config deleted; suite re-verified green. | The spec's own reasoning is that the first two layers are per-run and this defect has been introduced twice by copying a config. A YAML-key assertion alone would still pass if someone changed how `QueryGenerator` derives `include_properties`, which is the actual mechanism that failed in 10.4 — asserting the rendered prompt closes that. Asserting the API exemption rather than skipping it means the exemption cannot quietly stop being true either. "A guard that has never been shown to fail is not a guard" is the spec's phrasing, so the failure was demonstrated and the output kept rather than asserted in prose. |
| 2026-08-30 | 11.4 (walltime sizing) | Size G1/G2/G4 walltimes from 11.2 leg 4's **29.6 s** per k=5 SL decode ("that is the number 11.4 must size its G4 walltime from"), i.e. ~4.2x the 32B's ~7 s. | Kept the generous walltimes but recorded the **measured** rate: the G2a sweep did 3 x 120 = 360 k=5 decodes in ~32 min of generation = **~5.3 s per decode**, and G1 re-runs took 12-15 min for 100-120 items. That is ~5.6x faster than 11.2's figure and slightly *faster* than the 32B's ~7 s. | 11.2 leg 4 timed a **single cold decode** immediately after a 4-bit load, so it carried allocator warm-up, CUDA-graph/kernel autotune and first-token compile cost that amortise to nothing over a batch — the same effect its leg 2 saw and discarded ("8.97 / 6.49 / 6.54; first step pays allocator warm-up") but which leg 4 had no second sample to expose. No walltime was cut on the strength of this (the requested times are slack, not estimates), but a future step should size from ~5 s/decode, not 29.6 — the difference is ~3 h vs ~30 min on a 360-decode sweep. |
| 2026-08-31 | 11.5 (Part A — reporting strata; **pre-implementation spec amendment**, the step is `not started`) | Part A reports paired-bootstrap CIs on the three deltas over three strata only: **overall**, the **`is_ambiguous_gold` subset**, and **per `ambiguity_type`**. Acceptance criterion 3 then requires 10.4's "at noise level" and 10.7's "churn" readings to be "restated with the measured intervals or withdrawn — including whether criterion #1 and #2's margins clear zero". Nothing in the spec distinguishes a question C2 answered wrongly from one C2 never generated a query for. | Added a **fourth stratum — SL-abort exclusion** — to Part A (new subsection in the spec; new acceptance criterion 3, old 3/4/5 renumbered to 4/5/6). Every metric and delta is reported a second time over the item set with aborted items removed, the exclusion taken as the **union across conditions** so all three stay paired on one common set. Reported **beside** the headline, never in place of it. The bootstrap **method** is untouched: same paired resampling, same B=10,000, same fixed seed, same read-only guarantee; only the set of strata grows. No new module — still `experiments/bootstrap_ci.py`. Part B is unchanged and still OPTIONAL. | **A bootstrap quantifies sampling noise around an estimate; it does not remove bias in that estimate — and there is a known bias inside C3−C2.** C2 builds its Schema Linker at `beam_k=1` (`experiments/build_condition2.py`) and C3 at `beam_k=5` (`experiments/build_condition3.py`), both at `min_valid_beams=1`. When no beam parses in-schema the linker raises `SchemaLinkerError` and `Orchestrator.run` breaks with `failure_reason="schema_linker_no_valid_beams"`: the item scores `invalid_query` with **no query ever generated** — no QG call, no CyVer retry. Holding one beam, C2 aborts strictly more often than C3; C1 never calls the linker and never aborts. Measured 2026-08-31 over all seven `qual_records_*.json` (C2/C3 aborts, ΔEX all → ΔEX excluded): mistral-7B ext **18/5, +3.0 → −1.2 (SIGN FLIP)**; qwen-32B ext 6/0, +16.0 → +12.8; qwen-72B v3 6/0, +1.7 → +1.8; qwen-72B ext 11/11, +7.0 → +7.9; qwen-32B v3 8/7, +5.0 → +5.4; claude v3 3/2, +15.0 → +15.4; claude ext 0/0, +5.0 → +5.0. Where the counts match, pairing already cancels the contribution; where they differ it does not. **Left unamended the step would have made this worse, not neutral:** pairing cancels question-difficulty variance, so the bootstrap returns a *tight* interval around the biased delta and criterion 3 would have certified mistral-external's +3.0 as clearing zero — a margin that inverts under a one-line subset change. The abort set is free to compute from existing artefacts (`generated_cypher_final` empty/whitespace; verified item-for-item identical to falsy `final_cypher_syntax_hint` in C2 and `ad_predicted_ambiguous is None` in C3 on all seven files), so the amendment costs no GPU and no re-run. **Scope limit recorded in the spec:** the stratum removes aborted items from the arithmetic, it does NOT make C2 and C3 decode identically — only a C2 re-run at `beam_k=5` with an argmax commit does that, and that is GPU work outside 11.5. The excluded figure must therefore never be written up as a corrected or debiased result. Underlying `beam_k` asymmetry and the 5.4 model-residency defect are both left unfixed per workflow rule 2 (do not silently fix an earlier step mid-step). |
| 2026-08-31 | 11.5 (part A) | Deliverable is "`experiments/bootstrap_ci.py` … + a `docs/` table or a section appended to `docs/results-walkthrough-qwen-72b.md`", and acceptance 5 requires every existing `results/`/`docs/` artefact byte-unchanged. | Wrote a **new** `docs/variance-and-error-bars.md` (the spec's "a `docs/` table" option), never touching the 72B walkthrough, and split the hand-written interpretation into `docs/error-bars-notes.md`, spliced into the generated doc by an additive `--notes` flag (skipped silently when the file is absent). Also emit `results/bootstrap_ci.json` — the same intervals machine-readable, a new file. | Appending to the walkthrough would have edited an existing artefact, which acceptance 5 forbids. The readings acceptance 4 asks for (which claims are upheld, which column's strata disagree, which reading is defended) are **column-specific prose**; hardcoding them inside a tool meant to run over "every existing `qual_records_*.json`" would go stale the moment a new column is added, while putting them in a separate doc would separate the claims from the intervals that back them. Splicing a notes file keeps one published artefact and a generic tool. |
| 2026-08-31 | 11.5 (part A) | Method: "Resample question ids with replacement, n = the number of questions (120 for v3, 100 for external)", then report the same intervals "per `ambiguity_type`". | Resample **within each stratum**, n = that stratum's size (120/100 for `overall`, 80 for `ambiguous`, 20 for a per-type cell, and the post-exclusion count in the abort-excluded view). Each stratum gets its own draw from the same fixed seed. | The spec's n is stated for the overall stratum; a per-type interval has to be conditional on that cell or it is not an interval on that cell (drawing 120 ids and keeping the ~20 that land in the cell would vary the cell's own n across iterations and mix in a multinomial term the caveats are not about). Within-stratum resampling is what makes the reported n=20 width (median 15 pp) the width of the cell the 10.4 caveat is about. Pairing is unaffected — every condition still reads off one id vector per iteration. |
| 2026-08-31 | 11.5 (part B) | Part B (a second training seed at 32B) is "OPTIONAL — drop without penalty", and acceptance 6 says: if not run, say so explicitly in the tracker row. | **Not run** (user instruction at kickoff). No `_seed2` YAMLs, no adapters, no Cov@5 replicate; stated explicitly in the 11.5 tracker row and in a closing section of `docs/variance-and-error-bars.md`. | Recorded so a future session does not read the absence as an oversight. Consequence carried forward: every interval in this step is **sampling variance over questions only**; training stochasticity is unmeasured on every column, so the n=1 caveat on training stays in force in the writeup. |
| 2026-08-31 | 11.5 (part A) | (deps unspecified) | Added `numpy` to `pyproject.toml`. | The paired bootstrap draws a (10,000 x n) index matrix per stratum and gathers six outcome vectors through it — ~100 strata across the seven columns. Vectorised this is ~1 s; in pure Python it is minutes per run, which makes the tool too slow to re-run casually while writing up. numpy was already present in the `t2c` env as a torch dependency; this only declares it. |
| 2026-08-31 | 11.4 / 10.7 writeups (post-11.5 reconciliation) | 11.5 acceptance criterion 5 freezes every existing `docs/` and `results/` artefact byte-unchanged, and criterion 4 requires the 10.4/10.7 claims to be "restated with the measured intervals or withdrawn". The 11.5 session correctly honoured the freeze: it restated those claims inside its own new `docs/variance-and-error-bars.md` and touched nothing else. That left `docs/results-walkthrough-qwen-72b.md`, `docs/results-walkthrough-qwen-32b.md` and 11.4's tracker row still asserting readings the intervals do not support. | **Edited the two frozen walkthroughs and 11.4's tracker row** (user-authorised 2026-08-31, as a deliberate act separate from 11.5). **Withdrawn:** the 72B page's claim that the external C2 < C1 ordering "is a property of the external substrate + the SFT recipe rather than a one-run artefact", and its scorecard marking criterion #2 ❌ on external -> ⚠ **not measurable**; the same withdrawal recorded in 11.4's tracker row with the superseded text struck through rather than deleted. **Upheld and strengthened:** the 32B page's "churn, not a systematic regression" reading, which the bootstrap confirms. **Downgraded:** criterion #1's 72B-v3 margin from met to indicative (clears zero on the headline stratum, not under abort exclusion). **Annotated:** the 32B external `temporal` cell as the one per-type result that outruns the ~15 pp n=20 noise floor, and the two 32B columns as the only ones where criterion #1 clears zero under both strata. Each edit carries a dated amendment note and a link to `variance-and-error-bars.md`; no measured value was altered, only the claims drawn from them. | Two files in `docs/` were making incompatible statements about the same result, and the walkthroughs - not the new variance document - are what the dissertation is drafted from, so the stale reading was the one a reader would reach first. The specific danger was the 72B page's "visible in both large open models, which makes it a property of the substrate + recipe": two columns agreeing in **sign** is not evidence when **neither** margin clears zero, and that sentence promotes a non-result to a finding an examiner could dismantle with the project's own error bars. Struck-through rather than deleted in the tracker so the superseded reading stays auditable. Deliberately NOT done: no number, table or per-item mechanism was changed - the rebinding, `surname`->`name` and `beam_k=1`-abort analyses remain exactly as measured, since what 11.5 denies is only that they sum to a resolvable net effect. The `beam_k` asymmetry itself is still unfixed (workflow rule 2); only a C2 re-run at `beam_k=5` with an argmax commit would close it. |
