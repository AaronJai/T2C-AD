# Progress Tracker

> Single source of truth for build status across sessions. Update the relevant row at the
> **start** of a step (→ `in progress`) and at the **end** (→ `done`, with Outputs +
> Handoff). Do not start a step whose dependencies aren't all `done`.

**Status legend:** `not started` · `in progress` · `done` · `blocked` (note why in the row)

---

## Phase 0 — Contracts & Foundations *(no model, no DB)*

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 0.1 | `0.1-types.md` | — | done | `pipeline/types.py` + `pipeline/__init__.py`. Exports all shared dataclasses (`SchemaElement`, `SchemaRepr`, `SchemaCandidate`, `CandidateMapping`, `EntityCandidate`, `EntityLookupResult`, `AmbiguityResult`, `SchemaMapping`, `ValidationResult`, `ExecutionResult`, `BenchmarkItem`, `EvaluationResult`, `PipelineState`), the 7 type literals, and `_build_cypher_pattern`. Handoff: every later step imports its contracts from `pipeline.types`; no type here is redefined elsewhere. All 4 contract tests pass (`tests/test_types.py`). **Amended 2026-06-10** (spec 0.1 amendment): `PipelineState` gained `evaluation_history` + `first_validation_result` (per-attempt data for 4.2) and `previously_tried_mappings` is never cleared; see decisions-log 2026-06-10. |
| 0.2 | `0.2-llm.md` | — | done | `pipeline/llm/` package: `base.py` (`BaseLLM` ABC, `GenerationConfig`, `Completion`), `huggingface.py` (`HuggingFaceLLM`), `openai.py` (`OpenAILLM`), `anthropic.py` (`AnthropicLLM`), `__init__.py` (re-exports + `build_llm` factory). Handoff: later steps `from pipeline.llm import BaseLLM, GenerationConfig, Completion, build_llm`. SL (3.1)/QG (3.6) hold an injected `BaseLLM`; condition builders (5.3) call `build_llm(spec)` from config. No module hardcodes a model name. Now-set acceptance (clean import, abstract ABC + FakeLLM ranking, factory dispatch/ValueError) pass via `tests/llm/test_base.py` (6 tests). Integration checks (real HF beam scores, OpenAI/Anthropic smoke) deferred — need GPU/API keys. **Amended 2026-06-10**: `transformers>=4.40,<4.46` pinned in `pyproject.toml` (diverse beam search removed from core transformers in 4.56; `evaluation_strategy` renamed in 4.46); see decisions-log 2026-06-10. |
| 0.3 | `0.3-schema.md` | 0.1 | done | `pipeline/schema.py` exposes `build_pole_schema_repr() -> SchemaRepr` (9 node labels, 28 relationship paths, node + temporal-edge property lists; `format="nodes_and_paths"`). Built once from fixed definitions, not read from Neo4j. Handoff: prompt-building stages (3.1, 3.4, 3.6, 5.1, 6.1) import it and serialise via `SchemaRepr` methods from 0.1 — no consumer constructs a `SchemaRepr` by hand. Now-set acceptance (counts; label/rel-type consistency with `SyntheticPoliceKG.cypher`; serialiser substrings + one cypher line per path) passes via `tests/test_schema.py` (3 tests). Integration check (parity with live `db.labels()`/`db.relationshipTypes()`) deferred — needs Neo4j. |
| 0.4 | `0.4-benchmark-loader.md` | 0.1 | done | `pipeline/data/__init__.py` + `pipeline/data/benchmark_loader.py` exposing `load_benchmark(path) -> list[BenchmarkItem]`, `ambiguous_items(items)`, `items_by_type(items)`. No remapping — loads the canonical file and validates defensively (invalid/empty/compound `ambiguity_type`, `is_ambiguous`↔type mismatch, interpretation-presence mismatch all raise `ValueError`). Handoff: 5.4 (eval harness) and 2.2/2.3 (probe) import these to load the identical 125-item set; `ambiguous_items` gives the 50-question subset, `items_by_type` the stratified groups (schema=13, entity=11, intent=11, temporal=15, None=75). All acceptance criteria pass via `tests/data/test_benchmark_loader.py` (6 tests). **Phase 0 complete.** |

## Phase 1 — Training Data Prep

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 1.1 | `1.1-pattern-extraction.md` | 0.1 | done | `pipeline/schema_linker/__init__.py` + `pipeline/schema_linker/pattern_extraction.py` exposing `extract_schema_pattern(cypher) -> Optional[str]` (canonical committed-pattern format) and `has_relationship_type(pattern) -> bool` (the rel-type exclusion predicate). Self-contained depth-aware MATCH-clause parser — CyVer not used (needs live driver; see decisions-log). Handoff: 1.2 builds SL targets and 1.3 builds QG `committed_pattern` by calling `extract_schema_pattern`, then excluding rows where it returns `None`, `has_relationship_type` is False, or the CyVer SyntaxValidator (live driver) fails. Output format is the contract `_build_cypher_pattern` (0.1) and 3.5 must match. All acceptance criteria pass via `tests/schema_linker/test_pattern_extraction.py` (14 tests); CyVer SyntaxValidator filtering deferred to 1.2/1.3 (needs Neo4j). |
| 1.2 | `1.2-sl-training-data.md` | 1.1, 0.3 | done | `pipeline/schema_linker/prompts.py` (`SL_SYSTEM`, `build_sl_prompt(question, schema_block)` — the shared train/inference prompt contract) + `pipeline/schema_linker/training_data.py` (`load_ozsoy_split`, `build_sl_training_instance`, `build_sl_dataset` → writes `data/ozsoy_schema_linker_train.jsonl` + `_eval.jsonl`, returns kept/excluded-by-reason report). Per-row filter = parse-fail (`extract_schema_pattern` None) / node-only (`has_relationship_type` False) / CyVer `SyntaxValidator`. CyVer pass is conditional on a live Neo4j driver (env-configured) and deferred otherwise — see decisions-log. Handoff: 2.1 trains on the two jsonl files (`{"prompt","completion"}` lines, completion-only loss); 3.1 imports `build_sl_prompt` verbatim. Now-set acceptance (criteria 1–3) passes via `tests/schema_linker/test_training_data.py` (6 tests); full-run yield (~85–90%) + CyVer syntax filtering deferred (needs dataset + Neo4j). |
| 1.3 | `1.3-qg-training-data.md` | 1.1 | done | `pipeline/query_generator/__init__.py` + `pipeline/query_generator/prompts.py` (`SYSTEM_PROMPT_WITH_PATTERN`, `SYSTEM_PROMPT_ZERO_SHOT`, `build_qg_prompt(question, schema_block, committed_pattern=None)` — shared train/inference/C1 prompt; with-pattern → C2&C3, without → C1 zero-shot) + `pipeline/query_generator/training_data.py` (`build_qg_training_instance`, `build_qg_dataset` → writes `data/ozsoy_qg_train.jsonl` + `_eval.jsonl`, returns counts report). QG `completion` = full gold Cypher verbatim; `committed_pattern` = `extract_schema_pattern(gold)`. Per-row filter = parse-fail ONLY (node-only rows are KEPT — valid QG targets; no rel-type/CyVer filter, unlike 1.2; see decisions-log 2026-06-07). Same `seed=13` as 1.2 → consistent SL/QG partition. Handoff: 2.4 trains on the two jsonl files (`{"prompt","completion"}` lines, completion-only loss); 3.6 (inference) + 5.3 (C1 zero-shot) import `build_qg_prompt` verbatim. Now-set acceptance (criteria 1–3, incl. the train↔inference format-consistency guard) passes via `tests/query_generator/test_training_data.py` (9 tests); full-run yield (~90% of 35.9k) deferred (needs dataset). **Phase 1 complete.** **Amended 2026-06-10**: `build_qg_prompt` gained inference-only `error_feedback=None` (retry-feedback block; byte-identical output when None - training data/in-flight fine-tune unaffected); see decisions-log 2026-06-10. |

## Phase 2 — Training & Probe Checkpoints *(GPU + Ozsoy data)*

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 2.1 | `2.1-sl-training.md` | 1.2, 0.2 | done | `pipeline/sft.py` (`build_model_inputs` — completion-only loss masking; `run_sft(config) -> adapter_path` — LoRA/QLoRA harness reused unchanged by 2.4; `measure_token_lengths` — `max_seq_length` sub-task helper) + `config/models.yaml` (base registry: `mistral7b`, `llama31-8b`; per-model `base`/`target_modules`/`dtype`/`load_in_4bit`) + `config/schema_linker_train.yaml` (shared hyperparameters only; `max_seq_length: 1024`, not 512) + `experiments/train_schema_linker.py` (`--model_key`; `build_run_config` merges registry+hp → namespaced `checkpoints/{model_key}/sl_adapter`; unknown key exits with known list). Handoff: 2.4 reuses `pipeline/sft.py` + `config/models.yaml` verbatim (QG = own YAML + `qg_adapter`); 3.1 loads `checkpoints/{model_key}/sl_adapter` via `HuggingFaceLLM(base, peft_adapter_path=...)`; 2.2/2.3 probe that adapter; 5.4 resolves base+adapter from the registry. Now-set acceptance (criteria 1–3) passes via `tests/test_sft.py` (7 tests; full suite 55). Deferred (GPU): 3-epoch train + eval-loss + adapter weights, sibling-dir namespacing, and the token-length measurement — now run on Kaya (job 490628): SL p99=3473, `max_seq_length` raised 1024 → 3584 in `config/schema_linker_train.yaml`; see decisions-log 2026-06-08. First full-run attempt (job 490755) OOM'd at step 0 (`max_seq_length: 3584` × `per_device_train_batch_size: 4` exceeds 16 GB V100); `config/schema_linker_train.yaml` adjusted to `per_device_train_batch_size: 2` / `gradient_accumulation_steps: 16` (effective batch unchanged); see decisions-log 2026-06-08. Smoke test (`kaya/05_smoke_sl.slurm`, job 490807) confirmed headroom: stable 14400 MiB/16384 MiB, no OOM, ~126s/optimizer step — full SL run ≈50h, fits the `gpu` partition's 72h cap (`10_train_sl.slurm` `--time` raised `24:00:00 → 3-00:00:00`); `run_sft` gained resume-from-checkpoint + idempotent skip-if-saved so a chained job can pick up mid-run if needed; see decisions-log 2026-06-08 ("Kaya ops" row). Queued for the full run. |
| 2.2 | `2.2-diversity-penalty-sweep.md` | 2.1, 1.2, 0.2, 0.3, 0.4 | done | `experiments/probe_utils.py` (`generate_beams`, `normalised_entropy`, `roc_auc`, `covered`, `hallucinated`, `gold_patterns`) + `experiments/diversity_penalty_sweep.py` (`score_penalty`, `select_penalty`, `write_results`, `write_inference_config`, `main`). Sweeps `diversity_penalty ∈ {0.2,0.5,1.0}` with k=5 diverse beams; picks max entropy AUC under the Cov@5 ≥ 0.85 guardrail (hallucination as tiebreak), falling back + flagging if no penalty qualifies. Loads the adapter from the namespaced `checkpoints/{model_key}/sl_adapter` (deviation from spec's bare path — see decisions-log). Handoff: 2.3 reuses `probe_utils`; once the GPU sweep runs, `config/schema_linker_inference.yaml` carries the locked `diversity_penalty` that 3.1's `GenerationConfig` reads, and the recorded entropy AUC at the chosen penalty is the figure 2.3 must beat (vs ≈0.62 baseline). Now-set acceptance (criteria 1–2) passes via `tests/test_probe_utils.py` (20 tests; full suite 75). **GPU sweep complete** (`kaya/30_sweep.slurm`, job 511000): wrote `results/diversity_penalty_sweep.json` + `config/schema_linker_inference.yaml` (`diversity_penalty: 0.2`, chosen via the documented fallback — Cov@5=0.000 for all three penalties tested; see decisions-log 2026-06-15). Required an 0.2 bugfix to `pipeline/llm/huggingface.py._decode` (group-beam-search `beam_indices`/`outputs.scores` off-by-one in `transformers==4.45.2`; see decisions-log 2026-06-15). |
| 2.3 | `2.3-probe-rerun.md` | 2.1, 2.2 | done | `experiments/entropy_probe.py`: three pure scorers (`h_norm_candidates` [headline, normalised candidates], `h_norm_beams` [raw], `top_beam_dominance` [`1−max_prob`]) reusing 2.2 `probe_utils` (`generate_beams`/`normalised_entropy`/`roc_auc`/`_normalise_pattern`); `compute_auc_table`, `build_finding`, `write_results`, `read_diversity_penalty` (fails clearly pointing to 2.2 if `config/schema_linker_inference.yaml`/key absent), `generate_all_beams`+`cache_beams`/`load_cached_beams` (caches `results/beams_pole.jsonl` so ablations rescore GPU-free), `main`. No code contract exported — artefact is evidence. Now-set acceptance (criterion 1: three scorers compute from hand-built beams + feed `roc_auc`) passes via `tests/test_entropy_probe.py` (15 tests; full suite 90). Two interpretive decisions logged (signal-mapping; namespaced adapter path). **GPU probe run complete** (`kaya/40_probe.slurm`, job 511001): wrote `results/entropy_probe_pole.json` + cached `results/beams_pole.jsonl` (125 items, `diversity_penalty=0.2`). All three signals land ~0.587–0.594, flat vs the ~0.62 general-model baseline and flat across ablations (spread 0.007 ≤ 0.01) — per spec this is acceptable (entropy is a secondary signal; 3.3 may use the simplest scorer, `h_norm_candidates`/normalised entropy, without loss). **Phase 2 fully complete** (training + both probes) for `mistral7b`. |
| 2.4 | `2.4-qg-training.md` | 1.3, 0.2, 2.1 *(sft.py only)* | done | `config/query_generator_train.yaml` (shared QG hyperparameters only; identical in shape to the SL YAML, differing in `data` paths → `data/ozsoy_qg_{train,eval}.jsonl`; `max_seq_length: 1024`) + `experiments/train_query_generator.py` (`--model_key`; `build_run_config` merges `config/models.yaml` registry + this YAML → namespaced `checkpoints/{model_key}/qg_adapter`; reuses `pipeline.sft.run_sft`/`build_model_inputs` from 2.1 verbatim — no QG-specific harness or masking; unknown key exits with known list). Handoff: 3.6 loads `checkpoints/{model_key}/qg_adapter` via `HuggingFaceLLM(base, peft_adapter_path=...)` paired with the matching base for greedy Cypher generation. Now-set acceptance (criteria 1–2) passes via `tests/test_train_query_generator.py` (4 tests; full suite 94). Deferred (GPU): 3-epoch train + eval-loss + adapter weights; QG token-length sub-task — now run on Kaya (job 490628): QG p99=3486, `max_seq_length` raised 1024 → 3584 in `config/query_generator_train.yaml` (shared cap with SL; see decisions-log 2026-06-08). First full-run attempt (job 490756) OOM'd at step 0 (same `max_seq_length`/batch-size cause as 2.1); `config/query_generator_train.yaml` adjusted to `per_device_train_batch_size: 2` / `gradient_accumulation_steps: 16` (effective batch unchanged); see decisions-log 2026-06-08. Re-run pending — and at the ~126s/optimizer-step rate the SL smoke test measured (job 490807), QG's 3030 steps need ≈106h, more than the `gpu` partition's 72h max, so a single job cannot finish it. `pipeline.sft.run_sft` gained resume-from-checkpoint + idempotent skip-if-saved (it now picks up the last `save_strategy: epoch` checkpoint and is a no-op once `adapter_model.safetensors` exists); `20_train_qg.slurm` `--time` raised `24:00:00 → 3-00:00:00` and its header documents chaining two such jobs via `sbatch --dependency=afterany`; see decisions-log 2026-06-08 ("Kaya ops" row). **Phase 2 complete (per trained model_key)** once both adapters finish training. |

## Phase 3 — Inference Components *(runtime I/O order)*

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 3.1 | `3.1-schema-linker.md` | 0.1, 0.2, 0.3, 2.1, 2.2 | done | `pipeline/schema_linker/inference.py` (`sl_generation_config`, `extract_pattern_from_completion`), `postprocessing.py` (`ParsedPattern`, `parse_schema_pattern`, `align_beam_candidates`, `normalise_candidate_scores`, `build_candidate_mapping`), `linker.py` (`SchemaLinker`, `SchemaLinkerError`). Handoff: 3.2 reads `CandidateMapping.mentions` to scope entity labels; 3.3 reads relationship-type slot scores (incl. `rel_n_temporal`) for schema entropy; 3.4/3.5 get the full distribution; orchestrator calls `top1_mapping()` on the unambiguous bypass. `SchemaLinker.link(question, schema) -> CandidateMapping`; inject `HuggingFaceLLM(base, peft_adapter_path=checkpoints/{model_key}/sl_adapter)` + read `diversity_penalty` from `config/schema_linker_inference.yaml` (5.3). Now-set acceptance (criteria 1–5) passes via `tests/schema_linker/test_postprocessing.py` (11 tests; full suite 108). **Two deviations** (decisions-log 2026-06-15): `build_candidate_mapping` gained keyword-only `min_valid_beams` (wires the linker's unused param); temporal slot encodes the active filter on `SchemaElement.parent`. **Criterion 6 measured GPU-free** from the deterministic cached `results/beams_pole.jsonl` (identical prompt/config/adapter to `link`, so a GPU re-run is byte-identical): the parser **resolves the `--`-suffix artefact**, lifting **Cov@5 0.000 → 0.208** (EM@1 0.144). **⚠ Cov@5 0.208 ≪ ≥85% target (success criterion #5)** — an intrinsic `mistral7b/sl_adapter` quality gap (schema-plausible but structurally mismatched beams; not single-hop truncation, not node-set strictness), **flagged for 2.1/2.2**, not a 3.1 defect; 3.1's `CandidateMapping` contract is satisfied and unblocks 3.2. |
| 3.2 | `3.2-entity-lookup.md` | 0.1, 3.1 *(live Neo4j)* | done | `pipeline/entity_lookup/`: `registry.py` (`ENTITY_REGISTRY` — Person/Organisation/Case/Location are entity-searchable; Vehicle/Phone/Incident excluded), `cache.py` (`CachedNode`, `EntityCache` + `EntityCache.load(driver, database_name=None)` — one Cypher query per label, skips name-less nodes, immutable), `span_extractor.py` (`extract_entity_spans` — regex Operation/multi-word-proper/single-cap-excl-first-word/quoted + `_KNOWN_SUBURBS`, longest-first deduped with subspan removal), `matcher.py` (`fuzzy_match_score` = rapidfuzz `token_set_ratio`/100; `compute_posterior_scores` — uniform-prior Bayesian posterior over ≥threshold matches, sums to ~1), `lookup.py` (`entity_lookup(question, candidate_mapping, cache, fuzzy_threshold=0.75, max_candidates_per_mention=10) -> EntityLookupResult`; `_candidate_labels` always includes Person+Organisation). `__init__.py` re-exports all. Added `rapidfuzz` to `pyproject.toml`. Handoff: 3.3 reads `EntityLookupResult.entity_mentions[span]` posteriors for entity entropy (single match → posterior 1.0 → entropy 0); 3.4/3.5 inject matched `EntityCandidate`s as prompt context; 5.1 calls `EntityCache.load(driver)` once and passes the cache + a `CandidateMapping` (from 3.1) into `entity_lookup`. Acceptance criteria 1–4 (no-DB) pass via `tests/entity_lookup/test_entity_lookup.py` (11 tests). **Criterion 5 ran for real on Kaya** (job 511153, node k005, `bolt://localhost:7687` via the `55_neo4j.slurm` Apptainer pattern): `EntityCache.load` returned **exactly** Person 35 / Organisation 5 / Case 4 / Location 18 (total 62 name-searchable), CachedNodes correctly shaped (e.g. Location name_values=[suburb, address], display_name=suburb; Case display_name="Operation Ironside — …"). One interpretation logged (decisions-log 2026-06-15): >max-candidates truncation renormalises the retained posteriors to keep `EntityCandidate.posterior_score` a valid distribution for 3.3. |
| 3.3 | `3.3-bayesian-scorer.md` | 0.1, 3.1, 3.2 | done | `pipeline/ambiguity/__init__.py` (re-exports `compute_ambiguity_scores`) + `pipeline/ambiguity/bayesian_scorer.py` exposing `_shannon_entropy_normalised(scores) -> float` and `compute_ambiguity_scores(candidate_mapping, entity_lookup) -> (schema_entropy, entity_entropy)`, both ∈ [0,1]. Pure arithmetic, no GPU/DB. schema_entropy = max H_norm across relationship_type slots (incl. temporal `active` variants); entity_entropy = max H_norm across entity mentions with ≥2 candidates; 0.0 when no qualifying slot. Implemented verbatim from spec — no deviations. Handoff: 3.4 imports `compute_ambiguity_scores` and injects both scores into the AD prompt as secondary evidence (entropy informs, LLM decides — never a hard gate). All 4 acceptance criteria + worked examples pass via `tests/ambiguity/test_bayesian_scorer.py` (12 tests; full suite 131 under the `t2c` conda env). **Phase 3 core (3.1–3.3) complete.** |
| 3.4 | `3.4-ambiguity-detector.md` | 0.1, 0.2, 0.3, 3.1, 3.2, 3.3 | done | `pipeline/ambiguity/prompts.py` (`AD_SYSTEM_PROMPT` — taxonomy + curated POLE schema + 4 authored few-shot examples; `_format_candidate_block`, `_format_entity_block` — shared serialisers; `build_ad_user_turn`) + `pipeline/ambiguity/detector.py` (`ambiguity_detector(question, candidate_mapping, entity_lookup, ad_llm, threshold_schema=0.6, threshold_entity=0.8) -> AmbiguityResult`; `_parse_ad_output` — JSON extraction + validation with deterministic entropy-threshold fallback; never raises). LLM primary, entropy secondary (informs prompt, gates only the fallback). Handoff: 3.5 imports `_format_candidate_block`/`_format_entity_block` from `pipeline.ambiguity.prompts` and reads `AmbiguityResult.detected_types`; the orchestrator gates on `is_ambiguous` (False → `candidate_mapping.top1_mapping()`; True → Disambiguator). `pipeline/ambiguity/__init__.py` left re-exporting only `compute_ambiguity_scores` (keeps the package torch-free; import the detector from its module). Offline acceptance criteria 1–4 pass via `tests/ambiguity/test_detector.py` (17 tests; full suite 148 under the `t2c` env). Two deviations logged (decisions-log 2026-06-15): authored few-shot text ("Pass 4 §5.4" source absent from repo); temporal slot renders the `{active:true}` filter. **Criterion 5 ran for real on Kaya** (`kaya/45_ad_probe.slurm` + `kaya/ad_probe.py`, job 511162, V100/k035, ~5.7 min — combined GPU+Neo4j: real SchemaLinker `CandidateMapping` + live Entity Lookup + base `Mistral-7B-v0.3` as the AD LLM, 9-question stratified sample): the stage runs end-to-end, never raised, and returns well-formed `AmbiguityResult`s with coherent rationales (criterion met mechanically). **Honest finding:** all 9 classified `is_ambiguous=False` because `schema_entropy=entity_entropy=0.00` on every item — the SL collapses to single-candidate mappings (the known `mistral7b/sl_adapter` Cov@5=0.208 / 2.2 Cov@5=0.000 diversity gap), so the AD's secondary signal is flat and base 7B defers to it. Attributed upstream (3.1/2.1/2.2), not a 3.4 defect; a stronger/instruct backend (5.3 API condition) is the documented path and full Detection F1 lands in 4.2. See decisions-log 2026-06-15.|
| 3.5 | `3.5-disambiguator.md` | 0.1, 0.2, 3.1, 3.2, 3.4 | done | `pipeline/disambiguator/prompts.py` (`DIS_SYSTEM_PROMPT` — system prompt + 3 authored few-shot examples; `_format_tried_block`; `build_disambiguator_prompt`, reusing 3.4's `_format_candidate_block`/`_format_entity_block`) + `pipeline/disambiguator/disambiguator.py` (`disambiguator(question, ambiguity_result, candidate_mapping, entity_lookup, dis_llm, previously_tried, mode="automated") -> Optional[SchemaMapping]`; `_parse_disambiguator_output` — JSON extract + null→None + repeat/malformed→best-untried fallback; `_pattern_to_committed` — best-effort committed dict via candidate match + `parse_schema_pattern`; `_best_untried_candidate`/`_ranked_committed` — deterministic score-ranked fallback). `__init__.py` re-exports `disambiguator`, `DIS_SYSTEM_PROMPT`, `build_disambiguator_prompt`. Handoff: orchestrator (5.2) calls `disambiguator(...)`; on a `SchemaMapping` appends it to `previously_tried` and sends `cypher_syntax` to the QG (3.6); on `None` ends the disambiguation loop. `mode="interactive"` raises `NotImplementedError` (6.2 builds the clarification path). Offline acceptance criteria 1–5 pass via `tests/disambiguator/test_disambiguator.py` (12 tests; full suite 160 under the `t2c` env). Two deviations logged (decisions-log 2026-06-16): authored few-shot text ("Pass 4 §6.3" source absent); criterion-6 run findings. **Criterion 6 ran for real on Kaya** (`kaya/47_dis_probe.slurm` + `kaya/dis_probe.py`, job 512038, V100/k027, ~5.6 min — real SchemaLinker + live Entity Lookup + base `Mistral-7B-v0.3` as the dis LLM, 7-question ambiguous sample, 2 attempts each): committed a `SchemaMapping` for **7/7**; retry path honoured `previously_tried` (repeat→fallback fired) and returned `None` when exhausted. Honest note: base 7B sometimes emits out-of-schema patterns (committed verbatim by design — cypher_syntax authoritative, CyVer 3.7 catches them); the known `mistral7b/sl_adapter` diversity gap means some mappings are single-/empty-candidate. **Phase 3 detection/disambiguation (3.4–3.5) complete.** |
| 3.6 | `3.6-query-generator.md` | 0.1, 0.2, 1.3, 2.4 | done | `pipeline/query_generator/generator.py`: `QueryGenerator(llm)` with `generate(question, schema_mapping, schema, error_feedback=None) -> str` (greedy, `max_new_tokens=256`; reuses `build_qg_prompt` from 1.3 so inference == training format) + `_clean_cypher(text) -> str` (fence strip + trailing-prose trim + literal-`\n`/`\t`/`\r` unescape). Branch on `SchemaMapping.cypher_syntax`: `!= ""` → committed-pattern prompt (C2/C3); `== ""` → zero-shot (C1). Handoff: orchestrator (5.2) calls `QueryGenerator(HuggingFaceLLM(base, peft_adapter_path=checkpoints/{model_key}/qg_adapter)).generate(...)` and passes the returned **executable** Cypher to CyVer (3.7); on a syntax failure 5.2 re-calls with composed `error_feedback`. Offline criteria 1–3 pass via `tests/query_generator/test_generator.py` (7 tests; full suite 167 under `t2c`). **Criterion 4 ran for real on Kaya** (`kaya/49_qg_probe.slurm` + `kaya/qg_probe.py`, jobs 512508→512707, V100/k036, ~3 min, GPU only — no Neo4j): trained `mistral7b/qg_adapter`, 8-question stratified sample, both modes — emitted clean `MATCH … RETURN` for **8/8 in both committed-pattern and zero-shot (16/16)**. **One deviation** (decisions-log 2026-06-16, user-approved): `_clean_cypher` unescapes literal `\n` (Ozsoy gold stores clause breaks as the escape `\n`; 1.3 keeps it verbatim, the 2.4 adapter reproduces it) — normalised at the QG cleanup point so output is valid Cypher for 3.7/3.8; upstream Ozsoy-`\n` property flagged for the record, not re-trained. |
| 3.7 | `3.7-cyver-validator.md` | 0.1 *(live Neo4j)* | done | `pipeline/validation/__init__.py` (re-exports `cyver_validator`) + `pipeline/validation/cyver_validator.py` exposing `cyver_validator(cypher, components) -> ValidationResult`. Runs Syntax → Schema → Properties against the live graph via the pre-built validators duck-typed on `components` (`syntax_validator`/`schema_validator`/`properties_validator`/`database_name`); short-circuits and returns typed routing on first failure: syntax invalid → `("syntax","query_generator")`; schema `<1.0` → `("schema","schema_linker")`; properties `not None and <1.0` → `("properties","schema_linker")`; `properties_score is None` (no props) or all-pass → `(None,"accept")`. CyVer's diagnostic dicts retained in `metadata`. No `SchemaRepr` passed; CyVer import lives in 5.1, not here (module is import-light — only `pipeline.types`). **Implemented verbatim from spec — no deviations, no decisions-log entry.** Handoff: orchestrator (5.2) reads `route_to` — `accept` → DB executor (3.8); `query_generator` → regenerate (inner loop ≤2); `schema_linker` → re-link (outer loop ≤3). 5.1 constructs the three real `CyVer` validators once and injects them via `PipelineComponents`. Offline acceptance criteria 1–5 pass via `tests/validation/test_cyver_validator.py` (6 tests; full suite 173 under the `t2c` env). **Criterion 6 ran for real on Kaya** (`kaya/57_cyver_probe.slurm` + `kaya/cyver_probe.py`, job 513073, k011, ~22 s, Neo4j only — no GPU): the three real validators built against the live SyntheticPoliceKG graph routed all 5 crafted queries correctly — valid(props)→accept (schema=1.0,props=1.0), valid(no-props)→accept (props=None), bad-syntax→query_generator (error_type=syntax), unknown-label `Persn`→schema_linker (schema=0.5), unknown-property `naem`→schema_linker (props=0.0). Confirms the `props=None→accept` edge case and CyVer's partial-score behaviour against POLE. |
| 3.8 | `3.8-db-executor.md` | 0.1 *(live Neo4j)* | done | `pipeline/execution/__init__.py` (re-exports `db_executor`) + `pipeline/execution/db_executor.py` exposing `db_executor(cypher, driver, database="neo4j", timeout_s=10.0) -> ExecutionResult`. Runs the query in a managed read transaction (`session.execute_read`) whose server-side timeout is armed via `@neo4j.unit_of_work(timeout=timeout_s)`; records = `[record.data() ...]`; success path → `success=True, result_set, error=None, execution_time_ms`; any `Neo4jError`/`DriverError` (syntax/constraint/transient or server-side timeout) → `success=False, result_set=[], error=str(e)`, time still set. Empty result set is a legitimate success. Module is import-light (only `pipeline.types` + `neo4j`). **One deviation** (decisions-log 2026-06-16): timeout set via `unit_of_work` (driver 6.2 `execute_read` has no per-call `timeout=`) and catch broadened to `(Neo4jError, DriverError)` per the spec's "broadest relevant base" note. Handoff: 4.1 (Semantic Evaluator) executes benchmark ground-truth Cypher with the **same** executor + driver and frozenset-compares `ExecutionResult.result_set`; 5.1 holds the driver on `PipelineComponents`; 5.2 calls `db_executor` on CyVer `accept`. Offline acceptance criteria 1–3 pass via `tests/execution/test_db_executor.py` (4 tests; full suite 177 under the `t2c` env). **Criterion 4 ran for real on Kaya** (`kaya/59_db_executor_probe.slurm` + `kaya/db_executor_probe.py`, job 513261, k005, ~25 s, Neo4j only — no GPU): against the live SyntheticPoliceKG, real POLE queries returned rows (5 Person), empty result → `success=True, result_set=[]`, a relationship query returned the `Incident` node as a plain property dict (neo4j temporal `Date`/`Time` preserved), and a heavy `[*1..30]` traversal hit the timeout → `success=False, execution_time_ms≈2499`, `error=Neo.ClientError.Transaction.TransactionTimedOutClientConfiguration` (confirms `unit_of_work` armed the server-side timeout). **Phase 3 complete — all eight inference components (3.1–3.8) exist; `question → … → ExecutionResult` is implementable. Phase 4 (4.1 Semantic Evaluator, 4.2 metrics) is next.** |

## Phase 4 — Evaluation *(live Neo4j)*

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 4.1 | `4.1-semantic-evaluator.md` | 0.1 *(live Neo4j)* | done | `pipeline/evaluation/__init__.py` (re-exports `semantic_evaluator`) + `pipeline/evaluation/semantic_evaluator.py` exposing `semantic_evaluator(execution_result, benchmark_item, generated_cypher, cyver_result, condition, retry_count, driver, database_name, embedding_model=None, is_first_attempt=False) -> EvaluationResult`, plus helpers `_result_sets_equal`/`_is_id_field`/`_normalise_value`/`_run_query`/`_execute_ground_truth`. Scores EX (vs `cypher_default`) and relaxed AREA (vs any interpretation), executing ground truth **live** with the same driver/`.data()` extraction as 3.8. failure_mode: `invalid_query` (exec failed or CyVer≠accept; never executes GT) / `None` (correct) / `valid_non_default` (matches a non-default interp, ambiguous items only) / `wrong_result`. Comparison is frozenset-equality (order/dup-insensitive, float-normalised); optional `embedding_model` adds a row-aligned cosine fallback for free-text fields only (IDs via `_is_id_field` + numerics always exact; no model → exact only). **One deviation** (decisions-log 2026-06-24): `_normalise_value` now recurses into unhashable container values (`dict`/`list` from a generated `RETURN p`/`collect()`) so the fast-path frozenset is total — the first full 125×3 run (job 683734) crashed on C1 with `TypeError: unhashable type: 'dict'`. **Second deviation** (decisions-log 2026-06-27, user-approved): `_result_sets_equal` is now **column-name-insensitive** — records compared by ordered value tuple (RETURN order), not `record.data()` column names, so a correct answer under a different variable/alias (`x.name` vs gold `p.name`, `RETURN x` vs `RETURN v`) counts as correct; both fast path and cosine fallback fixed. A job-914770 spot-check showed name-keying made EX a strict lower bound (QG uses Ozsoy naming, C1 zero-shot — neither matches the gold's `p`/`i`/`loc_i`/`v`). Suite now 22 evaluator tests. Handoff: orchestrator (5.2) records the `EvaluationResult` on `PipelineState` and, in Condition 3, retries the disambiguator when `failure_mode='wrong_result'` with retry budget left; 4.2 aggregates per-question `EvaluationResult`s into the metric bundle. Offline acceptance criteria 1–5 pass via `tests/evaluation/test_semantic_evaluator.py` (15 tests; full suite **192** under the `t2c` env). **Criterion 6 ran for real on Kaya** (`kaya/61_semantic_evaluator_probe.slurm` + `kaya/semantic_evaluator_probe.py`, job 639269, k010, ~23 s, Neo4j only — no GPU): all four branches confirmed against the live SyntheticPoliceKG — default==default→correct (Q-001), a non-default interpretation with a differing live result set→`valid_non_default` (Q-016), garbage rows→`wrong_result`, exec-failed→`invalid_query`. **New dep:** `sentence-transformers` added to `pyproject.toml` + installed into `t2c` (5.6.0; verified the `transformers<4.46` diverse-beam pin is untouched — the module imports only `torch`, the caller builds the SentenceTransformer). |
| 4.2 | `4.2-metrics.md` | 0.1, 4.1 | done | `pipeline/evaluation/metrics.py` exposing `QuestionRecord` (terminal aggregate — `final`/`history`/`first_validation`, with `first_evaluated()`), `MetricBundle` (Table 1–3 fields), `compute_metrics(records, ad_predictions=None) -> MetricBundle` (every denominator over questions, all n present; `ad_predictions=None` → DSR/Detection NaN), and `format_metric_tables(bundles: dict[Condition, MetricBundle]) -> str` (three markdown tables, C1→C2→C3 column order over present conditions; NaN cells render `—`; Table 3 is C2/C3 only). `compute_metrics`/`MetricBundle`/`QuestionRecord` implemented verbatim from spec — no deviations. `metrics.py` not re-exported from `pipeline/evaluation/__init__.py` (left as 4.1's `semantic_evaluator`-only export — 5.4 imports from `pipeline.evaluation.metrics`). Note: per the given `compute_metrics`, `semantic_repair_pct` is 0.0 (not NaN) for C2, so Table 3's C2 cell renders `0.0`, not `—` — both the metric code and the "render `—` when NaN" rule are followed literally; no special-case. Handoff: 5.4 assembles one `QuestionRecord` per question per condition from each `PipelineState` (`evaluation_result`→`final`, `evaluation_history`→`history`, `first_validation_result`→`first_validation`; C3's `ad_predictions` separately), calls `compute_metrics` per condition, and prints `format_metric_tables`. All 5 acceptance criteria (incl. the amended per-attempt semantics 4a–d) pass via `tests/evaluation/test_metrics.py` (10 tests; full suite **202** under the `t2c` env). **Phase 4 complete.** |

## Phase 5 — Integration

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 5.1 | `5.1-components.md` | 0.1, 0.2, 3.1, 3.2, 3.6 *(live Neo4j)* | done | `pipeline/components.py` exposing `PipelineComponents` — a `@dataclass(kw_only=True)` container holding the constructed stage objects (`query_generator: QueryGenerator`, `schema_linker: Optional[SchemaLinker]`, `ad_llm`/`dis_llm: Optional[BaseLLM]`) plus shared graph resources (`neo4j_driver`, `database_name`, the three pre-built CyVer validators `syntax_validator`/`schema_validator`/`properties_validator`, `entity_cache`, `schema`, `embedding_model`, `use_prefilter`). `PipelineComponents.build(*, ...)` opens the neo4j driver, builds the three CyVer validators once with it, and loads `EntityCache` (`load_entity_cache=True` default; `False` → `entity_cache=None`); context-manager (`__enter__`/`__exit__`/`close`) closes the driver. CyVer import lives here (per 3.7 handoff); `SentenceTransformer` is `TYPE_CHECKING`-only to keep import light. **Implemented verbatim from spec — no deviations.** Handoff: 5.3 constructs one `PipelineComponents` per condition (C2 `beam_k=1`, C3 `beam_k=5`); 5.2 reads `schema_linker.link`, `query_generator.generate`, `ad_llm`, `dis_llm`, `entity_cache`, the three validators (via `cyver_validator`), `neo4j_driver`, `database_name`, `schema`, `embedding_model`, `use_prefilter`; 3.7 reads the validators + `database_name` off it. Now-set acceptance criteria 1–3 pass via `tests/test_components.py` (6 tests; full suite **208** under the `t2c` env). Deferred (live Neo4j): real `EntityCache.load` counts + CyVer construction against the POLE graph. |
| 5.2 | `5.2-orchestrator.md` | 3.1–3.8, 4.1, 5.1 | done | `pipeline/orchestrator.py` exposing `Orchestrator` (`run(question, benchmark_item, condition, components) -> PipelineState`) + module-level `_compose_error_feedback(cypher, vr)`. Drives one question through the three feedback loops — OUTER schema retry (≤3, `retry_count++`, `previously_tried` PERSISTS, CyVer diagnostics carried as `error_feedback`), DISAMBIGUATION semantic retry (≤1, C3 only, no budget cost), INNER QG+CyVer syntax retry (≤2, no budget cost, re-prompts QG with `error_feedback`) — and condition routing (baseline→empty mapping; schema_grounded→top1; disambiguation_enhanced→SL+EntityLookup+AD+Disambiguator). Uses the actual Phase 3 APIs (`schema_linker.link`, `entity_lookup`, `ambiguity_detector`, `disambiguator`, `query_generator.generate`, `cyver_validator`, `db_executor`, `semantic_evaluator`); `cosine_prefilter` (6.1) imported lazily only when `use_prefilter`. Records per-attempt data for 4.2 (`first_validation_result` set once; `evaluation_history` appended each eval, `evaluation_result`==last) + C3 `ad_predictions` once per outer iter. **One deviation** (decisions-log 2026-06-24): the SL `link` call is wrapped in `try/except SchemaLinkerError` → on a question the SL parses to 0 in-schema beams (3.1's raise) it sets `is_failed`/`failure_reason="schema_linker_no_valid_beams"` and breaks, so the full 125×3 run (job 710534) records that question as `invalid_query` instead of aborting on it (suite now 10 orchestrator tests). Handoff: 5.4 calls `Orchestrator().run(...)` for every question × condition, builds a `QuestionRecord` from `evaluation_result`/`evaluation_history`/`first_validation_result` and (C3) reads `ad_predictions`; 5.3 builds the per-condition `PipelineComponents` it consumes. Acceptance criteria 1–8 (no GPU/DB; stub stages) pass via `tests/test_orchestrator.py` (8 tests; full suite **216** under the `t2c` env). End-to-end over the benchmark deferred to 5.4 (live system).|
| 5.3 | `5.3-condition-builders.md` | 0.2, 3.1, 3.6, 5.1 | done | `experiments/build_condition1.py` (`build_condition1` — zero-shot baseline; single `build_llm(base_model_spec)`, `schema_linker/ad_llm/dis_llm=None`, `load_entity_cache=False`), `experiments/build_condition2.py` (`build_condition2` — `SchemaLinker(sl_llm, beam_k=1)` + SFT QG, two HF adapter backends, no AD/Dis, no cache), `experiments/build_condition3.py` (`build_condition3` — `SchemaLinker(sl_llm, beam_k=5, diversity_penalty=...)` + SFT QG + few-shot AD/Dis with swappable `ad_spec`/`dis_spec`, `dis_llm is ad_llm` when both default, `load_entity_cache=True`). **Implemented verbatim from spec — no signature/code deviation.** SL/QG backends are hardcoded HuggingFace (no swap point); AD/Dis are the config swap points (default base model, overridable to OpenAI/Anthropic via a `build_llm` spec). Handoff: 5.4 calls each builder with config values (base+adapters from `config/models.yaml`/`checkpoints/{model_key}`, `diversity_penalty` from `config/schema_linker_inference.yaml`, AD/Dis specs from `config/pipeline.yaml`), runs the orchestrator over the benchmark inside the `with` block, and closes it; swapping the base model or AD/Dis backend is a config edit, no code change. Offline acceptance criteria 1–4 pass via `tests/test_condition_builders.py` (6 tests; full suite **222** under the `t2c` env). **Criterion 5 ran for real on Kaya** (`kaya/63_build_condition_probe.slurm` + `kaya/build_condition_probe.py`, job 670833, 2×V100/k042, Neo4j in-job): **C1 PASS** (single 4-bit model loaded, working `PipelineComponents`, valid zero-shot Cypher); **C2/C3 OOM** loading the 2nd model — the fixed-signature C2/C3 builders load separate full-precision 7B models with `device_map="auto"`, which exceeds Kaya's 2×16 GB V100 ceiling (~29 GB C2 / ~43 GB C3). Not a builder defect: every constituent load is individually Kaya-proven (3.1/3.6/3.4/3.5/3.2) and the constrained-memory path is the spec's **5.4** concern ("4-bit / one base + adapter-switching"). **Flagged for 5.4:** to run C3 on V100s the harness must load one 4-bit base and share/adapter-switch for SL/QG/AD. See decisions-log 2026-06-23. **RESOLVED 2026-06-23 (Tier-1):** `build_condition2`/`build_condition3` gained keyword-only `load_in_4bit`/`torch_dtype` (defaults `False`/`bfloat16` preserve prior behaviour) threaded into the internally-built HF specs only (caller `ad_spec`/`dis_spec` passed through verbatim); `run_evaluation` reads them from the active registry entry. 4-bit brings C2≈9 GB / C3≈14 GB — fits a single 16 GB V100, no multi-node needed. Full suite **232** (+3 builder tests). See decisions-log 2026-06-23 (5.3/5.4 row). **C3 OOM fix 2026-06-26 (decisions-log):** the full run (job 770784) cleared C1+C2 then OOM'd in C3's SL `beam_k=5` generate — three independent `device_map="auto"` loads piled lopsidedly onto GPU 1 while GPU 0 was underused. `build_condition3` now pins SL→cuda:0 (own card), QG+AD→cuda:1 when ≥2 GPUs (else unchanged 'auto'); slurm sets `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`. Full suite **238** (+2 builder tests). |
| 5.4 | `5.4-run-evaluation.md` | 0.3, 0.4, 4.2, 5.2, 5.3 | done | `config/pipeline.yaml` (neo4j + `active_model` + benchmark/SLI paths + `ad`/`dis` swap points + `results_dir`) + `experiments/run_evaluation.py` exposing `run_condition(benchmark, components, condition) -> (records, ad_predictions)` (one `QuestionRecord` per item; never-evaluated state → synthesized `invalid_query` final with `is_first_attempt=False`, `history==[final]`, `first_validation` carried; `ad_predictions` concatenated across states) and `main(config_path="config/pipeline.yaml")` (resolves base+`checkpoints/{active_model}/{sl,qg}_adapter` from `config/models.yaml` via `active_model`; reads locked `diversity_penalty` from `config/schema_linker_inference.yaml`; runs C1/C2/C3 sequentially in `with` blocks; `compute_metrics(..., None)` for C1/C2, real `ad_preds` for C3; prints `format_metric_tables`; `_persist` writes `results/tables_{key}.md` + `results/metrics_{key}.json`). Fail-loud: unknown `active_model`→`SystemExit`, unset `NEO4J_PASSWORD`→`KeyError`, missing `diversity_penalty`→`KeyError`. **One config-content deviation** (decisions-log 2026-06-23): committed `ad: null`/`dis: null` (default to the active base, the spec's own documented fallback) instead of the spec example's literal `Mistral-7B-v0.1` `ad` block — avoids loading a 2nd inconsistent base (registry base is v0.3) and the 5.3 multi-model OOM. Now-set acceptance criteria 1–3 pass via `tests/test_run_evaluation.py` (7 tests; full suite **229** under the `t2c` env). Deferred (live Neo4j + GPU): the full 125×3 run producing populated Tables 1–3. **OOM resolved 2026-06-23 (Tier-1):** `run_evaluation` now threads the registry's `load_in_4bit`/`dtype` into all three builders (C1 spec, C2/C3 SL/QG + C3 default AD), so the run loads 4-bit/fp16 like training did and C2/C3 fit a single 16 GB V100 — see decisions-log 2026-06-23 (5.3/5.4 row). Full suite **232** under `t2c`. **✅ FIRST FULL END-TO-END RUN COMPLETE — 2026-06-27** (`kaya/65_run_evaluation.slurm`, job 914770, 2×V100/k029, ~4h50m, Neo4j in-job): all 125×3 conditions ran with no OOM/crash (the 4.1 dict fix + 5.2 SchemaLinkerError guard + 5.3 C3 device-pinning all held); wrote `results/tables_mistral7b.md` + `results/metrics_mistral7b.json`. This clears 5.4's last deferred acceptance criterion. **Headline (mistral7b, EX/EA as fractions):** C1 0/125, C2 1/125 (0.008), C3 1/125 (0.008); KG-valid 22.4/10.4/12.8%; syntax 92.8/48.8/66.4%; C3 Detection F1 0.233 (prec 0.50, rec 0.15), DSR 0. **Two honest reads:** (1) near-floor accuracy and C3≈C2 — the documented upstream `mistral7b` adapter quality gap (SL Cov@5 0.208/EM@1 0.144 per 3.1; base-Mistral AD under-detection per 3.4, rec 0.15 confirms), NOT a pipeline defect; the `ad`/`dis` API swap points are the documented path to real numbers. (2) **⚠ EX/EA are a LOWER BOUND — evaluator column-name strictness (spot-check 2026-06-27):** `_result_sets_equal`'s `norm` keys each row on the `record.data()` column name (`p.name`, `v`, …), so a substantively-correct answer is scored wrong when the generated query uses a different variable letter/alias than the benchmark gold (verified: `{'x.name':v}`≠`{'p.name':v}`). The QG (Ozsoy-trained naming) and C1 (arbitrary naming) rarely match the gold's `p`/`i`/`loc_i`/`v`, so true EX > reported. **EVALUATOR FIXED 2026-06-27** (decisions-log 4.1, user-approved): `_result_sets_equal` is now column-name-insensitive (value-tuple comparison), so EX/EA no longer punish variable/alias mismatch. ⚠ **The job-914770 results in `results/` predate this fix and are stale — re-run `sbatch kaya/65_run_evaluation.slurm` to regenerate Tables 1–3 before any thesis use.** Full suite **243**. **✅ POST-FIX RE-RUN COMPLETE — 2026-06-29** (job 957276, 2×V100/k027, ~5h, clean — 0 Python tracebacks; the large `.err` is expected operational noise: Neo4j EXPLAIN property-key warnings + AD entropy-fallback messages): regenerated `results/tables_mistral7b.md` + `results/metrics_mistral7b.json` — these are now the valid, fix-consistent numbers (no longer stale). The column-name fix recovered exactly **one** C1 question: **C1 EX 0.0→0.8, C2 0.8, C3 0.8** (Pass@1 same); Table 2 ambiguity all 0.0; C3 Detection F1 23.3 (prec 50.0, rec 15.2). Confirms the prior read — the artifact was a thin layer over the genuine `mistral7b` quality floor (SL Cov@5 0.208, base-Mistral AD under-detection); the `ad`/`dis` API swap points remain the documented path to real numbers. **Phase 5 complete.** |

| 5.5 | `5.5-api-backend-end-to-end.md` | 0.2, 2.2, 2.3, 3.1, 5.3, 5.4 | done | Removes the local-only assumption so ONE config-selected model (local **or** API) runs the whole pipeline incl. the sweep/probe. `pipeline/schema_linker/inference.py::sl_sampling_config` + `SchemaLinker(..., *, decoding=None)` make SL decoding backend-aware (beam for local, temperature sampling for API; `link()` 3.1 signature unchanged). `AnthropicLLM.generate_chat` issues N calls for `num_return_sequences>1` (uniform −1.0 → frequency weighting). `build_condition2/3` are spec-driven (`sl_spec`/`qg_spec`/`base_spec` + `sl_decoding`; HF-only 4-bit/device-pin guarded by backend). `run_evaluation._resolve_model_specs`/`_resolve_sl_decoding` branch on registry `kind` (`local`/`api`); `config/models.yaml` gained `claude-sonnet`/`gpt-4o`; `config/schema_linker_inference.yaml` carries a per-model `decoding` block (legacy flat `diversity_penalty` kept). Sweep/probe generalized: `probe_utils.generate_beams(llm: BaseLLM, …, decoding)`, `diversity_penalty_sweep` sweeps temperature for API (back-compat `select_penalty`), `entropy_probe.read_sl_decoding` + model-keyed caches. **C2/C3 reframed as stage-presence (not SFT) for API** (user-confirmed 2026-06-30; see decisions-log + project-overview §6 should be read with this caveat for API runs). Offline criteria 1–3 pass; **full suite 258** under `t2c` (local Mistral path byte-identical — decoding defaults to beam). New non-GPU runner `kaya/67_run_evaluation_api.slurm`. Deferred (API key + live Neo4j): the small API smoke run + full 125×3 API run (criterion 4). Handoff: set `active_model` to an API key + export `ANTHROPIC_API_KEY`/`OPENAI_API_KEY`; lock the SL sampling temperature via the 2.2 temperature sweep, then `sbatch kaya/67_run_evaluation_api.slurm`.

## Phase 6 — Optional / Extension

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 6.1 | `6.1-prefilter.md` | 0.1 | not started | |
| 6.2 | `6.2-gradio-demo.md` | 0.3, 3.4, 3.5, 5.1, 5.2, 5.3 | not started | |

## Phase 7 — Dataset v3 *(concise schema + rebuilt benchmark + in-domain SFT)*

> Created 2026-07-06 after the first full E2E run (job 957276) localised the root cause
> to SL intrinsic coverage (Cov@5 0.208 vs ≥0.85). v2 artefacts (`benchmark-updated.json`,
> `SyntheticPoliceKG.cypher`, `results/*_mistral7b.*`) are **frozen as the recorded
> baseline** — Phase 7 never edits them. See `ai-workflow-rules.md` §6 and
> `project-overview.md` §5/§6 for the v2/v3 canonicality rule.

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 7.1 | `7.1-kg-v3-design.md` | — | done | `data/SyntheticPoliceKG-v3.cypher` (96 nodes: Person 40 / Incident 12 / Case 4 / Location 16 / Vehicle 10 / Phone 14; exactly the 6 v3 labels + 11 v3 relationship types; no `Person.role`) + `data/benchmark-v3-skeleton.json` (120 entries `Q-V3-001…120`, prose-only — no Cypher). Skeleton entry shape = spec example verbatim: `question_id, question, num_hops, is_ambiguous, ambiguity_type, default_interp, planned_interpretations, data_support` (null items: `planned_interpretations:[]`). Distribution 40 null / 20 schema / 20 entity / 20 intent / 20 temporal (80 ambiguous, each 2–3 interps + non-empty `data_support`). Every rel type exercised by ≥3 questions (min ASSOCIATED_WITH=6). **Acceptance:** criteria 3–4 verified programmatically (see `scratchpad/gen_benchmark.py`); criteria 1–2 (fresh-Neo4j load + Cypher seed spot-queries) **deferred to 7.2's runner on Kaya** but confirmed by static parse of the .cypher (`scratchpad/validate_kg.py`): all seed minima hold — 6 duplicate-surname clusters (Chen/Tran/Kim/Nguyen/Smith/Bennett), shared given names James(3)/Sarah(2), 2 alias collisions (Wei Chen→'David', Michael Nguyen→'James'), Albany Highway collision (Cannington + Victoria Park); every Person has ≥1 LIVES_AT, 9 moved house, 5 vehicle ownership transfers, 5 phone reassignments; every incident's suspect/witness/investigator sets non-empty + pairwise-distinct; cross-role persons PER-V3-009 & PER-V3-019. **Note:** num_hops distribution is 80/38/2 (1/2/3-hop) vs the spec's *soft, explicitly-non-binding* ~45/50/25 aim — a healthy 67/33 single/multi split, aligned with v3's goal of not over-spending difficulty on long paths; not a deviation (spec says "NOT a stratification constraint"). No decisions-log entry (implemented to spec). Handoff: 7.2 authors `data/benchmark-v3.json` (adds `cypher_default` + per-interpretation Cypher) and proves the `data_support` claims executably; 7.3 renders this schema as `build_pole_v3_schema_repr()`; 7.4 enumerates patterns over the 11 rel types. Any later change to the 6-label/11-rel inventory is a spec deviation → decisions-log. **Amended 2026-07-07 (user-directed):** (a) all instance ids de-versioned (`PER-V3-001`→`PER-001`, …, `Q-V3-nnn`→`Q-nnn`) across the v3 artefacts + v3 prompts/tests; (b) Case `case_name` split into bare operation name + new `descriptor` property (KG + `build_pole_v3_schema_repr()` Case props + `ENTITY_REGISTRY_V3` Case `name_props`) — see decisions-log 2026-07-07 (both rows). |
| 7.2 | `7.2-benchmark-v3.md` | 7.1 *(live Neo4j)* | done | `data/benchmark-v3.json` (120 items authored to the canonical loader contract — `cypher_default` + per-interpretation Cypher; no skeleton `planned_interpretations`/`data_support` fields carried over) + `experiments/validate_benchmark.py` (version-agnostic 8-gate harness: `validate(path, report)`, DB-free `run_offline_gates`/`gate_distribution`/`gate_hops`/`gate_coverage`, live `gate_syntax_and_schema`/`gate_nonempty`/`gate_divergence`, `relationship_count`/`rel_types_in` helpers; `NEO4J_*` env contract; JSON report + non-zero exit) + `kaya/70_neo4j_v3.slurm` (v3 graph server, separate `$MYGROUP/neo4j_data_v3` dir, wipe+reload by default / `KEEP=1` opt-out) + `kaya/72_validate_benchmark_v3.slurm` (self-contained CPU harness runner). num_hops set from `extract_schema_pattern(cypher_default).count('[')` so gate 7 holds by construction (dist 0/1/2/3-hop = 1/94/22/3). **Two seed/impl choices logged** (decisions-log 2026-07-06): added 2 active `OWNS` edges (PER-V3-013→VEH-007, PER-V3-018→VEH-008) so Q-V3-103/116 "current only" is non-empty (gate 5); gate 4 schema-closure implemented via CyVer Schema+Properties validators against the loaded v3 graph. **Amended 2026-07-07 (user-directed "Group A"):** 5 gold defaults/interps corrected (Q-067/088/091/092/111) — see decisions-log 2026-07-07; count-neutral, offline gates re-green, live gates 5/6 re-confirm on the required harness re-run. **✅ ALL 8 GATES GREEN ON KAYA** (`kaya/72_validate_benchmark_v3.slurm`, job 1010692, k008, ~40s — Neo4j 5.26 in-job on the fresh v3 graph): report committed to `results/benchmark_v3_validation.json` (`passed: true`; coverage min VICTIM_OF/ASSOCIATED_WITH = 6 ≥ 3). Offline unit tests (criterion 1: gate 2 wrong-distribution/dup-id/dup-text, gate 7 mislabeled-num_hops/over-3-hop) pass via `tests/test_validate_benchmark.py` (10 tests; full suite **268** under `t2c`). `load_benchmark`+`ambiguous_items`+`items_by_type` return 120 / 80 / {schema,entity,intent,temporal}=20 each + None=40 (criterion 3). **`data/benchmark-v3.json` is now the v3 canonical benchmark** (per `ai-workflow-rules.md` §6). Handoff: 7.3 points `config/pipeline.yaml` at `data/benchmark-v3.json`; 7.4 uses its question texts as the disjointness blocklist; 7.5 gate G0 re-runs this harness. **Amended 2026-07-07 (id de-versioning, decisions-log 2026-07-07):** ids are now `Q-nnn`/`PER-nnn`/… — offline gates re-verified green locally. **✅ Post-amendment Kaya revalidation complete 2026-07-07** (`kaya/72_validate_benchmark_v3.slurm`, job 1011357, k009, ~45s, fresh `$MYGROUP/neo4j_data_v3` reload of the current `SyntheticPoliceKG-v3.cypher`, which also carries the id de-versioning + Case `case_name`/`descriptor` split): all 8 gates green (`results/benchmark_v3_validation.json`, `"passed": true`; coverage min ASSOCIATED_WITH=6 ≥3), superseding the stale job-1010692 report. Full suite **280** re-confirmed green under `t2c`. Clears the pre-7.4/7.5 revalidation gate for all three 2026-07-07 amendments (id de-versioning, Case name split, Group A gold fixes). Note: `results/` is gitignored — the refreshed report is a local Kaya artefact, not a tracked file; nothing to commit there. |
| 7.3 | `7.3-v3-integration.md` | 7.1, 7.2 | done | Whole pipeline runs against v3 **by config only** (`dataset_version`), v2 byte-identical. `pipeline/schema.py`: `build_pole_v3_schema_repr()` (6 labels / 11 paths / temporal edge props) + `build_schema_repr(version)` selector. `pipeline/entity_lookup/registry.py`: `ENTITY_REGISTRY_V3` (Person/Case/Location — no Organisation) + `registry_for_version(version)`; `EntityCache.load(..., *, registry=None)` threads it (default → frozen v2); `span_extractor._KNOWN_SUBURBS` gains the v3 suburbs (West Perth/Perth/Mt Lawley). `pipeline/ambiguity/prompts.py::AD_SYSTEM_PROMPT_V3` + `pipeline/disambiguator/prompts.py::DIS_SYSTEM_PROMPT_V3` (v3 schema block + few-shots re-grounded in v3 seed data). **Prompt wiring (deviation, user-approved — see decisions-log 2026-07-06/7.3):** additive keyword-only `system_prompt` on `ambiguity_detector`/`disambiguator` (default = v2 constant); `ad_system_prompt`/`dis_system_prompt` fields+kwargs on `PipelineComponents` (default v2); one-line orchestrator read at each call site — all defaults byte-identical. `config/pipeline.yaml` gains `dataset_version: v3` / `benchmark: data/benchmark-v3.json` / `results_tag: v3` (+ documents the `kaya/70_neo4j_v3.slurm` target). `experiments/run_evaluation.py`: `resolve_dataset(cfg)` (pure — schema/registry/prompts/tag, default v2 when absent) threaded into `build_condition3`; `_persist` suffixes `_{results_tag}` (empty → today's v2 names). `build_condition3` gains `entity_registry`/`ad_system_prompt`/`dis_system_prompt` kwargs. `entropy_probe.py`/`diversity_penalty_sweep.py` gain `--dataset_version` (schema select) + `--results_tag` (artefact-path suffix). Offline acceptance criteria 1–3 pass; full suite **280** under `t2c` (v2 rendered prompts + resolved paths byte-identical). Criterion 4 (live v3 Neo4j entity-cache volumes + ≥2 fuzzy candidates for a colliding mention) deferred to Kaya (`kaya/70_neo4j_v3.slurm`). Handoff: 7.4 imports `build_pole_v3_schema_repr`; 7.5 runs with `dataset_version: v3` + `results_tag: v3`; v2 config stays fully runnable for the baseline comparison. |
| 7.4 | `7.4-pole-sft-data.md` | 7.1, 7.2, 7.3 *(GPU + API key)* | done | `pipeline/pole_sft_data.py` exposing `enumerate_v3_patterns() -> list[str]` (deterministic depth-≤3 graph walk over `build_pole_v3_schema_repr()`, both traversal directions, `{active:true}` variant per temporal hop — 840 in-schema patterns, all 11 rel types covered; see decisions-log 2026-07-07 for the volume/algorithm call), `build_pole_sft_dataset(config) -> dict` (template + optional-LLM-paraphrase question synthesis, `apply_disjointness_gate` against `benchmark-v3.json`+skeleton via rapidfuzz `token_set_ratio`, writes the four two-key jsonl files via `build_sl_prompt`/`build_qg_prompt` verbatim so train==inference format, returns a generation report), `build_mixed_training_file` (Ozsoy-replay mix for continue-SFT), `load_blocklist_questions`/`apply_disjointness_gate` (also unit-tested standalone). `config/pole_sft_data.yaml` (paraphrase-LLM spec via `build_llm` — never hardcoded; generation knobs incl. new `max_rows` cap; `ozsoy_replay` mix-ratio block) + `config/schema_linker_train_v3.yaml`/`config/query_generator_train_v3.yaml` (continue-SFT: `model.init_adapter_path_name` + `training.output_name`, 2 epochs, lower LR) + `experiments/build_pole_sft_data.py` (CLI: generates the 4 jsonl + the 2 Ozsoy-mixed training files + the report). **Small anticipated additions** (per spec's own note, recorded here not decisions-log): `pipeline/sft.py::run_sft` gained `config["model"]["init_adapter_path"]` (loads an existing adapter via `PeftModel.from_pretrained(..., is_trainable=True)` instead of a fresh `LoraConfig`, when set); `experiments/train_schema_linker.py`/`train_query_generator.py`'s `build_run_config` gained optional `hp["training"]["output_name"]` + `hp["model"]["init_adapter_path_name"]` lookups (both default to the unchanged 2.1/2.4 behaviour, verified byte-identical `checkpoints/{model_key}/{sl,qg}_adapter` output when absent). Kaya runners: `73_build_pole_sft_data.slurm` (CPU+API key), `74_train_sl_v3.slurm`/`75_train_qg_v3.slurm` (GPU continue-SFT), `76_sl_v3_cov_check.slurm` + `kaya/sl_v3_cov_check.py` (criterion 4, reuses `experiments/probe_utils.py` verbatim). **Offline acceptance (criterion 1) fully passes**: `enumerate_v3_patterns` — every pattern validates against `parse_schema_pattern`, every one has a relationship, all 4 temporal types have an `{active:true}` variant, deterministic across calls; two-key jsonl contract verified; `extract_schema_pattern(qg_completion) == sl_completion` holds for every generated row (train-format consistency, mirroring the 1.3 guard); disjointness gate provably drops a planted near-duplicate. `tests/test_pole_sft_data.py` (6 tests; full suite **286** under `t2c`). **Criterion 2 ran for real (offline half only — no `ANTHROPIC_API_KEY` in this session)**: `data/pole_{sl,qg}_{train,eval}.jsonl` + `data/pole_{sl,qg}_train_mixed.jsonl` + `results/pole_sft_generation_report.json` written — 840 patterns enumerated, 3116 template-only candidates (paraphrase step skipped), 76 dropped by the disjointness gate, 2000 kept after the `max_rows` cap (1800 train/200 eval each); every one of the 11 relationship types clears the ≥30-row floor by a wide margin (min coverage 155, `CALLED`). **LLM-paraphrase leg of criterion 2 deferred** (needs an API key — re-run `python -m experiments.build_pole_sft_data` once `ANTHROPIC_API_KEY` is exported; template-only rows are already real/usable — not needed for criteria 3–4 below, which ran on the template-only data). **✅ Criteria 3–4 ran for real on Kaya.** Criterion 3 (`kaya/74_train_sl_v3.slurm` + `75_train_qg_v3.slurm`, 2×V100, `--gres=gpu:2`): SL job 1011401 (~13h24m) — `eval_loss` 0.00061→0.00032 over 2 epochs; QG job 1011402 (~13h30m) — `eval_loss` 6.7e-05→3.0e-05. Both wrote `checkpoints/mistral7b/{sl,qg}_adapter_v3` cleanly, no OOM. Criterion 4 (`kaya/76_sl_v3_cov_check.slurm` + `kaya/sl_v3_cov_check.py`, 25-item stratified sample, k=5 diverse beams): first real run (job 1025226) failed at import (`ModuleNotFoundError: experiments` — the script runs as a bare file so the repo root wasn't on `sys.path`, unlike the `-m experiments.foo`-invoked jobs; fixed with the same `sys.path.insert` shim `kaya/build_condition_probe.py` already uses). Re-run (job 1026051) succeeded but measured sample Cov@5=0.240 — below 7.5's G1≥0.60 bar. Raw-beam inspection (job 1026080, after adding per-item beam/gold printing) found most misses were beams that got the schema link right but anchored from the opposite node, or carried the known dangling `--` suffix (decisions-log 2026-06-15) — both already treated as equivalent by the live SL postprocessing but not by the intrinsic `covered()` guardrail. **Fixed `covered()` to compare structurally (decisions-log 2026-07-10)** — re-run (job 1026181): **sample Cov@5 = 0.640**, clearing G1. Remaining real gaps: `schema`-type items 0/5 (adapter chains multiple relationship types into one beam instead of separate candidates, or picks a wrong relationship), a couple `entity` items miss a required `{active:true}` filter — genuine 2.1-training-quality residuals, not measurement artefacts, worth keeping in mind for 7.5's G1 (full 120-item run). Handoff: 7.5 measures `checkpoints/mistral7b/{sl,qg}_adapter_v3` against the G0–G4 gates (25-item sample already clears G1's ≥0.60 bar; full 120-item Cov@5/EM@1 + the G2 sweep/probe re-run + G3/G4 still to run); the v3 train YAMLs + `pole_sft_data.yaml` document exactly how to regenerate data/adapters for any other `model_key`. **⟳ REOPENED 2026-07-11 (schema-type SFT gap):** criterion-4's `schema` items were 0/5 (adapter chains the four Person→Incident role types into one compound beam / reuses a wrong relation for the deliberately-vague benchmark register, never emitting the clean single-hop `(Person)-[:role]->(Incident)` beam the gold set needs). Added a single-hop **schema-ambiguity register** to `pipeline/pole_sft_data.py` (`_SCHEMA_CONNECTOR_FORMS` + `_schema_ambiguous_{pairs,rows}` + `_schema_connector_np`, config knob `schema_connector_samples_per_form=30`) generated *outside* the recursive `_chain_np` walk (the earlier `_REL_CLAUSE`-based fix leaked into deep chains and dropped Cov@5 0.640→0.520), role relation round-robined so the vague register is paired with all four role types — 240 raw → 117 kept in the 2000-row set, balanced ~26–33/role; also targets the Q-045 "associated with" collision. Data regenerated (template-only, matching the 0.640 baseline's mode; paraphrase leg still deferred). Tests 9→12 green. Baseline v3 adapters preserved at `checkpoints/mistral7b/{sl,qg}_adapter_v3_baseline_cov0640`. **✅ RETRAIN COMPLETE 2026-07-12** (continue-SFT from v2, 2×V100): SL job **1028720** (~13h40m, eval_loss 0.0053→0.0046 over 2 epochs) + QG job **1028721** (~13h27m, eval_loss 6.4e-5→3.2e-5) wrote fresh `checkpoints/mistral7b/{sl,qg}_adapter_v3`. Cov re-check (job **1028730**, 25-item sample, k=5 diverse beams): **sample Cov@5 = 0.800, up from 0.640** — **`schema` items 0/5 → 5/5** (the fix's target; the beams now spread across the Person→Incident role types as *separate* single-hop candidates — e.g. Q-042 proposes SUSPECTED_OF/VICTIM_OF/WITNESSED — which is exactly the interpretation diversity 7.5's AD/disambiguator consume, and Q-045's "associated with" collision now resolves to INVESTIGATES/VICTIM_OF, not ASSOCIATED_WITH). Per-type: schema 5/5, temporal 5/5, null 5/5, entity 2/5 (unchanged — Q-063 misses `{active:true}`, Q-064/065 gold uses generic `[r]`), intent 3/5 (**down 1 from baseline's 4/5** — Q-082 "incidents in Northbridge" now over-chains an extra SUSPECTED_OF hop, plausibly nudged by the denser Person↔Incident role-edge training; a single sample item, flagged for 7.5's full 120-item G1 to confirm it's not noise). Note the SL eval_loss is higher than the prior v3 run's 0.00032 **because the eval set now includes the intentionally one-to-many schema-connector rows (irreducible loss by design)** — the two runs' eval losses are not comparable. Baseline v3 adapters preserved at `…_v3_baseline_cov0640`. See decisions-log 2026-07-11. Handoff: 7.5 measures these refreshed `checkpoints/mistral7b/{sl,qg}_adapter_v3` against the G0–G4 gates (25-item sample clears G1's ≥0.60 comfortably; full 120-item Cov@5/EM@1 + G2 sweep/probe + G3/G4 still to run). |
| 7.5 | `7.5-v3-revalidation.md` | 7.1, 7.2, 7.3, 7.4 *(GPU + live Neo4j)* | done | **✅ ALL FIVE GATES GREEN ON KAYA (2026-07-12), cheapest-first, none skipped past a failure.** New v3 gate runners authored (7.5 produces evidence; the v2 probes were v2-hardcoded): `kaya/g1_sl_coverage_v3.py`+`77_g1_coverage_v3.slurm` (G1), `78_sweep_v3.slurm`/`79_probe_v3.slurm` (G2, pass `--dataset_version v3 --results_tag v3`), `kaya/ad_probe_v3.py`+`80_ad_probe_v3.slurm` (G3, v3 schema/registry/prompt/adapter), `81_run_evaluation_v3.slurm` (G4, serves the v3 Neo4j graph). **G0 (job 1032327):** offline suite 294 pass + all 8 benchmark gates green on the live v3 graph. **G1 (job 1032328):** full 120-item **Cov@5 0.683 / EM@1 0.667** at pre-sweep dp=0.2 — clears the ≥0.60 bar (v2 was 0.208). **G2a sweep (job 1032339):** re-locked **`diversity_penalty: 1.0`** (Cov@5 0.725, AUC 0.682; documented fallback — no penalty cleared 0.85, but 1.0 wins both axes). At the locked dp=1.0 (offline rescore) **Cov@5 0.725 / EM@1 0.667** — per-type schema 0.95/temporal 1.00/null 0.70/intent 0.55/entity 0.45. **G2b probe (job 1032344, dp=1.0):** headline entropy AUC **0.689 > 0.62 → criterion #6 MET** (v2 0.592, flat); ablations NOT flat (spread 0.028) — beam diversity is real on v3 (decisions-log 2026-07-12). **G3 AD probe (job 1032391, base LLM, 10-q):** entropy signal now **live** (entity items `entity_entropy=1.00`, 2/2 correct) — v2's 3.4 probe was all-0.00; base 7B still under-detects schema/temporal/intent (4/10, high prec/low rec) → API-AD is the documented path (**deferred, no API key**). **G4 E2E (job 1032398, 120×3, ~3h24m, 0 tracebacks):** wrote `results/tables_mistral7b_v3.md` + `metrics_mistral7b_v3.json`. **Headline (%, EX/EA):** EX C1 3.3 / C2 10.8 / C3 9.2; EA 4.2 / 23.3 / 23.3; **ambiguous EA 2.5 / 25.0 / 27.5 → C3 > C2 (criterion #1 MET)**; per-type EA schema **0/20/30** (C3 gain), temporal 5/55/55, entity 0/20/20, intent 5/5/5; Detection F1 30.6 (prec 75.0, rec 19.2), DSR 5.0; KG-valid 42.5/65.8/65.0%. **v2 scored 0/7 criteria; v3 meets #1 and #6 outright and materially improves the rest** (substrate-vs-architecture: same pipeline code, only the substrate changed). Scorecard: `docs/results-walkthrough-v3.md` (v2-vs-v3 columns). **⚠ Cosmetic:** `format_metric_tables` (4.2) prints hardcoded v2 counts "(125)/(50)/(n=13…)"; real v3 denominators are 120/80/20-each (values correct) — flagged for 4.2, not fixed here (out of 7.5 scope). **Phase 7 complete.** Handoff: the dissertation now has a frozen v2 baseline + a validated v3 substrate + the same instrumented pipeline on both; the next lever is a stronger AD/Dis backend (config-only). Optional 6.1/6.2 unaffected. | |

## Phase 8 — Claude Sonnet 4.6 end-to-end *(API model runs the whole pipeline on v3)*

> Created 2026-07-13. Earlier (reverted, uncommitted) Claude attempts showed the SL
> scoring/parsing stack — tuned on the fine-tuned Mistral's tidy one-line output —
> counted Claude's correct-but-instruct-styled answers as wrong (Cov@5 0.067; parser
> tolerance alone lifted it to 0.583). Phase 8 fixes both sides (API instruct prompt
> variant + unconditional format-tolerant parsing with a frozen-cache no-regression
> guard), corrects Anthropic backend decoding (greedy→temperature 0.0, no top_p,
> SDK retries), and re-runs the 7.5-style gated chain on `claude-sonnet-4-6`
> (registry key `claude-sonnet`; chosen over Sonnet 5, which rejects non-default
> sampling params). C2/C3 stage-presence framing and temperature-sampling SL are the
> locked 5.5 decisions. Mistral v2/v3 evidence stays frozen; all Phase 8 artefacts
> carry the `claude-sonnet` key + `_v3` tag.

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 8.1 | `8.1-sl-api-output-contract.md` | 3.1, 5.5, 7.3, 7.5 | done | Both-sides SL API fix (instruct prompt variant + unconditional format-tolerant parsing), the local beam/completion path byte-identical. `pipeline/schema_linker/prompts.py`: `SL_SYSTEM_API` + `build_sl_prompt_api(question, schema_block)` (explicit output contract + 3 v3-grounded few-shots: single-hop role edge / temporal `{active:true}` / 2-hop chain); `SL_SYSTEM`/`build_sl_prompt` left **byte-identical** (asserted). `inference.py::extract_pattern_from_completion` now strips markdown fences (mirrors QG `_FENCE_RE`) → first pattern-like line (parenthesised node group) → fallback to first non-empty line (bare-pattern input byte-identical). `postprocessing.py::parse_schema_pattern` repairs a colon-less label (`(Person)`→label when an exact case-sensitive match in `schema.node_labels`, node anonymised; real aliases `(p)`/`(x)` untouched); extra property maps already non-invalidating (only `active` load-bearing). `probe_utils.py::normalised_entropy` now counts canonical `_canonical_pattern` signatures (junk→raw-string fallback). Prompt selection: keyword-only `prompt_style="completion"` (default) on `SchemaLinker` + `probe_utils.generate_beams` (`"instruct"`→`build_sl_prompt_api`); `link()` 3.1 signature unchanged. **Two documented exceptions** (decisions-log 2026-07-14): (1) the additive API prompt variant — the deliberate exception to 1.2's "do not fork it" (locked decision #3); (2) canonical-signature entropy (locked decision #2). **No-regression rescore guard (offline, from frozen `results/beams_mistral7b_v3.jsonl`, the dp=1.0 cache):** Cov@5 **0.725** / EM@1 **0.667** and every per-type cell **byte-for-byte unchanged**; **headline probe AUC 0.689→0.667** (accepted, user-directed 2026-07-14 — 0.689 was inflated by the dangling-`--`/direction/var-name artefacts on 51/120 items that canonical counting collapses by design; 0.667 still clears the 0.62 baseline so 7.5 criterion #6 holds). Consequence: `h_norm_candidates`==`h_norm_beams` now (both canonicalise), degenerating 2.3's ablation and superseding 7.5 G2b's "spread 0.028"; the 2.3 test premise updated. All acceptance criteria pass offline; **full suite 313** green under `t2c`. Handoff: 8.2 wires `prompt_style` from registry `kind: api` into the run harness/sweep/probe/gate runners (8.1 only exposes the parameter); 8.3's G0.5 smoke eyeballs raw Claude completions against this parser. |
| 8.2 | `8.2-anthropic-backend-runners.md` | 0.2, 5.5, 7.3, 8.1 | done | Anthropic backend decoding corrected + registry-driven, GPU-free v3 API gate runners. `pipeline/llm/anthropic.py`: greedy (`do_sample=False`) → `temperature=0.0`, sampling → `config.temperature`; **`top_p` never sent** (Claude 4.x rejects temperature+top_p); client built with `max_retries=5`. `experiments/diversity_penalty_sweep.py`: `candidate_decodings(kind, backend)` — anthropic sweeps temperature-only (no `top_p`), openai keeps `top_p:0.95`; `score_decoding`/`main` thread `prompt_style` (api→instruct). `experiments/run_evaluation.py`: `_resolve_prompt_style(kind)` (api→instruct, local→completion) threaded to C2/C3 builders. **Deviation (decisions-log 2026-07-14):** `build_condition2/3` gained keyword-only `prompt_style="completion"` (necessary plumbing for contract #3; local path byte-identical). Kaya: `g1_sl_coverage_v3.py` + `ad_probe_v3.py` generalised to resolve the SL from the registry (`kind: api` → `build_llm` + sampling + instruct prompt; `--decoding` override on G1, default pre-sweep temp 0.7); new `kaya/api_smoke_v3.py` (G0.5: egress check + count_tokens prefixes + raw-vs-parsed SL/AD/Dis/QG report, no Neo4j/GPU). **Full GPU-free API gate-chain runners for 8.3** (all `--partition=work`, no `--gres`, API-key guard): `82_api_smoke_v3` (G0.5), `83_g1_coverage_api_v3` (G1), `84_sweep_api_v3` (G2: sweep + probe rescore from G1 cache), `85_ad_probe_api_v3` (G3 — the no-GPU AD-probe variant the spec called for: API SL + API AD, v3 Neo4j in-job), `86_run_evaluation_api_v3` (G4, v3 Neo4j in-job). Offline acceptance 1–4 pass; **full suite 323** under `t2c` (local mistral path byte-identical). Live execution (G0.5→G4 on `claude-sonnet`, cheapest-first with go/no-go gates) is 8.3's job — needs `ANTHROPIC_API_KEY` + live v3 Neo4j. Handoff: 8.3 flips `config/pipeline.yaml active_model: claude-sonnet` (config-only) and runs `82→83→84→85→86` in order (G0 = offline suite + `validate_benchmark`, no new runner). |
| 8.3 | `8.3-claude-e2e-gated-run.md` | 8.1, 8.2 *(live v3 Neo4j + ANTHROPIC_API_KEY; no GPU)* | done | **✅ FULL GATED API CHAIN GREEN ON KAYA (2026-07-14, `claude-sonnet`=`claude-sonnet-4-6`), cheapest-first, none skipped past a failure.** Config flipped `active_model: claude-sonnet` (config-only); `ad`/`dis` null → the one API model runs every stage (SL by temperature sampling, no GPU). **Total spend ≈ $25–45** (uncosted from console; ~4–5k calls, no cache discount — all prompt prefixes < 2048 tok per G0.5). **G0 (job 1055742):** offline suite **323 pass** + all 8 benchmark gates green on the live v3 graph; API key verified via free `count_tokens`. **G0.5 (jobs 1055747→1055754):** raw-output smoke **caught a phase-blocking 8.2 backend bug for ~$0.10** — `pipeline/llm/anthropic.py` sent `system=None` on user-only prompts (SL-sampling + zero-shot-QG paths) and the live API rejects `"system": null` with `400 "system: Input should be a valid array"`; **fixed** (omit `system` when empty, never null) + regression test `test_user_only_prompt_omits_system_not_null`, verified against live API, decisions-log 2026-07-14. Post-fix smoke clean: egress OK, SL bare-pattern contract holds, AD/Dis JSON parses, QG cleans to executable Cypher; count_tokens prefixes SL 703 / AD 1725 / Dis 1029 (all < 2048 → no cache wiring). **G1 (job 1055760):** full 120-item **Cov@5 0.658 / EM@1 0.650** at pre-sweep T=0.7 — clears ≥0.60 (schema 0.25 is a `covered()` measurement artefact — Claude packs all four role edges into one comma-separated beam; temporal 1.00). Beams cached → `results/beams_claude-sonnet_v3.jsonl`. **G2a (job 1055791):** no temperature cleared the 0.85 guardrail → documented fallback locked **T=0.3, k=5** (Cov@5 0.667) in `config/schema_linker_inference.yaml::decoding[claude-sonnet]`. **G2b (job 1055791, CPU rescore):** entropy **AUC 0.525 < 0.62 → criterion #6 NOT met** (flat, spread 0.000) — honest: an instruct model at low T samples near-deterministically, so the *secondary* entropy signal is uninformative; consistent with the LLM-primary design (decisions-log 2026-07-14). **G3 (job 1055843, API SL + API AD, v3 Neo4j in-job — the leg 7.5 deferred):** **10/10 correct, TP=8 FP=0 FN=0 TN=2 (recall 1.00, prec 1.00)** vs base-7B's 4/10 — detection carried by LLM reasoning even at entropy 0.00; live entity signal also fires (Q-082 entity_entropy 1.00). **G4 (job 1055879, 120×3, 2h50m, 0 tracebacks):** wrote `results/tables_claude-sonnet_v3.md` + `metrics_claude-sonnet_v3.json`. **Headline (%):** EX C1 5.8/C2 5.0/C3 4.2; EA 5.8/5.0/5.0; **Detection F1 73.9 (prec 83.6, rec 66.2)** — 2.4× v3-mistral's 30.6; KG-valid 83.3/69.2/63.3; syntax 100/96.7/97.5; schema 100/100/100; structural-repair 71.0/47.9/51.6. **Honest read:** the API model **solves detection** (criterion #3 — the thing v3-mistral was weakest on and Phase 8 exists for) and drives validity/repair (#7), but **exact-match EX stays at the floor (~5%)** because C2/C3 are **stage-presence not SFT** — the un-fine-tuned instruct QG answers in its own shape (`RETURN p, r, i` whole nodes, extra cols) that rarely matches the gold projection, so the ~60–78pt validity→correctness gap is `wrong_result`. The bottleneck **moved from detection to output-contract conformance**. **Scorecard: `docs/results-walkthrough-v3-claude-sonnet.md`** (three-column v2 \| v3-mistral \| v3-claude-sonnet; claude-sonnet meets #3 + #7 decisively, misses #2/#4/#5/#6). **All Phase 8 artefacts carry the `claude-sonnet` key + `_v3` tag; mistral v2/v3 artefacts untouched (acceptance #3).** **Phase 8 complete.** Handoff: the dissertation now has the same instrumented pipeline on a frozen v2 substrate (mistral), a validated v3 substrate (mistral), and v3 with a strong instruct API model — the substrate-vs-architecture-vs-model-capability argument. Next lever (future work): output-contract conformance for the API path — QG output-shape prompt or a shape-invariant/semantic EX. Optional 6.1/6.2 unaffected. **⟳ FOLLOW-UP 2026-07-16 (decisions-log only, no new phase spec):** implemented that next lever — `QueryGenerator` gained a `prompt_style` switch (mirrors `SchemaLinker`'s 8.1 pattern): `"instruct"` gets `include_properties=True` + a new `build_qg_prompt_api` (explicit output contract + 4 few-shots reused verbatim from Phase 7.4's `data/pole_qg_train.jsonl`), fixing two root causes found by grepping the G4 log — property hallucination (`include_properties=False` meant the QG never saw the real property names at all, on any backend) and the output-shape mismatch (QG had zero few-shot examples, unlike SL/AD/Dis). `"completion"` default is byte-identical to pre-fix; `build_condition1` untouched. Full suite 332 passed. **✅ VERIFIED LIVE 2026-07-16 — RESULTS TRANSFORMED, ADOPTED AS CANONICAL.** G0.5 re-run (job 1061322) showed a clean before/after on Q-041 (no more invented `i.name`; tight `RETURN DISTINCT p.name`). G4 re-run (job 1061343, `results_tag: v3-qgfix`, then promoted to canonical `_v3`): **EX 5.0→37.5 (C2), 4.2→52.5 (C3); EA 5.0→57.5 (C2), 5.0→62.5 (C3); DSR 0.0→49.2; Detection F1 unchanged (73.9/83.6/66.2, correct sanity check)**. C1 barely moved (expected, untouched by the fix). **Success criteria #1 and #2, previously floor-noise/failed, are now cleanly MET** — claude-sonnet now meets #1/#2/#3/#4 outright, more than either mistral column. Old numbers preserved at `results/*_v3_pre-qgfix.{md,json}`; `config/pipeline.yaml::results_tag` reverted to `v3`. `docs/results-walkthrough-v3-claude-sonnet.md` + `CURRICULUM.md` Module 8 rewritten. **Separately resolved the Cov@5 attribution question**, cheaply, using the untouched pre-7.4 checkpoints (no new training): v3-schema/generic-only Cov@5 = **0.483** (job 1061277), v3-schema/no-adapter = **0.000** (job 1061290) — vs. v2/generic-only 0.208 and v3/+7.4-SFT 0.725. **Both the schema redesign and Phase 7.4's in-domain SFT are real, roughly comparably-sized contributors** — the "isolates the substrate" framing in `results-walkthrough-v3.md`/`CURRICULUM.md` was corrected to name both factors. A companion AD-probe ablation (jobs 1061545/1061546, same base AD, varying SL adapter) found detection recall **flat** across Cov@5 0.483/0.725 (identical TP/FP/FN/TN) — confirming detection recall is gated by AD-model capability, not SL/Cov@5 quality, exactly what the claude-sonnet AD-swap result already suggested from the other direction. All changes and job IDs recorded in decisions-log 2026-07-16 (three entries: implementation, attribution ablations, live verification). |

## Phase 9 — External Benchmark Validation *(real-world dataset, not self-authored — supervisor-directed)*

> Created 2026-08-02. Motivated by supervisor feedback on the qualitative-analysis walkthrough
> and diagram: the exact-match improvement "resonates with author intent" since the student
> authored both `benchmark-updated.json`/`benchmark-v3.json` and their ground truth. Suggested
> next step: validate against an existing/external benchmark or graph not authored by the
> student — e.g. an existing Text2Cypher corpus (CySpider was named as an example) or another
> POLE-schema dataset found online — to test whether the disambiguation architecture
> generalises beyond a benchmark co-designed to fit it. v2/v3 stay frozen and remain the
> primary evaluated benchmarks; this phase is a supplementary external-validity check, not a
> replacement.

| Step | Spec file | Depends on | Status | Outputs / Handoff note |
|------|-----------|------------|--------|------------------------|
| 9.1 | `9.1-pole-external-design.md` | — | done | Spec file created 2026-08-02 (retroactive — records already-completed design work, no new decisions). Dataset chosen: `neo4j-graph-examples/pole` — real Greater Manchester, UK street-crime data (Aug 2017) on a POLE (Person/Object/Location/Event) schema, ~61.5k nodes / 105.8k relationships, downloaded as a Neo4j dump and loaded into local Neo4j Desktop (off-cluster, not Kaya). Schema audited via `db.labels()`/`db.relationshipTypes()`/`db.propertyKeys()` + targeted Cypher: 11 node labels (Person, Location, Phone, Email, Officer, PostCode, Area, PhoneCall, Crime, Object, Vehicle), 17 relationship types. **Key structural finding vs v2/v3:** no role-split Person→Crime edges (single generic `PARTY_TO`, only 55 edges total) and — more significantly — **no dated/state-change relationships anywhere in the graph** (every relationship's property list is empty except `FAMILY_REL.rel_type`; confirmed via a full `type(r)/keys(r)/count(*)` audit). All temporal signal lives on node properties (`Crime.date`, `PhoneCall.call_date`/`call_time`) instead. **Temporal ambiguity was therefore redefined for this external set** (user-decided, not a default): same-day-vs-trailing-week date/time-window ambiguity, rather than v2/v3's edge-state (`active`/`from_date`/`to_date`) mechanism — recorded here so the two "temporal" categories are never conflated in the write-up. Schema ambiguity instead draws on two confirmed axes: the Person-Person KNOWS-family (`KNOWS`/`KNOWS_SN`/`KNOWS_PHONE`/`KNOWS_LW`/`FAMILY_REL`, confirmed default-vs-narrow structure — every specific-type edge has a parallel generic `KNOWS` edge, confirmed via a `specific_without_generic=0` check) and Crime-participant scope (`PARTY_TO` person vs `INVOLVED_IN` vehicle/object). **100-item skeleton drafted** (20 each: entity/schema/intent/temporal/unambiguous-control), same shape as 7.1's `benchmark-v3-skeleton.json` (`question_id`/`question`/`num_hops`/`is_ambiguous`/`ambiguity_type`/`default_interp`/`planned_interpretations`/`data_support`, no Cypher yet), every item grounded in real Cypher query results gathered interactively rather than invented. Currently held outside the repo as a working draft (`benchmark-pole-external-skeleton-draft.json`), **not yet copied into `data/`**. **Update 2026-08-02 (same day):** the 4 flagged items are resolved — **all 100 items in the skeleton are now `CONFIRMED`.** I10 (Chief Inspector "aside from Monelli Kort") and I11 (originally area "aside from M1", swapped to Sergeant per explicit user decision — the area axis's only real tie, 562/BL2-BL4-M14, was already claimed by I03) reworded to exclude the top-N unique ranks by name so the remainder lands on a real tie further down (I10: 4-way tie n=37; I11: 2-way tie n=42). I15 swapped from the non-divergent BL2-vs-M6 pair to a fresh confirmed total-vs-violent divergent pair, M14-vs-M6, not already used by I04 or I14. T20 filled in with real BL1 per-day August counts (15 Aug same-day=26 vs trailing-week 09-15 Aug=182). Full reasoning + raw verification-query outputs recorded in `decisions-log.md`'s 2026-08-02 follow-up row. Spec file `9.1-pole-external-design.md` written 2026-08-02, retroactively recording this step (see decisions-log for the "why a retroactive spec" note). |
| 9.2 | `9.2-pole-external-benchmark.md` | 9.1 | done | `data/benchmark-pole-external.json` (100 items authored to the canonical loader contract — 20 each entity/schema/intent/temporal/unambiguous; `cypher_default` + per-interpretation Cypher against the real POLE-external schema, generated by `scratchpad/gen_pole_external_benchmark.py`) + `experiments/pole_query.py` (new ad hoc query CLI: `_open_driver` reused from `validate_benchmark.py`, `format_success_lines`/`format_output` pure row-printing/summary logic, `run_query`/`main`) + `experiments/validate_benchmark.py` (additive `BenchmarkProfile` dataclass; `V3_PROFILE` reproduces the old hardcoded constants byte-for-behavior; new `POLE_EXTERNAL_PROFILE` — 17 relationship types from `docs/neo4jpole.md`, `expected_distribution={None:20,schema:20,entity:20,intent:20,temporal:20}`, `expected_total=100`, `min_coverage=1`; `gate_distribution`/`gate_coverage`/`run_offline_gates`/`validate()` take a `profile` param defaulting to `V3_PROFILE`; CLI gains `--profile {v3,pole_external}`). **Three authoring deviations logged** (decisions-log 2026-08-02): interpretation count capped at 4 for 4 entity items with a real 5-way surname tie (Fuller/Murray/Nguyen/Austin); S06/S07 `default_interp` corrected from the skeleton's undercounted "2 people" to the live-verified "3 people" (Bruce Baker via KNOWS_SN was missed by the earlier no-DB-access session); 3 of the 20 unambiguous control items repurposed (2 vehicle-reg → `HAS_PHONE`/`HAS_EMAIL` lookups, 1 postcode → `POSTCODE_IN_AREA` lookup) since those 3 relationship types had zero coverage otherwise. **✅ All 8 gates green** (`results/benchmark_pole_external_validation.json`, live local instance `bolt://localhost:7687`/db `neo4j` — this session had direct network access, unlike 9.1's): loads/distribution/syntax/schema-closure/non-empty/divergence/hop-honesty/coverage all pass; every one of the 17 relationship types clears `min_coverage=1` (min observed 1, for `HAS_PHONE`/`HAS_EMAIL`/`POSTCODE_IN_AREA`/`CALLER`/`CALLED` — no 0-coverage type to record). `load_benchmark`+`ambiguous_items`+`items_by_type` return 100/80/{entity,schema,intent,temporal}=20-each (criterion 4). Offline acceptance (criteria 1–2) pass via `tests/test_validate_benchmark.py` (+6 new tests: POLE_EXTERNAL_PROFILE balanced-set pass, wrong-distribution catch, zero-coverage catch, v3-profile-still-default regression) and `tests/test_pole_query.py` (9 new tests: success/failure/limit/empty-result paths against a fake `ExecutionResult`, no driver opened) — full suite 340 passed (+ 5 pre-existing unrelated failures in `tests/test_run_evaluation.py`, confirmed present before this step's changes too, out of 9.2 scope). Handoff: `data/benchmark-pole-external.json` is now canonical (criterion 3 met); 9.3 points the local-run config at this file and wires the schema representation / entity registry / AD-Dis prompts needed to run the pipeline against it. **Amended 2026-08-03 (user due-diligence pass, decisions-log 2026-08-03):** S02/S04/S05/S06/S09/S10 (degenerate default-vs-narrow items — same result set either way, a structural property of this graph's KNOWS-family edges, not a per-item slip) fully replaced with 6 genuinely-divergent items on the digital-comm axis, 3 of them the schema block's first 2-hop items; 9 of the 20 temporal items (the "M1 around date" repeats) replaced with phone-call-axis items, rebalancing 19-crime/1-call to 10/10. Count-neutral (still 100/80/20-each). **Re-validated live, all 8 gates green**, report overwritten at `results/benchmark_pole_external_validation.json` (supersedes the original per-user-direction, not kept side by side). Offline suite (`test_validate_benchmark.py` + `test_pole_query.py`, 23 tests) re-confirmed green. Canonical status and 9.3 handoff unchanged — this is a content amendment, not a new step. **Follow-up same day (decisions-log 2026-08-03):** user reverted the S01 fix (the `[r]` wildcard and the explicit union are provably identical, so retyping doesn't create ambiguity — same conclusion as S02, S01 just needed replacing too) and it was swapped for a 4th fresh digital-comm item (Alan Hicks). That removed the file's only `KNOWS`/`FAMILY_REL` coverage; fixed by repurposing 2 of the 5 repetitive `Officer badge → rank` control items (U04/U05) into single-type `FAMILY_REL`/`KNOWS` lookups, mirroring 9.2's own coverage-gap precedent. Re-validated live, all 8 gates green, report re-overwritten, offline suite (23 tests) green. |
| 9.3 | `9.3-pole-external-local-run.md` | 9.2 | done | External POLE benchmark wired into the pipeline **by config only** (mirrors 7.3); all offline pieces built and tested, then the live run completed for real in-session once the user supplied a running local Neo4j + an `ANTHROPIC_API_KEY` (decisions-log 2026-08-05). `pipeline/schema.py::build_pole_external_schema_repr()` (11 labels / 17 relationship paths, 9.1-audited; `INVOLVED_IN` lists the dominant Vehicle→Crime pattern, Object exception documented in the docstring) + `build_schema_repr`/`adapter_suffix_for_version` gain a third `"pole_external"` branch (suffix `"_pole_external"`, never raises). `pipeline/entity_lookup/registry.py::ENTITY_REGISTRY_POLE_EXTERNAL` (Person/Officer/Location; Vehicle/Phone/Email/PostCode/Area/Crime/PhoneCall/Object excluded per the exact-identifier rule) + `registry_for_version` third key. **One flagged ambiguity resolved by asking the user** (decisions-log 2026-08-05, per `ai-workflow-rules.md` §3): `Location` has no single id property in this schema — user chose keying it by `address` (`id_prop: "address"`) over relaxing `EntityCache`'s non-null-id assumption; zero changes needed to `pipeline/entity_lookup/cache.py`. `pipeline/ambiguity/prompts.py::AD_SYSTEM_PROMPT_POLE_EXTERNAL` + `pipeline/disambiguator/prompts.py::DIS_SYSTEM_PROMPT_POLE_EXTERNAL` — real schema block + four/three few-shots grounded in real confirmed benchmark items (Q-POLE-S01 Alan Hicks digital-comm schema axis; Q-POLE-E01/E02 Andrea George/Anne Rice entity duplicates; Q-POLE-I01 Inspector n=44 tie; Q-POLE-T16 BL1 same-day-vs-trailing-week temporal), not the non-divergent KNOWS-family broad-vs-narrow framing (9.2 amendment); Dis prompt's temporal "How to Choose" rule rewritten for the no-`active`-property graph (same-day default). `experiments/run_evaluation.py::resolve_dataset` gains the third branch verbatim per spec. `config/pipeline_pole_external.yaml` (new, standalone — `dataset_version: pole_external`, `results_tag: pole_external_local`, `active_model: claude-sonnet`, benchmark `data/benchmark-pole-external.json`; reuses the existing `schema_linker_inference.yaml` `claude-sonnet` decoding block, temperature 0.3/k=5, locked by the Phase 8 sweep — not re-swept for this schema). `experiments/analyze_qualitative.py` reused unmodified (confirmed benchmark-agnostic — joins on `question_id`, no version branching). Offline acceptance criteria 1-3 pass: full suite green (356 passed; the 5 `tests/test_run_evaluation.py` failures are pre-existing/unrelated, confirmed present on the unmodified branch via `git stash`, out of 9.3 scope) via new `tests/test_pole_external_integration.py` (mirrors `tests/test_v3_integration.py`'s structure — registry/prompt/detector/disambiguator/`resolve_dataset` coverage, incl. explicit v2/v3 regression-guard asserts) + additions to `tests/test_schema.py` (11/17 counts, label/rel-type sets, serialisers, selector, adapter-suffix); `resolve_dataset({"dataset_version":"pole_external",...})` verified live in-session to resolve the schema/registry/prompts/benchmark path with no model load and no network touch. **✅ CRITERIA 4-6 RAN FOR REAL, 2026-08-05** (`python -m experiments.run_evaluation config/pipeline_pole_external.yaml`, ~3h15m wall clock, 100×3 conditions, 0 tracebacks, against the live local external-POLE Neo4j — 61,521 nodes confirmed — then `python -m experiments.analyze_qualitative` on the resulting `qual_records_*.json`): wrote `results/tables_claude-sonnet_pole_external_local.md` + `metrics_claude-sonnet_pole_external_local.json` + `qual_records_claude-sonnet_pole_external_local.json` + `qualitative_analysis_claude-sonnet_pole_external_local.md`. One environment blocker fixed en route (`pip install tf-keras` — a `sentence_transformers`/`transformers` TF-Keras-3 import incompatibility, unrelated to 9.3 code; decisions-log 2026-08-05). **Headline (%):** EX C1 20.0/C2 44.0/C3 47.0; EA 20.0/48.0/53.0; **ambiguous EA 6.2/37.5/42.5 → C3 > C2 (success criterion #1 MET)**, monotonic C1≤C2≤C3 on both EX/EA (criterion #2 MET); Detection F1 77.6 (prec 96.3, rec 65.0); DSR 38.9; KG-valid 83.0/100/100; semantic repair 0.0/11.3 (C3's retry loop measurably repairs some cases here, unlike v3's 0.0/0.0). **Per-type EA:** entity 0→85→95 (the dominant C3 win — matches this schema's real duplicate-name density), schema 0→20→30, intent 25→45→45 (flat, hardest type as expected), **temporal 0→0→0 across all three conditions** — investigated via the qualitative report and found NOT to be a disambiguation-architecture failure: the AD/Dis correctly detect and commit the right schema path, but `SchemaMapping`'s output contract (0.1) only carries schema-element choices, not literal WHERE-clause values — this graph's temporal ambiguity (same-day-vs-trailing-week on a raw `DD/MM/YYYY` string) is a value-level choice the Disambiguator's contract cannot express, compounded by the un-fine-tuned QG emitting ISO-format date comparisons against DD/MM/YYYY data. A genuine external-validity finding (recorded in decisions-log 2026-08-05, not fixed — out of 9.3's scope per `ai-workflow-rules.md` §2). v2/v3 artefacts in `results/` untouched; every new artefact carries the `pole_external_local` tag (acceptance criterion 6 met). **Phase 9 complete.** Handoff: the dissertation now has a genuine external-validity data point (real graph, real questions) run with the same instrumented pipeline and the same qualitative-analysis depth as v2/v3, entirely locally, no Kaya. A Kaya-backed run remains optional future work, not required. |
| 9.4 | `9.4-pole-external-temporal-fix.md` | 9.3 | done | Follow-up to 9.3's temporal 0/0/0 finding; 2026-08-06 investigation (decisions-log) found three compounding, fixable defects rather than one architectural gap (AD 0% recall on the most-recent-vs-full-history mechanism; Dis's `committed_pattern` dropping the resolved date/scope decision; QG API prompt with no `pole_external` branch at all). **All three offline fixes implemented 2026-08-06:** `pipeline/ambiguity/prompts.py` — TEMPORAL definition widened to cover both the day-window and most-recent-vs-full-history mechanisms; `_AD_FEW_SHOT_POLE_EXTERNAL` gained a 5th example (Q-POLE-T05 grounded). `pipeline/disambiguator/prompts.py` — TEMPORAL "How to Choose" rewritten so `committed_pattern` embeds the resolved literal (`toInteger(split(...))` day-of-month idiom for the window mechanism incl. `CALLER|CALLED` bidirectional alternation; `ORDER BY ... LIMIT 1` for the scope mechanism); 2 new few-shots added (Q-POLE-T13, Q-POLE-T05 grounded) alongside the rewritten Example 3 (Q-POLE-T16), 5 few-shots total. `pipeline/disambiguator/disambiguator.py` — **no code change**; verified live (not just traced) that `_pattern_to_committed`/`parse_schema_pattern` already handle a trailing WHERE/ORDER BY/LIMIT clause without raising and pass `cypher_syntax` through verbatim (decisions-log 2026-08-06). `pipeline/query_generator/prompts.py` — new `_QG_FEW_SHOT_API_POLE_EXTERNAL` (3 examples: Crime.date day-window, PhoneCall day-window + `CALLER\|CALLED`, most-recent-contact `ORDER BY`/`LIMIT`) + `build_qg_prompt_api` gained a `few_shot: Optional[str] = None` param; the "copy WHERE/ORDER BY/LIMIT verbatim" instruction line is appended to the system prompt only when a `few_shot` override is supplied, so v2/v3 stay byte-identical (decisions-log). `pipeline/query_generator/generator.py` — `QueryGenerator` gained `few_shot_override`, applied only on the `prompt_style="instruct"` path; the local completion path (`build_qg_prompt`) is untouched. `experiments/run_evaluation.py::resolve_dataset` gained the `"qg_few_shot"` key (`None` for v2/v3, `_QG_FEW_SHOT_API_POLE_EXTERNAL` for pole_external) and `main()` threads `ds["qg_few_shot"]` into all three condition builders. `experiments/build_condition{1,2,3}.py` each gained a `qg_few_shot: Optional[str] = None` passthrough to `QueryGenerator(...)`. **Offline acceptance criteria 1–4 pass:** full suite green (367 passed; the same 5 `tests/test_run_evaluation.py` failures pre-existing/unrelated, reconfirmed via `git stash` — out of 9.4 scope) — `tests/test_pole_external_integration.py` grew from 9 to 22 tests (5-few-shot counts, Dis committed-pattern-survives-verbatim parametrized over all 3 new patterns, QG few-shot byte-identity + override wiring, `resolve_dataset["qg_few_shot"]` for all 3 versions); `tests/test_condition_builders.py`'s `_FakeQueryGenerator` updated to accept `few_shot_override` (12 tests, all green). **✅ CRITERION 5 RAN FOR REAL, 2026-08-06** (detached `nohup` run against the user-supplied local Neo4j + `ANTHROPIC_API_KEY`, ~1h wall clock, 100×3 conditions, then `analyze_qualitative.py` on the resulting `qual_records_*.json`): overwrote `results/tables_claude-sonnet_pole_external_local.md` + `metrics_...json` + `qual_records_...json` + `qualitative_analysis_...md` (same tag, per spec — a fix to the same run, not a new variant). **Temporal EA: 9.3's 0.0/0.0/0.0 → 0.0/85.0/95.0 (C1/C2/C3) — success bar MET decisively** (nonzero in C2 and C3; C3 95.0 > C2 85.0, real disambiguation value-add). Mechanism-level breakdown confirms all three defects closed: Crime.date day-window 0/10→9/10→9/10; PhoneCall day-window+bidirectional 0/3→**1/3→3/3** (the clearest C3>C2 case, directly from the Dis's embedded `CALLER\|CALLED` fix); most-recent-vs-full-history 0/7→7/7→7/7 (AD 5th few-shot + ORDER BY/LIMIT convention). Positive side effects: overall EX 44.0/47.0→**59.0/64.0** (C2/C3), overall EA 48.0/53.0→**64.0/70.0**, Detection F1 77.6→**82.0** (recall 65.0→71.2), DSR 38.9→**64.4**. Intent EA moved within noise (45/45→40/35) and entity ticked down slightly in C2 (85→80, C3 flat at 95) — both attributed to API sampling variance across two separate live runs, not regressions (neither type was touched by any 9.4 change); full reasoning in decisions-log 2026-08-06. v2/v3 artefacts untouched (criterion 6 met). **Phase 9's temporal finding is now "was three specific, fixed defects," not "architecturally unaddressable" — the stronger dissertation result. Step 9.4 fully done.** Handoff: no further action needed; the dissertation can cite the fixed per-type temporal numbers above in place of 9.3's 0/0/0 finding. |

---

## Target module layout (build map)

```
pipeline/
├── types.py                         [0.1]  all shared dataclasses incl. SchemaRepr/SchemaElement
├── llm/
│   ├── base.py                      [0.2]  BaseLLM, GenerationConfig, Completion
│   ├── huggingface.py               [0.2]
│   ├── openai.py                    [0.2]
│   └── anthropic.py                 [0.2]
├── schema.py                        [0.3]  build_pole_schema_repr() + POLE constants
├── data/
│   └── benchmark_loader.py          [0.4]
├── schema_linker/
│   ├── pattern_extraction.py        [1.1]
│   ├── training_data.py             [1.2]
│   ├── prompts.py                   [1.2]  build_sl_prompt (shared train/inference)
│   ├── inference.py                 [3.1]
│   ├── postprocessing.py            [3.1]
│   └── linker.py                    [3.1]
├── query_generator/
│   ├── training_data.py             [1.3]
│   ├── prompts.py                   [1.3]  build_qg_prompt (shared train/inference/C1)
│   └── generator.py                 [3.6]
├── entity_lookup/
│   ├── registry.py                  [3.2]
│   ├── cache.py                     [3.2]
│   ├── span_extractor.py            [3.2]
│   ├── matcher.py                   [3.2]
│   └── lookup.py                    [3.2]
├── ambiguity/
│   ├── bayesian_scorer.py           [3.3]
│   ├── detector.py                  [3.4]
│   └── prompts.py                   [3.4]
├── disambiguator/
│   ├── disambiguator.py             [3.5]
│   └── prompts.py                   [3.5]
├── validation/
│   └── cyver_validator.py           [3.7]
├── execution/
│   └── db_executor.py               [3.8]
├── evaluation/
│   ├── semantic_evaluator.py        [4.1]
│   └── metrics.py                   [4.2]
├── components.py                    [5.1]
├── orchestrator.py                  [5.2]
├── sft.py                           [2.1]  build_model_inputs, run_sft (shared SL+QG harness)
├── pole_sft_data.py                 [7.4]  enumerate_v3_patterns + POLE SL/QG jsonl builders
└── prefilter.py                     [6.1]

experiments/
├── train_schema_linker.py          [2.1]
├── train_query_generator.py        [2.4]
├── probe_utils.py                  [2.2]  beam + entropy + AUC helpers
├── diversity_penalty_sweep.py      [2.2]
├── entropy_probe.py                [2.3]
├── build_condition1.py             [5.3]
├── build_condition2.py             [5.3]
├── build_condition3.py             [5.3]
├── run_evaluation.py               [5.4]
├── validate_benchmark.py           [7.2]  executable benchmark gates (live Neo4j)
└── build_pole_sft_data.py          [7.4]  CLI for the POLE jsonl generation

app/
└── gradio_demo.py                  [6.2]

config/
├── models.yaml                     [2.1]  base-model registry (key → base, target_modules, dtype, 4bit)
├── schema_linker_train.yaml        [2.1]  shared SL hyperparameters (no per-model fields)
├── schema_linker_inference.yaml    [2.2]  locked diversity_penalty for 3.1
├── query_generator_train.yaml      [2.4]  shared QG hyperparameters (no per-model fields)
├── pipeline.yaml                   [5.4]  neo4j + active_model + AD/Dis backend config (+ dataset_version/results_tag [7.3])
├── pole_sft_data.yaml              [7.4]  paraphrase-LLM spec + generation knobs
├── schema_linker_train_v3.yaml     [7.4]  continue-SFT (POLE + Ozsoy replay mix)
└── query_generator_train_v3.yaml   [7.4]  continue-SFT (POLE + Ozsoy replay mix)

data/
├── benchmark-updated.json          [v2 — canonical for Phases 0–6; frozen baseline]
├── SyntheticPoliceKG.cypher        [v2 KG — frozen baseline]
├── SyntheticPoliceKG-v3.cypher     [7.1]
├── benchmark-v3-skeleton.json      [7.1]  questions + planned interpretations (no cypher)
├── benchmark-v3.json               [7.2]  v3 canonical once its validation harness passes
├── pole_sl_{train,eval}.jsonl      [7.4]
└── pole_qg_{train,eval}.jsonl      [7.4]

checkpoints/                        # namespaced by model_key (supports N bases)
└── {model_key}/                    # e.g. mistral7b/, llama31-8b/
    ├── sl_adapter/                 [2.1 output]
    ├── qg_adapter/                 [2.4 output]
    ├── sl_adapter_v3/              [7.4 output — continue-SFT on POLE v3 mix]
    └── qg_adapter_v3/              [7.4 output — continue-SFT on POLE v3 mix]
```
